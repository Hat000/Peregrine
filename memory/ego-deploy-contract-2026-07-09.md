---
name: ego-deploy-contract-2026-07-09
description: "VQ2 ego (21-dim) deploy contract — obs layout, footguns, adapter build (fly_ego), checkpoint logistics"
metadata: 
  node_type: memory
  type: project
  originSessionId: c864e5bb-f53f-4b54-b78a-a2216bb2c906
---

# EGO DEPLOY CONTRACT (mapped 2026-07-09, 2× opus readers w/ file:line evidence)

**Full maps (SSOT):** workflow output `...scratchpad session c864e5bb.../tasks/w8pkqub8x.output` → superseded by `handoff/ego-deploy-2026-07-09/REPORT.md` on branch `claude/ego-deploy-2026-07-09` (worktree `C:/Users/Fengy/Downloads/Projects/Anduril-ego-deploy`, based on galileo HEAD c325585 = A19 drain fix).

## 21-dim obs (raw physical units — NO obs normalization anywhere; actor's internal LayerNorm is the only scaling)
[0:3] vel BODY-FLU · [3:5] roll,pitch (leveled, NO yaw) · [5:8] body rates FLU · [8] **prev RESCALED normed_thrust g-units [0,3.765]** (not [0,1] collective, not tanh) · [9:11] coarse_sector {-1,0,1}² of CURRENT gate · [11:16] slot0 rel_pos xyz BODY-FLU + conf + visible_area · [16:21] slot1 (next gate). Assembled `peregrine_racing_ego.py::ego_actor_obs` (concat 325-333); EGO_OBS_DIM=21 (line 81) — docstrings say 26/3-slots = STALE.

## Deploy footguns (each silently kills the policy)
1. **rel_pos/vel/rates are in the UNFLIPPED TAIL-FIRST body frame** — deploy must apply fly_rl's Rz_π virtual flip to all three (camera flip in training only picks visibility, never rotates obs).
2. **No sidecar json in ego ckpts** → fly_rl legacy fallback = thrust [0,5] = 33% overdrive. HARDCODE thrust [0,3.765], rates ±3.14 (verified: cluster quad.yaml + sbatch override).
3. **Champion trained coast-OFF, stale_horizon 0.5 s** → the crossing is flown on ZEROED slots. Deploy must mask (not coast) at loss-of-lock; wire analog det_proxy = age < ~0.2 s (detections ~7-15 Hz ≠ training's 30 Hz geometric det).
4. Single-gate champions: **slot1 was zero their whole life — PIN it zero at deploy** (H6: filling it = collapse). Post-gate-pass behavior = OOD by construction (episodes ended at the gate).
5. **cmd_rate_scale: vq2_case_c profile = 0.4 (seeker tuning); RL plant sysid'd at 1.0 → ego path forces 1.0.**
6. Checkpoint = actor.pth alone ({actor_mean, actor_logstd}); obs_dim inferred from first-layer width; actor_logstd never deploys (test=tanh(μ)). ~163 KB.
7. fly_rl smoke-forward hardcodes torch.zeros(1,17) (fly_rl.py:1898) — must be gated/widened for 21.
8. coarse_sector = static per-gate course annotation in training (build_coarse_map:180-235); deploy analog = quantized leveled-frame direction of slot0 rel_pos.

## Checkpoint logistics
Runs at `/scratch/network/fl3689/diffaero_repo/outputs/train/ego_single_gate_varied_gvf_lpara[_anneal]_seed0_<tag>/`; subdirs best/ · periodic/ · checkpoints/ (final, appears at completion). Pull = `adroit.py x "base64 -w0 <path>"` → decode → sha256 vs remote (no chunking needed at 163 KB; connector has NO download mode). **vczext best+final actor+critic pulled + sha-verified** → scratchpad ckpts/.

## Build — DONE + AUDIT-CLEARED + PUSHED (2026-07-09 night)
Branch `claude/ego-deploy-2026-07-09` @ **e45398f** (5 commits off c325585, PUSHED to origin). `--ego-ckpt` → `_fly_ego` = gate-seeker scaffolding (Navigator vq2_case_c, shared prewarmed detector, drain-fix video thread) + RL `policy_step` pipeline; guard set = strict SUPERSET of _fly_gate_seeker's. Obs assembly pinned element-exact vs verbatim training reference (tests/test_ego_deploy_obs.py — edit training's ego_actor_obs ⇒ update the test copy deliberately). **5-agent adversarial audit: ZERO flight-blocking.** Roll-differential anomaly adjudicated = (b) policy trait (roll does the steering, constant offset at OOD static probe; differential monotonic in rel_y; vertical pair thrust-correct) — NOT a sign bug; frame chain independently re-derived CLEAR (bake sign/axis algebraically proven ≡ localization's world-frame lever). NaN-latch hardening landed (e45398f). Disclosed divergences (accepted): det_hold masks ~0.2 s late vs training's geometric det (watch masked-slot0 duty in `<session>/ego_obs.jsonl`); sector vert from first-fix elevation; visible_area range ref camera-vs-body (<3%). #1 open risk: map-free NavState velocity = pure IMU dead-reckon (unbounded drift vs training's gain-0.15-bounded).

## Checkpoints for flight (release `ego-ckpts-2026-07-09` on Hat000/Peregrine — artifact pipe, gitignored locally)
- **vn16_final_actor.pth = PRIMARY** (real-noise 16k, DET thread **0.5450**, collision 0.447/miss 0.008; 2.7× old real-noise champion; stochastic still climbing at cutoff ⇒ budget-limited, ambiguous-middle-leaning-A). sha 8f1d679e…
- vczext_final_actor.pth = backup (noise-0 8k, DET 0.837, OOD on real vision noise). sha 82959cb9…
- Launch (machine w/ sim + YOLO weights, MAIN .venv, worktree/checkout of the branch):
  `.venv\Scripts\python.exe rl\fly_rl.py --ego-ckpt rl/checkpoints/vn16_final_actor.pth --seeker-weights "<gate.pt>" --no-bridge --flights 1 --label ego_vn16_a1`
  Ritual: fly_rl FIRST → "Waiting PASSIVELY" banner → GO. Pass criteria = integration: arm/GO clean, loop ≥~27 Hz, frames gap-free, `[ego-diag]` fresh-lever count climbing, slot0 fed, bounded approach of gate 1; threading gate 1 = stretch; post-gate-1 = OOD (single-gate policy, episodes ended at the gate).

## A1 FLIGHT + ROOT CAUSE (2026-07-10) — THE DIVE OPENER
**A1 flew (2 flights, ShadowPC, vn16): drone never left the pad — 0.22-0.25 g sustained, detection clean (conf 1.0, fresh lever/tick in f2), frame supply PASSED A19 criteria, 0 collisions, race clock ran out.** Artifacts on branch @ 3662227 (ego_obs.jsonl ×2 + REPORT + frames).
**ROOT CAUSE (bisected + verbatim-code-verified): NOT the adapter, NOT the policy — the WORLD.** Chain: (1) exact wire tick-0 obs reproduces flight thrust locally to 3 decimals (0.253 g) ⇒ obs→action causality closed. (2) Wire obs is IN-DISTRIBUTION: verbatim course-sampler+quat computation gives spawn rel_x −11.2±2.5 (wire −10.2 ✓), rel_z +3.6±4.7 (wire +4.27 ✓), pitch = VQ1_SPAWN_PITCH_RAD −0.31 exactly (wire −0.3107 ✓ — the TILTED PAD is modeled; wire pitch anomaly = in-distribution). (3) Concat order verified by eye (vel,rp,rates,thrust,sector,slot0,slot1) — adapter matches. (4) ⇒ 0.25 g + roll/pitch ±1.2 + yaw rail = the TRAINED OPENER: **gravity dive** — training "floor" = OOB bbox −12 m (peregrine_racing.py:552; lethal-floor rule only fires there), so spawns have 12 m of free air below; the VQ2 warehouse floor is at 0. Dive = energy-optimal under the progress reward; impossible from a floor-resting start.
**Probe traps learned:** static probes of rel_x>0 (never seen w/ conf=1 in training) gave garbage-in-garbage-out 3 g — nearly misdiagnosed as a flip bug; vczext outputs max thrust for ALL inputs (directional probes on it = non-diagnostic). The virtual flip on rel_pos/vel/rates IS correct (wire matched training distribution WITH it).
**~~FIX = floor-trained ckpt~~ SUPERSEDED — TRACE VERDICT (32-env real rollout, npz sha b11b9551): GRAVITY-DIVE REFUTED.** Idle-phase z-drop ≤28 mm worst / −7 mm median; deep below-spawn excursions = gate-directed descents at episode END (corr 0.871 w/ gate-below-spawn), not openers. **TRUE MECHANISM = GROUND-CONTACT DEADLOCK:** trained opener = ~133 ms airborne low-thrust attitude re-orient (tick-0 thrust med 0.226, yaw rail +3.14 in 32/32 — the wire's 0.22-0.25 g IS the trained opener verbatim) whose exit is body-rate-feedback-gated (|rates| 0→3.8 rad/s at punch); pad contact blocks rotation at 6% collective ⇒ rates/vel/thrust_prev freeze the obs in the opener state ⇒ deadlock. Training never contains "commands produce no rotation" (spawns airborne-at-rest, free to rotate).
**Also traced:** slot0 masked 62.6% of ALL ticks (40.8% detectable) — policy flies 0.4 s blackouts open-loop on rates/thrust_prev; ep1 completions 0.87-1.13 s ×32; tail-first conventions confirmed empirically (rel_x<0 99.9%, vel_x≈−6.7 approach; positive-x probe basin = OOD artifact — training has 1/3840 ticks x>0).
**FIX LADDER (revised):** (fast, A2-unblocking) deploy-side AUTONOMOUS TAKEOFF ASSIST in _fly_ego — collective floored ~1.05 g from GO until |rates|>ε or z-climb ~0.3 m, then full handover; fully autonomous ⇒ §7-legal. (durable) ground-contact-at-spawn model in training (pin rotation/position while thrust<weight on the pad) — needed before speed ramp; floor_at_spawn + gates_above_spawn (47fbe75) remain correct/kept (real constraints; gates_above kills the late gate-directed floor strikes). vflr0 single-gate floor finetune HELD (floor rarely binds the real opener — would not fix A1).

## Wave-7b multi-gate chain saga (2026-07-10 afternoon)
- Boot seed0 (vdflr0, homing reward, noise-0, 4k): **reach ACHIEVED by ~75%** (99% at-gate episode ends, 12.8 m/s) then **COLLAPSED in the final quarter** (collision→1.00, l_episode 0.5 s, ram-the-gate) as exploration std annealed through ~0.15→0.10 — degenerate optimum (homing progress accumulates > contact terminal). FLIGHTCHECK killed the chain (its criterion = threading; the real problem was the collapse).
- 🚩 **POISONED-BEST FOOTGUN (cost 20 min of 12k budget):** warm-started fullstack from boot `best/` — but `best/` had been REFRESHED during collapse onset (the degenerate optimum RAISES the best-tracked return). vdff0 inherited collision 0.90 from update 0 → killed at 12%. **LESSON: TB-verify the health of ANY warm base at its save timestamp BEFORE spending budget; a collapsed run's best/ AND final ckpts are both suspect.**
- 🚩 **Capping updates does NOT dodge an anneal-driven collapse** — schedules are n_updates-relative and COMPRESS. The lever is the FLOOR (`++algo.noise_std_floor=0.12` via sbatch EXTRA).
- **Boot outcomes:** dgbf1f (seed1, floor 0.12) = DEAD BASIN from birth (l_episode 0.4-0.5 s all 3000 upd — seed variance strikes again, n=2: seed0 healthy, seed1 dead). **dgbf0f (seed0 RETRY, 4000 upd, floor 0.12) = CLEAN COMPLETION: reach held to the END (at-gate ends ~100%, collision 0.49/miss 0.51, l_episode 1.1, loss −0.04, NO collapse) ⇒ std floor 0.12 = the confirmed anti-collapse fix (same seed collapsed at floor 0.03).** End ckpt trustworthy (final rows healthy).
- **vdff1 LAUNCHED (3304535): dual_gate_fullstack_floor 12k warm from dgbf0f/checkpoints — the composition verdict run.** (fullstack keeps its own floor 0.03 — vn16's convergence lineage says the champion reward is collapse-safe at 0.03; the collapse was homing-reward-specific.)

## A2 FLIGHT (2026-07-10 night) — ASSIST CONFIRMED; vn16 IS A SPINNER
**Takeoff assist WORKS: A1 ground-freeze CLOSED.** `[ego-assist] ACTIVE` at GO → `HANDOVER at t=0.716s trigger=rates` (intended measured-rate mechanism, not timeout); thrust punched 1.1→3.66 g next tick, pegged ~3.76 g — matches offline replay. Artifacts on branch @ 5643366 (13-tick ego_obs.jsonl + REPORT + frames).
**Frame chain WIRE-CONFIRMED with real rotation (the check A1 couldn't do):** obs[5:8](t+1) vs wire cmd = sign-agree 1.00/1.00 on x/z, gains +2.45/+2.71 — dead on the training empirics (ss_gain x +3.5, y +1.5, z **−3.5**; yes the trained yaw feedback map is INVERTED — alias intact, and y is equally confounded in training and wire fits, r²≈0.46, not a divergence). Adapter fully exonerated with rotation data. Method: at choked loop rates the ss-gain test gets CLEANER (full settling per held command).
**🛑 HEADLINE: vn16's trained gait = SPIN-SCAN.** Training trace: **79% of ALL ticks yaw-railed ±3.14; |w_z| ramps to ~9.3 rad/s by tick 8 and CRUISES there**; steering = phase-modulated roll/pitch; vision fix ~once per revolution ⇒ **this IS the 62.6% slot0 blackout duty** (mystery closed). The wire flight executed the trained gait faithfully.
**Crash mechanism (ceiling strike t=4.3 s, env collision, gates=0):** loop choked to 2.8 Hz (12 concurrent Claude sessions; 100% ticks over budget, worst 481 ms). A spinner at 9 rad/s rotates **~190° between 2.8 Hz commands** ⇒ roll/pitch steering applied at random spin phase ⇒ zero net lateral authority at pegged climb thrust ⇒ ballistic into warehouse ceiling. Loop rate is EXISTENTIAL for a spinner (at 30 Hz = 17°/tick, controllable). Wire rel_z +4.25 m = 88th pct of training spawns (in-dist; climbs to 6.2 m exist in training).
**🛑 ATTRIBUTION FINAL (2026-07-11, a5/a7 bundles): crash is LOOP-RATE-INDEPENDENT (reproduced at 2.8/17.2/20.1/23.9 Hz) and the choke root cause was ORPHANED fly_rl pilot procs holding YOLO on the shared GPU (2 orphans = 2.8 Hz; kill-before-launch = mandatory pilot hygiene), NOT Claude sessions.** Real bottleneck = GPU-context serialization (CPU 38%/GPU 41% during a choke). **✅ LEVELER CORRUPTION CONFIRMED (a5, 185 Hz tlog, yaw-invariant tilt test): leveler-vs-gyro-truth residual median 7.4°/tick, p90 19.7°, max 44.7°; already 18° at t=0.53 s (opener); flight |accel| median 2.98 g / p90 4.03 g = gravity ref gone. Same corrupted R also mis-projects v_flu (obs[0:3]).** Crash chain: corkscrew → high-g+rotation → leveler garbage → steering on lies → tumble deepens → env hit. Validates BOTH directives ([[feedback-no-spin-hard-requirement]] · [[feedback-no-gt-actor-obs]]); training-leveler acceptance benchmark = reproduce ~7°/20° under comparable trajectories. 🚩 **TLOG FOOTGUN: raw HIGHRES_IMU gyro is ALL-AXES NEGATED vs true FRD** (nav corrects it; offline tlog parsers must negate — pinned vs obs rates corr ±1.0). Ceiling still unmodeled (patch queued). a5/a7 bundles on branch (f39bace) incl. 185 Hz tlogs + ego_timing.jsonl; TRT engine confirmed in-flight (detect 24→10.5-20.7 ms, run-to-run GPU-serialization variance); 30 Hz levers = floor_height gating bug + vp_yaw async (task #27).
**Consequences (REVISED by [[feedback-no-spin-hard-requirement]], Fengyou 2026-07-10):** (1) 🛑 **NO-SPIN = HARD REQUIREMENT** — a high-DET spinner is NOT a champion; despin workflow `wf_07cf7d6f-1a4` builds the package on chaum (PRIMARY: fatal all-axes spin aborts w/ collision-class terminal + realized-yaw clamp at application; SECONDARY: blur-gated detection + rw_perception + dead-knob cleanup incl. inert `max_w_z:1.0`; default-off, new _percept stages). (2) **A3-as-vn16-repeat DOWNGRADED** — the gait itself is disqualified; next flight = first no-spin ckpt. Detector-under-spin data comes OFFLINE instead: vision relay OUT for measured detect-vs-|w| curve from the A2 recording (calibrates the blur gate — no invented constants). (3) Whole ego reward family = spinner-prone (heading-blind homing, free yaw): dgbf0f/vdff1 likely spinners too — verify via trace at vdff1 read; vcz16 gait trace queued (gpu-test). Deploy knobs (`--yaw-scale`/`--max-rate`) exist zero-code but are diagnostics only, not the fix.

## DESPIN PACKAGE + Wave-8 (2026-07-11 early AM)
**🛑 BLUR CURVE CANCELLED + BLUR GATE OFF (Fengyou directive 2026-07-11): the VQ2 wire camera is a RENDERED SIM FEED with (assumed) NO MOTION BLUR — A2 corroborates (detector held conf 1.0 through the early 9 rad/s spin; gate lost by FOV exit, not blur). The blur gate is therefore DIShonest for this target + it blinds tumbling boot explorers (learnability suspect). Vision-relay blur-curve ask WITHDRAWN. Detector's real wire constraint = FOV transit — already modeled by the raycast rule. Blur knobs stay in code, OFF everywhere; anti-spin = aborts+clamp (guarantee) + r_perc (carrot).**
**🛑 vperc0 (seed 0) BOOT DEAD: coll 1.00 / l_episode 0.2 / speed ~2 FLAT from ~600 upd to end (dgbf0f was flying by 15%: coll 0.63, speed 8.5; identical env at init ⇒ optimization diverged, not env bug). Diagnosis: the spin basin was the ONLY exploration-discoverable flight basin; fenced off, plain homing exploration finds nothing and learns cheap suicide.** 🛑 **RUNG 2 (vperc2, blur OFF + clamp 0.7 + 8k) DEAD TOO at the ~700-upd liftoff read (coll 1.00, speed driven DOWN to 1.4)** ⇒ blur + clamp-size were NOT the binders; remaining deltas vs dgbf0f = {yaw clamp existing at all, r_perc}. **PRIME SUSPECT: the only exploration-discoverable flight path goes THROUGH the yaw/spin basin — any clamp kills it.** Forfeit wiring checked = byte-identical to the old boot at these defaults (not the cause). **RUNG 3 LAUNCHED (2026-07-11): vhov0 (3305684) = hover_hold 2k UNDER THE FULL FENCE (aborts 3.5/0.4 + rev 1.5/4 + clamp 0.7, blur off; hover_hold is OFF-LADDER pre-package so fence comes via EXTRA ++) — Fengyou STILLNESS GATE before any warm start: trace ∥ω∥ p90 <~0.5 rad/s + level + position held (naive altitude-hold is satisfiable while yawing in place — check explicitly). Manual chain (FLIGHTCHECK would kill a hover→boot auto-chain: hover never threads). + vdclamp0 (3305685) = DISCRIMINATOR: _percept boot 4k, clamp 0.0 + aborts ON — if it learns then mass-aborts ⇒ path-through-spin confirmed (curriculum land); if it learns sub-abort flight ⇒ clamp was the binder ⇒ clamp-annealing viable.** 🚩 Deploy yaw-clamp value tracks the FLOWN ckpt's trained clamp (task #28).
**🛑🛑 RUNG-3 REVELATION (vhov0, fenced hover 2k): 98% of episodes END IN SPIN ABORTS even under pure altitude-hold reward ⇒ THE FENCE KILLS EXPLORATION ITSELF — std-0.3 action noise on the ~3.5×-gain plant swings |ω| past 3.5 rad/s routinely; the abort executes explorers, not strategies. (Same signature on vdclamp0: l_episode pinned at the 0.4 s sustain clock.) Every fenced-boot death (vperc0/2, vhov0) is THIS, not basin geometry. FIX LADDER: (1) vhov1 (3305689) = fenced hover + QUIET exploration (std_hold 0.10/floor 0.08) — final-policy constraint unchanged; (2) if needed: abort-threshold ANNEAL (start ~8 rad/s → end 3.5; flown ckpt still satisfies the hard fence); (3) action-bias-toward-hover (July-8 H2 option). NEVER ship a loosened END-state fence.**
**🛑🛑 RUNGS 1–2 REFUTED (2026-07-11 AM reads): vhov1 (quiet std 0.10/0.08) DEAD IDENTICALLY — metrics/spin_abort_rate 1.00, l_episode ~0.50 s, peak tilt 150° ⇒ noise magnitude is NOT the mechanism. vdclamp0 (clamp 0.0) DEAD too (success max 0.0000, exit_spin 65–94% + exit_floor ~35%) ⇒ the yaw clamp was never the binder. REFRAME: the fence executes the EARLY INCOMPETENT PHASE — a random-init policy on this unstable plant exceeds ‖ω‖ 3.5 within ~15 steps, every episode ends at the 0.4 s clock, the batch has NO survivors to learn from. PROOF the task is otherwise learnable: the SAME hover_hold stage hit 91% hover PRE-fence (run `ego_hover_hold_seed0_hh`, job 3297613: exit_timeout 0.90, l_ep 38.7 s, alt_err 1.2 m, speed 1.5 — but peak_tilt 76°, so it is NOT still; the stillness gate still applies). RUNG 4 LAUNCHED: vhov2 (3305693) = vhov1 + `+init_from` the hh ckpt (competent init survives the fence ⇒ gradient exists; zero code change, single variable). Fallbacks: abort-threshold ANNEAL (start ~8 → end 3.5, END fence untouched) then action-bias-toward-hover.**
🚩 **L17 TB METRIC DECODER (prevented a misread): `env_loss/*` = per-STEP batch means; `metrics/*` = per-EPISODE-END classes. Convert: per-step abort ≈ episode-share × dt/l_episode (vhov1: 1.00×0.0333/0.50=0.067≈0.060 ✓; vdclamp0: 0.65×0.0333/0.47=0.046≈0.043 ✓). Corollary: env_loss/alt_hold_reward 0.97 alongside 100% spin-death just means the alive steps sit near spawn altitude — it is NOT evidence of hovering competence.**
**🛑 RUNG 4 REFUTED (vhov2, warm-from-hh under fence): DEAD IDENTICALLY — metrics/spin_abort_rate 1.00 from the FIRST eval point (min 0.9966), l_ep 0.47 s, peak tilt 124°, mean_speed 6.1. The hh hover champion ITSELF trips the fence instantly: its pre-fence optimum is a WOBBLE-HOVER whose opening transient sustains ‖ω‖>3.5 realized (91% timeout-survival ≠ fence-compliant; peak_tilt 76° was the tell). So no policy in hand — random OR champion — survives second one under the fence; 2000 upd of gradient never fixed it (homogeneous batch, no contrast). RUNG 5 LAUNCHED: vhov3 (3305699) = UNFENCED still-hover SHAPING — aborts 0, rw_rate 0.001→0.2 (ego_reward R6 = −rw_rate·‖ω‖, the realized-rate lever; negligible at the ω≈0.5 target, dominant at the wobble's ~3.5), clamp 0.7 kept, quiet std, warm hh. The FENCE STAYS THE GUARANTEE — shaping only builds the stepping stone: vhov3 → stillness-gate trace → vhov4 = fence-warm confirm (a still policy survives trivially) → boot warm. If boot-from-still-hover mass-dies learning locomotion, THEN build the abort-threshold in-run anneal (start ~8→3.5, END fence untouched; mirror the proven noise_scale_anneal mechanism).**
**🛑 RUNG 5 BACKFIRED (vhov3, unfenced rw_rate 0.2): GIVE-UP rediscovered — converged to a 9.6 m/s SPRINTER dying by wide plane-crossing at 4 s (exit_plane_miss 0.83, alt_err 4.2 m, mean ‖ω‖ still ~3.1). Ledger: rate pen 0.62/step ≈ hold bonus 0.73/step ⇒ living went ~net-zero and a zero-banked-progress plane-miss is a near-free exit. 🚩 L19: on the give-up-resistant hover stage, ANY added per-step penalty must keep living STRONGLY net-positive — pair penalty hikes with hold-bonus hikes; exit_plane_miss rising = the give-up tell. vhov3 ckpt POISONED. RUNG 6 = vhov4 (3305702): rw_rate 0.05 + rw_altitude_hold 2.0 (living ~+1.65/step at ω=3), warm hh again = manual rw_rate LADDER (raise pressure only after achievable ω drops, verify each rung).**
**✅✅ _pef ACCEPTANCE PASSED (vpefpc1, 300-upd boot on gpu-test): PRECHECK RC=0; ALL L16 keys emit; eskf_tilt_err_deg mean 11.8–25.0 / p90 21.9–58.4 at sf_mag_g_mean 1.78–2.81 (≥ the B(iii) band, NOT ~0° — the aliasing channel exists in-sim); kf_vel_err 1.7–3.6 m/s in tumble; accel duty 0.20–0.48 (gate stack alive); resolved dt=0.0333, n_substeps=5, capture=true, transport_delay_steps ABSENT ⇒ default 0 (validated config). THE _pef STAGES ARE FLIGHT-PIPELINE READY — they wait only on the winning boot recipe from the hover ladder.**
🚩 **L18 DEPLOY-SET RULE (vpefpc0 burn, 2026-07-11): the deploy set = EVERY file in `git show --stat` of the package commits, not a remembered list — rl/diffaero_dynamics.py (plant-side capture_specific_force patch, 927adc1+8055acd) was missed and the _pef guard aborted exactly as designed (RC=1, no budget). Scratch's copy == chaum pre-package content modulo CRLF (sha mismatch ≠ content mismatch — strip \r before adjudicating drift); ef6d1dfc deployed, vpefpc1 (3305698) relaunched on gpu-test.** Full scratch rl/ sha audit 2026-07-11: ego chain now matches HEAD 17eb6a9 (7 files); rl/tools/* undeployed (offline benches); other rl/*.py diffs are pre-existing non-ego lineage drift — do NOT "fix".
**🛑 RUNGS 5a/5b (unfenced still-hover shaping) DEAD — HOVER STEPPING-STONE ABANDONED (2026-07-11): vhov3 (rw_rate 0.2 warm-hh) = FLY-AWAY (exit_plane_miss 0.83, alt_err 4.2 m, speed 9.6); vhov4 (rw_rate 0.05 + hold×2) = FLOOR-DIVE (exit_floor 0.97, alt_err 11.9 m, l_ep 4.8 s). KEY INSIGHT: the high-rate wobble IS this lineage's only stabilization mechanism — fence it ⇒ dies at the clock; price it ⇒ stops correcting and falls (the July-8 floor-dive redux). vhov4's rot_accum ~20.7 rad ≫ the 9.42 rev bar ⇒ BOTH fence triggers bind, any anneal must scale both. PIVOT = direct BOOT anneal (fence-only-delta argument: dgbf0f = same boot unfenced flying by 15%; vperc0 = same boot fenced dead).**
🟢 **SPIN-ABORT ANNEAL LANDED chaum `88c9a19` (launcher, 4th resolve→holder→mutate hook): `+env.spin_abort_anneal` scales BOTH triggers (rate+rev) by one factor, LINEAR start_scale (2.6) → 1.0 over the front 1−hold_frac, then END-HOLD at the EXACT fence for the last hold_frac (0.25) — reverse of the start-hold anneals because the guarantee lives at the END; END fence NEVER loosened. L16 armed-bases guard (anneal on fence-off stage RAISES). Launched: vpaa0 (3305815, _percept boot 4k, GT-att diagnostic twin, DISPOSABLE) + vpefa0 (3305816, _pef boot 4k, FLIGHT lineage) — blur off, clamp 0.7, PRECHECK=1. Read: liftoff by ~15% (dgbf0f profile) + spin_abort_rate rising then falling as the fence tightens + FLIGHTCHECK; _percept-learns/_pef-dies fork = estimator-corruption-tax-at-boot (per the _pef block comment).**
**🛑 vhov5 (fresh + UNFENCED + rw_rate 0.05 + QUIET std 0.10): floor-dive 1.00, l_ep 1.9 s, tilt 167°, DET metrics FROZEN (identical trajectory every eval) — never even found the wobble. One-variable vs hh (fresh + std 0.30 + rw_rate 0.001 = 91% hover) ⇒ 🚩 THE EXPLORATION BIND: LOUD noise is LOAD-BEARING for discovering flight at all, and loud noise on a rate-command plant = learning while tumbling ⇒ spin-robust gaits win. (Also: my give-up calibration used CONVERGED alt-hold ~1/step; the INCOMPETENT phase realizes ~0.55 ⇒ dive≈break-even — calibrate give-up margins on the incompetent phase, not the optimum.) vhov6 (3305824, gpu-test): hh's LOUD std + rw_rate 0.05 = the last hover variable. The bind strengthens BOTH the boot-anneal path (loud noise, delayed executioner) AND level-mode fallback (attitude-command exploration wobbles ±degrees, can't tumble).**
**🛑🛑 vpaa0 FINAL (annealed _percept boot): THE ANNEAL WORKS BUT LIFTOFF STILL FAILS — spin-death share peaked 59% mid-tightening then FELL to 5–11% (the policy ADAPTED to the tightening fence = the mechanism is sound), yet exit_floor 0.91 @ l_ep 0.33 s, zero gates, DET collision 1.0. Fence-only-delta hypothesis DEAD. Ceiling EXONERATED (lethal ceiling default-OFF + unarmed in every stage; exit_ceiling = legacy soft top-OOB, dgbf0f had it too).** 🚩 **dgbf0f .hydra AUTOPSY (the flying baseline): it flew UNDER a fence — OLD inc8 knobs spin_rate_abort=10.0/time 3.0 s — and its finals show REAL flight (exit_floor 0.9%, deaths = gate-frame 48% + plane-miss 51% = the rammer heritage reaching the gate). Its noise held 0.30 the WHOLE run (hold_frac 1.0) vs _percept's 0.3→0.03 decay. Remaining early-phase delta vs vpaa0 ≈ YAW CLAMP 0.7 (+ tiny r_perc). The 2×2 had a hole: vdclamp0 = clamp-off+FULL fence (spin death); vpaa0 = clamp-on+annealed (no liftoff) → LAUNCHED the missing cell vpaa1 (3305832) = clamp-OFF + annealed fence, + vbrep0 (3305833, gpu-test) = dgbf0f replica on today's code (old-name fence knobs; possibly inert post-cleanup = even more permissive; adjudicates code drift). PRIME SUSPECT: the yaw clamp blocks the lineage's only liftoff-discovery path (the yaw/pitch re-orient opener) — if vpaa1 lifts off, the clamp needs its own anneal (loose→0.7), same medicine.**
**🛑🛑 MATRIX CLOSED (2026-07-11 evening): vbrep0 (dgbf0f replica, today's code) = FLIES (floor 0.93→0.27 falling at 2k cutoff, speed 11+, frame/plane deaths, some passes) ⇒ code drift EXONERATED. vpaa1 (clamp-OFF + annealed fence) = flew MID-RUN via the spin gait then the tightening fence EXECUTED it (87% spin-deaths at end — the free-yaw gait refuses to de-spin under fence pressure alone). vpefa0 = twin of vpaa0 (obs fidelity NOT the binder) + _pef acceptance keys READ: eskf_tilt 19.5°/46.7° under tumble, kf_vel 0.8–3.7, keys emitting = estimator emulation ALIVE, no stop-ship. SYNTHESIS: the lineage's ONLY discoverable flying gait = the SPIN gait; clamp blocks its discovery (vpaa0), fence executes it (vperc/vpaa1), neither ⇒ flying spinner (vbrep0). r_perc exonerated (vpaa1 flew with it).** 🟢 **LAST ACRO CELL LAUNCHED: vpja0 (3305847) = JOINT ANNEAL — yaw-clamp anneal (`d908539`, 5th hook: scale 4.57×0.7≈3.2 rad/s ≈ rail → 0.7, END-HOLD by 60%) + fence anneal (END-HOLD by 75%): the clamp tightens FASTER so de-spinning stays ahead of the executioner; both land at exact finals. If vpja0 dead ⇒ acro exhausted ⇒ LEVEL-MODE PIVOT recommendation + step-response protocol for Fengyou.**
🟢 **PERCEPTION-CARROT CELL (Fengyou's pointing-reward proposal, 2026-07-11 evening): vpcb0 (3305850) = boot + fence anneal + clamp 0.7 FIXED from birth + r_perc FARM-THEN-WEAN anneal (`9372f52`, 6th hook mutating _egorw.perception: 25×base=0.5/tick at birth → 0.02 farm-neutral END-HOLD by 70%). Rationale: r_perc's suspected meaningful-weight failure (airborne gate-STARING farm — vglpns-era "detonation", later marked CONFOUNDED by the fresh-eyes audit; Fengyou's old-session relay 2026-07-11 confirms: it CANNOT be collected off-gate, the farm is on-gate stare-at-distance) is a fence-legal clamp-compatible NON-spin attractor exploration can find — the stepping stone every hover rung failed to reach; the wean dissolves the farm before graduation. BONUS mechanism (old-session relay): the carrot is an ANTI-DIVE gradient from tick 0 (sinking drops the gate out of frame-centre → r_perc collapses) — and the boot's current deaths ARE 0.33 s floor-dives; rw_corridor 4.0 (the hover-validated vertical lever) is ALREADY armed in the boot and doesn't save it (corridor pays for transit competence a 10-tick policy lacks; the carrot pays for tick-0-achievable pointing). READ: stare phase = exit_timeout appears (a first for any boot today); wean = timeouts convert to plane_miss/frame/pass. Single new variable vs vpaa0. If vpcb0 lifts off where vpaa0 didn't ⇒ the carrot is THE learnability lever with the guarantee-friendly config (clamp on from birth). Racing vpja0 (joint anneal) head-to-head.**
**🛑🟢 THE REFRAME (2026-07-11, Fengyou "body-rate flies level" push + live reads): SPIN and FLOOR-DIVE are TWO STACKED, SEPARABLE failures. Clamp yaw to 0.7 (vpaa0/vpcb0) and the drone STOPS spinning (exit_spin ~0.09) but FLOOR-DIVES (exit_floor 0.91) — the spin was MASKING the dive (a spin-scanning drone's thrust keeps it aloft). vpja0 (free yaw early) = SPINNER (exit_spin 0.97, fence executes). So: NON-spinning body-rate flight IS reachable in our setup (vindicates Fengyou — we flew CTBR level in VQ1/inc7-8); the clamped policy just DIVES. ⇒ LEVEL MODE WOULD NOT FIX THE DOMINANT FAILURE (the dive is vertical-control-at-cold-start, not attitude/spin) — the hold on level-mode is CORRECT, sharper reason than expected. Perception carrot (vpcb0) did NOT rescue the dive; rw_corridor 4.0 already armed doesn't either (both pay for competence a 10-tick cold policy lacks). PROVEN solvable: hover_hold 91% alt-hold; vhov6 airborne ~10s. ⇒ FIX = give the boot a stable-flight START so it only learns "go forward," not "discover flight" (discovery finds the dive/spin).**
🟢 **vwhb0 (3305867) = WARM-START THE BOOT FROM vhov6 (airborne hover ckpt) + clamp 0.7 + fence anneal + logstd reset 0.18. Single new variable vs vpaa0 = the warm start. Tests the reframe directly: start aloft (non-spin) → does forward-to-gate emerge without re-diving? vhov6 wobbles (rot_accum 21.8, tilt 92°) so fence-anneal (loose early) is REQUIRED or the warm-in wobble self-executes (killed vhov2). READ: exit_floor stays LOW from update 0 (warm start held altitude) + plane_miss/frame/pass rising = forward flight emerging = THE recipe. If it re-dives ⇒ the forward-progress reward overpowers the warmed altitude skill ⇒ need progress-reward ramp-in (0→full) as the next lever.**
**🟢🟢🟢 vwhb0 = THE BREAKTHROUGH — BOOT-LEARNABILITY CRISIS BROKEN (2026-07-11). FIRST "FLEW" VERDICT of the whole campaign: DET_EVAL thread 0.7753 (77.5% gate-thread success, deterministic deployed policy), collision 0.224, miss 0.0008, oob 0 · n_passed_gates 1.73/2 · exit_floor 0.02 (final) · exit_spin 0.005 (NON-SPINNING) · l_ep 3.0 · speed 8.8. Deaths = gate-FRAME clips (refinement problem, the good kind). RECIPE = warm-start the boot from the vhov6 airborne-hover ckpt + clamp 0.7 + fence anneal (2.6→1.0 END-HOLD) + logstd reset 0.18; NO progress ramp needed. 🚩 THE MID-RUN RE-DIVE (floor→0.95 @ 15%) WAS A TRANSIENT — it climbed OUT by convergence; do NOT judge a warm boot before ~40%. Ckpt SAVED (actor.pth/critic.pth). Confirms the reframe end-to-end: spinner masked a vertical-control gap; non-spin body-rate flight IS reachable (Fengyou's "body-rate flies level"); warm-from-stable-hover was THE lever. vpja0 FINAL = spinner (0.97); vpcb0 FINAL = diver FAILED (DET coll 1.0).**
🟢 **EXTENDING THE WIN (2026-07-11): vwff0 (3305890) = _percept FULLSTACK (12k) warm from vwhb0 = prove the full multi-gate racing stack (GT diagnostic of the ceiling). vpefwh0 (3305891) = _pef BOOT (4k) warm from vhov6 = the DEPLOYABLE no-GT flight lineage on the SAME proven recipe (tests warm transfer across the GT→no-GT obs boundary; if it breaks, run a no-GT hover_hold+ego_faithful first). If vpefwh0 flies → chain _pef fullstack → FLIGHT CANDIDATE (deploy with --ego-yaw-clamp 0.7). vwhb1 (progress-ramp variant) now likely SUPERFLUOUS — vwhb0 won without it; keep the hook, it's cheap insurance.**
**🛑 vwhb1 FINAL = FAILED 0% (DET thread 0.0, exit_spin 0.60, speed 1.3) — the progress-ramp HURT, decisively. Mechanism: ramping forward-pull in from 0 REMOVED the early objective → the warm hover policy drifted into the SPIN basin during the low-pull phase and never climbed out. 🚩 INVERTED LESSON: FULL progress from tick 0 (vwhb0) is what yanks the warm hover into forward flight BEFORE exploration captures it into a spin; the vwhb0 mid-run re-dive is a HEALTHY dip-then-recover, NOT a bug — do NOT suppress early forward pull to prevent it. progress_ramp hook stays default-OFF, NOT for this lineage. vwff0/vpefwh0 correctly use the no-ramp recipe.**
**🟢🟢 vwff0 = FULLSTACK CEILING PROVEN: FLEW, DET thread 0.8281 (82.8% — BETTER than the vwhb0 boot's 77.5%). The full multi-gate racing stack converges from the vwhb0 boot and climbs. But GT-attitude ⇒ DIAGNOSTIC only (never flies, no-GT directive). 🛑 vpefwh0 = no-GT _pef boot warm-from-vhov6 FAILED 0% — the GT→no-GT warm-transfer boundary broke (vhov6's altitude reflexes tuned to CLEAN attitude shatter under the 7-45° lying-estimator obs; = audit's fragile-warm-transfer / H6). ⇒ THE no-GT LINEAGE NEEDS A no-GT HOVER to warm from. RUNNING: vhovpef0 (3305930) = FRESH no-GT hover (hover_hold + ego_faithful + n_substeps=5 + capture_specific_force + est_dt_ticks_hi=4, vhov6's loud-std/clamp-0.7/no-fence recipe) = the clean-transfer base for a future no-GT boot; vpefwh1 (3305931) = HEDGE, _pef boot warm from vwhb0 (the GT FLYER, richer prior than a hover — tests whether a flying prior survives the obs boundary). NEXT: vhovpef0 flies → warm _pef boot from IT (clean same-contract transfer) → _pef fullstack → FLIGHT CANDIDATE. GT ceiling 82.8% says the recipe is right; the only gap is porting it across the no-GT boundary.**
**🛑 BOTH no-GT ports hit the ESTIMATOR-CORRUPTION WALL (localized, singular): vhovpef0 (fresh no-GT hover) FLOOR-DIVES (alt_err 12 m, floor 1.0 — lying obs breaks altitude-hold even for a hover, where clean-obs vhov6 stayed aloft); vpefwh1 (_pef boot warm from vwhb0 GT-flyer) PARTIAL — reaches gates (coll 0.82/miss 0.18 vs vpefwh0's pure dive) but deployed 0.03% thread. ⇒ the lying ATTITUDE is the entire GT(82.8%)→noGT(~0%) gap. 🚩 KEY LEVER: the no-GT boots ran est_dt_ticks_hi=4 = WORST-CASE choked loop (≤133 ms → ≤45° attitude err); at a clean ~30 Hz loop (lean-profile target) the SAME estimator is ~5°. Making the boot DISCOVER flight under the hardest estimator = the exact mistake the warm-from-hover reframe fixed. → vpefwh2 (3305957) = _pef boot warm from vwhb0 at ticks_hi=1 (CLEAN 30 Hz, ~5° err). If it flies ⇒ DEPLOYABLE flyer that REQUIRES ~30 Hz flight (achievable), then dt-CURRICULUM (anneal ticks_hi 1→4) to harden for choked points. If clean-dt ALSO fails ⇒ the ~5° estimator error itself blocks vertical control ⇒ the leverage moves to the ESTIMATOR (audit: bias-calib + availability), a different workstream, and the GT-ceiling gap may be structural for now.**
**🟢🟢🟢🟢 vpefwh2 = THE DEPLOYABLE-PATH BREAKTHROUGH — NO-GT FLYER AT 96.9%. clean-dt hypothesis DEAD ON: DET thread 0.9691, collision 0.031, n_passed 1.95/2, exit_spin 0.0025 (NON-SPINNING), speed 8.6 — HIGHER than the GT boot (77.5%) AND the GT fullstack (82.8%). eskf_tilt_err mean 1.6°/p90 2.7° (clean 30 Hz est = even better than the ~5° estimate). ⇒ THE ESTIMATOR-CORRUPTION WALL WAS ENTIRELY THE WORST-CASE-dt CHOICE (est_dt_ticks_hi=4→45° err); at the realistic clean 30 Hz dt (ticks_hi=1) the estimator is ~2° and the no-GT policy flies BETTER than GT. Strategic fork RESOLVED: option A (dt lever) won; NO estimator-improvement workstream needed. RECIPE (deployable, no-GT, non-spin): warm _pef boot from vwhb0 GT-flyer + ego_est_dt_ticks_hi=1 + clamp 0.7 + fence anneal + logstd reset. ckpt SAVED. → vpeffs0 (3306085) = _pef FULLSTACK 12k from vpefwh2 = the DEPLOYABLE multi-gate racer (expect ≥96.9%, refines gate-frame clips). 🚩 DEPLOY CONSTRAINT: this ckpt trained at ticks_hi=1 = REQUIRES ~30 Hz flight loop (lean profile targets it; A8 hit 23.9); a choked <~30 Hz loop = OOD on dt → hold 30 Hz OR add dt-curriculum (anneal ticks_hi 1→4) as a hardening follow-on. FLIGHT-CANDIDATE PATH NOW COMPLETE: vpeffs0 graduate → pull ckpt → deploy fly_ego --ego-yaw-clamp 0.7 at ~30 Hz.**
**🏁 FLIGHT CANDIDATE READY (2026-07-12): vpeffs0 fullstack FLEW (DET thread 0.86, exit_spin 0.0004, eskf_tilt 0.65°/1.4°) — BUT 🚩 boot vpefwh2 (0.9691) BEAT the fullstack (0.86) on the SAME course (boot/fullstack overrides byte-identical incl. rw_exit_align=0) ⇒ the 12k fullstack extension DID NOT help, slightly drifted down = don't over-train this recipe; **PRIMARY flight ckpt = vpefwh2 (0.9691), backup = vpeffs0.** NON-SPIN CONFIRMED without a trace: spin_rot_accum_mean ~1.6 rad / 4 s window ≈ ~0.4 rad/s SUSTAINED ‖ω‖ = BELOW the 0.5 stillness target and «« the 2.36 guarantee ceiling; exit_spin ~0 ⇒ flies essentially LEVEL, no corkscrew. Both actor.pth PULLED (base64-daemon via scratchpad/pull_ckpt.py, sha-verified) → staged `Anduril-ego-deploy/data/ego-ckpts-vpef-2026-07-12/` (gitignored). 🚩 ckpt has NO sidecar — deploy loads via load_ego_actor (bounds hardcoded ±3.14). Flight happens on ShadowPC ⇒ ckpt must cross via ARTIFACT PIPE (GitHub release asset; awaiting Fengyou green-light on the publish). LAUNCH BOX: fly_ego --ego-ckpt vpefwh2_actor.pth --ego-yaw-clamp 0.7 at ~30 Hz (dt requirement: trained ticks_hi=1). dt-curriculum (1→4) = choked-loop-robustness follow-on if 30 Hz not held.**
**🚩 COURSE IS RANDOMIZED (clarification — "same course" = same RANGES, NOT fixed gates): peregrine_racing_ego.py:788 `_apply_course_overrides(first_build=False)` RESAMPLES a fresh random course PER-ENV PER-RESET via sample_courses (heading random-walk). Varied: seg_len 10-20 m, spawn_dist 8-15 m, gate height 0.5-6 m below + drops, turn angles (heading walk), spawn-yaw jitter 0.25. So 96.9% = over thousands of distinct 2-gate geometries, real generalization within-envelope. boot-vs-fullstack "same course" = byte-identical override RANGES (apples-to-apples). OPEN GAPS: (1) trained on 2 gates, VQ2 = 20 (egocentric slot0/slot1 ⇒ N-gate = chained 2-gate transitions, SHOULD transfer — VALIDATING via vpefg6 (3306171) = vpefwh2 warm-eval on course_n_gates=6, gpu-test); (2) randomization ranges = our VQ2-geometry ESTIMATE, real-track mismatch = a gap to close (re-measure track spacing when available).**
**🟢 vpefg6 (6-gate generalization, warm from vpefwh2, ONLY 400 upd adapt) FINAL: CHAINS — DET thread 0.356, n_passed 3.8/6, success 0.30→0.35 climbing, exit_spin 0.0035 (non-spin holds on 6 gates), deaths = gate-frame clips 0.67. 🚩 96.9%(2gate)→35.6%(6gate) is NOT a generalization failure = PER-GATE PRECISION COMPOUNDING (per-gate ~85% ⇒ 0.85^6≈0.38 ✓). Lever to a 20-gate run = train DIRECTLY on longer courses (lifts per-gate precision under multi-gate regime). ⇒ egocentric slot0/slot1 ⇒ 2-gate training TRANSFERS to N-gate as chained transitions (the hypothesis holds); refinement = tighter approach margin + train directly on longer courses. 🚩 CKPT FORMAT: training actor.pth = {'actor_mean','actor_logstd'}, 21-dim, loads DIRECTLY via load_ego_actor — NO conversion needed (verified both vpefwh2/vpeffs0). 🚩 SELF-PUBLISH BLOCKED: gh not on laptop + GCM won't surface token headlessly + won't handle raw PAT plaintext ⇒ release-upload handed to Fengyou (runs where gh authed). Release tag = ego-ckpts-vpef-2026-07-12. Pilot prompt delivered (fly vpefwh2 PRIMARY + vpeffs0 behavioral; --ego-yaw-clamp 0.7 MANDATORY; ≥30 Hz dt HARD req; LAUNCH RITUAL kill-orphans→fly_rl-banner→GO; §7; zero-contact validity).**
**✅ RELEASE PUBLISHED (2026-07-12, repo now PUBLIC): `ego-ckpts-vpef-2026-07-12` on Hat000/Peregrine (both actor.pth assets). Pilot: `gh release download ego-ckpts-vpef-2026-07-12`. 🚩 gh RECIPE (banked — gh installed C:\Program Files\GitHub CLI\gh.exe v2.96): `gh auth login --with-token` FAILS (git/GCM token lacks read:org scope), but `GH_TOKEN=<tok> gh release …` WORKS (release only needs repo scope). Get the token headlessly via `printf 'protocol=https\nhost=github.com\n\n' | git credential fill | sed -n 's/^password=//p'` (bash pipe works; the earlier PowerShell attempt failed on parsing, NOT GCM). Never echo the token; pipe machine-to-machine only. Laptop-only (ShadowPC has its own gh for downloads).**
🟢 **PROGRESS RAMP-IN LANDED chaum (7th anneal hook): `+env.progress_ramp` ramps _egorw.progress 0→base over front, END-HOLD full for last hold_frac (reuses _spin_abort_schedule start_scale=0). vwhb1 (3305876) = warm-from-vhov6 + progress-ramp-in + clamp 0.7 + fence anneal = the combined re-dive fix (single new var vs vwhb0 = the ramp). READ: exit_floor stays LOW THROUGH the forward-transition (the failure vwhb0 showed at 15%) + plane_miss/frame/pass climbing as progress ramps in = THE boot recipe → _pef fullstack chain. If still re-dives ⇒ vertical control deeper than reward-scheduling (revisit corridor weight / spawn-above geometry).**
**vhov6 (loud std 0.30 + rw_rate 0.05): AIRBORNE again (floor exits 10%, l_ep 10.6→12.4 still climbing at the 2000-upd cutoff) but the WOBBLE PERSISTS (rot_accum 21.8 ≈ vhov4) and it drifts out of the box (plane_miss 62%/side 24% — hover_hold has no xy term). Hover-question status: loud noise = necessary for flight discovery (confirms the exploration bind); rw_rate 0.05 does NOT displace the wobble; budget too short to converge. Secondary to the boot path now.**
🟢 **LEVEL-MODE FALLBACK (Fengyou raised 2026-07-11; assessment relayed): NOT now — but it REPLACES "hand-built inner attitude loop" as the #1 structural fallback if the anneal boots + vhov6 all fail. Case: same modeling burden as a handmade inner loop but the sim's OWN controller (zero inner-loop sim-to-wire gap); no-spin nearly BY CONSTRUCTION (strongest form of the directive); dissolves the exploration bind. Costs: Fengyou step-response sysid session (~20 min, the long pole) + plant rework + full retrain (2-4 days serial; lineage mostly gait-DQ'd anyway) + max-tilt speed ceiling (acceptable under BANK-FIRST). Decision rule: adjudicate on today's probes.**
🟢 **KP-PERSISTENCE DEBOUNCE LANDED (task #29, Fengyou-requested): chaum `c2586e2` (`ego_kp_persist_frames`, pure kp_persist_update once-per-tick post-blur-AND; episode+gate-advance resets; ≤1=OFF byte-identical golden-pinned) + ego-deploy `b6e9b5c` (`--ego-kp-persist`, same table between detect_gate_lever and builder.update). PARITY OK (judge: deploy uniformly stricter, sub-det_hold; miss-taxonomy asymmetry documented in _kp_persist_step docstring). ARM LATER with matched N on both sides (never one-sided); default-off everywhere; per-keypoint variant REJECTED (no deploy analog).**
🚩 **L20 WATCHER RULE: a poll loop over `adroit.py x` must check the CALL's exit code — channel death returns empty output and fakes "queue empty" (the vhov4 read was nearly mis-adjudicated on this). Pattern: rc≠0 ⇒ CHANNEL_ERROR + long backoff, never break. Also: each x call on a dropped daemon session fires a Duo push at Fengyou's phone — do NOT hammer probes.**
✅ **Deploy yaw-only clamp LANDED (task #28): ego-deploy `6838713` — `--ego-yaw-clamp` in fly_rl.policy_step (yaw-only, after yaw_scale, flip-invariant, default 0.0 = bit-identical off; pinned by test_ego_yaw_clamp_yaw_only). Pass the flown ckpt's trained ego_yaw_cmd_clamp_rad_s (0.7 current lineage). Pre-flight requirement CLEARED.**
**🟢🟢 NO-GT PACKAGE LANDED chaum `725adcd..8055acd` (PUSHED; 15 agents, 3 majors fixed incl. a measured choked-loop dt emulation knob): `_pef` stages (dual_gate_boot_floor_pef → fullstack_pef) = _percept + ego_faithful (deploy-parity ESKF leveler + vel KF + sampled rates) + n_substeps=5 + specific-force capture; default-off, vperc snapshot pins intact.** DISCOVERIES: **sim IMU is NOISELESS (pad window: literally 1 unique gyro/accel row ⇒ measured noise constant = ZERO, nothing invented)** · **the wire AHRS steps ONCE PER CONTROL TICK ⇒ the 5–27° wire attitude error = SAMPLING ALIASING of intra-tick motion, NOT sensor/filter noise** (dense-step control 0.36°); emulation = deploy-algorithm-EXACT (single-step parity 0.0003° vs wire) and reproduces the wire error profile at matched dt (5.17/26.8/34.3 vs measured 5.26/26.7/34.5). Gains inherited verbatim w/ provenance (eskf.py:255 "0.05" comment is STALE — real accel_noise 0.3). ⚠️ dt caveat: training tick 33 ms is 3.5–5× cleaner than the 10–24 Hz wire operating points measured so far — flights must hit ~30 Hz (lean profile target) or use the measured-dt emulation knob. PRECHECK acceptance gate wired (L16 keys eskf_tilt_err_deg_*; ~0° under matched |f| = STOP-SHIP). Tools: rl/tools/{imu_foundation,eskf_pad_state,leveler_bench}.py.
**✅ vcz16 gait trace (job 3304783): ALSO A SPINNER at noise-0 — cruise |w_z| 8.4 / |w_x| 6.3 rad/s, roll ±140°, yaw cmd >2.0 in 92% of ticks (duty 58% vs vn16's 41%) ⇒ spin = REWARD-BASIN, not noise adaptation; every pre-_percept champion is gait-disqualified.** ✅ A8 (lean profile, 47c90d2): vq2_ego_lean FLIGHT-PROVEN — vp_yaw/floor_height absent from timing, 23.9 Hz matching a7 despite worse detector window, over-budget halved (40.7→20.5%); remaining Hz binder = detector GPU-serialization variance (10.5–18.7 ms). 73-tick bundle + tlog = third leveler dataset.
**Perception-honesty/HARD-NO-SPIN package LANDED on chaum `6914359..6615458` (PUSHED), default-off.** 15-agent workflow (minimal-risk design won; 3 majors caught+fixed in verify). New stages: `dual_gate_boot_floor_percept` → `dual_gate_fullstack_floor_percept`. Knobs: blur gate (lo 2.0/hi 4.0 **PLACEHOLDER pending the A2 blur curve**; NOT scaled by ego_noise_scale — camera physics), fatal spin abort (3.5 rad/s ∥ω∥ sustained 0.4 s + leaky 1.5 rev/4 s window, collision-class terminal on BOTH reward paths), applied-yaw clamp 0.35 (action space untouched), rw_perception 0.02 (= rw_time farm-neutrality bound). SSOT = DESIGN.md §P.
- 🛑 **HONEST GUARANTEE CEILING: no SUSTAINED rotation > ~2.36 rad/s** (leaky fixed point 2π·rev/window); a constant 1.8–2.35 rad/s corkscrew survives both triggers, priced only by r_perc — **first-run trace check MANDATED** (sustained ∥ω∥ plateau 1.8–2.35 + spin_rot_accum_mean near ~7–9.4); tighten rev_abort→0.75 or window→8 s if seen (binds 0.6–1.2, re-check threshold ordering).
- 🚩🚩 **DEPLOY YAW-ONLY CLAMP FOOTGUN (pre-flight REQUIREMENT for ANY _percept ckpt): add `clip(rate_flu[2], ±0.35)` to fly_rl.policy_step — a deploy-repo CODE change. `--max-rate 0.35` clips ALL axes (~9× roll/pitch authority cut = catastrophic OOD); `--yaw-scale` is MULTIPLICATIVE (railed 3.14→~3.8 realized > the trained abort). NEITHER existing knob works.**
- Stats caveat: _percept collision_rate INCLUDES spin aborts — subtract spin_abort_rate (exit_spin is its own class). Success reads: DET vs twin · spin_abort→0 · target_detectable_duty ≫ 40.8% · exit_timeout/front flat.
- 🛑 **vdff1 FINAL (job 3304535): NEGATIVE + LATE COLLAPSE — n_passed_gates peaked 0.074 mid-run → ~0; final-20 collision 0.94, speed 13→3.9; FLIGHTCHECK FAILED. Both cheap multi-gate paths now dead (fresh 0.000 / warm-from-rammer collapsed). "Champion reward collapse-safe at 0.03" FALSE for dual-gate-warm. vdff1 ckpts POISONED.**
- 🟢 **vperc0 LAUNCHED (job 3305662): _percept boot(4k)→fullstack(12k) chain, PRECHECK=1, deployed sha-verified (5 rl files), watcher armed.** = the no-spin learnability probe (GT-attitude, DISPOSABLE per no-GT rule).
- 🟢 **estimator-faithful obs workflow FIRED (wf_7c3f0930-9d3)** — deploy-parity ESKF leveler + vel KF on simulated IMU; sensor constants measured from a5/a7 pad-idle tlogs; acceptance = reproduce the 7.4°/20° wire leveler error profile. Combined (_percept + no-GT) stages = the weekend flight-candidate retrain.
- 🚩 Tooling: connector `upload` verb = raw SFTP → hangs headless (Duo). Rebuilt `deploy_ego.py` (scratchpad) = base64 chunks over the daemon `x` channel + sha verify; needs `MSYS_NO_PATHCONV=1` from Git Bash. Sbatch logs: stdout `exec`s to `/scratch/network/fl3689/peregrine_vq2_ego_<RUNTAG>.out` (the /home slurm-*.out has ONLY the header — earlier "SLURM buffering" read was actually this).

## Vision relays (2026-07-10, via Fengyou)
- **P2 RESOLVED — bake TRANSFERS to VQ2:** raw vert bias −0.294 m (same sign as VQ1, ~0.04 larger; range-FLAT, slope +0.003 m/m ⇒ metric-translation form re-confirmed); post-bake residual **−0.046 m** (n=39, given-pose, single-gate map + flip prior). DO NOT re-tune (would de-tune VQ1's +0.004). VQ2 body-frame post-bake residuals FRD: fwd −0.130±0.274 · right −0.003±0.032 · down +0.053±0.038 — **the real numbers for the next measured-bias training contract** (sigma.json vert +0.195 = GATE-frame convention, different frame, not a contradiction).
- Our adapter implements EXACTLY the correct bake form (`[0,0,vert_offset] + R_cam_from_body().T @ t_cam`) — the "WRONG form" bug their doc warns about is what our audit verified against. Boresight NOT the A1 culprit (vertical axis; A1's basin is x).
- **Model M shipped:** branch `claude/vision-m-deploy-2026-07-10` @ 824bc70 + release `vision-m-2026-07-06` (.pt 168 MB sha c4a8de71…, sm_89 fp16 engine 46 MB sha b3c312ad…). Vision's own view: negreal42 arguably as good — M = shelf option pending A6 sweep.
- Vision-side bridge `scripts/rl_gate_obs.py` (their branch) applies the bake automatically — overlaps our adapter's job; reconcile/pick ONE path when the A1 fix lands.
- Measurement traps (cost them runs): characterize_perception default map = VQ1 6-gate (use single-gate map for task2); always pass the given-pose prior (IPPE 2-fold flips, std ~0.8 m otherwise).

Related: [[audit-ego-inc9-2026-07-09]] · [[index-vision-estimator]] · [[index-control-sim]]


## §COARSE-MAP+FENCE (2026-07-12) — the two between-gate / gate-0 walls + the deploy fix

**Trigger:** vpef-zbias flight (handoff/ego-flight-vpef-zbias-2026-07-12). Both ckpts PASSED gate 0 (gates=1, first ego gate passes ever) with a TEMP −0.5 m perceived-gate z-bias, then failed. Isolated TWO distinct walls.

**WALL 1 — gate 0 = perception loss from over-aggressive head-down (CORRECTS the earlier "over-climb" read).** Camera is a +20° up mount, so cam_elev = body_pitch + 20°. The policy slams nose-down to −50° to thrust forward → cam points −30° below horizon → LOSES the gate (pose_seen→0, conf decays) until it eases pitch back (~−44°/cam −24° re-acquires). Gate-0 leg = a pitch-driven lose/regain oscillation, NOT altitude. Real lever = a LESS head-down attitude (keep the gate in frame). The −0.5 m z-bias is NOT the fix (mechanism unknown, approach pitch unchanged with it on; it only exposed wall 2) — DROPPED, not re-implemented.

**WALL 2 — between-gate = the coarse-map horizontal turn prior is blank in deploy.** VERIFIED the `_pef` flight ckpts (vpefwh2/vpeffs0) trained through `dual_gate_boot_floor_pef` → `dual_gate_fullstack_floor_pef`: `course_n_gates=2`, `seg_len 10–20 m`, `yaw_jitter 0.25`, with a LIVE per-gate coarse-map sector (`build_coarse_map` → obs[9:11]) AND slot1 (WINDOW=2) populated. Deploy `sector_mode=auto` can only ever emit horiz=0 (the wire has no next-gate geometry) and slot1_enabled=False. So the policy flew as if every next gate were dead ahead → after gate 0 vpefwh2 chased a ghost up-LEFT into the env; vpeffs0 skipped the right-turn gates 1-2 and drove into straight-ahead gate 3. (Confirms gates 1-2 are off to the RIGHT.)

**Sign convention (VERIFIED, sector fed RAW into obs[9:11] — the virtual flip touches vel/rates/rel_pos, NOT the sector):** build_coarse_map horiz = sign(azimuth(outgoing) − azimuth(incoming)) in world XY Z-up; atan2 increasing = CCW = LEFT ⇒ **horiz +1 = next leg LEFT, −1 = RIGHT**; vert +1 = UP, −1 = DOWN. "gate 1 upper-right of gate 0" ⇒ sector[0] = (−1, +1). Cross-checked vs the flight (gates 1-2 to the right).

**FIX — commit `f7f44a3` (branch claude/ego-deploy-2026-07-09, both default-OFF, 31 tests pass):**
- `src/racer/ego_obs.py`: `EgoObsBuilderConfig.sector_mode='map'` + `coarse_map` (G,2). Latched on the active-gate-change boundary (BEFORE first fix), so the turn prior is live through the blind approach — training's static semantics. auto/zero byte-untouched.
- `rl/fly_rl.py`: `--ego-sector-mode map --ego-coarse-map <json>` (`_load_coarse_map`, validated {-1,0,1}); `--ego-pitch-clamp DEG` = one-sided nose-down perception fence in `policy_step` (past obs[4]≤−cap, `rate_frd[1]=max(rate_frd[1],0)` — block further nose-down, always pass nose-up). Signs EMPIRICAL from the flight log (nose-down ⟺ obs[4]<0 AND rate_frd[1]<0) ⇒ virtual_flip-agnostic. 🚩 FOOTGUN caught in-build: `policy_step` carries a `@torch.no_grad()` decorator — inserting a helper between it and the def silently steals it (broke the real-ckpt forward with a requires-grad error); keep helpers ABOVE the decorator.
- `configs/vq2_coarse_map.json`: seed `{"sector": [[-1,1],[0,0]]}` (gate0 = gate1 upper-right, Fengyou live obs). Fengyou hand-authors the rest AS HE SURVEYS (no map available a priori).

**LAUNCH (add to the existing fly_ego command):** run1 (isolate gate 0) = `--ego-pitch-clamp 30` ALONE (does gate 0 pass WITHOUT the z-bias?); run2 (between-gate) = add `--ego-sector-mode map --ego-coarse-map configs/vq2_coarse_map.json` (does it bank RIGHT → gate 1?). 🚩 **1-BIT SIGN CHECK:** if it banks LEFT (wrong way), negate the gate0 horiz to +1 in the JSON. **Lead candidate = vpeffs0** (dt-hardened fullstack; flew cleaner/straighter than the primary; its "skip gates 1-2 to the right" is the cleanest coarse-map demonstration).

**Caveats:** pitch clamp is mildly OOD (policy trained to reach −50°) — watch for boundary chatter / stalled forward progress; loosen to 35-40° if it stalls. dt: both flights (24.0/17.2 Hz) are INSIDE the `_pef` trained choked band (ticks_hi=4, loop ≥7.5 Hz) — dt is NOT the between-gate blocker. **DEFERRED (harder, next generation):** slot1 next-gate perception + gate-ID association/tracking (ByteTrack) so slot0 carries the CORRECT next gate; longer-course (2→N) retrain for full 20-gate robustness. The coarse map alone should bridge gate-1 acquisition (slot0 re-targets via active_gate_index).
