---
name: now-archive-2026-07-05
description: "VERBATIM blocks relocated from MEMORY.md during the 2026-07-05 size compaction (flight bring-up A15b-A19 mega-block, VQ2 wire recon, parked-inc8, pre-flight durables, worktree/boresight/gate-4 footguns full text) — zero-loss archive, compacted pointers remain in MEMORY.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: cde9b47c-f2bf-4c9a-a13e-90325b348f41
---

Relocated 2026-07-05 by the RL commander (MEMORY.md hit 26KB vs the 24.4KB read limit; doctrine =
route detail DOWN a layer, leave pointers). Every block below is VERBATIM as it stood in MEMORY.md.

## [was NOW 2026-06-30 · ACTIVE] VQ2 SLOW-LAP FLIGHT BRING-UP mega-block
🟢 **ACTIVE (2026-07-01) — VQ2 SLOW-LAP FLIGHT BRING-UP is THE current thread.** Footage-verification loop on branch **`claude/loving-galileo-92f020`** (HEAD **`db4e016`** = flight-stack `35561c7` + instrumentation (`81c323a` vision-timing + `a32938a` video-thread) + LOAD-CUT `180009b` + DRAIN-TO-EMPTY receiver fix `db4e016`, all pushed; NOT merged to main). 🟢 **A15b (pre-warm CONFIRMED — worst-work 2873→~515 ms, live view no longer freezes; observation UNBLOCKED, the #1 blocker cleared).** Dynamically-confirmed diagnosis: **pursuit's vertical handoff commanded a SATURATED +1.5 rad/s NOSE-UP the instant it engaged → drone climbed into the ceiling** (flight 2: pitch −15.5°→+15° over 12 ticks). 🟢 **ROOT-CAUSED + FIXED (`35561c7`, `ff_owns_horizontal`): the decoupled controller's horizontal velocity-damping term `−kd_vel·vel_xy` was chasing the DEAD-RECKONED (fictional, growing ~1.8 m/s) horizontal velocity — carried in via vertical-align's `velocity_ned=[0,0,vz]` — which swamped the +1.2 forward feedforward, flipped `a_h` BACKWARD, and tilted the target nose-UP. Softening (kp_att/slew) couldn't fix it: wrong-direction target, not stiffness. FIX: when a feedforward `accel_ned` owns the horizontal, DON'T apply the velocity_ned term to the horizontal axes (alt-hold still reads velocity_ned[2] for vz). Offline-repro-confirmed +2.701→+0.595, climb thrust preserved; default-off=VQ1 byte-id; green_gate GREEN-except-known.** 🟢 **A16 (2026-07-01, HEAD `35561c7` flight-stack, branch `vq2-slowlap-a16-2026-07-01`): `ff_owns_horizontal` CONFIRMED — the nose-up-into-ceiling is GONE.** Pursuit now holds nose-down (pitch −17°→−8.3° monotonic ease toward the −7° lean; pitch-rate cmd +0.47→+0.08 decaying vs A15b's +1.5 saturated). 17 pursuit ticks, valid_empty=0. ❌ **But flight still CRASHES ~3.4 s (gates=0) and the flight phase is UNOBSERVED — frame-drops black out the view the INSTANT the race goes GREEN** (pilot corrected: the long "sitting" stretch was mostly the pre-GO waiting room; drops onset at GO when the armed loop + full per-frame vision pipeline `heading_vp`+`manhattan_lines` runs under load = STATE-TRIGGERED by flight-active). **Yaw fix (337c554) STILL UNTESTED** (crashed before the off-axis gate 2; pursuit stayed gate-centered). Perf: 13.8 Hz (worse than A15b 18.1), worst-work 504 ms, 8.5% over. 🚩 **RENDER-OVERLAY GOTCHA: `scripts/render_vision_video.py` overlays the CLASSICAL red_glow detector, NOT the YOLO that flew — the boxes on onboard.mp4 mislead** (the pilot's "wrong model" read was the overlay; flight used YOLO, valid_empty=0). 🟢🔑 **A17 (instrument-only) CRACKED the freeze: it's a FRAME-SUPPLY COLLAPSE from the vision compute — `detect`=150 ms (7× the ~21 ms expected!) + `vp_yaw` 67 + `floor_height` 37 ≈ 256 ms/tick synchronous vision starving the receiver; DETECT ran only 3× in 22 ticks → seeker got ~3 gate obs in ~2 s → the flight was DOOMED regardless of control. Restoring frame supply is a HARD PREREQUISITE.** (Video `max_gap` 1051 ms, `pub` 0.1 ms = starved not slow; 150 ms detect = CPU-fallback or GPU-contention w/ the co-located sim, AND it runs TWICE/frame.) 🟢 **LOAD-CUT FIX LANDED (`180009b`, pushed; HEAD): (1) detect DEVICE — resolve cuda-if-available + `model.to` + LOG the resolved device (`[detector] ... device=...` → A18 reveals if 150 ms was CPU-fallback); (2) SHARE the double-detect (`detect_cached`, once/frame_id, transparent memoization); (3) DECIMATE vp_yaw N=5 + floor_height N=3 (`NavigatorConfig.*_decimate`, default-1=byte-id, vq2_case_c set); (4) `[video-thread]` wire snapshot UNCONDITIONAL; (5) `--seeker-detector` default → yolo + fail-loud guard (rejects the RL-actor `.pth`; red_glow opt-in). green_gate GREEN-except-known (1545 passed); new `test_vision_compute_cuts.py` pins it.** 🛠️ Recovered INLINE after the implementing opus agent's process DIED mid-green_gate — work was complete in the tree, reviewed + re-verified (the death hazard again; inline from here). 🟡 **A18 RESULT (2026-07-01, clean GO, 44 s flight, branch `vq2-slowlap-a18-2026-07-01`): load-cut HALF-worked — loop UN-CHOKED 10→28 Hz (decimation ✓), but FRAME SUPPLY STILL BROKEN (the real blocker).** `detect`=129 ms on **cuda:0 (confirmed GPU, NOT a CPU-fallback)**, ran only **4× in 44 s**; `tsv=inf` all flight → seeker got ZERO poses → `cmds=0 pursuit=0` → drone flew an UNPILOTED egress beeline through gate 0 then STALLED (pilot: "straight line, no veer = not piloted; the sim renders the world regardless of whether our detector reads it — a coherent view is NOT evidence vision worked"). 🔑🔑 **WIRE SMOKING GUN: `datagrams=910046 completed=256 dup=234275 (26%) evicted=2837`, published-frame gaps GROW progressively 360 ms→40 s** = an ACCUMULATING BACKLOG, not a steady drop. Mechanism (co-located sim on loopback → NO real loss → the sim's ~14× fragment-resend is redundant flood; ~20,700 datagrams/s): the receiver drowns / falls progressively behind → frames never complete. `decode_failed=0` → loss is UPSTREAM of decode (missing fragments, not corrupt). 🎯 **BLOCKER = FEED THE DETECTOR ~30 usable frames/s; nothing pilots until then (Fengyou directive).** 🟢🔑 **ROOT CAUSE CRACKED + OFFLINE-REPRODUCED (workflow `wf_03f4a6b8`): the UDP receiver did ONE `select()`+`recvfrom()` PER datagram → drain ceiling ~6,600 dg/s vs the sim's ~20,700 dg/s flood (~14× resend, loopback=no loss) → drains 1/3 → buffer overflows during each 129 ms detect stall → fragments dropped → backlog grows UNBOUNDED (=A18's 256/910046 + 40 s gaps). Bigger SO_RCVBUF ALONE is a TRAP.** 🟢 **FIX LANDED (`db4e016`, pushed): DRAIN-TO-EMPTY (tight `recvfrom` until EWOULDBLOCK, bounded 8192/batch, evict once/batch) → ceiling 6,600→~50k/s; + SO_RCVBUF 4→64 MiB (only WITH the drain). Offline-proven (exp5: completion 3-71→256-263, gaps flat 23 ms, evicted 0). Test `test_frames_drains_buffer_in_one_wake` pins it; green_gate GREEN-except-known (1546 passed).** `pursuit=0`/`n=4` = NOT a regression (detect_cached not decimated; the 256-vs-4 is a MEASUREMENT ARTIFACT — `[video-thread]` counts from connection-open incl. the pre-GO waiting room). detect=129 ms on GPU = genuine sim contention (config identical to the 21 ms benchmark); imgsz=320/416 + half cut HELD for A20 (accuracy risk, test separately). 🎯 **A19 = fly the drain fix (clean GO): pass = completed ≥~25/s, evicted ≤~50, gaps>1s=0, ring-gap FLAT; detect n→hundreds; pursuit>0 + tsv finite.** Drain fix alone feeds the detector ~7 Hz (capped by 129 ms detect until A20) ≫ A18's 0 → pursuit engages → FINALLY observe the flight + gate-2 yaw. Frame supply is THE prerequisite; do NOT chase control until the detector is fed. 🚩 **TWO standing COMMANDER-investigate items (from A15b):** (a) **FRAME-DROP / worst-work suspect — name CONFIRMED (2026-07-01): the per-frame VP/Manhattan stack in `navigator._maybe_run_vision` = `vision/heading_vp.py` `estimate_heading` (VP RANSAC, `vp_yaw_ransac_iters=256`) + `vision/manhattan_lines.py`, run every frame alongside YOLO detect + PnP.** Prime suspect for the ~19% recorder starvation; the 468–515 ms events are OCCASIONAL spikes (not steady — 256-iter VP is cheap), so the likely culprit is an occasional line-extraction/inference blowup on cluttered frames — needs per-STEP timing on a real run (A16 data) to pin the exact step before optimizing; (b) **standing render request: a per-frame "command sent" overlay** (nearest-tick body_rate vs frame timestamp) for cadence-vs-framerate visibility — build into a handoff-dir render tool, NOT the canonical `scripts/render_vision_video.py`. 🚩 **LAUNCH RITUAL (Fengyou, A15b): launch `fly_rl` FIRST → wait for its "Waiting PASSIVELY" banner → THEN GO** (GO-before-launch = a late-join where pursuit never releases; banked to sim-ops). MILESTONES: **A5 = FIRST GATE PASSED**; **A10 = self-localization TRUSTWORTHY (28 s)**; **A15 = recall SOLVED (`valid_empty` 74%→0%) + thrashing gone (337c554 yaw-frame + 8b702e7 softening held)**; **A15b = pre-warm (`12f366b`) confirmed → observation unblocked**. (A15's "sat in start gate" read was RETRACTED — never observable under the warmup freeze; the A5→A15 pre-warm chain detail = [[index-vision-estimator]] §VQ2 SLOW-LAP.) 🚩 **FULL STATE + resource map + seams + the fix chain = `handoff/commander-handoff-2026-06-30.md` (Gen 6→7 boot briefing).** Saga detail → [[index-vision-estimator]] §VQ2 SLOW-LAP. TWO active threads: flight-controller bring-up + vision-training. Pending after A16 (which confirms the `ff_owns_horizontal` fix): does pursuit HOLD nose-down + thread gate 1, then the 337c554 yaw un-mirror on off-axis gate 2 (both were blocked behind the nose-up = the old "Lead 1/blocker #3", now fixed pending A16); frame-drop/Manhattan-VP check; residual perception hardening (Blender scaffold only); loop-rate/choke. 🚩 **Background agents DIED repeatedly this session (process exits) — on a death, CHECK the working tree for recoverable uncommitted work before redoing; go INLINE for load-bearing tasks.** 🚩 green_gate carries ONE known UNRELATED pre-existing RED (`test_diagnose_session`, handoff-slimming fixture gap — don't chase; "GREEN-except-that" = pass). Seams for vq2-only tuning: `deploy_profile.vq2_case_c` `seeker_overrides`→GateSeekerConfig + `controller_overrides`→make_seeker_controller (both default-off, VQ1 byte-id). venv = MAIN checkout `…/Anduril/.venv` (NOT the worktree).

## [was NOW 2026-06-27] VQ2 SPEC + LIVE WIRE RECON block
🟢 **VQ2 SPEC DROPPED 2026-06-24 (VADR-TS-003) — case-C CONFIRMED LOAD-BEARING. 🟢 VQ2 LOAD-DAY LIVE RECON 2026-06-29 (build 1.0.3379): all §9.3 blocks CONFIRMED ON LIVE SIM — ATTITUDE/LOCAL_POSITION_NED/ODOMETRY/GLOBAL_POSITION/GATE_INFO BLOCKED in BOTH training AND competition (byte-identical message sets; the "training may expose more" hedge REFUTED). REMAINING wire = HEARTBEAT(10Hz) + HIGHRES_IMU(117Hz, accel+gyro ONLY — 🚩 NO MAG, NO BARO; fields_updated=63, bits 0-5) + 30Hz JPEG cam (udpin:14550/video:5600, 24-byte header unchanged) + ENCAPSULATED_DATA=RACE_STATUS(4Hz, active_gate_index PRESENT — gate ordering SOLVED on wire) + ACTUATOR_OUTPUT_STATUS(95Hz, 4 motors); TIMESYNC = RESPONSE-ONLY. 🚩🚩 NO MAGNETOMETER/NO BAROMETER = YAW + Z MUST COME FROM VISION (ESKF demotes to roll/pitch leveler; EqVIO becomes estimator SPINE). 🟢 CONTROL-MODE HANDSHAKE SOLVED: BODY_RATE `SET_ATTITUDE_TARGET` (type_mask=0b10000000) + ARM = working CTBR path; earlier tumble was wrong-message (`SET_POSITION_TARGET_LOCAL_NED` ignored in ACRO). TRACK = "Now You See Me, Now You Don't" (dark warehouse, stations 01-20). Full recon → [[index-vision-estimator]] §VQ2-WIRE-RECON-2026-06-29.** ❓RESOLVED: (a) training==competition wire — YES (byte-identical); (b) track NOT randomized per-load (deterministic confirmed); (c) training does NOT expose blocked telemetry. Spec banked at repo root `260624_Technical_Spec_0003.pdf`. → [[reference-sim-interface]] · [[reference-competition-materials]]

## [was NOW 2026-06-27] PARKED RL WORK (inc8) block
📁 **PARKED RL WORK (inc8, from the 2026-06-20 DORMANT period — HELD, do NOT prune).** The inc8 RL speed-increment is paused while we bring up the VQ2 slow lap. Preserved: branch **`inc8-deterministic-retrain`** (05809ba) + a **STASHED reach-rate result** (`claude/funny-nash-d07d7b` @ 0edea93 + `handoff/inc8-reach-rate-2026-06-19/`) = "levers #1+#2 lifted det reach 0.467→~0.72 @ r=0.30", HELD pending Fengyou's review. NEXT inc8 lever (when resumed) = RL to lift gate-4 reach/pass-rate. Assets: detector clean-ensemble (282abb9) + C2 gate-relative estimator chain (05ed750, MERGED). 🚩 Footgun: **ShadowPC/other clones push FEATURE branches, NEVER `main`** — reconcile any divergent main via `origin` deliberately, never force. Prior handoffs archived: `handoff/commander-handoff-2026-06-18.md`.

## [was NOW 2026-06-27] PRE-FLIGHT VQ2 DESIGN + inc8-RL SAGA durables block
📁 **PRE-FLIGHT VQ2 DESIGN + inc8-RL SAGA (mid-June 2026 — mostly SUPERSEDED by the empirical A5→A15 flight bring-up; full detail in the sub-indices, load-bearing footguns duplicated below).** Durable results worth knowing: **C2 gate-relative estimator chain MERGED** (05ed750: RewindKF + gate-relative +L + relinnov gate; gated OFF = VQ1 byte-id); **obs contract FROZEN** (d5, obs_dim=20, [17:20]=confidence+age; critic get_state 37-dim); **VQ2 detector clean-ensemble 282abb9** (training-loss lever, now DEPLOYED via `--seeker-weights` — see ACTIVE); **gate-4 is a REACH/PASS-RATE problem** (real σ_p0 bar ≈0.15 lat; the ≲0.08 "closure bar" was a margin_envelope DOUBLE-COUNT — RETRACTED); **binding margin = vertical boresight/attitude bias ε_vert≈0.215 m** (δ_map-proven a real sighting bias, +L does NOT cancel, CALIBRATABLE), NOT contact radius; **inc8 = case-C deployability** (self-localizing, not a speed jump); **camera mount FIXED at 20° spec-exact** (VFoV mislabeled → fy=320). Full → [[index-vision-estimator]] · [[index-rl-training]] · [[project-fullstack-burn]] · [[reference-competition-materials]].

## [was Operating directives] COMMANDER MODEL rev 2 (superseded by rev 3)
🚩 **COMMANDER MODEL (rev 2, 2026-06-27):** ONE persistent commander that DELEGATES IN THE BACKGROUND; copy-paste relay RETIRED **except ShadowPC** (the ONLY remaining paste; it runs the competition sim). Laptop → spawn Agents/Workflow directly; Adroit → the adroit-connector (`…\Adroit\adroit-connector\adroit.py`, paramiko SSH). Commander keeps SOLE memory authority + merge gate. Retire the last paste = build a shadowpc-connector. → [[index-strategy-meta]] §SHADOWPC-CONNECTOR.

## [was footgun] GATE-4 MARGIN FOOTGUNS full text
🚩 **GATE-4 MARGIN FOOTGUNS (PARKED with inc8 — detail all in [[index-vision-estimator]]):** contact radius 0.38 = empirical crash HALO not geometry (central 0.30); binding margin = vertical boresight/attitude bias **ε_vert≈0.215 m** (δ_map-proven a real sighting bias, +L does NOT cancel, CALIBRATABLE), NOT contact radius; gate-0 near-level is the WRONG posture for gate-4 (use gate-3 steep-crash data); camera mount FIXED 20° spec-exact (VFoV mislabeled → always fy=320); common-mode-across-gates ≠ +L cancellation (only a direct map-vs-truth pin distinguishes; δ_map chose "genuine bias").

## [was footgun] WORKTREE HYGIENE full text
🚩 **WORKTREE HYGIENE (2026-06-14):** the harness AUTO-creates one isolated worktree per spawned session — that ALONE satisfies non-interference. Workers MAY make their own worktrees freely (rev 2, 2026-06-14 — Fengyou's call: isolation > clutter, and enforcing no-add is needless friction). The overall commander OWNS consolidation — merge EVERY worker branch into main at the gate, then prune MERGED branches/worktrees so the list stays manageable. The `.claude/worktrees/*` pool is harness-managed + RECYCLES (observed: an "empty" worktree was reclaimed as oneoff-regsuite) — NEVER manually prune it (racy, can knife a launching session); only `git worktree prune` (gone dirs) + remove confirmed-DONE manual worktrees. Code-workers REPORT their branch name UP; the overall commander keeps the branch↔role ledger, merges, and prunes BRANCHES on merge. → [[index-strategy-meta]] §worktrees

## [was footgun] BORESIGHT FORM-DISCRIMINATION full text
🚩 **BORESIGHT FORM-DISCRIMINATION NEEDS WIDE-SEPARATED RANGES (P1, 2026-06-14):** the head-on ε test must use **≥2 ranges with Δrange ≥ ~10 m** (e.g. 12 m & ≥24 m) to separate ANGULAR (∝range) from METRIC (flat) — "≥3 ranges" alone is INSUFFICIENT (closely-spaced 20/22/24 = DEGENERATE → pins magnitude, NOT form). g2/g4 at ~4 m only if per-fix σ_vert ≤ ~0.005 m (heavy frame-averaging). FORM is load-bearing: it sets WHETHER −ε goes in the extrinsic rotation (angular) or the +L lever (metric), AND the ESKF watchdog (rotational δB can't absorb a metric offset). Bake site = frames.R_camera_from_body (audit: no code bug). → [[index-vision-estimator]] §P1 BORESIGHT AUDIT + CALIB-V2
