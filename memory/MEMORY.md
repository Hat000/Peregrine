**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-18.)

## NOW · 🟢 VISION/FLIGHT THREAD — VQ2 SLOW-LAP BRING-UP (vision-stack commander's domain)
- 🟢 **Branch `claude/loving-galileo-92f020` HEAD `db4e016`** (= `35561c7` ff_owns_horizontal + instrumentation `81c323a`/`a32938a` + LOAD-CUT `180009b` + DRAIN-TO-EMPTY `db4e016`; pushed, NOT on main). Chain: A15b pre-warm ✓ (observation unblocked) → A16 nose-up-into-ceiling GONE → A17 cracked the freeze (synchronous vision compute starves the receiver; detect 150 ms ×2/frame) → LOAD-CUT landed → A18 loop un-choked 10→28 Hz but frame supply STILL broken (UDP receiver drained 1 datagram/wake: ceiling 6.6k vs the sim's 20.7k dg/s resend flood) → DRAIN-TO-EMPTY fix landed (offline-proven; SO_RCVBUF 64 MiB only WITH the drain). 🎯 **A19 = fly the drain fix: pass = completed ≥~25/s, evicted ≤~50, gaps>1 s = 0, detect n→hundreds, pursuit>0 + tsv finite. Frame supply is THE prerequisite — do NOT chase control until the detector is fed (Fengyou).** Yaw fix `337c554` still untested. detect=129 ms on GPU = sim contention; imgsz/half cut HELD for A20. Milestones: A5 first gate · A10 self-loc trustworthy · A15 recall solved. 🚩 Standing: (a) per-STEP timing to pin the Manhattan-VP worst-work spikes; (b) "command sent" render overlay (handoff-dir tool, NOT canonical render script). 🚩 LAUNCH RITUAL: `fly_rl` FIRST → "Waiting PASSIVELY" banner → THEN GO. 🚩 RENDER-OVERLAY GOTCHA: `render_vision_video.py` overlays red_glow, NOT the YOLO that flew. 🚩 Background-agent DEATH hazard: on a death check the tree for recoverable work; go INLINE for load-bearing. venv = MAIN checkout `.venv` (not worktrees). FULL saga verbatim → [[now-archive-2026-07-05]] · `handoff/commander-handoff-2026-06-30.md` · [[index-vision-estimator]] §VQ2 SLOW-LAP.

## NOW · 🟢🟢 TRACK A — BEAT vpeffs0 (2026-07-13) — SSOT [[track-a-beat-vpeffs0-2026-07-13]]
- 🎯 **GOAL (Fengyou /goal):** match vpeffs0 threading BUT no velocity runaway · NO deploy walls · zero yaw oscillation · very high success on FULL 20-gate course (randomized distance + xy shifts). 🚩 **BEAT vpeffs0, do NOT reproduce it** — warm from vpeffs0's ckpt + annealed improvements. 🚩 **PRESERVE the yaw→gate-in-view→altitude coupling** (no abs-alt sensor; gate holds altitude; anti-dither = penalize yaw JITTER only, NOT gate-tracking yaw; pitch's lever = rw_perception NOT the cap). → [[feedback-beat-dont-reproduce]] · [[feedback-preserve-yaw-altitude-hold]]
- 📁 07-13 framing + arm portfolio SUPERSEDED → [[track-a-beat-vpeffs0-2026-07-13]] · [[feedback-beat-dont-reproduce]]. Durable: FRESH boot FAILS despin (vwhb0 scaffolding) · **Cam FOV 90°H/58.7°V (fx=fy=320)**.
- 🏁 **RATCHET THREAD CLOSED 2026-07-18 — full saga+numbers SSOT [[replay-ratchet-2026-07-17]].** Durables: 🚩 **PIN "GO ≥3 s post-reset" ALL flights** (wake-0 variance ↓1000×) · settled champion-ckpt baseline **{2,2,1,2,2}** (g0 deterministic 58 ms; champion-5 = tail; wall = g2 descend) · **tape RETIRED** (open-loop horizon 3.15 s; offset = takeoff-state; → #84) · 🛑 **ARRESTOR RETIRED by Fengyou** (arrests fine, disturbs surrounding control; @271880c parked → #85; reinforces [[feedback-no-deploy-bandaids]]) · 🚩 **champion recipe TRUTH = PANEL LOG not meta.json** (TRT M-engine + assist 1.3 + log-echo map; **configs-blob map rows 3/4 CORRUPTED post-flight; canonical = `configs/vq2_coarse_map_champion_logecho.json`; panel currently runs divergent working-tree map**) · bank path = settled GO + closed-loop policy + retries + better policies.
- **Controls 629d2a2 default-off (vel-cap/att-cap/anti-dither) · coarse-map deadband retune staged.** 🚩 RECIPE (.hydra): ticks_hi=1, yaw 0.7, blur=false · deploy `push_file.py` · tracer `rl/ego_rollout_trace.py` (read RAW `self.env.env`; wrapper blocks `_`-attrs).
- 🛩️ **FLYABLE DOCKET (ShadowPC, deploy branch `claude/ego-deploy-2026-07-09`) → [[track-a-beat-vpeffs0-2026-07-13]] §DOCKET.** Releases (clamp PER-RELEASE, mixing=OOD): `ego-ckpts-vpef-2026-07-12` (vpefwh2+vpeffs0, 0.7) · `ego-ckpts-tracka-2026-07-13` (aw0+m8, 0.35) · `ego-ckpts-tracka-yaw07-2026-07-13` (m8b+rs0+aw1, 0.7) · `ego-ckpts-vpef8gate-2026-07-14` (vpef8nc, 0.7 — CRASHES deploy) · `ego-ckpts-v1-2026-07-18` (v1As0+v1Rs0 @upd17500, 0.7). `gh release download <tag>`→fly_rl (≥30Hz, kill orphans→banner→GO, zero-contact=validity). GT-only NOT published.
- 🔬 **Track-A stall forensic (2026-07-13/14) → SSOT [[trackA-stall-forensic-2026-07-13]].** Durable ops footguns: fence ONLY the axis the course DOESN'T use (pitch free; roll/speed/vel-cap NO); select on exit_frame/HAZARD/n_passed NEVER value; multi-seed; monitor TB not stdout (`scratchpad/tb_parse.py`); speed-gov REJECTED → [[feedback-no-deploy-bandaids]].
- 🟢🟢 **v1 CAMPAIGN — SSOT+all numbers [[perception-sim2sim-gap-2026-07-15]].** Durables: **ffs0=champion/warm-base** · 8-gate fine-tune ADDED the yaw hunt · ceiling=SPEED ACCUMULATION · "flat-plant=wall" OVERTURNED-CLOSED (trained EXPANSIVE; **NO more plant tuning**; faithful_rate=~2% ride-along, never credit it). **v1 swept (3312393; numbers SSOT) → RELEASE `ego-ckpts-v1-2026-07-18` → 🛑🛑 PICK-FLIGHTS FAILED (@6bd989e): deploy A=1 gate, R=1/0/0/0 vs settled baseline {2,2,1,2,2}; wire cmd-yaw 0.64 absmean/4.4-6.8 flips vs champion 0.151/2.2 ⇒ SIM YAW_EVAL TRANSFERS 1:1; my 4.0-flips-gate override WRONG (REINSTATED + NEW cmd_absmean≤0.3 gate) → [[feedback-training-faithful-deploy]] (new lineage flies TRAINING action space; champion recipe = champion only).** 🔬 **TWIN FORENSICS (scratchpad REWARD-LEDGER.md + FLIGHT-FORENSICS.md; detail SSOT): RUNAWAY = DISCOUNT arithmetic (break-even crash-risk 5-13%/s ⇒ zero-margin edge-riding; lineage-wide, NO brake anywhere, brake punished 3×) · YAW = ~6 Hz limit cycle, gain 12-16× champion, satur 68-90% vs 0% (scanning refuted; champion quiet = 2-gate luck) · pitch-free A = −65° dive (fence was masking) · velocity already obs[0:3], unpriced.** ➡️ **v1.5 (@ca9da6c+191b58f, warm-FFS0 — v1 gain poisoned; abort 12/satur 7.5/duty+band/jerk/dither; pitch FREE): smoke 5/5 → 🏆 ARMS+CENSUS 3312994 DONE 07-19 all-clean seed-replicated (detail SSOT): Q_s1 final 4.46 g/1.33 flips/0.143 abs/0.09% satur, max_speed 11.97 never-touched-abort = LEAD · W_s0@periodic_prev 4.28/0.66/roll 33.4 = quiet fallback (beats own final +0.55 g) — all quieter than champion 2.45, every gate w/ margin. → RELEASE `ego-ckpts-v15-2026-07-19` PUBLISHED (v15Qs1_final e3bcd9ab + v15Ws0_periodic_prev 29f493e2, md5-verified, clamp 0.7). 🎯 AWAITING PICK-FLIGHTS — TRAINING-FAITHFUL: pitch FREE, yaw 0.7, settled GO ≥3 s, log-echo map, M-engine+assist 1.3; Q_s1 first, labels v15pick_*; score vs {2,2,1,2,2}; sim wire-prediction absmean 0.14 / flips 1.3 (Q) · 0.7 (W), ~1:1 transfer.** 🚩 pitch sign (code-authoritative): `rate_frd[1]=+a_pitch` ⇒ **a_pitch<0 = nose-DOWN** · 🚩 pose-blind wire: telemetry=DEFENDANT, ask human eyes · 🚩 0-byte agent transcript ≠ dead → TREE mtime; ≤2 heavy agents.

## 📁 EGO/inc9 THREAD (ARCHIVED — produced vpeffs0, the Track A base) — SSOTs → [[ego-deploy-contract-2026-07-09]] · [[audit-ego-inc9-2026-07-09]] · [[vq2-rl-commander-2026-07-05]]
- 📁 **Pivot gate-frame → EGOCENTRIC position-free (21-dim, critic 16); stack `claude/optimistic-chaum-6c893b`; SSOT `docs/vq2-egocentric-gen/DESIGN.md`.** 🚩 DIAGNOSE FROM TRAINING METRICS (renders untrustworthy) · isotropic 3D homing = AUGMENT not replace · TERMINAL EQUALIZATION 100/100/200 · deploy tooling `push_file.py`. 🚩 **L16: absent hook prints on a LIVE job ≠ inert (SLURM buffers stdout) — adjudicate from a COMPLETED log/TB.**
- 🛑🛑 **AUDIT 2026-07-09 → [[audit-ego-inc9-2026-07-09]]:** perception = estimator-corruption-limited · **multi-seed MANDATORY (~2.8× seed variance)** · KF leverage = bias-calib+availability NOT filtering · **VQ2 CLOSES ~MID/LATE JULY ⇒ BANK-FIRST (finish 20 ANY speed; §9.2/9.4 unlimited attempts, fast-invalid FREE)** · KEEP YOLO-pose+PnP+bias-bake+APPO · **NO GT on the wire · P2 bake TRANSFERS, do NOT re-tune.**
- 📁 **DESPIN lineage → [[ego-deploy-contract-2026-07-09]] §DESPIN:** vwhb0→vpefwh2→vpeffs0; ticks_hi=1 flies best; ego ckpts NO sidecar; TAKEOFF ASSIST `4343aba`. 🚩 L18 deploy set=`git show --stat` · L20 watchers check x rc.
- 🛑 **Two DEPLOY walls (Track A makes intrinsic): (1) gate-0 = perception loss from over-head-down pitch (lever = LESS head-down / rw_perception, NOT a cap); (2) between-gate = coarse-map (canonical NOW the log-echo file; horiz −1=RIGHT; banks LEFT ⇒ negate) → [[ego-deploy-contract-2026-07-09]] §COARSE-MAP+FENCE.** Gate-frame superseded → [[vq2-rl-commander-2026-07-05]].

## Background (mostly superseded; verbatim → [[now-archive-2026-07-05]])
- 🟢 **VQ2 SPEC + WIRE RECON → [[reference-sim-interface]]:** HIGHRES_IMU 117 Hz accel+gyro only · 30 Hz JPEG · RACE_STATUS 4 Hz w/ active_gate_index · dark warehouse 01-20.
- 📁 **PARKED inc8 (HELD):** `inc8-deterministic-retrain` (05809ba). 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.** → [[index-rl-training]]. VQ1 PASSED; inc7 judged best.
- 📁 **Durables:** C2 gate-relative MERGED · inc8 obs-20 frozen · detector clean-ensemble 282abb9 · live research: asym-critic, recon-map MOAT, bootstrap-vision-RL → [[vq2-stack-research-2026-06-27]] · [[now-archive-2026-07-05]].

## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read FIRST on boot; EDIT LAST before handoff (add the lesson, prune the stale, log your generation). MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL (rev 3, 2026-07-05, Fengyou): DOMAIN-SPLIT — VISION-STACK commander (ShadowPC side) + RL commander (laptop session), each SOLE memory authority over its OWN domain; commander↔commander relays via Fengyou, ONE copy-paste code box.** rev-2 mechanics stand (laptop → Agents/Workflow; Adroit → adroit-connector `serve`/`x`). verbatim → [[now-archive-2026-07-05]] · [[index-strategy-meta]] §SHADOWPC-CONNECTOR.
- 🚩 **CANARY (rev 2, 2026-06-14):** persistent COMMANDER sessions address **Fengyou** by name in EVERY message (missing name = context degradation → rotate). Workers don't need it.
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages; bank IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt: SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA req + escape hatch + report path.
- 🚩 **DELEGATE BUILDS TO AGENTS (2026-07-12, Fengyou):** multi-file implementation (features, edits, test authoring) = spawn agents/Workflow to edit+test+verify; commander thread = judgment/diagnosis/review/relay only. Ultracode on ⇒ default to Workflow/Agent for builds. → [[feedback-delegate-builds-to-agents]]
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees. 🛑 **NO-SPIN = HARD REQUIREMENT (2026-07-10): enforce by CONSTRUCTION (fatal all-axes spin aborts + realized-yaw clamp), NEVER incentive shaping; high-DET spinner ≠ champion; NO energy penalties.** → [[feedback-no-spin-hard-requirement]] 🛑 **NO-GT IN ACTOR OBS (2026-07-10): actor sees ONLY estimator-faithful state (deploy estimators on simulated sensors); GT legal only in critic/reward/terminations; GT-obs ckpts never fly.** → [[feedback-no-gt-actor-obs]]
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Workers surface NEW parked items in MEMORY-DELTA; commander files them.
- 🚩 **FLIGHT CURRICULUM: close the loop SLOW first, ramp after (2026-06-29).** 🟢 A5 = first gate passed (map-free, no mag/baro). → [[feedback_slow_is_smooth]] · [[index-vision-estimator]].
- 🚩 **BLENDER VQ2 DATA PIPELINE:** render harness matched to glowing-red/dark-warehouse appearance — HIGH-FIDELITY ONE-LOOK, not broad DR. → [[project_blender_vq2_data_pipeline]]
- 🚩 **MOONSHOT: championship-fast, not just finishing.** North star = Swift (Nature 2023). Slow bootstrap → perception hardening → RL speed ramp. → [[project_moonshot_speed]]
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly (no ceremony); the C6/POC hold is a SEQUENCING gate (Fengyou's), not risk. Validity rule (zero gate contact) + sim time = the only flight constraints. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN.**
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]] §VQ2-WIRE-RECON.
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) → stream `SET_ATTITUDE_TARGET` BODY-RATE (mask 0b10000000, FRD, thrust [0,1]) ≥30 Hz; `fly_rl.py` drives. Use `DroneState.gyro_body`, NOT `angular_rate_body`. → [[index-vision-estimator]].
- 🚩 **CRITIC FOOTGUN: use `algo=appo`** (ppo leaves the privileged critic unconnected = seed collapse; critic input_dim **37**) → [[index-rl-training]].
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7). `fly_rl.py` default ckpt retired → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG = self-consistent alias — DO NOT "fix"** (tail-first training + virtual π flip compensate; camera_flip touched the emulated camera only) → [[index-control-sim]].
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session. → [[index-control-sim]].
- 🚩 **`main` CANONICAL · 1091 tests collected (2026-06-20)** · `*.pt`/`*.pth` + ckpt JSONs + data/runs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`burn-artifacts-*`, `gh release download`), NEVER git-add. → [[project-fullstack-burn]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG — 24 m flip). Pinned by tests/test_obs_sign_faithfulness.py. Future estimator→obs MUST deliver +L. → [[index-vision-estimator]].
- 🚩 GATE-4 margin footguns (parked w/ inc8) → [[now-archive-2026-07-05]] · [[index-vision-estimator]].
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]].
- 🚩 **WORKTREE HYGIENE:** auto-worktree per session = isolation; commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` pool RECYCLES — NEVER manually prune; only `git worktree prune`. → [[index-strategy-meta]] §worktrees.
- 🚩 **BORESIGHT bake = METRIC TRANSLATION** (vert_offset_m −0.25; form-discrim needs Δrange ≥~10 m) → [[index-vision-estimator]] §P1.

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🧠 **[[vq2-rl-commander-2026-07-05]]** — RL-commander thread SSOT (audit, B1 fixes, job 3295856, fidelity plan, vision-commander asks).
- 🗄️ **[[now-archive-2026-07-05]]** — verbatim blocks relocated in the 2026-07-05 compaction (flight saga, wire recon, parked-inc8, durables, footgun full texts).
