**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-27.)

## NOW · 🛑 VELOCITY CHANNEL + CENSUS + v2.0 (2026-07-27) — SSOTs [[velocity-channel-2026-07-27]] · [[failure-census-2026-07-27]]
- 🛑 **WITHDRAWN — "obs lateral-velocity carries NO INFORMATION" was a ROTATION ARTIFACT** (see the de-rotation footgun below). De-rotated it reads **+0.93 @4-8 m, +0.99 inside 4 m**; commander corroborated by stratification (slope climbs monotonically as |yaw rate| shrinks; negative **only** in the top bin, 0.9% of 24 799 pairs). 🚩 Re-measure any case-study claim built on un-de-rotated lever differencing.
- 🛑🛑 **ROOT CAUSE (CORRECTED) — TRAIN/DEPLOY PARITY GAP + GT LEAK on obs[0:3]** (structural, no stats). **DEPLOY**: actor vel = `nav_state.velocity_ned` (`fly_rl.py:2857`), map-free navigator, `tsv` never finite (`:1920`, 100% of ticks) ⇒ **open-loop strapdown, ZERO velocity reference**. **TRAINING**: `vel_model` defaults **`legacy`** unless `env.ego_faithful=true` (`peregrine_racing_ego.py:1353`) — seeds at **exact truth** (`ego_estimator.py:471`), pulls **15% toward TRUE body velocity at every fix** (`:815-819`) on **92.3%** of ticks ⇒ truth-tracking servo, error ~0.01 m/s. 🛑 **NO launcher v16→v20 arms `ego_faithful`** ⇒ **~100× gap** AND **GT in the actor obs** ([[feedback-no-gt-actor-obs]] violation). 1–1.4 m/s over a 1–2 s approach = 1–2 m belief error vs a ~1.5 m opening ⇒ **sufficient alone**. An **honest-but-un-referenced** channel the policy was trained to trust as near-truth.
- ▶️ **PRESCRIBED ARM: `env.ego_faithful=true` / `vel_model='kf'`** — exists, DEFAULT-OFF, **never used in any run**, **never touches truth** ⇒ closes the GT leak + most of the gap at once. 🚩 still applies position-fix corrections the map-free wire lacks — verify first.
- 🚩🚩 **FRAME GOTCHA — misread ⇒ every log analysis inverts:** logged `rel_flu` is TRUE body FLU **UNFLIPPED** (neg lat = gate RIGHT), but `obs[0:3]` vel / `obs[5:8]` rates / `obs[11:14]` rel_pos are **VIRTUAL-FLIPPED** (`diag(-1,-1,1)`, `ego_obs.py:36,:124-127`) ⇒ **true v_left = −obs[1]**. Two conventions in ONE log line.
- 🟢 **CENSUS N=572** (`ed6de9f8`): **launch/release 20.1%** (biggest determinable mode) · **at-gate AXIS-UNDETERMINED 33.7%** · blackout 11.2% (**v15/v16 14.4%→v18/v19 3.9%, p=3.5e-04** = the emission fix, visible in deaths) · lateral 7.7–22.2% · vertical only 4.4–7.2%. 🚩 **v2.0's release-dive regression attacks the LARGEST death mode in the WRONG direction ⇒ v2.1 is the top training priority.** 🚩 **PROBABLE 2ND INVISIBLE OBSTACLE: 30 deaths at 14.1 m from gate 4, IQR 1.9 m** (tighter than the known gate-5 one) — test = `aim_off` on the gate-4 leg, ~6 flights.
- 🏁 **v2.0 SHIPPED CONDITIONALLY** (`ego-ckpts-v20-2026-07-26`, v20Vs0) — **pilot authorised over commander objection.** vert env **5.884 vs v19Ws0 4.893 (+20%)**, flat 5.973 vs 5.880 ⇒ **v19 course-FRAGILE, v20 course-ROBUST**. 🛑 **release dive REGRESSED ~2× (450/451)**; 3/6 first wire flights died at gate 0. **v19Ws0 remains the SAFE lead.** 🚩 bit-identical COURSE ≠ bit-identical POLICY.
- 🎥 **POST-IMPACT RECORDER LANDED** (`c0d3f6ef`): the impact was **IMPOSSIBLE to log** — stop conditions `break` at the TOP of the tick, forensics row written at the BOTTOM ⇒ detecting tick discarded whole. Median at-gate log ends **1.43 m / 0.222 s short** ⇒ **69% of at-gate deaths have NO determinable kill axis**. 🚩 **`video_index.jsonl` runs 70–130 ms LONGER than `ego_obs.jsonl` ⇒ the impact is probably already ON VIDEO in flights we already hold.** Also: ego path stored only a collision COUNT ⇒ gate-vs-environment unrecoverable corpus-wide (now fixed).
- ✅ LANDED `d1-velocity` (`b5fc6bfd`, DEFAULT-OFF — it REFUTED its own premise; do NOT fly as the gate-3 fix) · `p0-classifier` (`ed6de9f8`). Cluster C control arm **3323864**. 🛑 MIRRORED-COURSE PROBE DEAD — course is FIXED.

## 📁 DEPLOY + TRAINING (2026-07-22) — SSOT [[deploy-emission-2026-07-22]] · campaign [[perception-sim2sim-gap-2026-07-15]]
- 🏁 **v1.8 / v1.9 SHIPPED** (`ego-ckpts-v19-2026-07-24` = v19Ws0, the SAFE lead). v1.9 = roll-rate penalties on action **ch1** + **`ROLL_EVAL`**, default-OFF byte-identical; signflips 2.6→2.26 ∧ n_passed 5.72→6.03 ∧ dive IMPROVED. Q arms REJECTED (dive 450/451). 🛑 **EMA REFUTED** — the swept knob never touched the obs at all.
- 🟢 **GATE-1 RESOLVED (07-24):** wall was **v16-SPECIFIC (launch-untrained/OOD)** — v16 0/8 vs v18Qs1+darkred+z_bias 0.30 = 11/12. Vertical target is **pitch-coupled body-frame and CORRECT** (only coarse SECTOR obs[9:11] is gravity-leveled). z_bias = a CONSTANT band-aid. 🟢 vision-emission fix `b13a07f` WORKED — close-in blackout largely fixed in v19 (no-fresh-fix 23.6→7.3%, flicker 8.8→0.7%) ⇒ **the coast backlog was aimed at yesterday's problem**; patch 3 `4ca994f` = `pass_drop_range_m=2.5`.
- 🚩 **ROLL is the UNGUARDED axis:** census gates YAW+PITCH but NEVER ROLL (fix: `ROLL_EVAL`); reward penalised yaw/pitch RATE but roll only ANGLE. 🛑 **NO roll ANGLE fence** — backfires on the 60° turns; rate penalty ONLY. 🛑 **TERMINAL DIVE (open defect):** fence blocks **nose-DOWN only** (`fly_rl.py:766` `max(cmd,0)`) ⇒ `pitchcmd==+0.000` = a BLOCKED dive; inside 6 m ~100% fenced with the gate ABOVE. LOAD-BEARING, masks training.
- 🚀 **CLUSTER OPS:** `gpu-medium` QOS = **2 CONCURRENT GPUs**. h11g1=4×A100 · h11g2=8×MIG `3g.20gb` · h11g3=4×V100; s/update 0.86/1.25/1.48 (all fit 20h). 🚩 default `--exclude=adroit-h11g3` blocks V100 — pass empty `SBATCH_EXCLUDE=`. `push_via_daemon.py` uploads over the LIVE daemon. → [[velocity-channel-2026-07-27]]

## 📁 CLAMP + MAP DURABLES (2026-07-21) — detail SSOT [[perception-sim2sim-gap-2026-07-15]]
- 🛑🛑 **PITCH CLAMP IS BIMODAL, NOT A DIAL:** resting `obs[4]` = **−0.3107 rad = −17.80°** (n=100); fence = `obs[4] <= −clamp_rad` (`fly_rl.py:757`) ⇒ **clamp ≤17.8 = fence ON AT REST · ≥17.9 = OFF**. Every clamp result in project history is a fence-on/off dichotomy.
- 🚩 **det_hold owns the ADVANCE** (0.2 → true-gate lock ×8/8; 0.3 → WRONG far lock ×6/6) · **stale_horizon owns the CLOSE-IN blind meter** (last ~0.18 s flown OPEN-LOOP) · **deploy default 0.2/0.6** · **RE-FLY RULE = ‖gyro‖>2.5 for ≥3 CONSECUTIVE ticks** (the old "first 0.75 s" rule was a CLAMP DETECTOR) · **A RECORD IS NOT A BASELINE**; map cohorts statistically NULL (p=0.63).
- 🗺 **MAP SURVEY INSTRUMENT = FLIGHT PATH, never the camera** (traps: `obs[3:5]` VIRTUAL-FLIPPED = fake climb; at-pass row authored in the incoming frame = fake left). 🚩 **TWO RANGE CAPS:** `max_valid_range_m` 30 (exposed) vs **`max_acquire_range_m`=22 (NO panel field)** — 0.30% of ticks; `continuity_reject` ~38× bigger. 🚩 **v1.7 M1 PREMISE FALSIFIED** (0/161 hand over level) ⇒ **M1 INTRODUCED the mismatch** — mean said better, the TAIL said worse.

## 📁 STANDING OPERATIONAL DURABLES → **[[durables-operational]]** (flight/recipe ritual · run-selection · training-diagnosis · settled verdicts · Track A framing + reference constants)
- 🎯 Goal still standing: **BEAT vpeffs0 on the full 20-gate course.** Thread history → [[track-a-beat-vpeffs0-2026-07-13]] · [[replay-ratchet-2026-07-17]] · [[trackA-stall-forensic-2026-07-13]] · [[ego-deploy-contract-2026-07-09]] · [[audit-ego-inc9-2026-07-09]] · [[vq2-rl-commander-2026-07-05]] · [[now-archive-2026-07-05]].

## Operating directives — ALWAYS APPLY
- 🚩 **`COMMANDER.md` (repo root) = boot prompt + self-improvement loop.** Read FIRST on boot; EDIT LAST before handoff. MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL rev 4 (Fengyou): vision commander DEPRECATED — RL commander = SOLE commander, owns the deploy repo/branches DIRECTLY** (read+edit+push; NO relay boxes; Fengyou = pilot + hands on ShadowPC). Laptop → Agents/Workflow; Adroit → adroit-connector `serve`/`x`. → [[index-strategy-meta]]
- 🚩 **CANARY:** persistent COMMANDER sessions address **Fengyou** by name in EVERY message (missing name = context degradation → rotate). Workers don't need it.
- **BUDGET rev 5:** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING rev 5 (thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages, banks IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt: SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA req + escape hatch + report path.
- 🚩 **DELEGATE BUILDS TO AGENTS (Fengyou):** multi-file implementation = spawn agents/Workflow to edit+test+verify; commander thread = judgment/diagnosis/review/relay only. → [[feedback-delegate-builds-to-agents]]
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; SLURM only on Adroit; concurrent sessions use separate worktrees. 🛑 **NO-SPIN = HARD REQUIREMENT, enforce by CONSTRUCTION** (fatal spin aborts + realized-yaw clamp), NEVER incentive shaping; NO energy penalties → [[feedback-no-spin-hard-requirement]]. 🛑 **NO-GT IN ACTOR OBS: actor sees ONLY estimator-faithful state**; GT legal in critic/reward/terminations only → [[feedback-no-gt-actor-obs]].
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Workers surface NEW parked items in MEMORY-DELTA.
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly; the C6/POC hold is a SEQUENCING gate, not risk. Validity (zero gate contact) + sim time = the only flight constraints. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]
- 🚩 **TAILS NOT MEDIANS (Fengyou):** verdicts lead with per-flight worst-events (max/p99, tail counts, single-event kill attribution) — one strong roll kills a flight with a perfect median. Every adjudication + A/B carries a tail column. → [[feedback-tails-not-medians]]
- 🚩 **FLIGHT CURRICULUM: close the loop SLOW first, ramp after.** 🟢 A5 = first gate passed (map-free, no mag/baro). → [[feedback_slow_is_smooth]]
- 🚩 **MOONSHOT: championship-fast, not just finishing** (north star = Swift, Nature 2023) → [[project_moonshot_speed]] · **BLENDER VQ2 pipeline = HIGH-FIDELITY ONE-LOOK, not broad DR** → [[project_blender_vq2_data_pipeline]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN.**
- 🛑🛑 **NEVER READ BODY-FRAME GATE GEOMETRY WITHOUT REMOVING THE DRONE'S OWN ATTITUDE/ROTATION.** One error class, **TWO** banked-then-withdrawn root causes (07-27): (a) lever-differencing without gyro de-rotation — 8 m × 0.3 rad/s yaw fakes **2.4 m/s** of lateral motion (real std ~1.7), and since the controller **yaws to correct drift** it is **anti-correlated with truth BY CONSTRUCTION** = the bogus −0.110; (b) raw `rel_flu[2]` at the flown **+23.8°** median pitch calls **66.2% of CONFIRMED GATE PASSES a vertical strike** (levelled → 6.0%). **CORRECT INSTRUMENT = `scripts/failure_profile/vg.py`** (gyro-de-rotated lever ⇒ true trajectory vs the gate, KF-free). 🚩 **Calibrate any strike classifier on CONFIRMED PASSES** — a pass cannot be a strike, so FP rate is directly measurable. → [[failure-census-2026-07-27]]
- 🚩 **REPRODUCING A NUMBER IS NOT VERIFYING IT.** The commander re-derived the −0.110 slope with the *same confounded instrument* and called it confirmed. **Name the artifact that could produce this exact sign and magnitude, then stratify by that artifact's own size** — an independent mechanism, not a recompute.
- 🚩 **A PROBE THAT RUNS ≠ A PROBE THAT MEASURES.** Hand-built "at rest" obs are **OOD** (tell: every arm returns **~zero collective**) and rank arms differently than logged obs — launch-window questions go through `replay_sweep_v18.py` over real flights, never a synthetic. **Decode channels from the wire path**: ego action = **`[thrust, roll, pitch, yaw]`**, so `act[2]` is pitch; with virtual_flip `rate_frd[1]=+act[2]` ⇒ nose-DOWN = `rate_frd[1]<0`. → [[deploy-emission-2026-07-22]] §Method
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]]
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) → stream `SET_ATTITUDE_TARGET` BODY-RATE (mask 0b10000000, FRD, thrust [0,1]) ≥30 Hz; `fly_rl.py` drives. Use `DroneState.gyro_body`, NOT `angular_rate_body`.
- 🚩 **`[daemon] unauthorized` = TWO daemons, not bad auth.** Windows `SO_REUSEADDR` lets a restarted `adroit.py serve` bind 8765 while the stale one answers with its old token. `netstat -ano | findstr 8765` → two PIDs ⇒ **kill the OLDER**; re-running `serve` only adds a third.
- 🚩 **CRITIC FOOTGUN: use `algo=appo`** (ppo leaves the privileged critic unconnected = seed collapse; critic input_dim **37**) → [[index-rl-training]]. **Judged runs use `rl/submit_rl.py`** (pins inc7); `fly_rl.py` default ckpt retired → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG = self-consistent alias — DO NOT "fix"** (tail-first training + virtual π flip compensate) · **ODOMETRY quat is R_y(π)-CONJUGATED** — run `scripts/frame_residual_report.py` after EVERY live session → [[index-control-sim]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG — 24 m flip). Pinned by tests/test_obs_sign_faithfulness.py. Future estimator→obs MUST deliver +L. · 🚩 **BORESIGHT bake = METRIC TRANSLATION** (vert_offset_m −0.25; form-discrim needs Δrange ≥~10 m) · 🚩 GATE-4 margin footguns parked w/ inc8 → [[index-vision-estimator]].
- 🚩 **`main` CANONICAL · 1091 tests** · `*.pt`/`*.pth` + ckpt JSONs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`gh release download`), NEVER git-add → [[project-fullstack-burn]]. 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.**
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]]
- 🚩 **WORKTREE HYGIENE:** auto-worktree per session = isolation; **ONE WORKTREE PER CONCURRENT AGENT** (sharing moves HEAD under a live agent and can wipe uncommitted edits); commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` RECYCLES — never manually prune; only `git worktree prune`. → [[index-strategy-meta]]

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🚀 **[[deploy-emission-2026-07-22]]** — v1.8 adjudication, vision-fix deltas, stale-frame flicker, patch 3, terminal dive, next-gen env spec.
- 🧠 **[[vq2-rl-commander-2026-07-05]]** — RL-commander thread SSOT · 🗄️ **[[now-archive-2026-07-05]]** — verbatim blocks relocated in the 07-05 compaction.
