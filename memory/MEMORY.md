**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-05; relocated verbatim blocks → [[now-archive-2026-07-05]].)

## NOW · 🟢 VISION/FLIGHT THREAD — VQ2 SLOW-LAP BRING-UP (vision-stack commander's domain)
- 🟢 **Branch `claude/loving-galileo-92f020` HEAD `db4e016`** (= `35561c7` ff_owns_horizontal + instrumentation `81c323a`/`a32938a` + LOAD-CUT `180009b` + DRAIN-TO-EMPTY `db4e016`; pushed, NOT on main). Chain: A15b pre-warm ✓ (observation unblocked) → A16 nose-up-into-ceiling GONE → A17 cracked the freeze (synchronous vision compute starves the receiver; detect 150 ms ×2/frame) → LOAD-CUT landed → A18 loop un-choked 10→28 Hz but frame supply STILL broken (UDP receiver drained 1 datagram/wake: ceiling 6.6k vs the sim's 20.7k dg/s resend flood) → DRAIN-TO-EMPTY fix landed (offline-proven; SO_RCVBUF 64 MiB only WITH the drain). 🎯 **A19 = fly the drain fix: pass = completed ≥~25/s, evicted ≤~50, gaps>1 s = 0, detect n→hundreds, pursuit>0 + tsv finite. Frame supply is THE prerequisite — do NOT chase control until the detector is fed (Fengyou).** Yaw fix `337c554` still untested. detect=129 ms on GPU = sim contention; imgsz/half cut HELD for A20. Milestones: A5 first gate · A10 self-loc trustworthy · A15 recall solved. 🚩 Standing: (a) per-STEP timing to pin the Manhattan-VP worst-work spikes; (b) "command sent" render overlay (handoff-dir tool, NOT canonical render script). 🚩 LAUNCH RITUAL: `fly_rl` FIRST → "Waiting PASSIVELY" banner → THEN GO. 🚩 RENDER-OVERLAY GOTCHA: `render_vision_video.py` overlays red_glow, NOT the YOLO that flew. 🚩 Background-agent DEATH hazard: on a death check the tree for recoverable work; go INLINE for load-bearing. venv = MAIN checkout `.venv` (not worktrees). FULL saga verbatim → [[now-archive-2026-07-05]] · `handoff/commander-handoff-2026-06-30.md` · [[index-vision-estimator]] §VQ2 SLOW-LAP.

## NOW · 🟢 RL-COMMANDER THREAD — VQ2 EGOCENTRIC GENERATION (inc9) BUILD (laptop session, 2026-07-06→07)
- 🟢 **PIVOT: gate-frame single-gate obs → EGOCENTRIC multi-gate POSITION-FREE obs. SSOT = `docs/vq2-egocentric-gen/DESIGN.md` (Fengyou-approved).** Why: gate-frame obs launders through a drifting world-NED KF (never had the position-immunity credited); Swift's policy sees velocity + RELATIVE gate geometry, not absolute pos ([[reference-prior-art]]); case-C absolute pos unobservable/redundant. Egocentric kills the handoff FRAME-TELEPORT + drops world position; real VQ2 spacing 10–20m makes multi-gate co-visibility possible (impossible at VQ1 23.7–38.5m). **FULL STACK BUILT + adversarially verified (opus workflows), inc8 UNTOUCHED, new ENV_ALIAS (`+env.ego=true`, default OFF=inc7 byte-id):** B `rl/gate_visibility.py` (8-kp ≥4-of-8 + image-annulus occlusion; emergent blackout **1.29m COMPUTED**) · A `rl/ego_estimator.py` (relative-state, NO world pos; rel_pos + IMU-primary velocity [INDIRECT-corrected] + confidence + visible-area; MEASURED IMU) · C `rl/peregrine_racing_ego.py`+`peregrine_train_ego.py` (**21-dim** obs [WINDOW=2 slider: vel+rp+rates+coll+coarse_sector(2)+2 slots{rel_pos+conf+visible_area}]; HARD kill-on-contact; privileged critic 16) · REWARD `rl/ego_reward.py` (**refined-B champion-consensus** per Swift/SkyDreamer/Geles/MonoRace: segment-projected POTENTIAL progress rw_progress=1.0 + L-inf centering passage + **progress-scaled terminal — SPRINT-AND-CLIP DEFEAT PROVEN by rollout** + fixed 60° cone + minuscule smoothness + gate-gated exit-line). D+E LANDED (4-stage ladder + sbatch; warm-start + course-wiring bugs fixed). Suites green: visibility 9 · estimator 12 · obs 12 · reward 17 · curriculum 9 · course-wiring 11 (**70**). 🚩 **γ=0.9975 LOAD-BEARING** (terminal-dominance thins at γ0.99 at deployment scale). 🚩 **algo=appo MANDATORY + runtime critic-assert** — anti-damping ("caution emerges from truth-seeing critic") is FALSE under ppo (symmetric obs-only critic = the inc8 SEED-COLLAPSE root cause). Measured: gate-normal DROPPED (19°/11°/6%-flip) → **visible-area foreshortening** (range-free ratio, σ0.054, COARSE ≳20-25° alignment) replaces it · IMU (accel white ~0.008 UNIFORM, gyro COLORED AR(1) ρ0.75, bias-dominated drift). 🟢 **COMMITTED `668347f` (feature branch, NOT pushed). Adroit SMOKE PASSED → ladder ego_a FAILED single_gate (0%) → **4 BUGS FOUND+FIXED (`5a252a4`+`3566799`): RC1 = the EMULATED CAMERA POINTED BACKWARD (tail-first control vs nose-first camera; `_cam_R_wb` virtual-flip, 0→100% detectable) · RC2 = noise RUNAWAY (wired `apply_noise_schedule` ceiling std0.30) · RC3 = approach −EV (rw_progress 1→6, miss→30) · OOB/MISS conflation (OOB back to 200). EGO NOW STRUCTURALLY WORKS: sees gate + approaches + stays in arena + ~0 contact; LAST blocker = APERTURE CENTERING (drone flies wide, 90% miss).** 🚩 **ADROIT FOOTGUN: `/scratch/…/peregrine_repo` is a PLAIN DIR (not git) that DRIFTED to an unmerged B2 base; the ego needs MAIN's `peregrine_racing.py` (B2's native course_n_gates wiring COLLIDES with the ego's `resolve_course_overrides`). Reconciled: B2 → `/scratch/network/fl3689/b2_base_backup_2026-07-07`; peregrine_racing+course overwritten w/ main; inc8_estimator_emul KEPT (ego-imported symbols byte-id → honors "inc8 UNTOUCHED"). Deploy-without-Duo = base64 chunks via the serve-daemon (`adroit.py x`), NOT SFTP `upload`. Smoke fixes (committed): ego `loss_components['total_loss']` + `export.jit/onnx=false` (diffaero ONNX exporter rejects the CTBR body action_frame).** 🎯 NEXT (morning, deliberate reward-DESIGN — left for Fengyou, NOT guessed overnight): add inc7-style DENSE `through_centering` lateral pull to `ego_reward.py` (refined-B dropped it → no dense alignment during approach → wide flyby; `-rw_centering·clamp(‖perp‖,max≈3m)` reusing `segment_arc_position`) + lower rw_progress 6→~2-3 (over-rewards rushing: banked 50 >> finish 20) / raise rw_finish→~60-80. Re-run single_gate diag (`ego_diag.sbatch` + noise knobs) BEFORE ladder; watch TB `miss_rate`↓/`success_rate`↑. **Cluster IDLE (jobs killed).** Handoff `handoff/ego-inc9-overnight-2026-07-07.md`. Deferred: misassociation · gate-ID/ByteTrack · anneal. Detail → [[vq2-rl-commander-2026-07-05]] · DESIGN.md §9.
- 📁 **SUPERSEDED (gate-frame thread, curr_b2 job 3296780):** B2 FLOWN → regression → B2b revert; the dual-gate handoff failure = the frame-teleport that MOTIVATED this pivot. Lesson kept: std_hold 0.35 ceiling too tight (starved discovery exploration); validate new knobs on single_gate BEFORE _COMMON. Full → [[vq2-rl-commander-2026-07-05]].

## Background (mostly superseded; verbatim → [[now-archive-2026-07-05]])
- 📁 **REACTIVATION 2026-06-27 (superseded)** — map = `docs/reactivation-2026-06-27/REACTIVATION_BRIEFING.md`.
- 📁 **VQ2 STACK DESIGN RESEARCH (2026-06-27, pre-flight, mostly superseded).** SSOT = [[vq2-stack-research-2026-06-27]] → `docs/reactivation-2026-06-27/candidate-stack-v2.md`. Still-live headlines: asym-privileged-critic = #1 RL fix; recon-map MOAT (VGGT+gate-PnP); bootstrap-vision-RL (pure=0%).
- 🟢 **VQ2 SPEC (VADR-TS-003, banked at repo root `260624_Technical_Spec_0003.pdf`) + LIVE WIRE RECON 2026-06-29 (build 1.0.3379):** §9.3 blocks CONFIRMED (ATTITUDE/LPN/ODOM/GLOBAL/GATE_INFO blocked in training AND competition, byte-identical). Wire = HEARTBEAT 10 Hz · HIGHRES_IMU 117 Hz (accel+gyro ONLY) · 30 Hz JPEG (14550/5600, 24-B header) · RACE_STATUS 4 Hz (**active_gate_index PRESENT**) · ACTUATOR_OUTPUT_STATUS 95 Hz · TIMESYNC response-only. Track = "Now You See Me, Now You Don't" (dark warehouse, stations 01-20, deterministic per-load). Resolved: training==competition wire; track not randomized; training exposes nothing extra. Full → [[now-archive-2026-07-05]] · [[index-vision-estimator]] §VQ2-WIRE-RECON · [[reference-sim-interface]].
- 📁 **PARKED inc8 (speed increment — HELD, do NOT prune):** branch `inc8-deterministic-retrain` (05809ba) + stashed reach-rate result (`claude/funny-nash-d07d7b` @ 0edea93 + `handoff/inc8-reach-rate-2026-06-19/`: det reach 0.467→~0.72 @ r=0.30, pending Fengyou). Next lever on resume = gate-4 reach/pass-rate. 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.** Full → [[now-archive-2026-07-05]] · [[index-rl-training]].
- **VQ1 PASSED; inc7 LIVE-CONFIRMED = current best.** Phase 2 = RL training (ACTIVE above) + VQ2 vision/flight (ACTIVE above).
- 📁 **Durable pre-flight results:** C2 gate-relative estimator chain MERGED (05ed750; OFF = VQ1 byte-id) · obs contract FROZEN (d5 obs-20, [17:20] conf+age; critic get_state 37-d) · detector clean-ensemble 282abb9 (deployed via `--seeker-weights`) · gate-4 = REACH/PASS-RATE problem (σ_p0 ≈0.15 lat; the ≲0.08 bar RETRACTED) · camera mount 20° spec-exact (fy=320). Full → [[now-archive-2026-07-05]] + sub-indices.

## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read FIRST on boot; EDIT LAST before handoff (add the lesson, prune the stale, log your generation). MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL (rev 3, 2026-07-05, Fengyou): DOMAIN-SPLIT — VISION-STACK commander (ShadowPC side) + RL commander (laptop session), each SOLE memory authority over its OWN domain; commander↔commander relays via Fengyou, ONE copy-paste code box.** rev-2 mechanics otherwise stand (laptop → Agents/Workflow directly; Adroit → adroit-connector `…\Adroit\adroit-connector\adroit.py`; shadowpc-connector still the wanted paste-retirement → [[index-strategy-meta]] §SHADOWPC-CONNECTOR). rev 2 verbatim → [[now-archive-2026-07-05]].
- 🚩 **CANARY (rev 2, 2026-06-14):** persistent COMMANDER sessions address **Fengyou** by name in EVERY message (missing name = context degradation → rotate). Workers don't need it.
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages; bank IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt: SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA req + escape hatch + report path.
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees.
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Workers surface NEW parked items UP in MEMORY-DELTA; commander files them — never re-scatter.
- 🚩 **FLIGHT CURRICULUM (2026-06-29): close the loop SLOW first, ramp speed after.** 🟢 A5 = FIRST GATE PASSED (2026-06-29, map-free, no mag/baro). → [[feedback_slow_is_smooth]] · [[index-vision-estimator]].
- 🚩 **BLENDER VQ2 DATA PIPELINE:** render harness matched to glowing-red/dark-warehouse appearance — HIGH-FIDELITY ONE-LOOK, not broad DR. → [[project_blender_vq2_data_pipeline]]
- 🚩 **MOONSHOT: championship-fast, not just finishing.** North star = Swift (Nature 2023). Slow bootstrap → perception hardening → RL speed ramp. → [[project_moonshot_speed]]
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly (no ceremony); the C6/POC hold is a SEQUENCING gate (Fengyou's), not risk. Validity rule (zero gate contact) + sim time = the only flight constraints. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN** (zero-contact is THE validity rule).
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]] §VQ2-WIRE-RECON.
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) then stream `SET_ATTITUDE_TARGET` BODY-RATE (type_mask=0b10000000, FRD rates, thrust [0,1]) ≥30 Hz; sim default ACRO = CTBR; `fly_rl.py` drives this. Deploy: use `DroneState.gyro_body`, NOT `angular_rate_body` (ODOMETRY-blocked). → [[index-vision-estimator]].
- 🚩 **inc8 CRITIC SYMMETRIC FOOTGUN:** `algo=ppo` leaves the privileged critic NEVER connected = seed-collapse root cause. Use `algo=appo` (GuardedAPPO, critic input_dim **37**). → [[index-rl-training]].
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7, no MAV_CMD 31000). `fly_rl.py` default ckpt = retired inc4 → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG is a self-consistent alias — DO NOT "fix".** (Training flies TAIL-FIRST; fly_rl's virtual π flip compensates. The 2026-07-05 camera_flip fixes the EMULATED CAMERA only — control conventions untouched.) → [[index-control-sim]].
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session. → [[index-control-sim]].
- 🚩 **`main` CANONICAL · 1091 tests collected (2026-06-20)** · `*.pt`/`*.pth` + ckpt JSONs + data/runs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`burn-artifacts-*`, `gh release download`), NEVER git-add. → [[project-fullstack-burn]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG — 24 m flip). Pinned by tests/test_obs_sign_faithfulness.py. Future estimator→obs MUST deliver +L. → [[index-vision-estimator]].
- 🚩 **GATE-4 MARGIN FOOTGUNS (parked w/ inc8):** contact radius 0.38 = crash HALO not geometry; binding margin = ε_vert≈0.215 m sighting bias (calibratable, NOT contact radius); gate-0 posture wrong for gate-4; common-mode ≠ +L cancellation. Full → [[now-archive-2026-07-05]] · [[index-vision-estimator]].
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]].
- 🚩 **WORKTREE HYGIENE:** harness auto-worktree per session = isolation satisfied; workers may make worktrees freely. Commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` pool RECYCLES — NEVER manually prune (racy); only `git worktree prune`. Full → [[now-archive-2026-07-05]] · [[index-strategy-meta]] §worktrees.
- 🚩 **BORESIGHT FORM-DISCRIMINATION needs ≥2 ranges, Δrange ≥ ~10 m** (closely-spaced trio = DEGENERATE — pins magnitude, not form). FORM decides where −ε bakes + the ESKF watchdog. Bake site = frames.R_camera_from_body. Full → [[now-archive-2026-07-05]] · [[index-vision-estimator]] §P1.

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🧠 **[[vq2-rl-commander-2026-07-05]]** — RL-commander thread SSOT (audit, B1 fixes, job 3295856, fidelity plan, vision-commander asks).
- 🗄️ **[[now-archive-2026-07-05]]** — verbatim blocks relocated in the 2026-07-05 compaction (flight saga, wire recon, parked-inc8, durables, footgun full texts).
