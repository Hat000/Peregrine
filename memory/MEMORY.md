**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026; fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. This is the orientation index; detail in topic files below.

## READ FIRST — current state (2026-06-11)
**🏁 VQ1 PASSED** (dashboard-confirmed). CTBR stack threads 6-gate course — valid finish ~35.3 s, in-plane miss `[0.37, 0.05, 0.06, 0.03, 0.10, ~0.71]` m (all < 0.75 m half-opening). **Offline-twin discipline** (tune offline, fly only to VERIFY) got us unstuck — keep it. Pivoting to **Phase 2 = RL + VQ2 vision**.

**✅ 2026-06-09 — SIM BUILD 1.0.3364 REGRESSION FIXED:** 4× deterministic 6/6 @ 34–35 s. Root cause: one-tick attitude step at takeoff→RUN → rate-clamp tumble; fix = `launch_ramp_s=0.6 s` sim-clock-keyed ramp + `--rate 100` pinned (commits 260972e+883b725+57f287f; detail [[project-ctbr-control-sysid]]). **VQ2 open items (NOT VQ1):** (A) "yaw-spin" — **✅ DISSOLVED (sweep 510da24):** unmodeled super-rate + Euler flip artifact + gate-post collision (contact dynamics, not anomaly — [[project-phase2-rl-vision-decisions]] §CHARACTERIZE-SWEEP); (B) decel into gates; (C) ~20 km/h cap — both RL levers.

**🚩 2026-06-10 — UNATTENDED SIM CONTROL:** S1.2 cold-launched FlightSim.exe + raced NO human. **MAV_CMD 31000** restarts race once race context exists — **NO-OP from HOME**; from HOME: Win32 `SetForegroundWindow` + **Enter ×2**. `rl/fly_rl.py --flights N` chains attempts. **🚩 THIRD idle state** (~90 min post-race): 31000 NO-OP, stale telemetry; recovery = Win32+Enter×2 — wired as auto-escalation in `rate_sysid.py`. **🚩 ZOMBIE DUAL-INSTANCE:** two instances on 14550 → drone arms but ignores; killing zombie kills BOTH → relaunch fresh. **🚩 Measurement footgun:** quat-finite-diff rate ALIASES vs LPN/ODO stagger; use raw ODOMETRY rate.

## The flyable stack (durable — RL training substrate)
- **Sim interface** (all 5 must-verify RESOLVED; [[reference-sim-interface]]): pos+vel GIVEN pristine (LOCAL_POSITION_NED 97 Hz + ODOMETRY 75 Hz; baro NaN → z from given); **🚩 ATTITUDE.pitch sign-inverted → use ODOMETRY quat**; gate map = 6 gates, 2.72 m OUTER / ~1.5 m inner (feed inner to PnP), broadcast once at level-load (`capture_track_map.py`, map-once-reuse); course DESCENDS 26 m; clocks INDEPENDENT (align recv-monotonic); video re-sent ~14× → dedup by frame_id; sim PAUSES physics off-race.
- **Fly on CTBR** (`SET_ATTITUDE_TARGET`; sim runs ACRO). Plant system-ID'd → faithful OFFLINE TWIN (`src/racer/twin.py`/`twin_fit.py`, RL plant): **rate_gain [2.50,2.50,2.23], physics rate_sign [+1,+1,−1] (YAW cmd only inverted), τ=0.019 s, hover=0.2656, linear_drag≈0.21/s**. **🚩 rate_gain amplitude-DEPENDENT (sweep 510da24): super-rate map `g(|c|)=G0/(1−s·min(|c|,π)/π)`, s≈0.30 ⇒ full-stick ~11 rad/s (NOT 7.85); flat-2.5 valid small-signal. ✅ Map integrated all three plants (S14, defaults None = legacy bit-identical; `faithful_config(super_rate=True)` = map-ON)** — [[project-phase2-rl-vision-decisions]] §S14. **🚩 TWIN-FALSIFY (2026-06-11, ShadowPC fable): TWO MORE FALSIFICATIONS — ① linear_drag WRONG: real drag QUADRATIC body-direction-dependent (c2≈0.052/m; 2.2× at 9 m/s ⇒ policies ~2× under-braking); ② linear collective WRONG: thrust curve CONVEX, full-stick ≈8 g ("3.77 g ceiling" was linear-model artifact). Survivals: rate map airspeed-invariant, control-rate invariant, no battery sag, determinism SD ~0.03 m/s. ✅ S16 AERO COMPLETE (commit eba4349): quad body-frame drag + convex collective knot table in all three plants (twin→rl_plant→DiffAero), parity defaults OFF = legacy bit-identical; 497 tests green; CPU-torch worst 8.9e-16 PASS; twin↔rl_plant pos/vel ≤5.1e-13. 🚩 Adroit V100 gate NOT yet run — `run_parity.sh` md5 tripwire updated; run FIRST at retrain. Detail [[project-phase2-rl-vision-decisions]] §TWIN-FALSIFY+§S16 + `handoff/laptop-s16-aero-integration-2026-06-11/REPORT.md`.**
- **🚩 PHYSICS≠TELEMETRY:** ODOMETRY quat REPORTS roll inverted + raw rate inverted on roll+pitch (reporting artifact, NOT physics); controller `body_rate_sign=[1,1,−1]`/`odo_att_sign=[−1,1,1]`/`odo_rate_sign=[−1,−1,1]`/`ff_gain=2.5` undo them. Arm = `MAV_CMD_COMPONENT_ARM_DISARM` p1=1. **Live config = `FAITHFUL_TUNED_GAINS` (`kp_alt 3.0/kd_alt 1.75`, raw vz) + `−0.4 m alt_offset` + `launch_ramp_s=0.6 s`.** Detail + Gate-0 saga + alt-relay + 1.0.3364 fix: [[project-ctbr-control-sysid]].
- **🆕 RL DEPLOYMENT RECIPE (S1.2, fable — corrects 3 sonnet S1.1 bugs; in `rl/fly_rl.py`):** ① **tanh(mean) → rescale** (thrust [0,5], rates ±3.14); ② FLU→FRD sign **[1,−1,−1]**; ③ collective obs init **0.0** rescaled; ④ training rate **30 Hz**; ⑤ resets **at rest** 1 m before random gate. **🚩 "OOD-at-start" RETRACTED** (artifact of wrong transfer; reset saturation is normal). Detail [[project-phase2-rl-vision-decisions]] §S1.2.
- **🆕 RATE SIGN SETTLED (S1.2):** raw ODOMETRY `angular_rate` → TRUE rate = **[−1,−1,1]**. **`state.velocity_ned` is PRISTINE** (quat+twist reproduce world vel to 0.00 m/s). [[project-ctbr-control-sysid]].
- **Alt balloon is OURS** (CTBR owns thrust). Limit cycle = delay-driven relay (~40 ms), ALWAYS-ON but VALIDITY-HARMLESS; deferred. **🚩 offline twin under-models live latency ~25% — do NOT trust offline "holds" claims** (the LESSON is the keeper).
- **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`) — do NOT reintroduce:** twist is BODY-frame, was stored as world; now rotated via `frames.world_vec_from_body_quat`.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate). v3 `--hard` = documented NEGATIVE (high-roll corner-swap is geometric aliasing → solver/prior, not training). **✅ Catastrophic-tail fix (4673517, 2026-06-09):** |fix|≥3 m 46%→6.3% via `association.py` (wrong-gate kill) + depth-sanity — **key: "frontal flips" = wrong-scale detector boxes, not solver; geometry consistency is the kill.** `PNP_FIX_COV_INFLATION` CLOSED. **✅ VISION-PKG2 COMPLETE (2026-06-10):** `ATTITUDE_NOISE_STD_RAD=1.4°` + `FIX_COV_FLOOR_STD=0.40 m` floor + `vision_max_range_m` 40→32; good-fix over-rejection 15.7%→1.2%, catastrophic leak 1.06%→0.53%, KF-accepted 147→171. **🚩 ~3.6°/range yaw bias NOT a calibration — REFUTED** (depth-scale artifact + both-sign per-gate + flight-specific roll wander → covariance, not calibration). **🚩 REAL BUG FIXED (37e7ab1):** `corner_to_center` gate frame 180° vs detector convention — PnP prior was anti-aligned. Full numbers: [[project-phase2-rl-vision-decisions]] §VISION-PKG2.

**✅ GATE MAPPER COMPLETE (2026-06-11, laptop fable; commits 46a4cc4→75fa42a; 528 tests green):** `src/racer/gate_mapper.py` — case A (pose-aided robust median/MAD+χ², per-gate cov) + case B (1-lap ~0.6 m) + case C (no pose, LS from attitude, 2–22 s). Consumes exact `capture_track_map.py` schema; navigator untouched (round-trip 1e-6 m). Detail [[project-phase2-rl-vision-decisions]] §GATE-MAPPER.

**Git / env:** **`main` CANONICAL** (branches retired 2026-06-07). **528 tests green**, `.venv` Python 3.13. `*.pt` + `data/runs` gitignored. Memory mirrored `memory/` ↔ `~/.claude`; ShadowPC passes findings via `handoff/`.

**✅ INC5 SHIPPED (2026-06-11, laptop fable + Adroit; commit 02fcce1; `rl/checkpoints/stage1_inc5_actor.pth` md5 bd1d670f…):** THE definitive aero-ON transfer candidate; SUPERSEDES inc4 (inc4 = KNOWN-BROKEN on corrected physics: aero-ON eval sr 0.461, 53.9% collisions, fails +1-step latency). V100 parity gate passed first (job 3267545, worst 2.665e-15). 5 jobs/2 rounds, zero NaN. Round 1 = inc4 warm-start → 6.89 s but roll p90 145° (real ~8 g makes speed too cheap for tilt hinge) — kept as **unconstrained-speed datum**. Round 2 winner: inc5_t96_s1 (seed 1, job 3267805, 6000 updates, rw_tilt=96): VQ1 held-out sr 1.000 / 2560 eps / median 9.52 s / gen 0.939 / roll p90 64.5° / deploy 6/6 incl +2-step latency; shipped, sidecar auto-loads. Alternate t48_s0 (9.22 s, gen 0.960, fails +2-step latency) banked not shipped. **`fly_rl.py` default still points at inc4 — pass inc5 ckpt explicitly until default flips.** Detail [[project-phase2-rl-vision-decisions]] §INC5.

**🚩 INC5+INC4 LIVE TRANSFER BLOCKED — DEPLOYMENT-LAYER BUG (2026-06-11, handoff/shadowpc-inc5-live-2026-06-11/): 0/10 inc5, 0/10 inc4, IDENTICAL failure: collective=0.000 from step 0 + yaw_rate pinned ±3.14 (tanh rails) → ballistic crash gate-0 frame steps 0–2.** Offline rollout from exact live handoff state: inc5 8.23 s 6/6, inc4 8.72 s 6/6 — **policies are fine; the live obs/action path is broken.** Leading hypothesis = **GARBAGE OBSERVATIONS live** (NaN/out-of-range) saturating all outputs; prime suspect = S15's sidecar-reading code path in `fly_rl.py` which had NEVER been exercised live (inc5 deploy matrix ran `offline_rollout.py` — a different code path). Worker's alternative (normed_thrust=0 = motor-cutoff floor; 5% collective fix) DEMOTED — saturation signature points upstream. Gate-position miscalibration RULED OUT (model-based threads same map 0.03–0.37 m; corner probe accurate; offline-from-live-state 6/6). **NEXT = fable live-deploy diagnosis on ShadowPC: step-0 obs/action logging, live-vs-offline diff.** Detail [[project-phase2-rl-vision-decisions]] §SHADOWPC-INC5-LIVE.

**✅ CORNER-PASS PROBE (2026-06-11): pass aperture confirmed live — offsets ≤0.64 m advance the gate; 0.60 m WITH contact; 0.74 m inconclusive (nav overshot outer frame).** Racing lines should hold ~0.6 m clearance; TOGT 4.13-vs-4.27 bracket: lean 4.27 (inscribed-circle) as planning-valid bound.

**✅ COAST-REPLAY CONFIRMED through S16 plant: 0.224 m/s speed RMS (target ~0.24) — aero integration reproduces campaign fits on real recordings.**

**🚩 NEW SIM OPS (MANDATORY for all future live harness work):** (a) sim AUTORESETS on sustained gate contact → RL pilot commanding through autoreset → drone enters uncontrolled spin with throttle latched — **NEVER leave RL pilot commanding without clean sim reset; detect reset/contact → cut commands immediately**; (b) add unrecovered-spin detection + auto-flag; (c) between flights do FULL escape→Enter reset (fresh countdown) — observed weird autoreset-from-crash states otherwise. **SIM LAUNCH PATH (ShadowPC):** exe = `"C:\Users\Shadow\Downloads\AI-GP Simulator v1.0.3364\AIGP_3364\FlightSim.exe"`; any button → login page; Enter → cached logins → homepage.

**🚩 STYLE ENVELOPE COST MEASURED — S2 input: ≤~65° roll costs ~2.3 s/lap on real aero (9.5 vs 6.9 s unconstrained). Envelope's anomaly-avoidance rationale is DISSOLVED (no anomaly + plant measured to full stick + full inversion). Relaxation = candidate speed ladder (tilt weight 96→48→…, one live-verified step at a time) after inc5 live transfer confirms twin fidelity.**

**🚩 POLICY DECISION RATE 30 Hz WAS NEVER A CHOICE — inherited DiffAero racing.yaml default (dt=0.0333).** Rate map + control loop are airspeed/rate-invariant. At VQ2 speeds 20–39 m/s: 30 Hz = 0.7–1.3 m between decisions vs 0.75 m gate margin. **QUEUED after live transfer verdict:** retrain at 60/100 Hz (dt override + PPO horizon retune + latency-DR step rescale), compare lap + gate precision vs 30 Hz baseline.

**🆕 PARALLEL ONBOARD SYSTEMS LEDGER (2026-06-11; gated on organizer ①⑤; design rule: threads feed the 30 Hz loop at tick boundaries only; keystone = KF rewind buffer). Tier 1:** async ~5 Hz full-res detector rewind-corrected into KF; ROI re-detection "zoom" (may crack case-C co-visibility wall; free offline exp); MPC-shadow safety floor + watchdog demotion (actuator, not alarm); critic-as-risk-monitor (free, needs one replay calibration). **Tier 2:** multi-frame far-gate PnP; optical-flow velocity witness; between-attempts residual fitting (gated on Q④); IMM filter bank. **REJECTED (no re-litigate):** in-race map mutation; policy-ensemble voting; wind estimator; in-race policy adaptation. Detail [[project-phase2-rl-vision-decisions]] §PARALLEL-SYSTEMS.

## NEXT — Phase 2 (RL + VQ2); VQ1 banked
**🚩 VQ1 = PASS/FAIL "navigate the course" (spec §8, 8-min cap), NOT ranked time (VQ2 = fastest-valid).** Submission = **Python stack THEY run unattended** → **FULLY AUTONOMOUS end-to-end** (auto-arm→fly→finish; §7: human interaction = DQ). Unlimited attempts, May→~mid/late July. **Adroit summer access CONFIRMED (2026-06-08).** **User actions: confirm registration active + email info@theaigrandprix.com for — ① does VQ2 stream `LOCAL_POSITION_NED`/`ODOMETRY` or is position vision-only? ② submission interface spec ③ VQ1 deadline ④ can stack carry state BETWEEN attempts? ⑤ eval-hardware compute — GPU?** Detail [[reference-competition-materials]].
- **VQ1 issues:** ✅ ① start transient FIXED; ② aggressive descents (peak sink 9.3 m/s) + ③ alt relay remain VQ2/RL.
- **RL = COMMITTED VQ2 path:** learned state→action policy behind `Setpoint` seam. **ALL stage detail: [[project-phase2-rl-vision-decisions]]** (§Staging, §S1.2, §CHARACTERIZE-SWEEP, §S14, §TWIN-FALSIFY, §TOGT-BOUND, §GATE-MAPPER, §S15, §INC5, §SHADOWPC-INC5-LIVE). **🚩 CRITICAL PATH: INC5 LIVE TRANSFER BLOCKED (deployment-layer obs-corruption bug) → NEXT: ① fable live-deploy diagnosis on ShadowPC — step-0 obs/action logging, live-vs-offline diff, identify & fix the broken obs/action path; ② once unblocked: envelope relaxation ladder + 60/100 Hz decision-rate experiment + TOGT re-solve with corrected aero; ③ S2 architecture decision post live-transfer verdict.**
  - **Substrate = DiffAero on Adroit** (bake-off done over Isaac): `src/racer/rl_plant.py` + `rl/diffaero_dynamics.py`, parity BIT-IDENTICAL (V100 DIV 4.4e-16); 2048-env PPO ~88.9 K steps/s.
  - **Inc-1 ✅ (0.97 success, `stage1_inc1_actor.pth`)** — 🚩 no plant DR (numpy backend silent), overfit-nominal. **S1.2 ✅ live pipeline VERIFIED, checkpoint NOT transfer-ready** — "backflip-diver" failure (roll 104–126° pre-gate); tail-first spawn fixed by virtual π body-z flip. Detail [[project-phase2-rl-vision-decisions]] §S1.2.
  - **S1.3 ✅ in-twin:** 3.765 ceiling + standing-start + ATT=2.0/JERK=0.5 → 100% 6/6, peak roll 65°; `rl/checkpoints/stage1_inc3_actor.pth` (md5 ea2bf8a2…, commit c05362f). 🚩 S1.3+inc-1 trained WRONG flat-2.5 plant → retrain gated; **S1.3-live DEMOTED to optional datum.**
  - **🚩 CHARACTERIZE-SWEEP (510da24) SUPERSEDES cc6921d 2nd-order/PI-windup model:** inner loop = static super-rate map + fast ~critically-damped loop; PI-windup DISPROVEN; crash-termination = collision-based. DR spec (s∈[0.25,0.35], τ∈[0.015,0.03], alpha_max∈[200,320]) ✅ SHIPPED S14.
  - **S14 ✅ (static-map, critical-path step 1):** map+slew in all three plants + torch DR, V100 worst 1.8e-15 PASS, 376→409 tests. **🚩 `peregrine_eval.py`/`offline_rollout.py` DEFAULT to legacy flat plant — all evals must run map-ON.** **🚩 `/scratch/network/fl3689/peregrine_repo` is FILE COPY, not git clone** — stage deploy-key clone if sessions grow.
  - **✅ S15 ENV-COHERENCE REDESIGN + S1.4 RETRAIN COMPLETE (2026-06-11, laptop fable, 7 jobs/3 rounds Adroit): `rl/checkpoints/stage1_inc4_actor.pth` + `.json` sidecar (md5 766ea71b, commit 66dd3c2). Held-out VQ1: 100.0% / 3582 eps / 7.03 s median. Generalization 0.845. 528 tests green. 🚩 inc4 KNOWN-BROKEN on corrected aero (sr 0.461, 53.9% collisions) — SUPERSEDED by inc5. Detail [[project-phase2-rl-vision-decisions]] §S15.**
  - **✅ INC5 SHIPPED (2026-06-11; commit 02fcce1; `stage1_inc5_actor.pth` md5 bd1d670f): aero-ON, sr 1.000 / 9.52 s median / gen 0.939 / roll p90 64.5° / deploy 6/6 +2-step latency. 🚩 LIVE TRANSFER BLOCKED — obs-corruption bug in `fly_rl.py` (collective=0 + yaw rail from step 0); offline-from-live-state 6/6 confirms policies fine.** Detail [[project-phase2-rl-vision-decisions]] §INC5 + §SHADOWPC-INC5-LIVE.
  - **S2 architecture decision pending:** decomposed plan-line + RL-tracker vs monolithic — decide post-live-transfer; TOGT structural-gap datum + style-envelope cost (2.3 s/lap) feed it.
  - **Perception model (VISION-PKG2 updated):** world-fix σ≈[0.73,0.47,0.29] m (N,E,D), bias [−0.4,+0.06,−0.28] m, range-flat to ~24 m; acceptance ~47% race-window frames, leak 0.53%; twin fix-cov = K2·analytic + 1.4° lever + 0.40 m floor + 32 m cap — Stage-2 twin input (§VISION-PKG2).
  - **🆕 STACK-REVIEW-VQ2 (2026-06-10):** architecture AFFIRMED (2025 A2RL champion MonoRace validates our class). **3 META gaps:** **① single-track training** (rank-impact #1 → procedural tracks + VQ1-held-out, in env redesign); **② ✅ TOGT BOUND CLOSED (2026-06-11):** ceiling **~4.27 s** (4.13 s if full square counts), ROBUST ~4.3–4.7 s with corrected aero — THRUST BINDS ~84% of lap; our 35.3 s is 8×; twin-tracked lap ≈8.3 s at k=1.85 (4.55→8.3 s **STRUCTURAL gap** → S2 tracking arch); reference line `rl/reference_line_vq1.json` + `rl/reference_line.py`; ✅ corner-pass probe done: ≤0.64 m advances the gate (0.60 m with contact); **lean 4.27 s (inscribed-circle) as planning-valid bound**; detail [[project-phase2-rl-vision-decisions]] §TOGT-BOUND; **③ vision-only (case C) readiness UNBUILT** (KF rewind buffer · range-anisotropic R · in-loop latency never measured).
  - **Vision for VQ2:** detector→robust-PnP→KF chain. 🚩 yaw-bias REFUTED — in covariance, not calibration. [[project-estimator-robustness]].

## Durable strategy & directives
- **Track A = VQ1 floor (BANKED) + VQ2 baseline.** Estimator = linear pos/vel KF. Detector = YOLO-pose → IPPE/robust-PnP. MVP = min-snap line + slow VALID finish > fast invalid.
- **Overfit GEOMETRY, randomize APPEARANCE.** NEVER fine-tune detector on clean VQ1 frames.
- **Planning + speed = VQ2 differentiator; vision = bounded threshold.**
- **RL = COMMITTED VQ2 path** now VQ1 is banked. [[project-master-plan]] C1.
- **User directives:** speed > keeping gate in view; vision must be excellent; determinism is per-track; offline between-runs processing LEGAL; be a good citizen on Adroit (own dirs, SLURM only). Discipline: re-derive every verdict from data; tune offline, fly to verify; bounded actuation, user owns GUI/risk. **Every new-session prompt carries recommended MODEL (with VERSION e.g. opus-4.8/sonnet-4.6/fable) + EFFORT (low→medium→high→ultracode).**
- **🆕 2026-06-10 — MAX plan + opus 4.8 commander; token-conservation RELAXED ("turn up the heat"):** use opus/fable at high freely on hard objectively-verifiable problems. Still match model to task.
- **🚩 FABLE SUNSET ~2026-06-22 (12 days from 2026-06-10):** MAXIMIZE Fable now — front-load all hard/design-heavy/objectively-verifiable work (plant integration, env+reward redesign, retrains, S2 architecture, vision/estimator math, detector DR) into Fable sessions; run parallel Fable lanes across three machines; bank findings/handoffs rigorously so post-Fable models inherit complete state. Routine ops wait for lesser models.
- **BUDGET POLICY (rev 2026-06-11):** one afternoon+overnight burned 32% of WEEKLY quota — target ~13–14% weekly/day: SATURATE the 5-hr window, conserve the weekly. Model tiering: **fable** for design-heavy/novel or poison-downstream-if-wrong work (plant integration/parity, env+reward design, architecture calls, falsification analysis); **opus-4.8** for hard-but-not-novel (complex coding to known recipe, deep debug, heavy analysis); **sonnet-4.6** for routine coding to spec, eval/transport/git, ALL memory-banking agents (**🚩 sonnet-4.6 draws SEPARATE usage pool — measured 0% while weekly was 4%; push all mechanizable work there**); **haiku-4.5** for trivial ops. Prefer one deep session over speculative parallel breadth; parallelize only wall-clock-bound work (sim real-time, SLURM). User posts usage updates; commander throttles. **Throttle heuristic (rev 2): weekly <~10% → fable takes anything critical-path; weekly >~20% mid-cycle → opus absorbs known-recipe work; sonnet always takes wall-clock-bound/mechanical (separate pool). 5-hr window is use-it-or-lose-it — near a reset with headroom left, GO HAM (unspent window budget is free).**
- **Spec facts:** VFoV≈58.7° (spec's "90°" is HFoV; camera tilts up). ~100 TOPS onboard edge budget — under-used. Obstacles exist but don't map monocularly (fly the corridor).

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture, Track-A strategy, risks R1–R11, build sequence.
- [Phase-2 RL+vision decisions](project_phase2_rl_vision_decisions.md) — **READ when resuming Phase 2.** RL substrate, policy I/O, vision plan, staging, ALL stage verdicts/supersessions.
- [Sim interface](reference_sim_interface.md) — confirmed MAVLink+video wire spec; all 5 must-verify items.
- [CTBR control + sysid](project_ctbr_control_sysid.md) — flyable stack: gains/signs, offline twin, Gate-0 saga, alt-relay.
- [Detector pipeline + PnP](project_detector_training_pipeline.md) — Adroit YOLO-pose training; v2/v3; weighted-PnP; adroit-connector ops.
- [AI Grand Prix context](project_ai_grand_prix.md) — competition overview.
- [Competition materials](reference_competition_materials.md) — spec facts, rules/FAQ, VQ1 mechanics, open unknowns.
- [Hardware](project_hardware_constraint.md) — laptop (dev) · ShadowPC (sim) · Adroit (GPU training).
- [Adroit cluster](reference_adroit_princeton.md) — Slurm/GPU; summer access confirmed.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries + 8-paper ledger.
- [Tooling eval](project_tooling_recommendations_eval.md) — 3rd-party triage; keepers ADRC/GTSAM/min-snap/Isaac.
- [Estimator robustness](project_estimator_robustness.md) — adaptive R, innovation-gate, map-avg, SEARCH state, optical-flow.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must be full VQ2 stack under-tuned.
- [Red-team passes #2+#3](project_red_team_pass_2.md) — historical triage; kept to avoid re-litigation.
- [Checkpoint transfer](feedback_checkpoint_transfer.md) — small RL checkpoints go in git; ShadowPC pulls.
