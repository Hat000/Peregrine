**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-18.)

## 📁 VISION/FLIGHT BRING-UP THREAD (CLOSED — vision commander deprecated rev 4; superseded by the deploy-patch work below)
- 📁 Frame-supply saga + milestones A5/A10/A15 → [[now-archive-2026-07-05]] · [[index-vision-estimator]]. Durables: 🚩 **LAUNCH RITUAL `fly_rl` FIRST → "Waiting PASSIVELY" banner → THEN GO** · 🚩 **RENDER-OVERLAY GOTCHA: `render_vision_video.py` overlays red_glow, NOT the YOLO that flew** · 🚩 background-agent DEATH ⇒ check the tree, go INLINE for load-bearing · venv = MAIN checkout `.venv`.

## NOW · 🛑 VELOCITY CHANNEL + v2.0 (2026-07-27) — ALL detail SSOT [[velocity-channel-2026-07-27]]
- 🛑🛑 **ROOT CAUSE: the obs LATERAL-VELOCITY channel carries NO INFORMATION.** Regressing vision-measured lateral motion on `obs[1]` (honest ⇒ slope +1.0) gives **slope −0.110, corr −0.057, 53% sign disagreement (4945 v1.9 ticks)**; NO lineage near +1. 🟢 **Vision EXONERATED by Fengyou ("bang on") ⇒ error is KF dead-reckoning (no lateral ref), NOT vision — split CLOSED.** Policy told it slides TOWARD the gate while sliding AWAY ⇒ under-corrects ⇒ side strike. 🚩 **Peak roll ask 33% of rail; 0/113 flights near the rails — authority abundant, policy doesn't ask.** "Centred at 6.7 m" = a ZERO-CROSSING of a moving error, not a capture. EXONERATED: frames/signs, replay identity, authority, plant gain, yaw, builder, staleness-as-cause, course-OOD, EMA.
- 🚩🚩 **FRAME GOTCHA — misread ⇒ every log analysis inverts:** logged `rel_flu` is TRUE body FLU **UNFLIPPED** (neg lat = gate RIGHT), but `obs[0:3]` vel / `obs[5:8]` rates / `obs[11:14]` rel_pos are **VIRTUAL-FLIPPED** (`diag(-1,-1,1)`, `ego_obs.py:36,:124-127`) ⇒ **true v_left = −obs[1]**. Two conventions in ONE log line.
- 🚩 **9-GATE ARITHMETIC — RECORDS ARE LUCK:** 573-flight corpus, per-gate survival **71/66/67/56/46/20/44/50%** ⇒ **P(reach g9) = 0.35% ≈ 1-in-285** ⇒ 2 predicted, 2 observed. The flaw is **CONDITIONAL on crossing velocity** (position feedback alone threads an aligned gate; velocity only bites out of turns) ⇒ survival degrades monotonically with depth. **Net: converts ~1-in-3 into ~1-in-285.**
- 🏁 **v2.0 SHIPPED CONDITIONALLY** (`ego-ckpts-v20-2026-07-26`, v20Vs0) — **pilot authorised over commander objection.** Head-to-head: vert env **5.884 vs v19Ws0 4.893 (+20%)**, flat 5.973 vs 5.880; **v19 is course-FRAGILE (5.88→4.89), v20 course-ROBUST (5.97→5.88)**. 🛑 **BUT release dive REGRESSED ~2× (450/451 worse than v19)**; first 6 wire flights 3/6 died at gate 0 ~2 s. **v19Ws0 remains the SAFE lead.** 🚩 bit-identical COURSE geometry ≠ bit-identical POLICY behaviour.
- ⏭️ **RUNNING at compaction:** agents `d1-velocity` (truthful lateral velocity, default-OFF, offline-validated first), `p0-classifier` (definitive real-vs-estimate-motion classifier + 650-session census — the 10-vs-41 corpus disagreement), `p0-recorder` (record 0.5 s PAST impact; **31/78 at-gate deaths currently end 0.1-0.15 s BEFORE impact**). Cluster: C control arm **3323864** RUNNING. 🛑 **MIRRORED-COURSE PROBE DEAD — course is FIXED.**

## 📁 DEPLOY + TRAINING (2026-07-22) — SSOT [[deploy-emission-2026-07-22]] · campaign [[perception-sim2sim-gap-2026-07-15]]
- 🏁 **v1.8 / v1.9 SHIPPED** (`ego-ckpts-v19-2026-07-24` = v19Ws0, the SAFE lead). v1.9 = roll-rate penalties on action **ch1** + **`ROLL_EVAL`**, default-OFF byte-identical; signflips 2.6→2.26 ∧ n_passed 5.72→6.03 ∧ dive IMPROVED. Q arms REJECTED (dive 450/451). M1 convicted release-pitch/exonerated miss_rate · M3 keeper · M4→1.0 · M2 inert. 🛑 **EMA REFUTED** — the swept knob never touched the obs at all. → [[deploy-emission-2026-07-22]].
- 🟢 **GATE-1 RESOLVED (07-24):** wall was **v16-SPECIFIC (launch-untrained/OOD)** — v16 0/8 vs v18Qs1+darkred+z_bias 0.30 = 11/12. Vertical target is **pitch-coupled body-frame and CORRECT** (matches training; only coarse SECTOR obs[9:11] is gravity-leveled). Frontier moved to gate 3-4 = gate-to-gate `valid_poses_empty` + roll thrash. z_bias = a CONSTANT band-aid. → SSOT.
- 🚩 **ROLL is the UNGUARDED axis:** census gates YAW+PITCH but NEVER ROLL (fix: `ROLL_EVAL`); reward had rate penalties for yaw+pitch but roll only ANGLE (`rw_roll_recover=0.5` was ON, didn't damp the RATE osc). 🛑 **NO roll ANGLE fence** — backfires on the 60° turns; rate penalty ONLY.
- 🟢 vision-emission fix `b13a07f` WORKED; close-in blackout LARGELY FIXED in v19 (no-fresh-fix 23.6→7.3%, flicker 8.8→0.7%) ⇒ **the coast backlog was aimed at yesterday's problem**. Patch 3 `4ca994f` = `pass_drop_range_m=2.5` + candidate logging. → SSOT.
- 🛑 **TERMINAL DIVE (open training defect):** fence blocks **nose-DOWN only** (`fly_rl.py:766` `max(cmd,0)`) ⇒ `pitchcmd==+0.000` = a BLOCKED dive; inside 6 m ~100% fenced with the gate ABOVE the drone. Fence is LOAD-BEARING, masks training. → SSOT.
- 🚀 **CLUSTER OPS:** `gpu-medium` QOS = **2 CONCURRENT GPUs**. h11g1=4×A100 · h11g2=8×MIG `3g.20gb` · h11g3=4×V100; s/update 0.86 / 1.25 / 1.48 (all fit 20h). 🚩 default `--exclude=adroit-h11g3` blocks V100 — pass empty `SBATCH_EXCLUDE=`. `push_via_daemon.py` uploads over the LIVE daemon. **Backfill/queue/daemon footguns → [[velocity-channel-2026-07-27]] §CLUSTER OPS ADDENDA.**

## NOW · 🔬 CLAMP + MAP DURABLES (2026-07-21 — detail SSOT [[perception-sim2sim-gap-2026-07-15]])
- 🛑🛑 **PITCH CLAMP IS BIMODAL, NOT A DIAL:** resting `obs[4]` = **−0.3107 rad = −17.80°** (n=100); fence = `obs[4] <= −clamp_rad` (fly_rl.py:757) ⇒ **clamp ≤17.8 = fence ON AT REST · ≥17.9 = OFF**. Every clamp result in project history is a fence-on/off dichotomy.
- 🚩 **det_hold owns the ADVANCE** (0.2 → true-gate lock ×8/8; 0.3 → WRONG far lock ×6/6) · **stale_horizon owns the CLOSE-IN blind meter** (last ~0.18 s flown OPEN-LOOP). **Deploy default 0.2/0.6.**
- 🚩 **RE-FLY RULE = ‖gyro‖>2.5 for ≥3 CONSECUTIVE ticks** (the old "first 0.75 s" rule was a CLAMP DETECTOR).
- 🚩 **A RECORD IS NOT A BASELINE** (now corpus-wide: P(9 gates)=0.35%≈1-in-285, 2 predicted / 2 observed in 573 flights → [[velocity-channel-2026-07-27]]). Map cohorts statistically NULL (p=0.63).
- 🗺 **MAP SURVEY INSTRUMENT = FLIGHT PATH, never the camera.** Camera traps: `obs[3:5]` VIRTUAL-FLIPPED (fake climb), row authored at-pass in incoming frame (fake left). · 🚩 **TWO RANGE CAPS:** `max_valid_range_m` (30, exposed) vs **`max_acquire_range_m`=22 (NO panel field)** — costs only 0.30% of ticks, `continuity_reject` is ~38× bigger. → [[perception-sim2sim-gap-2026-07-15]].
- 🚩 **v1.7 M1 PREMISE FALSIFIED** (0/161 hand over level; gate elevation median **+2.4°** off axis) ⇒ **M1 INTRODUCED the mismatch**; v1.7 pitched 62% harder at release on 385/388. **Mean said better, the TAIL said worse** → [[feedback-tails-not-medians]].

## 📁 TRACK A (2026-07-13, historical) — SSOT [[track-a-beat-vpeffs0-2026-07-13]] · ratchet [[replay-ratchet-2026-07-17]] · stall [[trackA-stall-forensic-2026-07-13]]
- 🎯 GOAL: BEAT vpeffs0, full 20-gate course. 🚩 **PRESERVE yaw→gate-in-view→altitude coupling** (penalize yaw JITTER only) → [[feedback-beat-dont-reproduce]] · [[feedback-preserve-yaw-altitude-hold]]. **Cam FOV 90°H/58.7°V (fx=fy=320)**.
- 🚩 DURABLE OPS: **PIN "GO ≥3 s post-reset"** (wake-0 variance ↓1000×) · **recipe TRUTH = PANEL LOG not meta.json** · clamp is PER-RELEASE, mixing = OOD · zero gate contact = validity · RECIPE (.hydra) ticks_hi=1, yaw 0.7, blur=false · 🛑 ARRESTOR/tape/speed-gov RETIRED → [[feedback-no-deploy-bandaids]] · fence ONLY the axis the course DOESN'T use · select on exit_frame/HAZARD/n_passed **NEVER value** · multi-seed · monitor TB not stdout.
- 🟢 **v1→v1.8 CAMPAIGN durables:** ceiling was SPEED ACCUMULATION (closed) · "flat-plant=wall" OVERTURNED (NO more plant tuning) · **SIM YAW_EVAL TRANSFERS 1:1 TO WIRE** (flips ≤4 ∧ absmean ≤0.3 ∧ satur ≤10%); fly the TRAINING action space → [[feedback-training-faithful-deploy]] · runaway was DISCOUNT arithmetic · billboard = death-zone obstacle · Fengyou counts gates **1-BASED** · telemetry = DEFENDANT, ask human eyes · 0-byte agent transcript ≠ dead (TREE mtime; **≤2 heavy agents**).

## 📁 ARCHIVED THREADS (EGO/inc9 → produced vpeffs0 · background) — SSOTs → [[ego-deploy-contract-2026-07-09]] · [[audit-ego-inc9-2026-07-09]] · [[vq2-rl-commander-2026-07-05]] · [[now-archive-2026-07-05]]
- 📁 **EGOCENTRIC position-free obs (21-dim, critic 16); SSOT `docs/vq2-egocentric-gen/DESIGN.md`.** 🚩 DIAGNOSE FROM TRAINING METRICS · 🚩 **L16: absent hook prints on a LIVE job ≠ inert (SLURM buffers) — adjudicate from a COMPLETED log/TB** · multi-seed MANDATORY (~2.8× variance) · BANK-FIRST finish-20-any-speed · **NO GT on wire** · P2 bake TRANSFERS.
- 🟢 **VQ2 SPEC + WIRE RECON → [[reference-sim-interface]]:** HIGHRES_IMU 117 Hz accel+gyro only · 30 Hz JPEG · RACE_STATUS 4 Hz · dark warehouse. 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.**

## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read FIRST on boot; EDIT LAST before handoff (add the lesson, prune the stale, log your generation). MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL (rev 4, 2026-07-19, Fengyou): VISION COMMANDER DEPRECATED — RL commander = SOLE commander, owns the deploy repo/branches DIRECTLY (read+edit+push ratchet/deploy branches; NO more relay boxes; Fengyou = pilot + hands on ShadowPC).** rev-2 mechanics stand (laptop → Agents/Workflow; Adroit → adroit-connector `serve`/`x`). rev-3 domain-split history → [[now-archive-2026-07-05]] · [[index-strategy-meta]].
- 🚩 **CANARY (rev 2, 2026-06-14):** persistent COMMANDER sessions address **Fengyou** by name in EVERY message (missing name = context degradation → rotate). Workers don't need it.
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages; bank IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt: SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA req + escape hatch + report path.
- 🚩 **DELEGATE BUILDS TO AGENTS (2026-07-12, Fengyou):** multi-file implementation (features, edits, test authoring) = spawn agents/Workflow to edit+test+verify; commander thread = judgment/diagnosis/review/relay only. Ultracode on ⇒ default to Workflow/Agent for builds. → [[feedback-delegate-builds-to-agents]]
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; SLURM only on Adroit; concurrent sessions use separate worktrees. 🛑 **NO-SPIN = HARD REQUIREMENT: enforce by CONSTRUCTION** (fatal spin aborts + realized-yaw clamp), NEVER incentive shaping; NO energy penalties → [[feedback-no-spin-hard-requirement]]. 🛑 **NO-GT IN ACTOR OBS: actor sees ONLY estimator-faithful state**; GT legal in critic/reward/terminations only → [[feedback-no-gt-actor-obs]].
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Workers surface NEW parked items in MEMORY-DELTA; commander files them.
- 🚩 **FLIGHT CURRICULUM: close the loop SLOW first, ramp after (2026-06-29).** 🟢 A5 = first gate passed (map-free, no mag/baro). → [[feedback_slow_is_smooth]] · [[index-vision-estimator]].
- 🚩 **BLENDER VQ2 DATA PIPELINE:** render harness matched to glowing-red/dark-warehouse appearance — HIGH-FIDELITY ONE-LOOK, not broad DR. → [[project_blender_vq2_data_pipeline]]
- 🚩 **MOONSHOT: championship-fast, not just finishing.** North star = Swift (Nature 2023). Slow bootstrap → perception hardening → RL speed ramp. → [[project_moonshot_speed]]
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly (no ceremony); the C6/POC hold is a SEQUENCING gate (Fengyou's), not risk. Validity rule (zero gate contact) + sim time = the only flight constraints. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]
- 🚩 **TAILS NOT MEDIANS (Fengyou 2026-07-19):** flight verdicts lead with per-flight worst-events (max/p99, tail counts, single-event kill attribution) — one strong roll kills a flight with a perfect median. All adjudications + A/Bs carry a tail column. → [[feedback-tails-not-medians]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN.**
- 🚩 **A PROBE THAT RUNS ≠ A PROBE THAT MEASURES.** Hand-built "at rest" obs are **OOD** (tell: every arm returns **~zero collective**) and rank arms differently than logged obs — launch-window questions go through `replay_sweep_v18.py` over real flights, never a synthetic. And **decode channels from the wire path**: ego action = **`[thrust, roll, pitch, yaw]`**, so `act[2]` is pitch; with virtual_flip `rate_frd[1]=+act[2]` ⇒ nose-DOWN = `rate_frd[1]<0`. → [[deploy-emission-2026-07-22]] §Method
- 🚩 **`[daemon] unauthorized` = TWO daemons, not bad auth.** Windows `SO_REUSEADDR` lets a restarted `adroit.py serve` bind 8765 while the stale one keeps answering with its old token. `netstat -ano | findstr 8765` → two PIDs ⇒ **kill the OLDER**; re-running `serve` only adds a third.
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]] §VQ2-WIRE-RECON.
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) → stream `SET_ATTITUDE_TARGET` BODY-RATE (mask 0b10000000, FRD, thrust [0,1]) ≥30 Hz; `fly_rl.py` drives. Use `DroneState.gyro_body`, NOT `angular_rate_body`. → [[index-vision-estimator]].
- 🚩 **CRITIC FOOTGUN: use `algo=appo`** (ppo leaves the privileged critic unconnected = seed collapse; critic input_dim **37**) → [[index-rl-training]].
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7). `fly_rl.py` default ckpt retired → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG = self-consistent alias — DO NOT "fix"** (tail-first training + virtual π flip compensate; camera_flip touched the emulated camera only) → [[index-control-sim]].
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session. → [[index-control-sim]].
- 🚩 **`main` CANONICAL · 1091 tests collected (2026-06-20)** · `*.pt`/`*.pth` + ckpt JSONs + data/runs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`burn-artifacts-*`, `gh release download`), NEVER git-add. → [[project-fullstack-burn]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG — 24 m flip). Pinned by tests/test_obs_sign_faithfulness.py. Future estimator→obs MUST deliver +L. → [[index-vision-estimator]].
- 🚩 GATE-4 margin footguns (parked w/ inc8) → [[now-archive-2026-07-05]] · [[index-vision-estimator]].
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]].
- 🚩 **WORKTREE HYGIENE:** auto-worktree per session = isolation; commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` pool RECYCLES — NEVER manually prune; only `git worktree prune`. → [[index-strategy-meta]] §worktrees.
- 🚩 **BORESIGHT bake = METRIC TRANSLATION** (vert_offset_m −0.25; form-discrim needs Δrange ≥~10 m) → [[index-vision-estimator]] §P1.

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🧠 **[[vq2-rl-commander-2026-07-05]]** — RL-commander thread SSOT (audit, B1 fixes, job 3295856, fidelity plan, vision-commander asks).
- 🚀 **[[deploy-emission-2026-07-22]]** — 07-22 SSOT: v1.8 adjudication, vision-fix deltas, stale-frame flicker, patch 3, terminal dive, next-gen env spec.
- 🗄️ **[[now-archive-2026-07-05]]** — verbatim blocks relocated in the 2026-07-05 compaction (flight saga, wire recon, parked-inc8, durables, footgun full texts).
