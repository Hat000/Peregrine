**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. 🚩 **VQ1 DEPRECATED 2026-06-28** — never train/test/optimize against the VQ1 track (VQ1 = throwaway smoke fixture only); PLANT/dynamics work stays track-agnostic. → [[feedback-vq1-deprecated-track-agnostic]]

## NOW (2026-06-29) · 🟢 VQ2 ACTIVE (live recon done, control handshake SOLVED; design/spike phase — NOT yet building the deployed stack)
- 🟢 **VQ2 LOAD-DAY LIVE RECON 2026-06-29 (build 1.0.3379) — case-C CONFIRMED LOAD-BEARING.** §9.3 blocks (ATTITUDE/LOCAL_POSITION_NED/ODOMETRY/GLOBAL_POSITION/GATE_INFO) BLOCKED in BOTH training AND competition (byte-identical msg sets — "training exposes more" REFUTED; track NOT per-load randomized — deterministic). REMAINING wire = HEARTBEAT(10Hz) + HIGHRES_IMU(117Hz, accel+gyro ONLY) + 30Hz JPEG cam (udpin:14550/video:5600, 24-byte hdr) + ENCAPSULATED_DATA=RACE_STATUS(4Hz, `active_gate_index` PRESENT → gate ordering SOLVED on wire) + ACTUATOR_OUTPUT_STATUS(95Hz, 4 motors, plant-sysid gift); TIMESYNC response-only. TRACK = "Now You See Me, Now You Don't" (dark warehouse, stations 01-20). Spec `260624_Technical_Spec_0003.pdf` (VADR-TS-003). NO-MAG/NO-BARO = live footgun below; control handshake SOLVED (below). Full wire detail → [[index-vision-estimator]] §VQ2-WIRE-RECON-2026-06-29 · [[reference-sim-interface]]
- 🚩 **NAVIGATE BY VISUAL GUIDANCE — ASSUME NO MAP (Fengyou 2026-06-27, firm).** Competition = detect NEXT gate, fly through it; gate-relative PnP needs only KNOWN gate size (1.5 m inner square), NOT a world map → the merged gate-relative design ALREADY covers this; map-building dependency DROPS. `gate_mapper`/`track_map.json` demote to TRAINING/DEV-only. Must also SELF-ESTIMATE ATTITUDE (AHRS from IMU; VQ2 gives none). → [[index-vision-estimator]]
- 🟢 **VQ2 STACK DESIGN RESEARCH (2026-06-27, divergent — FORKS OPEN, spikes pending, NOT building).** 25 papers + 3 Gemini scans digested. Headlines: recon-map MOAT (VGGT+gate-PnP turns no-map→known-map); asym privileged critic = #1 fix (6 sources: symmetric=catastrophic); discard-PnP-rotation (square flip); bootstrap vision-RL (pure=0%); defense-GNC edge (bearings-only Gap-Traversal Guidance + Modified-Polar EKF). SSOT → [[vq2-stack-research-2026-06-27]] → `docs/reactivation-2026-06-27/candidate-stack-v2.md` (+ `lit-review/`).
- 🟢 **READY ASSETS (carry into VQ2):** C2 gate-relative estimator chain MERGED (05ed750, gated OFF=byte-id; RewindKF OOSM + gate_relative +L anisotropic-cov + relinnov χ²(2,.999)=13.82 + estimator_obs seam + NavState cov export; G3 rel E_bias −0.000/RMS 0.131) · VQ2 detector CLEAN ENSEMBLE (282abb9: course 76→82% p=0.0005, a training-LOSS lever NOT data; weights gitignored; deploy-step-1 06876bc opt-in EnsembleGateDetector, UNWIRED) · inc8 base (obs_dim=20 frozen d5 layout; torch port b0b322e). → [[index-vision-estimator]] · [[index-rl-training]]
- 🚩 **GATE-4 / RL DOCTRINE (corrected 2026-06-19):** the σ_p0≲0.08 "closure bar" was a DOUBLE-COUNT; REAL bar σ_p0_lat≲0.15 (p99≲0.45); sim clearance 0.37–0.47 m. Gate-4 hard = REACH/PASS-RATE (sits off the racing line), NOT a tighter gate. **RL IS the right tool** (vision = SUPPORT, do NOT lean on it). inc8 = case-C deployability not a big speed jump (gate-4 vertical-σ-dominated). Milestones (S5 recenter flew 3/3; det-flyable retrain 42ddb0b reach 0.467; stashed reach-rate ~0.72 HELD pending Fengyou) + the LIVE launch SIGN FOOTGUN (`++env.lookat_g_yaw=-3.0 ++env.lookat_g_pitch=3.0`, double-plus, opposite empirical signs) → [[index-rl-training]] §inc8 · [[index-vision-estimator]] §TERMINAL-LOCK. ⚠ gate-4 is VQ1-track-flavored → VQ2 RL must train on randomized/synthetic layouts (S2 = `staged_monolithic_then_decomposed`).
- 🚩 **CRITICAL PATH (VQ2):** ① close the loop SLOW (vision self-loc lap, zero contact — control handshake already SOLVED) → ② perception hardening (detector + EqVIO yaw/Z spine) → ③ RL speed ramp. SIM = no physical risk (directive below); C6/POC = Fengyou's sequencing gate. → [[index-control-sim]] · [[index-vision-estimator]]
- 🟢 **REACTIVATION CLEANUP (2026-06-27):** git topology cleaned, `handoff/` slimmed 93→20 MB (binaries tarred to `Anduril-archive/`), memory reconciled, `~/.claude` mirror synced. Re-onboarding map → `docs/reactivation-2026-06-27/REACTIVATION_BRIEFING.md`. Preserved in-flight (do NOT prune): branch `inc8-deterministic-retrain` (05809ba) + stashed reach-rate (branch `claude/funny-nash-d07d7b` @ 0edea93 + `handoff/inc8-reach-rate-2026-06-19/`). Boot = read `COMMANDER.md` then this block; status = `git log --oneline -8` + `.venv/Scripts/python.exe scripts/green_gate.py`. (`main` push FINE: origin==local; ShadowPC pushes FEATURE branches only.)

## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read it FIRST on boot (role + generalizable doctrine: selective-merge default, sole-memory-writer, empirical-over-analytical signs, seed discipline, diagnose/bracket/risk-order, relay-hazard); EDIT it LAST before handoff (add the lesson, prune the stale, log your generation in its Lineage). This MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL (rev 2, 2026-06-27):** ONE persistent commander session that DELEGATES IN BACKGROUND; copy-paste relay RETIRED **except ShadowPC**. Laptop → spawn Agents/Workflow directly. Adroit → drive via the adroit-connector (`C:\Users\Fengy\Downloads\Projects\Adroit\adroit-connector\adroit.py`; `serve` daemon, `submit`/`x`/`upload`/`pull_file.py`; PINS hmac-sha2-256 vs VPN MAC-corruption). ShadowPC = ONLY paste relay; jobs by priority: (1) run competition sim [PRIMARY], (2) render training imgs, (3) train vision, (4) compute. Commander keeps SOLE memory authority + merge gate + SSOT. Retire last paste = build a shadowpc-connector → [[index-strategy-meta]] §SHADOWPC-CONNECTOR.
- 🚩 **CANARY (rev 2):** the OVERALL COMMANDER persistent session addresses **Fengyou** by name in EVERY message (missing name = context degradation → rotate session). Sub-commanders/workers do NOT need the canary.
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work** (the affordable design tier); **ultracode on opus** for hardest fan-out/verify; **sonnet-4.6 = low-stakes mechanizable** (banking, flight ops, git, reruns); haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index):** workers end with **MEMORY-DELTA** (≤10 lines); commander triages; sonnet banking agent banks IN BATCHES. **Route detail to the topic file + its domain sub-index; touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use the /consolidate-memory skill** (Fengyou, repeated) — maintain the thin index BY HAND: when MEMORY.md grows over limit, route bloated/superseded detail DOWN a layer and leave a one-line `[[pointer]]`. Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt carries SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA requirement + escape hatch + report path.
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees.
- 🚩 **ANTI-FORGET:** review **[[project-parked-backlog]]** at session start + whenever a trigger fires (POC unfreeze · Adroit contact · fresh recording). Workers surface NEW parked items UP in MEMORY-DELTA; commander files them in the register — NEVER re-scatter parked items back into topic files.
- 🚩 **FLIGHT CURRICULUM (2026-06-29): close the loop SLOW first, ramp speed after.** Slow = vision self-loc works (more frames/m, no blur, yaw+Z from vision). First milestone = slow zero-contact self-loc lap. → [[feedback_slow_is_smooth]]
- 🚩 **BLENDER VQ2 DATA PIPELINE (2026-06-29): build a Blender render harness matched to glowing-red/dark-warehouse VQ2 appearance — HIGH-FIDELITY ONE-LOOK, not broad domain randomization.** Extends learned detector beyond classical red-glow limits. → [[project_blender_vq2_data_pipeline]]
- 🚩 **MOONSHOT (2026-06-29): target = championship-fast, not just finishing.** North star = Swift (Nature 2023). Strategy = slow bootstrap → perception hardening → RL speed ramp. → [[project_moonshot_speed]]
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** measurement/non-POC sim flights carry no risk — commander AUTHORIZES directly (no risk-escalation ceremony). The C6/POC hold is a SEQUENCING gate (Fengyou's), NOT a risk gate; only the explicit POC unfreeze needs him. Zero gate contact + sim time = the only flight constraints. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN** (zero-contact is THE validity rule).
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed 2026-06-29, build 1.0.3379):** `HIGHRES_IMU.fields_updated=63` = accel+gyro bits 0-5 ONLY; mag + baro absent/NaN. YAW + ALTITUDE must come from VISION (EqVIO landmark bearings pin yaw+z); ESKF/AHRS = roll/pitch leveler only. Any code path assuming HIGHRES_IMU carries mag or baro is DEAD. → [[index-vision-estimator]] §VQ2-WIRE-RECON-2026-06-29
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2, 2026-06-29):** RECIPE = ARM (MAV_CMD 400, p1=1) then stream `SET_ATTITUDE_TARGET` BODY-RATE (type_mask=0b10000000, FRD rates + thrust [0,1]) ≥30Hz; sim default = ACRO = CTBR; `fly_rl.py` already drives this path. Earlier tumble = wrong msg (`SET_POSITION_TARGET_LOCAL_NED`/attitude-QUAT both ignored in ACRO). Deploy: use `DroneState.gyro_body` (HIGHRES_IMU), NOT `angular_rate_body` (ODOMETRY-blocked). → [[index-vision-estimator]] §VQ2-CONTROL-HANDSHAKE-2026-06-29
- 🚩 **inc8 CRITIC SYMMETRIC FOOTGUN — fix WIRED 2026-06-27:** training `algo=ppo` built the critic at input_dim==20 (symmetric) → the privileged `get_state` critic was NEVER connected = root-cause of seed collapse (confirmed ×3). Asymmetric path WIRED + smoke-clean: `GuardedAPPO(AsymmetricPPO)` + `algo=appo` → critic input_dim **37** (🆕 `get_state` is 37-dim not 36); `ppo` byte-identical. Launched job 3289989. PRODUCTIONIZE: re-apply 2 hunks to CRLF `main` (Adroit copy is LF-rewritten). → [[index-rl-training]]
- 🚩 **Judged/deploy runs use `rl/submit_rl.py`** (no `MAV_CMD 31000` on the wire; `fly_rl.py` default ckpt = retired inc4 → pass the ckpt explicitly if bypassing submit_rl). → [[index-control-sim]]
- 🚩 **VQ1-LEGACY control conventions = HISTORICAL, do NOT "fix" or rely on for VQ2** (VQ2 blocks ODOMETRY/ATTITUDE): CTBR legacy sign config = self-consistent alias; ODOMETRY quat is `R_y(π)`-CONJUGATED + ATTITUDE.pitch sign-inverted (VQ1 ran `scripts/frame_residual_report.py` after every live session — internal consistency can't catch a conjugation). PLANT-level CTBR/deploy/latency facts stay LIVE. → [[index-control-sim]]
- 🚩 **`main` CANONICAL · 1091 tests collected (.venv, verified 2026-06-20) · `*.pt`/`*.pth` + `rl/checkpoints/*.json` + `data/runs` gitignored.** Pre-merge gate = `scripts/green_gate.py` (GREEN/RED; stack-guard + load-bearing invariants + sentinel baseline **1091**; `--full` GREEN 1047 pass/44 skip). 🚩 **ARTIFACT PIPE:** binaries (ckpts/weights/recordings) ship as GitHub release assets (`burn-artifacts-*` tags, `gh release download --repo Hat000/Peregrine`), NEVER git-add. → [[project-fullstack-burn]]
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** The d1/d2 spec PROSE "estimator delivers −L" is WRONG (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py. The future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- 🚩 **CONTACT RADIUS 0.38 = EMPIRICAL CRASH HALO, NOT GEOMETRY** (rigid chassis caps at 0.2135 m; 0.38 = DR upper tail of a posture-matched steep-crash band). Central reporting radius = **0.30 m**; do NOT "fix" 0.38 as a geometry bug. → [[index-vision-estimator]]
- 🚩 **BINDING MARGIN = VERTICAL BORESIGHT BIAS ε_vert ≈ 0.215 m (~0.56° camera-pitch) + attitude/accel bias, NOT contact radius** (δ_map discriminator proves no map offset; +L does NOT cancel it; close via boresight calibration + ESKF attitude-bias est — CANNOT-SETTLE-OFFLINE survives). Gate-0-anchor caveat, the common-mode-≠-cancellation discriminator, and the boresight form-discrimination method (≥2 ranges, Δrange≥~10 m) are all detailed → [[index-vision-estimator]]
- 🚩 **CAMERA MOUNT FIXED 20° (SPEC §3.8, EXACT) — mount-uptilt lever DEAD; elevation fix-rate is POLICY-only.** VFoV=90° in spec is MISLABELED (it is HFoV; TRUE VFoV≈58.7° → ALWAYS decode vertical from fy=320). Boresight ε_vert is a calibratable seam, NOT a mount-angle error. → [[index-vision-estimator]] §Spec facts
- 🚩 **ADROIT AUP = SUSPENSION RISK:** SLURM only (NEVER login-node compute); compute nodes have NO INTERNET (pre-download git/pip/HF/weights on login/vis FIRST); output → /scratch NOT /projects; accurate --mem + 1-core-serial (over-alloc/multi-core-serial → suspension); zero-GPU-util jobs killed @2h; run `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]]
- 🚩 **WORKTREE HYGIENE:** the harness auto-creates one isolated worktree per spawned session (alone satisfies non-interference); workers MAY make their own freely. The commander OWNS consolidation — merge EVERY worker branch at the gate, then prune merged branches/worktrees. NEVER manually prune `.claude/worktrees/*` (harness-managed + recycles, racy); only `git worktree prune` + remove confirmed-DONE manual worktrees. → [[index-strategy-meta]] §worktrees

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, S2 decision, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VQ2 wire recon + control handshake, VISION-PKG2, detector pipeline, case-C readiness, boresight/ε_vert, estimator robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening, VQ1-legacy wire/sign (deprecated).
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling eval, walking-skeleton + feedback, commander/shadowpc-connector.
- 📂 **[[project-parked-backlog]]** — STANDING register of banked-but-parked threads, triaged F/T/D with revive-triggers. **REVIEW each session + when any trigger fires.**
- 📡 **[[reference-competition-materials]]** — COWORK-1 intel (Windows/8 GB-VRAM eval, GPU path ≪50 ms; MonoRace arXiv 2601.15222 = near-1:1 prior art; Round-2 3D-scanned photoreal env → P5 justified).
