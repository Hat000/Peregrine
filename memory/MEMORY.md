**Peregrine** — our entry in the Anduril **AI Grand Prix** autonomous drone-racing competition (May–Nov 2026; user competing for fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. This index is the orientation layer; detail lives in the topic files below.

## READ FIRST — current state (2026-06-07)
**FIRST CONTACT with the official sim SUCCEEDED** (on **ShadowPC**, co-located with `FlightSim.exe`; the laptop's Intel iGPU can't run the sim). All 5 must-verify items RESOLVED — full wire interface + findings in [[reference-sim-interface]]; raw notes in repo `handoff/shadowpc-firstcontact-2026-06-02/`. Headlines:
- **Position + velocity are GIVEN, pristine** (LOCAL_POSITION_NED 97 Hz + ODOMETRY 75 Hz). Baro field present but VALUE=NaN → z from given position. Keep vision→KF in-loop anyway (walking-skeleton); VQ2 may turn position OFF.
- **🚩 ATTITUDE.pitch is SIGN-INVERTED → drive orientation from the ODOMETRY quaternion** (done, commit 2b81fc1). This was the 35.6° "attitude bias" — NOT an ESKF trigger; do NOT enable the bias-state.
- **Gate map = 6 gates, width/height 2.72 m = OUTER square; inner opening ~1.5 m is what PnP uses** (do NOT feed 2.72 to inner-corner PnP). Broadcast ONCE at level-load to already-connected clients → connect at the HOME page (`wait_heartbeat=False`), then navigate in (`capture_track_map.py`). Deterministic course: map once, reuse.
- **Fly on CTBR** (body-rate + explicit collective via `SET_ATTITUDE_TARGET`; sim runs ACRO — any client traffic flips ANGLE→ACRO; easy-mode CLOSED with a recording = World A, velocity-in-ANGLE drives collective to MAX, `handoff/shadowpc-velocity-fork-2026-06-04/`). The plant is system-ID'd and an OFFLINE TWIN is fitted+validated (see NEXT). **Faithful params** (`src/racer/twin_fit.py`, from the `course1`+rate/hsweep sysid extract): **rate_gain [2.50,2.50,2.23], physics `rate_sign=[+1,+1,−1]` (only YAW command inverted), τ=0.019 s, hover=0.2656, `linear_drag≈0.21/s`** (≈4 m/s terminal at cruise pitch — the ideal twin lacked it). **🚩 PHYSICS vs TELEMETRY (laptop Task B caught this): the ODOMETRY quat REPORTS roll inverted + raw rate inverted on roll+pitch — a REPORTING artifact, NOT physics. The controller's measured `body_rate_sign=[1,1,−1]` / `odo_att_sign=[−1,1,1]` / `odo_rate_sign=[−1,−1,1]` / `ff_gain=2.5` undo them; a twin with roll-PHYSICS=−1 passes straight course1 but runs +y away closed-loop.** `tilt_comp` thrust/cos(tilt). **Arm = plain `MAV_CMD_COMPONENT_ARM_DISARM` p1=1.** Detail: [[project-ctbr-control-sysid]].
- **🎈 Altitude balloon = OURS, not the sim** (Task 3, 2026-06-06, live ×2 + GUI): in CTBR the motors track our commanded collective to ±0.002 (never pinned high) and vz is monotonic in collective → **NO sim auto-thrust in the CTBR path**. The balloon during runs is our own alt-PD/vz-damping commanding too much collective → **fixable OFFLINE**, not a sim constraint. (The broken pos/vel auto-thrust IS real but only on the ANGLE velocity-setpoint path, NOT CTBR.) `handoff/shadowpc-followups-2026-06-05/TASK3_BALLOON.md`. **→ The VERIFY altitude LIMIT CYCLE is exactly this — our alt-PD relay — driven by the LOOP TRANSPORT DELAY (~40 ms), NOT the vz source (raw-vz fix did NOT remove it). It's ALWAYS-ON (runs through the moving descent, not static-only) but VALIDITY-HARMLESS (the 6/6 VQ1 finish threaded to 0.03–0.37 m). Clean-up deferred post-VQ1 (see NEXT D, 2026-06-07).**
- Clocks INDEPENDENT (video sim_time = server UNIX-epoch ns; IMU = boot-relative; align on recv-monotonic). Video frames re-sent ~14× → **dedup by frame_id (done, 04a4bc8)**. Sim PAUSES physics off-race → live control + system-ID need a RUNNING race.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate, 6k) — won the fixed-set eval (best corner + pose). v3 `--hard` tail-oversampling is a documented NEGATIVE result (regressed the bulk, didn't move the ~0.7% high-roll corner-swap = geometric aliasing → fix in the solver/temporal prior, not training data). Detail: [[project-detector-training-pipeline]].

**Git / env:** **`red-team-tier-a`** (ahead of `main` by ~30; faithful twin + live config + the raw-vz alt-loop change [2026-06-07; did NOT fix the cycle — see NEXT D — but harmless/kept]; laptop A/B/C also on `claude/quizzical-payne-a60690`, pushed/backed up). **288 tests green**, `.venv` Python 3.13. **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`): ODOMETRY twist (`vx/vy/vz`) is reported in `child_frame_id`=`MAV_FRAME_BODY_NED` (8, body z-down) but `_handle` stored it as `velocity_ned`=world → a world/body MIX (sign-flipped vx/vy whenever yawed+moving, e.g. the −180° course heading) that fed the KF + controller damping + planner heading. Now rotated body→world via `frames.world_vec_from_body_quat`. **RE-VALIDATED + CONFIRMED 2026-06-05 (offline replay of saga recordings, `handoff/shadowpc-framefix-revalidation-2026-06-05/`): the §5 "KF velocity lags 4×" WAS this bug — the mixed measurement read 0.21× truth (= the "−0.22 vs −0.92"); the fixed KF tracks truth to 0.05 m/s; rotation validated across pitch/roll to ±12°/±20°. The "control on raw given velocity" workaround read the SAME corrupted field (a phantom fix). §1 roll-inversion + §7 balloon are SEPARATE + still real.** `*.pt` weights + `data/runs` recordings are gitignored (move via the adroit-connector); project memory is mirrored into repo `memory/` (the cross-machine sync vehicle — ShadowPC passes findings via `handoff/`, not by editing `memory/`).

**NEXT — [2026-06-07] 🏁 VQ1 FLOOR DEMONSTRATED (valid 6/6 sim-recognized finish ~35.3 s). The offline-twin discipline WORKED; roadmap A/B/C/D DONE.** Now: make the finish RELIABLE (start-transient #1) + the RL/VQ2 pivot. Stop the live whack-a-mole = tune offline against a faithful twin, fly only to verify. (A✅) ODOMETRY frame fix `c3b5a8e` revalidated. (B✅) faithful `CtbrPlantConfig` fitted+validated from the sysid extract (`scripts/fit_twin.py`; one-step att <0.12° / vel <0.006 m/s → model structure correct; open-loop speed RMS 0.62; drag discovered). (C✅) re-tuned the decoupled CTBR outer gains on the faithful plant → **`FAITHFUL_TUNED_GAINS`/`_PLANNER` (`scripts/twin_tune.py`) thread all 6 gates offline (in-plane miss [0.13,0.61,0.14,0.26,0.14,0.05], worst 0.61<0.75 half-opening); the canonical-tuned gains do NOT transfer (diverge off gate 0) → the faithful re-tune was ESSENTIAL.**
- **🏁 (D) DONE — VQ1 FLOOR DEMONSTRATED (2026-06-07): a valid sim-recognized 6/6 finish, ~35.3 s** (`handoff/shadowpc-reverify-2026-06-07/rung23/`, re-derived on the laptop). Per-gate in-plane miss **[g0 0.37, g1 0.05, g2 0.06, g3 0.03, g4 0.10, g5 ~0.71]** m (all < 0.75 half-opening; g1–g4 tight, g0 = start-bank residual, g5 marginal/sim-confirmed). The `kp_alt 3.0/kd_alt 1.75` + raw-vz config threads; a **−0.4 m alt_offset** centred g0 (0.74→0.11/0.37). The rung-1 cycle was correctly accepted (validity-harmless). **NOTE the competition-submission mechanics are still unconfirmed** (email info@theaigrandprix.com) — we have a valid *flight*; whether that = a banked *submission* is open.
  - **3 findings (re-derived from the course extract; OFFLINE priorities for a RELIABLE finish):**
  - **① 🚩 START TRANSIENT is NON-DETERMINISTIC + severe (the #1 risk).** Same config, run-to-run: peak lateral **0.29 m (clean) vs 5.99 m (course)**, tilt 43°→72°. At the takeoff→RUN switch the attitude→rate command **saturates the 8 rad/s `max_body_rate` clamp** (level the −17.8° rest pitch + pitch into the `max_accel 12` lunge at once); the saturated, lightly-damped (`kp_att 10`/`kd_att 0.15`) response is tick-timing-sensitive → the bank is a dice-roll. A 6 m swing could miss g0. **Fix (offline): ramp the start speed/accel from standstill and/or soften the attitude step so it never slams the 8 rad/s clamp** (de-saturate → deterministic). The twin is deterministic so it can't show the dice-roll, but it CAN verify the start stays unsaturated.
  - **② Aggressive descents** — peak sink **9.3 m/s** with minor wobble, no clips, all gates passed. Smoothness/time = VQ2 racing-line/speed-profile territory.
  - **③ 🚩 CORRECTION to my earlier note: the alt relay is ALWAYS-ON, not "static-only".** Cruise thrust std **0.257, 47% at the 0.6 ceiling / 36% at the 0.05 floor** — the bang-bang runs through the MOVING descent too (the audible cycling all race). It is **validity-harmless** (gate misses 0.03–0.37 m prove position tracks fine), but the relay is NOT suppressed by motion as I claimed. Still deferred to post-VQ1 (gain-schedule / RL).
  - **Background (still valid):** the cycle is a delay-driven relay (loop delay ~40 ms = period÷4); the conflict is fundamental (descent needs `kd_alt≥1.75`, static hold needs `≤0.5`; rate-limit makes it worse); `hover_thrust 0.2656` validated; **🚩 the twin under-models live latency ~25% even calibrated — don't trust offline "holds" claims** (raw-vz fix `dd16091` was an offline-confidence miss).
  - **NEXT (offline, priority order): ① the start transient** (de-saturate the takeoff→RUN attitude command — the reliability blocker) → ② descent smoothness + relay clean-up (gain-schedule) → ③ time (racing-line). All of ①–③ are squarely **the committed RL/VQ2 pivot's** job ([[project-master-plan]] C1) — "we may not even need" the PD fixes if RL lands. Confirm the VQ1 submission mechanics in parallel.
- **Gate-centre RESOLVED (Task 2, vision-PnP, `handoff/shadowpc-followups-2026-06-05/TASK2_PNP_VERDICT.md`):** map z is the gate **BOTTOM EDGE** (the z-only `corner_to_center` lift is correct); horizontal is **NOT a 1.36 m corner** (~0.5 m near-range) → **no half-width shift**; gate-0 misses were the frame bug + gains, not the map. **NEW: a ~3.6°/range YAW bias** in the vision→world chain (cam-mount / given-attitude yaw / map-gate-yaw) → range-dependent lateral error in PnP fixes — a VQ2 vision-calibration target, irrelevant to model-based given-pose nav.
- **Soft spots / deferred:** the 2-point thrust-slope soft-spot is now CLOSED (two-sided climb+sink vprobe: hover 0.266, vertical drag ~0, slope g/hover≈36.7 — `scripts/fit_vertical.py`); g1 (first cross-track) marginal at ~0.5–0.59 m → a VQ2 racing-line/RL target, not tightenable with this PD; the KF vz lag is real (alt loop now on raw vz; the lateral already used raw given vel) — a tighter vz estimator is a VQ2 win; estimator robustness ([[project-estimator-robustness]]); red-team-3 deferrals ([[project-red-team-pass-3]]).

## Durable strategy & directives
- **Track A (model-based) = VQ1 floor + VQ2 baseline**, leaning on the sim's stabilizer (CTBR). Estimator = LINEAR pos/vel KF (attitude given). Detector = YOLO-pose corners → IPPE / robust weighted-PnP. MVP = min-snap line + conservative completion (a slow VALID finish beats a fast invalid one).
- **Overfit GEOMETRY, randomize APPEARANCE.** VQ1 is clean/desaturated/aids-on; VQ2 is photoreal/cluttered/aids-off ⇒ NEVER fine-tune the detector on clean VQ1 frames (would collapse the domain-randomization edge). Train in chaos, validate on chaos. ByteTrack sits behind the GateObservation seam.
- **Planning (racing line + speed) = the VQ2 differentiator (where rank is won); vision = a bounded threshold.** Chain mental model: must-not-BREAK (validity) vs sets-your-RANK (time).
- **RL = COMMITTED VQ2 path** (the moment VQ1 is banked): SWIFT-style learned state→action policy behind the `Setpoint` seam (NOT end-to-end pixels), trained on a massively-parallel twin whose dynamics match the system-ID'd plant + a MEASURED perception-noise/latency/dropout model (the speed ceiling). [[project-master-plan]] C1.
- **User directives:** speed > keeping the gate in view; vision must be excellent; determinism is per-track; offline between-runs processing is LEGAL (not a cheat); be a good citizen on Adroit (own dirs, SLURM only).
- **Spec facts:** VFoV≈58.7° (the spec's "90°" is the HFoV; camera tilts up). ~100 TOPS onboard edge budget — currently under-used (room to size up perception / deploy a learned policy). Obstacles exist but don't map monocularly (fly the corridor). **Open (user to confirm w/ info@theaigrandprix.com):** VQ1 deadline + attempts/submission mechanics (likely ~unlimited, best-valid-time counts).

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture + execution plan, Track-A strategy, components, risks R1–R11, build sequence, codebase state. Read when resuming. (Predates first contact — [[reference-sim-interface]] is the live sim truth.)
- [Sim interface — confirmed wire spec + first contact](reference_sim_interface.md) — the REAL MAVLink+video interface vs the thinner spec; FIRST CONTACT 2026-06-02 RESOLVED all 5 must-verify items (position given · ATTITUDE sign-flip→ODOMETRY quat · gate map 2.72 outer/1.5 inner + level-load trigger · CTBR · arming · clocks · frame-dedup).
- [CTBR control + inner-rate system-ID](project_ctbr_control_sysid.md) — the FLYABLE control stack (ShadowPC 2026-06-03/04): ACRO-only (easy-mode closed) · rate-loop gains/signs (~2.7× gain, hover≈0.26-0.27) · plant-matched decoupled CTBR controller (alt-hold + tilt-comp + velocity-targeting) · **the Gate-0 saga**: ROLL inverted on the ODOMETRY quat (lateral was positive-feedback → `odo_att_sign=[-1,1,1]` + roll-sign flips), map pos = gate bottom-centre (lift to opening; gates face -X), live map corrupt (use saved), KF velocity lags 4× (use raw given horizontal vel), false-pass detector fixed. **Superseded 2026-06-06: lateral cause was the ODOMETRY frame bug (fixed `c3b5a8e`); balloon is OURS not the sim (Task 3); plant now fitted to a faithful offline twin + live config — see READ-FIRST + NEXT.**
- [Detector training pipeline + weighted-PnP](project_detector_training_pipeline.md) — autonomous Adroit YOLO-pose training; v2 SHIPPED / v3 `--hard` negative result; multi-gate synthetic + tail diagnosis; robust weighted-PnP solver; adroit-connector ops (GOTCHA: run connector CLIs via PowerShell, not Git Bash).
- [AI Grand Prix context](project_ai_grand_prix.md) — the competition (Anduril, May–Nov 2026; user competing for fun).
- [Hardware constraint](project_hardware_constraint.md) — three machines: laptop (dev; Intel iGPU, can't run sim) · ShadowPC (sim host; TensorDock + Azure both retired) · Adroit Princeton (free GPU, detector training).
- [Adroit cluster notes](reference_adroit_princeton.md) — Princeton's Slurm/GPU cluster (free for affiliates; `ssh <netid>@adroit.princeton.edu`, `/home`+`/scratch`); the detector-training compute. Confirm summer access.
- [Competition source materials](reference_competition_materials.md) — spec PDF facts, verified rules/FAQ, open unknowns, MAVLink links.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries (license/reuse verdicts) + the 8-paper academic ledger.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must BE the full VQ2 stack under-tuned (vision+KF in-loop), never a crutch.
- [Estimator robustness](project_estimator_robustness.md) — true-drift vs PnP-noise in the KF (adaptive R · Mahalanobis innovation-gate · map-averaging) + a no-gate SEARCH state + optical-flow assist; the home for these when wiring the navigator.
- [Tooling recommendations eval](project_tooling_recommendations_eval.md) — 3rd-party tool triage: TOPP-RA spiked (`speed_profile.py`); perception-noise-model = a hard RL requirement; VIO parked as fallback; offline processing legal. R2 keepers: ADRC · GTSAM mapper · min-snap (rpg) · Isaac RL libs.
- [Red-team pass #2](project_red_team_pass_2.md) — pre-sim-drop triage: Tier-A correctness fixes applied+committed; ESKF/ring-buffer deferred; overstated/invalid claims logged so they aren't re-litigated.
- [Red-team pass #3](project_red_team_pass_3.md) — eve-of-drop triage: 7 fixes applied+committed (A2 skyward-launch · 1.1/1.2 fast-fly-through→U-turn); 5 claims rejected; 3.2/2.1/B4/A1/B1/2.2 deferred to navigator wiring / first contact.
