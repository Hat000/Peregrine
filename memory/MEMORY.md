**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`.

## NOW (2026-06-14)
- **VQ1 PASSED. inc7 LIVE-CONFIRMED = current best** (standing 5/5, gate-3 barrier gone). Phase 2 = RL + VQ2 vision.
- 🚩 **BINDING VQ2 RISK = ESTIMATOR** (not planner/policy). Case-C absolute world-frame nav = NO-GO at any speed; **FIX = gate-relative obs** (SOLID: map bias drops EXACTLY — rel E_bias −0.000 vs abs/submap +0.176; RMS 0.139). Build gate-relative REGARDLESS (Q① only decides load-bearing-vs-insurance). → [[index-vision-estimator]]
- 🚩 **HEADLINE: CANNOT-SETTLE-OFFLINE SURVIVES — L3 "CLOSE" REFUTED by the δ_map discriminator (case b, high-confidence; airtight lock test pending).** Binding factor = **VERTICAL BORESIGHT BIAS ε_vert ≈ 0.215 m (~0.56° camera-pitch mismatch), CALIBRATABLE** — NOT map, NOT body radius. Direct δ_map_vert measurement: gate-2 +0.044 m, gate-4 −0.067 m; MAX across 6 gates 0.077 m → NO −0.2 m map offset. L3's rel_vert −0.215 m decomposes as ε_vert ≈ +0.15…+0.26 m = genuine PERCEPTION/ATTITUDE sighting bias, which +L gate-relative fix does NOT cancel (only removes map-position term). L3's own rel in-plane p90 = 0.492 m → does NOT close at r=0.30 or r=0.38. NON-CIRCULAR: vertical bias depth-free (corr range_err vs off_D = +0.11), bearing-correlated (corr off_D vs bearing = +0.44) — camera/perception-geometry signature, not map. Path to close = boresight calibration + ESKF attitude-bias estimation → ε_vert→0. SILVER LINING: CALIBRATABLE. Morning lock test: static head-on LEVEL fix at g2/g4 (bearing≈0) isolates ε from map. L3 at-speed shadow DONE (13 laps, 0 contact; branch claude/blissful-kalam-75f52b). δ_map discriminator DONE (branch worktree-agent-a88bc717008e79cd9, commit d7c592e, NOT merged; handoff/dmap-vert-discriminator-2026-06-14/REPORT.md). **INC8 LEVERS = TWO, both vertical-axis: (1) 2-axis camera-pointing = fix-RATE, (2) BORESIGHT/attitude-bias estimation = fix-ACCURACY; both co-equal now.** ESKF attitude-bias estimation PROMOTED from secondary to co-equal margin lever. ✅ **FIX-SURROGATE MERGED to main (ffb2c74, 2026-06-14)** — code + calibrated checkpoints only (memory excluded, already banked); 15 tests green; green-by-construction (memory-only divergence). C6/POC hold INTACT (offline training infra, touches nothing live). **Critical path: C6 HELD → POC flight → inc8 2-axis camera-pointing reward + boresight/attitude-bias estimation; morning ShadowPC boresight LOCK test.** → [[index-vision-estimator]] · [[index-control-sim]]
- ✅ **MARGIN-CLOSURE-ENVELOPE DONE** (branch claude/charming-jemison-b111c5, commit 7654e99, pushed, NOT merged; handoff/margin-closure-envelope-2026-06-14/REPORT.md). 268-cell sweep, nmc=5000 bootstrap CIs, 4-lens adversarial pass. **σ=0.10 RELOCATES the blocker from σ → FIX-RATE** (CANNOT-SETTLE-OFFLINE survives, consistent with δ_map refutation). r=0.30 CLOSES IFF ALL of: fix-rate ≥ ~0.25 (≥0.35 @ 55 m/s) · attitude bias ≤ ~0.6° · GPU latency ≤ ~50 ms · **TERMINAL GATE-LOCK (camera held ≥60% of final approach)**. Measured 0.07 pooled / 1.4% @ gate-4 → r=0.30 OPEN. **CLUSTERING = dominant adversarial lens: terminal fix-DROUGHTS break closure at any mean rate → need TERMINAL gate-lock, not just high pooled mean.** Naive fix-diff velocity channel = HARMFUL at sparse fix-rate. Operational call: **race-the-cap is gated on CAMERA POINTING (TERMINAL gate-lock), not σ.** → [[index-vision-estimator]]
- 🚩 **3-WAY OVERNIGHT CONVERGENCE (2026-06-14):** σ NOT the blocker (0.10 < modeled 0.265); radius NOT the blocker (central r=0.30). **Dominant lever = FIX-RATE via camera pointing: need ≥ ~0.25 with TERMINAL gate-lock; measured 0.07 pooled / 1.4% @ gate-4.** BIAS AXIS: ceiling ≤ ~0.6°; δ_map pins ε_vert at 0.56° — nearly exhausts budget alone → boresight calibration / ESKF attitude-bias estimation REQUIRED (not optional). LATENCY AXIS: ≤ ~50 ms GPU-class (CPU-115 ms breaks v=55 cells). Inc8 load-bearing lever = TERMINAL GATE-LOCK (≥60% final approach), not just high average fix-rate. → [[index-vision-estimator]]
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** d1/d2 spec PROSE "estimator delivers −L" is wrong (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py (+L 4.77e-7 / −L control breaks 24 m). Future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- ✅ **C2-ESTIMATOR-CHAIN DONE & MERGED (05ed750, 723 green, 0 regressions).** RewindKF (productionized OOSM, horizon=0.5, assert_horizon_gt guard) + gate_relative_inplane_fix (+L, anisotropic cov) + relinnov gate χ²(2,.999)=13.82 + estimator_obs seam + NavState cov export. Gated by use_rewind_kf + use_gate_relative (BOTH OFF → VQ1 byte-identical). G3: rel E_bias −0.000 / RMS 0.131 / p90 0.197. → [[index-vision-estimator]]
- 🚩 **OBS CONTRACT FROZEN (d5 layout, 2026-06-13): obs_dim=20.** [0:17] unchanged bit-exact; [17] c_inplane, [18] c_along = clip(σ_ref/σ_hat,0,1) bounded confidence ratios (σ_ref≈0.05 m); [19] age_norm = clip(t_since_last_fix/TAU_STALE,0,1) TAU_STALE≈0.10 s. Critic get_state 33→36. Deploy: fly_rl gates on checkpoint sidecar obs-dim (inc7 17-dim unchanged). → [[index-vision-estimator]] · [[index-rl-training]]
- **S2 DECIDED = `staged_monolithic_then_decomposed`.** Speed gap = TILT ENVELOPE, not architecture. → [[index-rl-training]]
- **ACTIVE critical path:** C2 estimator chain DONE+MERGED (05ed750) → **C6 HELD → POC flight (fly inc7 on estimator, VQ1 slow, step 4) → inc8 = 2-axis camera-pointing reward + BORESIGHT/attitude-bias estimation; morning ShadowPC boresight LOCK test (static head-on level fix at g2/g4).** L3 DONE (branch claude/blissful-kalam-75f52b). δ_map discriminator DONE & REFUTES L3 close (case b; d7c592e, NOT merged). FIX-SURROGATE DONE (infra). → [[index-control-sim]] · [[index-vision-estimator]]
- 🚩 **SPEC CAMERA REVIEW (2026-06-14, §3.8):** Camera 20° is SPEC-EXACT (no tolerance). Mount FIXED → mount-uptilt lever DEAD; elevation fix-rate is POLICY-only. VFoV=90° in spec is MISLABELED (it is HFoV); true VFoV≈58.7° — ALWAYS use fy=320. Boresight ε_vert ≈ 0.56° = calibratable convention/projection seam (vision-extrinsics audit pending); NOT a wrong mount angle. → [[index-vision-estimator]] §Spec facts
- ✅ **PHASE-2 PATHS STAFFED & LAUNCHED (2026-06-14):** **P1 VISION-ACCURACY** (boresight ε_vert extrinsics-audit + calibration + ESKF attitude-bias = margin lever #2 / fix-ACCURACY; laptop worktree), **P2 INC8-RL** (2-axis camera-pointing + TERMINAL gate-lock reward; fix_surrogate tool = margin lever #1 / fix-RATE; laptop worktree → Adroit), **P3 SIM-OPS/EMPIRICAL** (boresight LOCK test [feeds P1] + at-speed terminal-visibility recording [pins the 3 race-the-cap conditions]; ShadowPC). **P4 deploy HELD, P5 detector dormant.** Each sub-commander owns its own branch/worktree off main; OVERALL COMMANDER = sole memory writer + merge gate + cross-path adjudicator. **POC** (held) = proof-of-concept flight = inc7 on the merged estimator stack at slow VQ1 speed (case-C onboard pose, a full race lap); P3 STATIC measurements (boresight lock) are NOT POC and are cleared. fix_surrogate MERGED (ffb2c74). 2 stale branches pruned.
- 🚩 **BORESIGHT verdict STILL OPEN — P3 offline range-resolve INCONCLUSIVE (2026-06-14); LIVE HEAD-ON FIX AUTHORIZED (Fengyou GO).** Cannot decompose the pinned −0.215 m gate-4 vertical into δ_map(CLOSE) vs ε_vert(OPEN) from oblique at-speed data; margin verdict pivots on it. **Arbiter = STATIC level HEAD-ON fix at gate-0/1 via proven CTBR, ≥2 ranges** (reveals angular-vs-metric functional form + magnitude/sign → pins P1's calibration). **SUPERSEDES the prior "g2/g4" lock-test framing** (boresight is COMMON-MODE → transfers to g4; g2/g4 ANGLE position-hold untested/higher-risk). Conservative case-(b)/OPEN posture HELD meanwhile — P1 builds boresight-cal + ESKF regardless. → [[index-vision-estimator]] §P3 OFFLINE RANGE-RESOLVE

- 🚩 **COMPETITION INTEL LANDED (COWORK-1, 2026-06-14; official public spec VADR-TS-002; handoff/cowork-2026-06-14/competition-intel.md):** (1) **LATENCY AXIS CLOSES** — VQ eval = Windows 11 desktop + discrete ~8 GB-VRAM GPU (CUDA-class), NOT CPU-only, NOT Jetson → our 15–25 ms GPU path ≪ 50 ms; 115 ms CPU path NEVER forced; retire CPU-latency as a qualifier risk. "~100 TOPS" = the PHYSICAL Neros drone (Sept/Nov), ~Jetson Orin NX = sim-to-real PORTABILITY target, not the qualifier box. (2) **ESTIMATOR LOAD-BEARING** — NO position on wire (official §4.3 = HEARTBEAT/ATTITUDE/HIGHRES_IMU/TIMESYNC; maybe start-coord + linear velocities; NO LOCAL_POSITION_NED/ODOMETRY) → self-localize REQUIRED → Phase-2 gate-relative pivot VALIDATED, Q① resolved. (3) **MonoRace (arXiv 2601.15222, TU Delft MAVLab, won A2RL 2025) = near-1:1 prior art, READ FIRST.** (4) Round 2 = real 3D-scanned env → appearance gap REAL → P5 photoreal JUSTIFIED. → [[reference-competition-materials]]
- 🚩 **BORESIGHT = OFFLINE EXTRINSIC, NOT online ESKF (COWORK-2, 2026-06-14; handoff/cowork-2026-06-14/boresight-calibration.md):** ε_vert is a CONSTANT, known-sign, sub-degree pitch offset (render-vs-decode convention) → **bake −ε into the camera↔IMU extrinsic ONCE** (zero runtime cost, closes the gate-4 bias term). Do NOT add an online residual pitch-bias state for that DOF (7% fix density → slow/confounded/double-counts via gauge ambiguity). ESKF keeps time-varying gyro/accel biases + optional tight-prior watchdog only. **DEMOTES the banked "ESKF attitude-bias = co-equal margin lever" framing.** P3 head-on test → ≥3 ranges, gravity-leveled. → [[index-vision-estimator]]
- 🚩 **WIRE-SPEC DISCREPANCY (CRITICAL FOOTGUN):** our ShadowPC sim streams LOCAL_POSITION_NED + ODOMETRY (all our frame-convention work); the OFFICIAL spec does NOT (§4.3). Our sim ≈ Elodin practice rig / early build. DEPLOYED stack must SELF-LOCALIZE (case-C — we do); all LPN/ODOMETRY GT tooling (GT velocity, R_y(π) conventions, L3 recordings, frame_residual) is OFFLINE-CALIBRATION-ONLY and may NOT transfer. RE-VERIFY the entire wire when the official VQ1 sim drops (imminent/late). → [[reference-sim-interface]]

- ✅ **P1 BORESIGHT AUDIT + CALIB-V2 DONE (2026-06-14):** bake site = frames.R_camera_from_body (NO code bug, fy=320 consistent render+PnP); unified frames.BoresightCorrection{pitch,roll,vert_offset} default-zero byte-identical, ANGULAR + METRIC forms. Infra-first merge APPROVED (apply-worker → default-zero, ε = one-line {form,value} later). ESKF re-scoped (drop online boresight corrector; keep accel-bias [gyro-bias likely dead — attitude GIVEN] + camera-pitch watchdog-prior-only). Branches p1-audit-extrinsics a1620b3 + p1-calib-v2 a980823 (NOT merged). FORM-discrimination needs WIDE-separated ranges (≥10 m) → routed to P3. → [[index-vision-estimator]] §P1 BORESIGHT AUDIT + CALIB-V2

- 🚩 **DOCTRINE REVISION (2026-06-14, ratified) + inc8 GREEN + 2 merges:** the banked ~4.6–4.7 s "honest bound" was the INVERTED/rate-infeasible TOGT optimum; **upright rate-feasible lap ≈ 8 s** (Γ 8.455 @ 75° cone); binding gate-4 speed **~30 m/s, NOT 37–55** → margin closure EASIER. **inc8 REFRAMED = CASE-C DEPLOYABILITY (self-localizing racer), not a big speed jump** (~9.76→~8 s ≈ 18%). inc8 estimator-emul escape-hatch = 🟢 GREEN ×2 (20-dim obs carries the camera-pointing signal honestly). **Reward spine = EXPLICIT arm A (2-axis terminal-lock) RATIFIED** (implicit under-produces pointing); portfolio A≥5/B3/C1; final FREEZE still Fengyou's. NO winner crowned until P3 at-speed σ-recal (gate-4 VERTICAL-σ-dominated σ_vert 0.28≫σ_lat 0.10 — same axis as boresight). Merged p2-inc8-rl + p2-inc8-refline → main 5a4afa6 (762 green); torch train-env port (Deliverable 2) GO. → [[index-rl-training]]

- 🚩 **STRUCTURAL CHANGE AGREED (2026-06-14, Fengyou) — trim multi-commander → SINGLE commander at the next clean stopping point** (after the current worker wave drains + merges). NEW MODEL: the overall commander writes ONE prompt per pathway; **Fengyou pastes each into an ULTRACODE session that self-orchestrates via Workflow** — this eliminates the sub-commander-writes-worker-prompts tier (one prompt tier, not two). ShadowPC/Adroit stay the SAME management level (Fengyou copies the prompt over to that machine). The commander keeps SOLE memory authority + merge gate + SSOT, and does NOT drive Workflow directly (the ultracode sessions do). DRIVER: multi-commander relay friction (Fengyou was the human bus between 5+ sessions). Stopping point = current in-flight merged (P1 apply-worker → p1-calib-v2; the still-waiting worker; P3 ε head-on).

- 🚩 **P3 BORESIGHT HEAD-ON (2026-06-14): ε MAGNITUDE PINNED = −0.25 m @ 23 m ≈ −0.61°** (sign gate-DOWN — vision reads the drone ~0.25 m HIGH relative to the gate; cross≈0; 4-way agreement: g0 head-on −0.248 / L3 g4 −0.215 / g2 −0.211 / static≡moving). **CAMERA-POINTING EMPIRICALLY VALIDATED — head-on fix-rate 67–77% vs inc7 1.4% (~50×)** = the inc8 pointing thesis confirmed on REAL data (not just the surrogate). **FORM (angular vs metric) NOT yet resolved** — 17σ range-signal LEANS angular but is confounded by the range-dependent SOLVEPNP_IPPE_SQUARE planar-flip (far 23 m = weak perspective = MORE flip-ambiguous, may flip-taint the −0.248 anchor) + a 14–20 m overshoot; gated on the IPPE both-flip + regime-separated analysis (~30–45 min, NO re-fly, data abundant N=13k). P1: magnitude deliverable NOW, HOLD calibration TYPE (calib-v2 is dual-form). → [[index-vision-estimator]] §P3 BORESIGHT HEAD-ON

- ✅ **P1 VISION-ACCURACY DONE + CONSOLIDATED (2026-06-14) — pathway PRUNABLE.** Boresight dual-form infra MERGED to main (c740743): frames.BoresightCorrection{pitch_rad,roll_rad,vert_offset_m} default-zero byte-identical → R_camera_from_body + localization +L-lever (both sites); 786 green, 0 regress, +L preserved, gated-off byte-identical to inc7. **ε (P3's measure) drops in as ONE LINE `frames.BORESIGHT = BoresightCorrection({form,value})` — applied by the OVERALL COMMANDER once P3's FORM lands.** RATIFIED Option-A deviation (localization reads frames.BORESIGHT LIVE = single-source-of-truth; fixed a snapshot-binding footgun). ESKF re-scope DESIGN preserved (524d4b2, handoff/p1-vision-accuracy-2026-06-14/): boresight dropped-online; gyro-bias DEAD (wire re-verify); 2-axis accel-bias Y,Z LIVE (NEES 1.97/2, <0.6° in ~2 laps), body-X dropped; watchdog read-only; gated OFF use_imu_bias_eskf → inc7 byte-identical. ESKF BUILD = DATA-CONDITIONAL (build iff first-contact accel-bias > budget, else watchdog-only). → [[index-vision-estimator]] §P1 PATHWAY COMPLETE

## Operating directives — ALWAYS APPLY
- 🚩 **CANARY (rev 2, 2026-06-14):** the **OVERALL COMMANDER persistent session** addresses **Fengyou** by name in EVERY message (missing name = context degradation → rotate session). **Sub-commanders and workers do NOT need the canary** (Fengyou, 2026-06-14).
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work** (≈0.5× fable — the affordable design tier); **ultracode on opus** for hardest fan-out/verify; **sonnet-4.6 = low-stakes mechanizable** (banking, flight ops, git, reruns); haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 4 — thin-index structure):** workers end with **MEMORY-DELTA** (≤10 lines); commander triages; sonnet banking agent banks IN BATCHES. **Route detail to the topic file + its domain sub-index; touch MEMORY.md ONLY for NOW / footgun / directive changes.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt carries SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA requirement + escape hatch + report path.
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees.
- 🚩 **ANTI-FORGET (2026-06-14):** review **[[project-parked-backlog]]** at session start + whenever a trigger fires (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Sub-commanders/workers surface NEW parked items UP in their MEMORY-DELTA; the commander files them in the register — NEVER re-scatter parked items back into topic files.

- 🚩 **SIM = NO PHYSICAL RISK (Fengyou, 2026-06-14):** measurement/non-POC sim flights carry no risk — the commander AUTHORIZES them directly (no risk-escalation/AskUserQuestion ceremony). The C6/POC hold is a SEQUENCING/milestone gate (Fengyou's), NOT a risk gate; only the explicit POC unfreeze needs him. Validity rule (zero gate contact) + sim time = the only flight constraints. ALSO: every commander↔sub-commander relay through Fengyou = ONE copy-paste code box (not prose). → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN** (zero-contact is THE validity rule).
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7, NO `MAV_CMD 31000` on the wire). `fly_rl.py` default ckpt = retired inc4 → pass inc7 explicitly if bypassing submit_rl.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG is a self-consistent alias — DO NOT "fix".** → [[index-control-sim]]
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session (internal consistency cannot catch a conjugation). → [[index-control-sim]]
- 🚩 **`main` CANONICAL · 723 tests green · `*.pt` + `data/runs` gitignored.** (C2-ESTIMATOR-CHAIN merged @ 05ed750; 700 + 23 new.)
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** The d1/d2 spec PROSE "estimator delivers −L" is wrong (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py (+L 4.77e-7 / −L control breaks 24 m). The future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- 🚩 **CONTACT RADIUS 0.38 = EMPIRICAL CRASH HALO, NOT GEOMETRY.** Rigid-body chassis geometry caps at 0.2135 m (3D half-diagonal; tilt adds only +0.015 m). 0.38 is the DR upper tail of a posture-matched steep-crash band [0.28,0.38]. DO NOT "fix" it as a geometry bug. Central reporting radius = **0.30 m** (band 0.26–0.33); 0.38 stays as worst-case stress knob. → [[index-vision-estimator]]
- 🚩 **BINDING MARGIN FACTOR = VERTICAL BORESIGHT BIAS ε_vert ≈ 0.215 m (~0.56° camera-pitch mismatch) + ATTITUDE/ACCEL BIAS, NOT CONTACT RADIUS.** δ_map discriminator (d7c592e) PROVES no map offset — ε_vert is a genuine perception/attitude sighting bias that +L does NOT cancel. cold@1.4°-bias p90 = 0.338 m fails at EVERY physically admissible radius. Radius correction is real but does NOT close the binding case. Path to close = boresight calibration (CALIBRATABLE, ~0.56°) + ESKF attitude-bias estimation. CANNOT-SETTLE-OFFLINE SURVIVES. → [[index-vision-estimator]]
- 🚩 **GATE-0-NEAR-LEVEL ANCHOR IS WRONG POSTURE FOR GATE-4.** Corner-pass probe (r_eff ~0.18–0.20 m) is gate-0, ~3 m/s CTBR bridge, near-level. Do NOT use that as the gate-4 radius. Gate-4 posture-matched data = gate-3 steep crashes (tilt ~55°, 12–18 m/s) → r_eff ≥ 0.26–0.38 (lower bounds). → [[index-vision-estimator]]
- 🚩 **CAMERA MOUNT FIXED AT 20° (SPEC §3.8, EXACT) — mount-uptilt lever DEAD.** Elevation fix-rate is POLICY-only (inc8 pitch/pointing). VFoV=90° in spec is MISLABELED — it is HFoV; TRUE VFoV≈58.7° (fy=320, H=360). ALWAYS decode vertical from fy=320. Boresight ε_vert≈0.56° is a calibratable convention/projection seam, NOT a mount-angle error. → [[index-vision-estimator]] §Spec facts
- 🚩 **COMMON-MODE-ACROSS-GATES ≠ PROOF OF +L CANCELLATION.** A vertical (or lateral) offset seen identically at multiple gates is consistent with BOTH (a) a track_map representation offset (corner_to_center half-height-lift) that +L CANCELS [→ margin closes] AND (b) a detector/camera-EXTRINSIC sighting bias that +L does NOT cancel [→ margin fails] — because one camera/detector sees every gate, an extrinsic bias is also common-mode. Only the DIRECT map-vs-sim-truth pin distinguishes them. **The δ_map discriminator (d7c592e, 2026-06-14) CHOSE (b): δ_map_vert ≈ 0 across all 6 gates → ε_vert ≈ 0.215 m = genuine perception bias → L3 "CLOSE" REFUTED.** → [[index-vision-estimator]] §δ_MAP VERTICAL DISCRIMINATOR

- 🚩 **ADROIT ACCEPTABLE-USE = SUSPENSION RISK (#62 RESOLVED 2026-06-14 — use PERMITTED with rules):** NEVER compute on login nodes (SLURM only); compute nodes have NO INTERNET (pre-download git/pip/HF/weights on login/vis FIRST); output → /scratch NOT /projects; accurate --mem + 1-core-serial (over-alloc / multi-core-serial → ACCOUNT SUSPENSION); zero-GPU-util jobs killed at 2h; run `checkquota` routinely. Full AUP → [[reference-adroit-princeton]]. Bake into every P2/P5 SLURM worker prompt.

- 🚩 **WORKTREE HYGIENE (2026-06-14):** the harness AUTO-creates one isolated worktree per spawned session — that ALONE satisfies non-interference. Workers MAY make their own worktrees freely (rev 2, 2026-06-14 — Fengyou's call: isolation > clutter, and enforcing no-add is needless friction). The overall commander OWNS consolidation — merge EVERY worker branch into main at the gate, then prune MERGED branches/worktrees so the list stays manageable. The `.claude/worktrees/*` pool is harness-managed + RECYCLES (observed: an "empty" worktree was reclaimed as oneoff-regsuite) — NEVER manually prune it (racy, can knife a launching session); only `git worktree prune` (gone dirs) + remove confirmed-DONE manual worktrees. Code-workers REPORT their branch name UP; the overall commander keeps the branch↔role ledger, merges, and prunes BRANCHES on merge. → [[index-strategy-meta]] §worktrees

- 🚩 **BORESIGHT FORM-DISCRIMINATION NEEDS WIDE-SEPARATED RANGES (P1, 2026-06-14):** the head-on ε test must use **≥2 ranges with Δrange ≥ ~10 m** (e.g. 12 m & ≥24 m) to separate ANGULAR (∝range) from METRIC (flat) — "≥3 ranges" alone is INSUFFICIENT (closely-spaced 20/22/24 = DEGENERATE → pins magnitude, NOT form). g2/g4 at ~4 m only if per-fix σ_vert ≤ ~0.005 m (heavy frame-averaging). FORM is load-bearing: it sets WHETHER −ε goes in the extrinsic rotation (angular) or the +L lever (metric), AND the ESKF watchdog (rotational δB can't absorb a metric offset). Bake site = frames.R_camera_from_body (audit: no code bug). → [[index-vision-estimator]] §P1 BORESIGHT AUDIT + CALIB-V2

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, S2 decision, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness, advisor-triage.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops (unattended mechanics), autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling eval, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 banked-but-parked threads, triaged F/T/D with revive-triggers. **REVIEW each session + when any trigger fires** (COWORK-1 organizer answers = cluster key un-gating 8 threads).
