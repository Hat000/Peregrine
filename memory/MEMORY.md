**Peregrine** — our entry in the Anduril **AI Grand Prix** autonomous drone-racing competition (May–Nov 2026; user competing for fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. This index is the orientation layer; detail lives in the topic files below.

## READ FIRST — current state (2026-06-10)
**🏁 VQ1 PASSED** (dashboard-confirmed). The **model-based CTBR stack threads the full 6-gate course** — valid sim-recognized finish ~35.3 s, in-plane miss `[0.37, 0.05, 0.06, 0.03, 0.10, ~0.71]` m (all < 0.75 half-opening). Built by the **offline-twin discipline** (tune offline against a faithful twin, fly only to VERIFY — this is what got us unstuck; keep it). Now pivoting to **Phase 2 = RL + VQ2 vision** (see NEXT).

**✅ 2026-06-09 — SIM BUILD 1.0.3364 REGRESSION FIXED.** 4× deterministic 6/6 @ 34–35 s, race_outcome-certified (commits 260972e+883b725+57f287f). Root cause: one-tick attitude step at takeoff→RUN → body-rate clamp → tumble (yaw≈−180° geometry coupled onto roll). Fix: `launch_ramp_s=0.6 s` authority ramp keyed off sim clock (rate/tick-phase invariant); `--rate 100` pinned. **VQ2 open items (NOT VQ1):** (A) yaw-spin at ±180° tilt — **✅ DISSOLVED (2026-06-10 characterize-sweep 510da24): there is NO open-loop sim anomaly.** The S1.2 "spin" decomposes into unmodeled super-rate gain (the real divergence driver) + an Euler yaw-flip representation artifact near inversion (quaternion smooth) + a gate-post COLLISION (id 1001, threat 2) at sim_t≈11.145 imparting −63 rad/s roll — the "spin" was contact dynamics; (B) decel into gates; (C) ~20 km/h cap — both RL/racing-line levers.

**🚩 2026-06-10 — UNATTENDED SIM CONTROL ACHIEVED (durable capability, the user's standing ask):** the fable S1.2 session **cold-launched FlightSim.exe and raced with NO human interaction.** Mechanics: **MAV_CMD 31000** (`client.send_sim_reset()`) restarts the race once a race context exists (fresh ~3 s countdown) — it is a **NO-OP from HOME** (no telemetry there); from HOME, **Win32 `SetForegroundWindow`** to focus the `AI-GP` window (`WScript.Shell.AppActivate` alone returns False) + send **Enter twice** (home → waiting room → race+countdown). `rl/fly_rl.py --flights N` chains attempts (per-flight recorder, auto-reset between flights, never resets into a ticking countdown). **This removes the manual-reset bottleneck for ALL future sim work.** **🚩 THIRD idle state found (2026-06-10 sweep):** after ~90 min idle post-race the sim parks on an off-race screen where **31000 is a NO-OP while telemetry still streams** (stale RACE_STATUS `started=True`, frozen sim_time); recovery = Win32-focus the AI-GP window + Enter ×2 — wired as **automatic escalation in `scripts/rate_sysid.py`** (15+ probe runs chained, zero GUI touching). `rate_sysid.py` also gained `--vel-damp`/`--pos-pull` station-keeping + `--mode anomaly`.

**The flyable stack (durable — and the RL training substrate):**
- **Sim interface** (first contact 2026-06-02, all 5 must-verify RESOLVED; detail [[reference-sim-interface]]): position+velocity GIVEN pristine (LOCAL_POSITION_NED 97 Hz + ODOMETRY 75 Hz; baro NaN → z from given pos); **🚩 ATTITUDE.pitch sign-inverted → take orientation from the ODOMETRY quaternion**; gate map = 6 gates, 2.72 m OUTER / ~1.5 m inner opening (feed inner to PnP), broadcast once at level-load (connect at HOME `wait_heartbeat=False`, `capture_track_map.py`, map-once-reuse); the course DESCENDS 26 m (g0 +1.4 → g5 −24.6 m); clocks INDEPENDENT (align on recv-monotonic); video re-sent ~14× → dedup by frame_id; sim PAUSES physics off-race.
- **Fly on CTBR** (body-rate + explicit collective via `SET_ATTITUDE_TARGET`; sim runs ACRO — easy-mode/ANGLE velocity path = World A, closed). Plant system-ID'd → faithful OFFLINE TWIN (`src/racer/twin.py`/`twin_fit.py`, the RL plant): **rate_gain [2.50,2.50,2.23], physics rate_sign [+1,+1,−1] (only YAW cmd inverted), τ=0.019 s, hover=0.2656 (two-sided-vprobe validated), linear_drag≈0.21/s**. **🚩 2026-06-10 (sweep 510da24): rate_gain is amplitude-DEPENDENT — a STATIC "super-rate" map `g(|c|)=G0/(1−s·min(|c|,π)/π)`, G0=shipped [2.501,2.504,2.231], s≈0.29–0.30 (r/p); sustained gain 2.50→3.50/3.53 at full stick ⇒ real authority ~11 rad/s, NOT 7.85. The flat-2.5 model stays valid small-signal (live course RMSE 0.22–0.41 at ≲3 rad/s cmds). **✅ Map INTEGRATED (S14, 2026-06-10, parity-gated twin→rl_plant→torch): `super_rate_s`/`alpha_max_rps2` in all three plants, defaults None = bit-identical legacy; `faithful_config(super_rate=True)` = measured map-ON (s=0.30, alpha_max=[260,260,80]); max clamp 25.0 never bites (map DC ceiling ~11.2 rad/s/axis).** **🚩 PHYSICS≠TELEMETRY: the ODOMETRY quat REPORTS roll inverted + raw rate inverted on roll+pitch (reporting artifact, NOT physics); the controller `body_rate_sign=[1,1,−1]`/`odo_att_sign=[−1,1,1]`/`odo_rate_sign=[−1,−1,1]`/`ff_gain=2.5` undo them.** Arm = `MAV_CMD_COMPONENT_ARM_DISARM` p1=1. Live config = `FAITHFUL_TUNED_GAINS` (`kp_alt 3.0/kd_alt 1.75`, raw vz) + `−0.4 m alt_offset` + `launch_ramp_s=0.6 s` (sim-clock-keyed 0→1 authority ramp at takeoff→RUN, rate/tick-phase invariant; commits 260972e+883b725). Detail + the Gate-0 saga + the alt-relay finding: [[project-ctbr-control-sysid]].
- **🆕 RL DEPLOYMENT RECIPE (S1.2, 2026-06-10, fable — CORRECTS 3 sonnet S1.1 bugs; this is the verified-correct way to deploy a DiffAero-trained actor live):** ① actor output = **tanh(mean) → rescale** to thrust [0,5] / rates ±3.14 rad/s (NOT the raw actor mean); ② FLU→FRD action sign = **[1,−1,−1]** (sonnet's [−1,+1,+1] was wrong on all 3 axes); ③ collective obs init = **0.0** with **rescaled** feedback (NOT 1.0); ④ training control rate = **30 Hz** (racing.yaml dt 0.0333; NOT 100); ⑤ training **resets at rest** 1 m in front of a random gate (the earlier "racing velocity" reset claim was wrong). Verified-correct from sonnet: obs layout (17), `R_W2G=diag(−1,−1,1)`, gate yaws all π, final-gate clamp, Euler-ZYX, actor arch. Implemented in `rl/fly_rl.py`. **🚩 The "OOD-at-start = root cause" verdict is RETRACTED** — it was an artifact of sonnet's wrong action transfer function, not a real diagnosis; saturation at reset is the policy's NORMAL launch behavior (it saturates in training too, then modulates).
- **🆕 RATE SIGN SETTLED DEFINITIVELY (S1.2):** raw ODOMETRY `angular_rate` → TRUE angular rate = **[−1,−1,1]** (correlation [+0.998,−0.998,+0.999] vs quat-finite-diff). The two prior memory values (plant `[−1,−1,1]` vs controller.py `[+1,−1,+1]`) were EACH self-consistent — measured against different references (true rate vs the derivative of the reported, roll-inverted attitude). **`state.velocity_ned` is PRISTINE** — the reported quat + reported body twist reproduce LOCAL_POSITION_NED world velocity to **0.00 m/s** even at high roll (the sim's reporting frame is internally self-consistent; only the physical roll RESPONSE is inverted). No client change needed.
- **Altitude balloon is OURS not the sim** (Task 3: in CTBR we own thrust — motors track collective to ±0.002). The alt loop has a **known limit cycle = a delay-driven relay (~40 ms loop delay = period÷4), ALWAYS-ON but VALIDITY-HARMLESS** (the 6/6 finish threaded to 0.03–0.37 m); the descent-track-vs-static-hold conflict is fundamental for a pure-PD loop → clean-up (gain-schedule / RL) deferred post-VQ1. **🚩 the offline twin under-models live latency ~25% even when calibrated — do NOT trust offline "holds" claims** (the raw-vz fix `dd16091` was an offline-confidence miss; the LESSON, not the fix, is the keeper).
- **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`) — do NOT reintroduce:** twist is `MAV_FRAME_BODY_NED` (body) but was stored as world → a world/body mix (sign-flipped vx/vy when yawed) that broke KF+damping+planner. Now rotated body→world (`frames.world_vec_from_body_quat`); was the phantom "KF lags 4×"; fixed KF tracks truth to 0.05 m/s.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate) — won the fixed-set eval (best corner + pose). v3 `--hard` tail-oversampling = a documented NEGATIVE result (the ~0.7% high-roll corner-swap is geometric aliasing → fix in the solver/temporal prior, not training data). Detail: [[project-detector-training-pipeline]]. **🆕 Vision catastrophic tail fix COMPLETE (commit 4673517, ShadowPC, 2026-06-09): raw catastrophic |fix|≥3 m: 46%→6.3% (143→12); KF-accepted leak: ~1.6%→~1.1% post-χ²+association+depth-sanity (0.6% of frames) — UNDER the ≤1.6% target. Good <1 m fixes went UP (103→107, zero regressions). 376 tests green.** New module `association.py` ships: projects each map gate through map+attitude+KF prior → apparent-size ratio [1/1.6, 1.6] + centre-offset ≤2.5 predicted-size units guard; kills the wrong-gate association (was 87 of the 143 catastrophic fixes on the receding-collinear course). Depth-sanity: rejects any fix whose solved depth disagrees with predicted range by more than max(1 m, 0.15·range); kills the "56 frontal depth flips" — **KEY INSIGHT: those were NOT solver flips but wrong-scale detector boxes (solved depth 1.2–13× true, p50 2.3×, reproj p50 1.0 px) — geometry consistency, not better solver tie-breaking, is the kill.** `PNP_FIX_COV_INFLATION` task CLOSED — no revert needed; behind the depth-sanity gate K=1.0 and K=2.0 leak identically (the prior objection evaporates). **2 residual leaks = honest long-range depth noise (25–38 m range, errors 3–5 m) on correctly-associated next-gate fixes → next lever is `attitude_noise_std` — ✅ DONE.** Vision engineering workstream (b) is COMPLETE for VQ1+Stage-1 — no longer blocked. **✅ VISION-PKG2 COMPLETE (2026-06-10, ShadowPC fable; commits 37e7ab1+1b7e753+9ccc88c+4831991; suite 412 green): measured covariance model SHIPPED** — `frames.ATTITUDE_NOISE_STD_RAD=1.4°` (one constant, all three former 1.0° sites) + NEW `FIX_COV_FLOOR_STD=0.40 m` isotropic floor (covers measured constant systematics; a σθ-only fit distorts to 3.21°) + `vision_max_range_m` 40→32 (kills the 38 m depth-tail leak at zero cost). Joint MLE over 165 offered fixes, exact-replication to 0 ulp. Acceptance (canonical 6/6, before→after): good-fix over-rejection <3 m **15.7%→1.2%** (<1 m 11.2%→0.0%; gate-0's 35% close-range →0), catastrophic leak **1.06%→0.53%**, KF-accepted 147→171, quality held (p50 0.85/p90 1.72 m). **🚩 THE ~3.6°/RANGE YAW BIAS IS NOT A FIXED CALIBRATION — REFUTED:** decomposes into a depth-scale artifact + both-sign per-gate offsets + a flight-specific roll-coupled wander (δ_yaw≈−0.52°/° roll, r²=0.87; composition + latency fits BOTH failed cross-validation on the 06-05 flight) ⇒ belongs in COVARIANCE, not calibration; re-measure the roll-wander on the next fresh 6/6 (`--dump-extras` + `composition_fit.py`). **🚩 REAL BUG FIXED (37e7ab1): `corner_to_center`'s gate frame was 180° in-plane vs the detector corner convention** — invisible to position (square symmetry) but the PnP disambiguation prior was ANTI-ALIGNED (frontal tie-break/P3P branch effectively random on real detections); fixed in `gates_from_track_records`, convention test added. Detail: `handoff/shadowpc-vision-pkg2-2026-06-10/` + [[project-phase2-rl-vision-decisions]] §VISION-PKG2.

**Git / env:** **`main` is CANONICAL** (the `red-team-tier-a` + merged feature/backup branches were retired 2026-06-07 in the pre-Phase-2 cleanup; `scripts/` pruned to the 17-file live+twin+vision+sysid stack). **412 tests green**, `.venv` Python 3.13. `*.pt` weights + `data/runs` recordings are gitignored (move via the adroit-connector). Project memory is mirrored repo `memory/` ↔ `~/.claude` — the cross-machine sync vehicle; ShadowPC passes findings via `handoff/`, not by editing `memory/`.

## NEXT — Phase 2 (RL + VQ2 vision); VQ1 is banked
**🚩 VQ1 = PASS/FAIL "navigate the course" (spec §8, 8-min cap), NOT a ranked time (that's VQ2's fastest-valid-time).** The submission is our **Python stack that THEY run unattended in a controlled eval** (interface "coming weeks", emailed to registered teams) → it must be **FULLY AUTONOMOUS end-to-end** (auto-arm→fly→finish, no human GO/disarm; §7: human interaction = DQ). Unlimited attempts, window May→~mid/late July. **Adroit summer access CONFIRMED (2026-06-08).** **User actions: confirm registration is active + email info@theaigrandprix.com for — ① (architecture-defining) does VQ2 stream `LOCAL_POSITION_NED`/`ODOMETRY` (given pose) or is position vision-only? ② the submission interface spec + ③ the VQ1 deadline + 🆕 (stack-review 2026-06-10) ④ can the stack carry state (map, tuned line) BETWEEN attempts in the controlled eval? (decides whether track-randomized generalization is mandatory vs insurance) + ⑤ the eval-hardware compute envelope — does the submitted stack get a GPU? (gates detector sizing; do NOT size to 100 TOPS for virtual quals).** Detail + open items: [[reference-competition-materials]].
- **3 known VQ1-stack issues → Phase-2 / RL targets** (re-derived from the course flight, `handoff/shadowpc-reverify-2026-06-07/`): **✅ ① start transient FIXED (2026-06-09, commit 260972e):** `launch_ramp_s=0.6 s` ramp eliminates rate-clamp saturation/tick-phase dice-roll; 4× deterministic 6/6 on 1.0.3364 confirmed — VQ1 blocker GONE (RL subsumption = VQ2 polish). **② aggressive descents** (peak sink 9.3 m/s → VQ2 smoothness/time); **③ always-on alt relay** (validity-harmless; gain-schedule / RL). Issues ②+③ remain VQ2/RL targets.
- **RL = COMMITTED VQ2 path** (now that VQ1 is banked): SWIFT-style learned state→action policy behind the `Setpoint` seam (NOT end-to-end pixels), trained on a massively-parallel twin matching the system-ID'd plant + a MEASURED perception-noise/latency/dropout model (the speed ceiling). RL would subsume ①–③. [[project-master-plan]] C1. **🆕 SUBSTRATE (2026-06-08 planning): DiffAero/Crazyflow (lightweight, GPU, DIFFERENTIABLE, multi-dynamics → inject our system-ID'd plant) OVER Isaac Lab (heavy SLURM install, PhysX≠our plant, rendering unused) — Stage-0 bake-off DONE → **DiffAero CHOSEN**, env GREEN on Adroit (pytorch3d prebuilt wheel `0.7.8+pt2.5.1cu121-cp311` +torchvision/open3d; smoke racing-PPO n_envs=64 ~3,220 env-steps/s on a V100, success 0.97 — a FLOOR, scales to thousands). **Plant PROVEN inside DiffAero on Adroit (2026-06-08):** `src/racer/rl_plant.py` (portable/telemetry-free) parity BIT-IDENTICAL to the twin (45 tests, suite 341); adapter `rl/diffaero_dynamics.py` torch backend = machine-epsilon identical (**gate `check_against_rl_plant` DIV 4.4e-16 on a V100 — PASS**); smoke-train racing-PPO n_envs=2048 **~88.9K env-steps/s, success 0→0.79, ep-len 0.5→32.6 s** (learns to race our plant). Wiring = a launcher (`peregrine_train.py`) monkeypatching `DYNAMICS_ALIAS` (ZERO edits to the diffaero clone). **Stage 1 increment 1 ✅ DONE → S1.2 live validation**: real RL I/O (gate-relative geometry + platform state → CTBR) + reward shaping + DR on rate_gain/hover/linear_drag (g=9.80665). **STAGE 1 INCREMENT 1 COMPLETE (2026-06-09, job 3261393, A100, 30:48):** racing-PPO n_envs=2048, 5000 updates → success_rate 0→0.97, ~89K steps/s. Policy: obs_dim=17 (vel+quat+gate_relpos+gate_normal+body_rates+collective), action=CTBR, DR on (rate_gain±10%/hover±5%/drag±30%/τ±30%). Checkpoint `stage1_inc1_actor.pth`. Aggressive (l_ep~1.9s, no speed shaping) — VQ2 concern, not VQ1. **🆕 S1.2 COMPLETE (2026-06-10, fable): deployment pipeline VERIFIED end-to-end, checkpoint NOT transfer-ready (flight 1: 0 gates, tumbled into gate-0 post).** The bridge worked PERFECTLY (CTBR stack delivered the drone dead-centre dy +0.04/dz +0.05 m at 5.1 m/s, 3 m before gate 0; policy's first action = exactly the offline-twin prediction). **THE REAL transfer failure = the policy is a "backflip-diver"** — its nominal twin maneuver rolls through **104–126° before every gate** (fine offline). Live the maneuver diverges: realized rates hit **9.7 rad/s vs the twin's first-order 7.85 ceiling** (SUPERSEDED diagnosis 2026-06-10: that was unmodeled static DC gain — the super-rate map — NOT transient overshoot), it blows through ±180° tilt (the then-suspected "yaw-spin anomaly" — **DISSOLVED by the sweep: super-rate gain + Euler yaw-flip artifact + a gate-post COLLISION**), and tumbles within 0.35 s. NOT reproducible in the twin → no deployment-side knob fixes it; fixed in retrain. Also fixed this session: a **tail-first spawn bug** (policy trained identity-reset + all gates at yaw π flies the course tail-first; sim spawns nose-first on a 17.8°-tilted pad) → a **virtual π body-z flip in fly_rl.py** (offline 0/6 → 6/6 under training physics). OOD verdicts (offline): raw standing start ≈ 0–1/6 (arrives at gate 0 at 33+ m/s); bridge handoff + virtual flip = **6/6 under training physics, 4/6 under the live collective ceiling** (the gate-4 wall is purely the thrust clip: trained 5 g vs live 3.77 g). **NEXT = S1.3 RETRAIN (Path C, ~30 min A100, `peregrine_racing_s13.sbatch` staged):** ① max_normed_thrust=**3.765** (live ceiling); ② **standing-start resets** (~23 m from gate 0 at rest = real race start; cfg-gated in peregrine_racing.py — removes the CTBR bridge); ③ **tilt/jerk regularization** (kills the >90°-roll style — then framed as anomaly avoidance, NOW (sweep 510da24) justified for smoothness/VQ2 style + staying in the well-modeled envelope only, since no anomaly exists; ALSO the VQ2 aggression fix); ④ latency DR (ring buffer, parity-checkable) + asymmetric rate-gain band **[−10%,+30%]** (needs an Adroit session on diffaero_dynamics.py + must re-pass the parity gate). **✅ S1.3 RETRAIN COMPLETE IN-TWIN (2026-06-10): all four done — ① 3.765 ceiling, ② standing-start (FIXED a real OOB-box spawn bug that caused an earlier 0%-success eval — the BUG, not the policy), ③ ATT=2.0/JERK=0.5 → 100% 6/6 in-twin, peak roll 65° (<80°, down from inc-1's 104–126° — backflip-dive GONE in-twin; tilt sweep {2,4,8}→2.0 won), ④ latency+rate-gain DR, parity 2.22e-16. 🚩 DELIVERABLE = the **2000-update att2 checkpoint** `rl/checkpoints/stage1_inc3_actor.pth` (md5 **ea2bf8a24eb4d149ff5dde2fda9432d9**, commit **c05362f**) — success 1.000 by update 2000, plateaued. The **5000-update run NaN'd** (PPO `loc...Real()`) at update ~2243 and wrote NO checkpoint (seed=0 reproducible; follow-up = new seed + grad-NaN guards, not needed for transfer). S1.3 source (diffaero_dynamics/peregrine_racing/peregrine_train_racing/peregrine_eval/s13_sweep.sbatch/run_parity.sh) committed separately. 🚩 CORRECTION: inc-1 had NO PLANT DR — the launcher defaulted to the numpy backend which SILENTLY ignores per-env DR; inc-1's 0.97 was overfit-to-nominal. S1.3 switched to the torch backend → DR is active for the first time. 🚩 S1.3 + inc-1 trained on the WRONG plant (flat-2.5; under-predict their own authority by up to 42% at full stick — sweep 510da24) → retrain on the map-ON plant — **gating now SATISFIED (S14 integration done 2026-06-10)**; the S13-LIVE transfer test is DEMOTED from disambiguator to optional cheap datum (fly opportunistically, non-blocking — the sweep answered the plant-fidelity question directly).** **🚩 CHARACTERIZE-SWEEP DONE (510da24, fable, 2026-06-10; 15 controlled runs, n=1→n=11) — STRUCTURALLY SUPERSEDES the cc6921d 2nd-order/PI-windup model.** The inner loop is a **STATIC amplitude-dependent gain map ("super-rate") in front of a fast ~critically-damped loop**: `g(|c|)=G0/(1−s·min(|c|,π)/π)`, G0=shipped [2.501,2.504,2.231], s≈0.29–0.30 (r/p, ±0.02); sustained gains 2.50→3.50/3.53 (roll/pitch) from |cmd| 0.3→3.14. **PI-windup DISPROVEN** (cmd 3.14 holds 11.1 rad/s for 1.2+ s with ZERO decay — a wound integrator must bleed); cc6921d's "DC anchored at 2.5 / G=3.8 rejected" was the wrong call (the optimizer was measuring the real saturated gain), and its ωₙ∈[21,68]/ζ∈[0.39,0.85] two-regime envelope was the LTI shadow of that wrong anchor (per-regime fits are 10–25× worse on each other's data). The S1.2 "9.7 vs 7.85 ceiling" = unmodeled DC gain, NOT transient overshoot. Transient on top: rise t80≈31–52 ms at all magnitudes, slew ≈250–280 rad/s² (r/p, ~78 yaw level); saturated step ~critically damped (ζ 0.84–1.04, 0–1% overshoot), small-signal keeps a real-but-minor ~10% overshoot (ωₙ≈73, ζ≈0.5); input delay 5–15 ms → transport-delay mechanism, NOT τ. **DR spec for RL (REPLACES both the disproven "+30% rate_gain band" AND the cc6921d ωₙ/ζ-envelope plan): static map with s∈[0.25,0.35], τ∈[0.015,0.03], alpha_max∈[200,320] — ✅ SHIPPED in S14** (clamp concern resolved: 25.0 sits above the map's ~11.2 rad/s DC ceiling, never bites). Integration spec = sweep WRITEUP §3 — ✅ IMPLEMENTED (S14, below). UPSIDE: real full-stick authority ~11 rad/s, not 7.85 — a HIGHER VQ2 speed ceiling. **NO SIM ANOMALY (open-loop):** sustained rotations through full inversion (to 11.2 rad/s, multi-rev, multi-axis combos incl. the exact S1.2 tumble cmd [+3.14,−3.14,+3.14], tilt abort off) all clean rigid-body, uncommanded axes flat zero ⇒ crash-termination = **COLLISION-based** (DiffAero's racing env already has it), do NOT encode a tilt-threshold pseudo-anomaly (tilt/jerk reg stays for smoothness/VQ2 style only) ⇒ **the env/reward redesign is UNGATED** (its gate was the anomaly-boundary measurement — there is none to encode). Caveats: probes near hover (racing-airspeed aero unmeasured — DR band covers); yaw gain is maneuver-dependent ~2.35 level (looks like a ~7.4 rad/s cap) vs ~3.1 tumbling — own pass only if it ever matters. **✅ S14 STATIC-MAP INTEGRATION COMPLETE (2026-06-10, fable; commits f730bd6+b14ca2e+77a0186; report `handoff/laptop-s14-staticmap-integration-2026-06-10/REPORT.md`) — critical-path step 1 DONE.** The measured super-rate map + slew limit is in ALL THREE plant implementations, parity-gated: twin.py (`super_rate_s`+`alpha_max_rps2`, defaults None = bit-identical legacy, test-pinned) · twin_fit.py (`faithful_config(super_rate=True)` = measured map-ON) · rl_plant.py (mirror; exports `SUPER_RATE_S_MEASURED`/`ALPHA_MAX_RPS2_MEASURED` as canonical DR centers) · diffaero_dynamics.py (torch mirror; disproven +30% band REMOVED → per-env per-axis s∈U[0.25,0.35], τ∈U[0.015,0.030] abs, alpha_max r/p∈U[200,320] / yaw ×80/260; map+slew always ON under DR; params-level transport-delay ring buffer ported to BOTH backends — S12 item 4a closed; wrapper latency DR untouched, no delay double-count into τ). Gates: gain-table anchor +1.6–2.9% of measured at all 8 r/p points; twin↔rl_plant bit-identical (60-case battery); **V100 gate job 3265870 worst 1.8e-15 (acceptance ≤1e-6) — PASS**, negative controls prove teeth; `check_diffaero_gate.py` now a config-matrix gate + NEW `rl/local_gate_harness.py` (laptop CPU-torch dry run before any Adroit roundtrip). Tests 376→409. **🚩 CAVEAT: eval/rollout scripts (`peregrine_eval.py`, `offline_rollout.py`) still DEFAULT to the legacy FLAT plant — right for the flat-trained checkpoints, but the S1.4 retrain + all future evals must run map-ON** (set the measured params or enable DR, which forces the map on). **🚩 ADROIT OPS: `/scratch/network/fl3689/peregrine_repo` is a FILE COPY, not a git clone (repo private)** — S14 pushed via md5-verified base64 over the x daemon; stage a real clone with a deploy key if Adroit sessions grow. **NEXT = S1.4: ENV-COHERENCE redesign + retrain on the map-ON plant** (one fable+adroit-connector session; reward+termination designed TOGETHER; user: reward conflicts with termination; COLLISION-based crash-termination; 🆕 scope ALSO includes **procedural track randomization + VQ1-course-as-held-out-eval**, stack-review meta-gap ①; measured DR now in place). Sweep detail: `handoff/shadowpc-characterize-sweep-2026-06-10/`. **Pending decision:** S2 may become a **decomposed plan-line + RL-tracker** (explicit racing line, RL only tracks → backflip-dive structurally impossible) vs another monolithic racer — decide on the post-static-map retrain result (the S1.3 live datum is now optional). Detail: `handoff/shadowpc-s12-rl-live-2026-06-10/`. **🚩 perception-noise model MEASURED (T1) — UPDATED 2026-06-10 by VISION-PKG2 (supersedes the 1.1% figure):** world-fix σ≈[0.73,0.47,0.29] m (N,E,D), bias [−0.4,+0.06,−0.28] m, range-flat to ~24 m; with the shipped measured covariance: **fix acceptance ~47% of race-window frames (was ~41%), catastrophic leak 0.53% of solved (bounded ≈3 m); twin fix-covariance = K2·analytic + 1.4° attitude lever + 0.40 m floor + 32 m range cap — THE Stage-2 twin input.** Differentiable-sim learning beats Isaac+PPO. **🚩 PNP_FIX_COV_INFLATION task CLOSED (2026-06-09):** behind the depth-sanity gate K=1.0 and K=2.0 leak identically (prior objection evaporates); no revert needed. The real over-rejection lever = `attitude_noise_std` — **✅ SHIPPED (VISION-PKG2 2026-06-10, see the Detector section: 1.4° + 0.40 m floor + 32 m cap; the missing short-range term was the FLOOR).** Detail: `handoff/laptop-pnp-cov-inflation-2026-06-08`. 412 tests. Full Phase-2 decisions (RL I/O+rewards, vision 2-workstream plan + real-frame perception char, gate-map cases, the AUTHORITATIVE sim collision signal, depth/100-TOPS flywheel, staging): [[project-phase2-rl-vision-decisions]].**
- **🆕 STACK-REVIEW-VQ2 (2026-06-10, fable, report-only): architecture AFFIRMED** — the 2025 A2RL champion (MonoRace, TU Delft) validates our class (mono gate perception + known-geometry calibration + learned controller); NO component needs replacement. Exposure = **3 META gaps**: **① single-track training vs unseen VQ2 course (potentially BINARY — rank-impact #1**; fix = procedural track randomization + VQ1-as-held-out-eval, folded into the env-coherence redesign); **② no time-optimal bound** for the VQ1 course (TOGT-Planner + CPC bound = upgrade #2; gap meter + RL progress-reward reference + S2 line); **③ vision-only (case C) readiness pieces unbuilt** (delayed-fix KF rewind buffer · range-anisotropic R, depth-axis ∝ r² · in-loop perception latency — NEVER measured, ShadowPC torch is CPU-only; the offline yaw-bias calibration solve sub-piece is **✅ RESOLVED-AS-REFUTED, VISION-PKG2 2026-06-10** — no fixed rotation exists to solve for; the other three remain OPEN). Distilled (incl. secondary keepers + the examined-and-REJECTED ledger — DA3, RT-DETR-class, VIO, GTSAM-in-loop, MPPI, motor-cmds, pixels): [[project-phase2-rl-vision-decisions]] §STACK-REVIEW-VQ2; full report `handoff/stack-review-2026-06-10/REPORT.md`.
- **Vision for VQ2** (aids-off, photoreal): the detector→robust-PnP→KF chain. **🚩 the ~3.6°/range "yaw-bias" is NOT a calibration target — REFUTED (VISION-PKG2, 2026-06-10):** depth-scale artifact + both-sign per-gate offsets + flight-specific roll wander → covered by the shipped covariance model (1.4° + 0.40 m floor), not calibrated out. Irrelevant to model-based given-pose nav. Estimator robustness levers: [[project-estimator-robustness]].

## Durable strategy & directives
- **Track A (model-based) = VQ1 floor (BANKED) + VQ2 baseline**, leaning on the sim's stabilizer (CTBR). Estimator = LINEAR pos/vel KF (attitude given). Detector = YOLO-pose corners → IPPE / robust weighted-PnP. MVP = min-snap line + conservative completion (a slow VALID finish beats a fast invalid one).
- **Overfit GEOMETRY, randomize APPEARANCE.** VQ1 is clean/desaturated/aids-on; VQ2 is photoreal/cluttered/aids-off ⇒ NEVER fine-tune the detector on clean VQ1 frames (would collapse the domain-randomization edge). Train in chaos, validate on chaos. ByteTrack sits behind the GateObservation seam.
- **Planning (racing line + speed) = the VQ2 differentiator (where rank is won); vision = a bounded threshold.** Chain mental model: must-not-BREAK (validity) vs sets-your-RANK (time).
- **RL = COMMITTED VQ2 path** (the moment VQ1 is banked — now): learned state→action policy behind the `Setpoint` seam, twin-trained with a measured perception/latency/dropout model. [[project-master-plan]] C1.
- **User directives:** speed > keeping the gate in view; vision must be excellent; determinism is per-track; offline between-runs processing is LEGAL (not a cheat); be a good citizen on Adroit (own dirs, SLURM only). Discipline that worked: re-derive every agent verdict from data; tune offline, fly only to verify; bounded actuation, user owns the GUI/risk. **🆕 Every new-session prompt carries a recommended MODEL (with VERSION, e.g. opus-4.8 / sonnet-4.6 / fable — not just the family) + EFFORT (low→medium→high→ultracode).** **🆕 2026-06-10 — user upgraded to the MAX plan + switched the commander session to opus 4.8 → token-conservation pressure is RELAXED ("turn up the heat"):** use **opus/fable at high freely** on hard, objectively-verifiable problems (RL deployment, reward shaping, adapter+parity work). Still match model to task (no opus for `git status`). **🚩 FABLE SUNSET ~2026-06-22 (12 days from 2026-06-10):** Fable access ends; it is the strongest model we'll have for this project → **MAXIMIZE Fable in this window**: front-load all hard/design-heavy/objectively-verifiable work (plant integration, env+reward redesign, retrains, S2 architecture decision, vision/estimator math, detector DR pipeline) into Fable sessions NOW, run parallel Fable lanes across the three machines, and bank findings/handoffs extra-rigorously so post-Fable models (opus 4.8 and below) inherit a complete written state. Routine ops (flights, evals, git, light tuning) can wait for lesser models. **MEASURED HEADROOM (2026-06-10): a full commander day (3 parallel worker lanes, multiple fable sessions, background agents) used only 39% of 5-hr / 12% of weekly quota — long autonomous sessions are the DEFAULT, never trim scope for budget; the user posts periodic usage updates to recalibrate.**
- **Spec facts:** VFoV≈58.7° (the spec's "90°" is HFoV; camera tilts up). ~100 TOPS onboard edge budget — under-used (room to size up perception / deploy a learned policy). Obstacles exist but don't map monocularly (fly the corridor).

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture + execution plan, Track-A strategy, components, risks R1–R11, build sequence. The durable gameplan; read when resuming. ([[reference-sim-interface]] is the live sim truth where they differ.)
- [Phase-2 RL+vision decisions (2026-06-08)](project_phase2_rl_vision_decisions.md) — **READ when resuming Phase 2.** RL substrate landscape (DiffAero/Crazyflow > Isaac + the bake-off), RL policy I/O+rewards, the vision 2-workstream plan + real-frame perception characterization (`scripts/characterize_perception.py`), gate-map/SLAM cases, the authoritative collision signal, depth/100-TOPS flywheel, staging (Stage 0/1/2).
- [Sim interface — confirmed wire spec + first contact](reference_sim_interface.md) — the REAL MAVLink+video interface; FIRST CONTACT 2026-06-02 resolved all 5 must-verify items (position given · ATTITUDE sign → ODOMETRY quat · gate map · CTBR · arming · clocks · frame-dedup).
- [CTBR control + inner-rate system-ID](project_ctbr_control_sysid.md) — the FLYABLE control stack: rate-loop gains/signs · the plant-matched decoupled CTBR controller · the faithful offline twin + live config · the Gate-0 saga (ODOMETRY frame bug, roll-report inversion, map anchor) · the VERIFY alt-relay finding (delay-driven, validity-harmless).
- [Detector training pipeline + weighted-PnP](project_detector_training_pipeline.md) — autonomous Adroit YOLO-pose training; v2 SHIPPED / v3 `--hard` negative result; multi-gate synthetic; robust weighted-PnP; adroit-connector ops (run connector CLIs via PowerShell, not Git Bash).
- [AI Grand Prix context](project_ai_grand_prix.md) — the competition (Anduril + DCL, May–Nov 2026; user competing for fun).
- [Competition source materials](reference_competition_materials.md) — spec PDF facts, verified rules/FAQ, the VQ1 submission mechanics (pass/fail, stack-submitted, autonomous), open unknowns.
- [Hardware constraint](project_hardware_constraint.md) — three machines: laptop (dev; Intel iGPU, can't run sim) · ShadowPC (sim host) · Adroit Princeton (free GPU, detector training).
- [Adroit cluster notes](reference_adroit_princeton.md) — Princeton's Slurm/GPU cluster (`ssh <netid>@adroit.princeton.edu`, `/home`+`/scratch`); the detector-training compute. Confirm summer access.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries (license/reuse verdicts) + the 8-paper academic ledger.
- [Tooling recommendations eval](project_tooling_recommendations_eval.md) — 3rd-party tool triage: perception-noise-model = a hard RL requirement; offline processing legal; keepers ADRC · GTSAM mapper · min-snap (rpg) · Isaac RL libs.
- [Estimator robustness](project_estimator_robustness.md) — adaptive R · Mahalanobis innovation-gate · map-averaging · no-gate SEARCH state · optical-flow assist; the home for these when hardening the navigator for VQ2.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must BE the full VQ2 stack under-tuned (vision+KF in-loop), never a crutch.
- [Red-team passes #2 + #3](project_red_team_pass_2.md) — historical pre-sim-drop triage logs (fixes applied; claims logged so they aren't re-litigated). Mostly resolved at first contact; kept for the deferral record. (#3: [project_red_team_pass_3.md](project_red_team_pass_3.md))
- [Checkpoint transfer workflow](feedback_checkpoint_transfer.md) — small RL policy checkpoints go in git; ShadowPC pulls instead of manual copy
