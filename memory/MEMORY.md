**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026; fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. Orientation index; detail in topic files.

## READ FIRST — current state (2026-06-13)
**🏁 VQ1 PASSED** (dashboard-confirmed). CTBR stack threads 6-gate course — valid finish ~35.3 s, in-plane miss `[0.37, 0.05, 0.06, 0.03, 0.10, ~0.71]` m (all < 0.75 m). **Offline-twin discipline** (tune offline, fly to VERIFY) got us unstuck — keep it. Phase 2 = **RL + VQ2 vision**.

**✅ INC7 LIVE CONFIRMED (2026-06-13, commit 7fe90df) — MILESTONE: first standing-start RL finishes in project history.** Standing ×5 = 5/5 FINISHED, gate-3 NEVER crashed (n_coll=0 all 5 flights). Lap times: F1 9.97 s (warmed sim), F2–F5 ≈11.45 s (fresh sim; use as deployment estimate). Bridge ×2: 2/2 FINISHED (B1 ~9.19 s RL-seg, B2 ~10.74 s). Frame canary TRUE +0.97 / AS-IS −0.81 across all 7 sessions (conventions intact). Command saturation NONE (max |tanh|≈0.547). Crab live max 63.2° (≈64° prediction PASS). **GEOMETRY-HONESTY THESIS LIVE-VALIDATED:** contact-true volumetric training fixed the gate-3 barrier on the FIRST try AND absorbed the known +3.5 m/s² N+D climb-bin residual via MARGIN rather than refit. Doctrine confirmed: robustness from honest geometry + structured DR; do NOT chase every residual. S20 stays correctly deferred. **🚩 NEW FINDINGS queued (non-blocking):** (A) gate-3 D-offset ~1.46 m — track_map gate-3 D-center likely mis-calibrated (flight clean, sim PASS; track_map offset confounds offline L-inf metric; bundle into SHADOWPC-VISION-CAL); (B) bimodal standing lap times (F1 9.97 vs F2–F5 11.45 s) — likely sim-physics-warmup at fresh race start (sim_t~5 s); both findings from existing 7 recordings, no new flights needed.

**✅ FRAME-AUDIT/LIVE-CONFIRM/CRAB-DIAG SETTLED (2026-06-12):** sim ODOMETRY quat is R_y(π)-conjugated (`q_true=q_raw·[1,−1,1,−1]`); inc6 LIVE CONFIRMED (bridge 2/2 zero-contact FINISHED, ~9 s RL-segment); crab (~55° tilt) = trained near-optimal posture, 3% drag cost, no reward change. **TRUE ROOT CAUSE of gate-3 barrier (TRAINING-DOCTRINE):** training env scores point-mass L-inf<0.75 m as pass; sim enforces volumetric body-halo contact. S18 lapse VOIDED. Full detail: [[rl-increment-history]] §LAPTOP-FRAME-AUDIT + §INC6-LIVE-CONFIRMED + §CRAB-DIAG + §TRAINING-DOCTRINE.

**✅ SIM BUILD 1.0.3364 REGRESSION FIXED (2026-06-09):** `launch_ramp_s=0.6 s` + `--rate 100` (detail [[project-ctbr-control-sysid]]). **VQ2 open items:** (B) decel into gates; (C) ~20 km/h cap — both RL levers.

**SIM OPS (full mechanics [[reference-sim-ops]]):** unattended control PROVEN. 31000 restart NO-OP from HOME → Win32 foreground + Enter×2; three idle states; zombie dual-instance on 14550; sim AUTORESETS on gate contact → guard cuts RL commands instantly (mandatory); fullscreen auto-minimizes → verify foreground; use raw ODOMETRY rate (quat-finite-diff aliases). **🚩 SPAWN-ARTEFACT (2026-06-12):** gate-3 HARD COLLISION full-reset → next respawn hits residual collision geometry → 0-tick crash at 0 gates; creates gate-3-crash → artefact → clean-spawn → gate-3-crash cycle; NOT a policy failure; affects per-batch stats.

## The flyable stack (durable — RL training substrate)
- **Sim interface** (all 5 must-verify RESOLVED; full wire spec [[reference-sim-interface]]): pos+vel GIVEN pristine (LPN 97 Hz + ODOMETRY 75 Hz; baro NaN → z from given); **🚩 ATTITUDE.pitch sign-inverted → use ODOMETRY quat**; gate map = 6 gates, 2.72 m OUTER / ~1.5 m inner (feed inner to PnP), `capture_track_map.py` map-once-reuse; course DESCENDS 26 m; clocks INDEPENDENT (align recv-monotonic); video dedup by frame_id; sim PAUSES physics off-race.
- **Fly on CTBR** (`SET_ATTITUDE_TARGET`; sim runs ACRO). OFFLINE TWIN (`src/racer/twin.py`/`twin_fit.py`, RL plant): **rate_gain [2.50,2.50,2.23], live physics rate_sign [+1,+1,+1] (vanilla — NO inversion on any axis; all prior inversion lore = R_y(π) telemetry alias), τ=0.019 s, hover=0.2656, linear_drag≈0.21/s (small-signal)**. **🚩 rate_gain amplitude-DEPENDENT: super-rate map `g(|c|)=G0/(1−s·min(|c|,π)/π)`, s≈0.30 ⇒ full-stick ~11 rad/s (NOT 7.85); flat-2.5 valid small-signal.** **Plant status: super-rate map ✅ S14 + quad drag/convex collective ✅ S16 + mixer per-motor clip ✅ S17 — all THREE plants complete; parity gate CLEARED job 3270602.** Falsification detail: [[rl-increment-history]].
- **🚩 CTBR/VQ1 LEGACY SIGN CONFIG (VQ1-proven, DO NOT "fix"):** The CTBR controller uses `body_rate_sign=[1,1,−1]`/`odo_att_sign=[−1,1,1]`/`odo_rate_sign=[−1,−1,1]`/`ff_gain=2.5`. These values form a self-consistent level-attitude alias (a roll mirror that is dynamically correct at near-level VQ1 attitudes) — tuned end-to-end, VQ1-proven, unchanged. **They are NOT the true physical conventions.** Arm = `MAV_CMD_COMPONENT_ARM_DISARM` p1=1. **Live config = `FAITHFUL_TUNED_GAINS` (`kp_alt 3.0/kd_alt 1.75`, raw vz) + `−0.4 m alt_offset` + `launch_ramp_s=0.6 s`.** Detail + Gate-0 saga + alt-relay + 1.0.3364 fix: [[project-ctbr-control-sysid]].
- **🆕 RL DEPLOYMENT RECIPE (current, 93023cf — supersedes bcc93f9/S1.2; in `rl/fly_rl.py`):** ① **tanh(mean) → rescale** (thrust [0,5], rates ±3.14); ② attitude from ODOMETRY quat conjugated: `q_true = q_raw·[1,−1,1,−1]` (wxyz); ③ rate sign **[−1,−1,−1]** (`_ODO_RATE_SIGN`; true rate = −w_raw); ④ wire command = `rate_flu·[+1,−1,+1]` (`_ACT_FLU_TO_FRD`); ⑤ collective obs init **0.0** rescaled; ⑥ training rate **30 Hz**; ⑦ resets **at rest** 1 m before random gate. Detail [[project-phase2-rl-vision-decisions]] §FRAME-AUDIT.
- **🚩 TRUE PHYSICAL CONVENTIONS (2026-06-12, 93023cf — supersedes bcc93f9):** ODOMETRY quat is R_y(π)-CONJUGATED (NOT true attitude AS-IS). True attitude: `q_true = q_raw·[1,−1,1,−1]` (negate x,z in wxyz; roll+yaw Euler negated, pitch intact). True angular rate: `ω_true = −w_raw` (all axes; gain 0.999). Live cmd→rate sign: **[+1,+1,+1]** — vanilla CTBR, no inversion. ODO twist (body vel) and accel_body PAIR with the raw quat → keep as-is. `state.velocity_ned` is PRISTINE. **VALIDATION DOCTRINE (strengthened):** internal consistency (quat-FD vs rate, level flight, twist round-trip) CANNOT catch a proper-rotation conjugation — only external invariants (FD of pristine vel_ned vs attitude-derived force) discriminate. Run `scripts/frame_residual_report.py` after EVERY live session. [[project-ctbr-control-sysid]] + [[project-phase2-rl-vision-decisions]] §FRAME-AUDIT.
- **Alt balloon is OURS** (CTBR owns thrust). Limit cycle = delay-driven relay, ALWAYS-ON but VALIDITY-HARMLESS; deferred. **🚩 LIVE LATENCY = 2 ticks (67 ms)** (command-vs-realized cross-correlation, supersedes ~40 ms lore); offline twin under-models latency — do NOT trust offline "holds" claims.
- **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`) — do NOT reintroduce:** twist is BODY-frame, was stored as world; now rotated via `frames.world_vec_from_body_quat`.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate; v3 `--hard` = NEGATIVE, corner-swap aliasing — [[project-detector-training-pipeline]]). **✅ Catastrophic-tail fix (4673517); VISION-PKG2 shipped (2026-06-10): 1.4° attitude lever + 0.40 m cov floor + 32 m range cap; over-rejection 15.7%→1.2%, leak 0.53%. ✅ VISION-FRAME-FIX (8d7b0b3): navigator.py:295 uses `R_world_from_odo_quat_wxyz`; east bias eliminated. 🚩 `corner_to_center` 180° flip FIXED (37e7ab1).** Detail [[project-phase2-rl-vision-decisions]] §VISION-PKG2.

**✅ GATE MAPPER COMPLETE (2026-06-11):** `src/racer/gate_mapper.py` cases A/B/C; exact `capture_track_map.py` schema; navigator untouched. [[project-phase2-rl-vision-decisions]] §GATE-MAPPER.

**Git / env:** **`main` CANONICAL**. **620/620 tests green**, `.venv` Python 3.13. `*.pt` + `data/runs` gitignored. Memory mirrored `memory/` ↔ `~/.claude`; ShadowPC via `handoff/`. **🚩 OPS:** concurrent laptop sessions must use separate git worktrees or stagger suite runs (cross-contamination confirmed 2026-06-12).

**🚩 GATE CONTACT = INVALID RUN** (rules-confirmed). Zero-contact is the VALIDITY condition.

**CORNER-PASS (2026-06-11; contact=invalid):** TOGT planning-valid bound = 4.27 s (inscribed-circle, contact-free); 4.13 s contact-tolerant = INVALID; shipped reference line ≤0.14 m from centre. Detail [[project-phase2-rl-vision-decisions]] §CORNER-PASS.

**✅ S17 + INC6 COMPLETE + TRAINING-DOCTRINE ESTABLISHED (2026-06-12).** Mixer: per-motor clip u_i=clip(c+S·d, idle, 1); κ_err=0.073. Corner tax c16 winner: VQ1 1.000/9.86 s, gen 0.982, 0% saturation. **🚩 S18 LAPSE VOIDED — do NOT use `--plant lapse`/`dr_lapse`.** **🚩 `fly_rl.py` default still points at inc4 — pass inc7 (or inc6) ckpt explicitly.** TRAINING-DOCTRINE (`docs/training_doctrine.md`): robustness from honest contact geometry + structured DR + diversity; NEVER reward damping; full checkpoint gauntlet incl. corridor clearance. Detail [[project-phase2-rl-vision-decisions]] §S17 + §TRAINING-DOCTRINE + [[rl-increment-history]] §S17 + §TRAINING-DOCTRINE.

**🚩 STYLE ENVELOPE COST MEASURED — S2 input: ≤~65° roll costs ~2.3 s/lap on real aero; anomaly-avoidance rationale DISSOLVED. Relaxation = 3-step ladder: (1) global rw_tilt 96→48, (2) free-cone 60°→75–80° (binds on 3 segments), (3) unconstrained (~6.6 s potential). Gated on gap analysis + gate-3 standing fix.** Detail [[project-phase2-rl-vision-decisions]] §INC5 + §TILT-CONCENTRATION.

**🆕 NEXT queue (post-inc7-live-confirm 2026-06-13):**
① ✅ **INC7 COMPLETE + LIVE CONFIRMED** — s0 SHIPPED + VALIDATED (`stage1_inc7_actor.pth`, md5 2AFF8D62569BA5FEC769028D72AF50E3, sidecar {act_max_thrust 3.765, act_max_rate 3.14}). VQ1 sr=1.000 / t_med=9.76 s / gen=0.924 / tails max 0.169 m / gate-3 barrier GONE (0 collisions offline AND 0/5 standing live). Deployment lap ~11.45 s standing (fresh sim). **🚩 `fly_rl.py` default still inc4 — pass inc7 ckpt explicitly.** **🚩 Budget MORE seeds for inc8-class retrains** (2/3 seeds viable; narrow convergent basin confirmed). Detail [[rl-increment-history]] §inc7 + §INC7-LIVE-CONFIRMED.
② ✅ **SHADOWPC-INC7-LIVE DONE (2026-06-13)** — 5/5 standing FINISHED, 2/2 bridge FINISHED, gate-3 never crashed. **🚩 Two open nuances queued (cheap sonnet analysis, no new flights):** (A) track_map gate-3 D-offset ~1.46 m (bundle into SHADOWPC-VISION-CAL); (B) bimodal lap times F1 9.97 vs F2–F5 11.45 s (characterize from 7 recordings).
③ ✅ VISION-FRAME-FIX DONE (2026-06-12, 8d7b0b3)
④ Envelope ladder step 1 (rw_tilt 96→48, NOW UNBLOCKED — inc7 live confirmed) — user selects thrust
⑤ S19 mixer contradiction; inc6 = historical fallback (inc7 is now live-confirmed current best).

**🚩 POLICY DECISION RATE 30 Hz WAS NEVER A CHOICE — inherited DiffAero racing.yaml default (dt=0.0333).** 30 Hz binds ONLY at (≥30 m/s × last-fix ≤10 m); fixes to 15 m → NOT bottleneck; envelope 65°→80° bigger lever. **🚩 GATING MEASUREMENT queued (SHADOWPC-VISION-CAL):** per-gate last-accepted-fix distance. Detail [[project-phase2-rl-vision-decisions]] §SPEED-CEILING-ANALYTIC.

**🆕 PARALLEL ONBOARD SYSTEMS LEDGER (2026-06-11; gated on organizer ①⑤):** async detector→KF; ROI zoom; MPC-shadow; critic-as-risk. REJECTED: map mutation; ensemble voting; wind estimator; in-race adaptation. Tier 1/2 specs: [[project-phase2-rl-vision-decisions]] §PARALLEL-SYSTEMS.

## NEXT — Phase 2 (RL + VQ2); VQ1 banked
**🚩 VQ1 = PASS/FAIL "navigate the course" (spec §8, 8-min cap), NOT ranked time (VQ2 = fastest-valid).** Submission = **Python stack THEY run unattended** → **FULLY AUTONOMOUS end-to-end** (auto-arm→fly→finish; §7: human interaction = DQ). Unlimited attempts, May→~mid/late July. **Adroit summer access CONFIRMED (2026-06-08).** **User actions: email info@theaigrandprix.com for — ① VQ2 streams `LOCAL_POSITION_NED`/`ODOMETRY`? ② submission interface spec ③ VQ1 deadline ④ stack carry state BETWEEN attempts? ⑤ eval-hardware GPU? ⑥ physical round after VQ2?** Detail [[reference-competition-materials]].
- **VQ1 issues:** ✅ ① start transient FIXED; ② aggressive descents + ③ alt relay remain VQ2/RL.
- **RL = COMMITTED VQ2 path:** learned state→action policy behind `Setpoint` seam. Stage verdicts: [[project-phase2-rl-vision-decisions]]; lineage: [[rl-increment-history]].
  - **Substrate = DiffAero on Adroit** (bake-off done over Isaac): `src/racer/rl_plant.py` + `rl/diffaero_dynamics.py`, parity BIT-IDENTICAL (DIV 4.4e-16); 2048-env PPO ~88.9 K steps/s. **🚩 GPU PREFERENCE (user directive 2026-06-12): request A100 over V100 for ALL trainings — every future sbatch targets A100; "V100 gate" parity naming is historical.**
  - **Checkpoint lineage (full history [[rl-increment-history]]):** inc1 (overfit-no-DR) → inc3 (wrong flat plant) → inc4 (aero-blind, RETIRED) → inc5 (mixer-blind, RETIRED) → inc6 (mixer+aero+map plant, corner-tax c16; LIVE CONFIRMED bridge 2/2; historical fallback) → **inc7 = CURRENT BEST (LIVE CONFIRMED 2026-06-13)** (`stage1_inc7_actor.pth` s0, md5 2AFF8D62569BA5FEC769028D72AF50E3; standing 5/5, gate-3 barrier GONE, sr=1.000, gen=0.924). inc6 = historical fallback (bridge-only live-confirmed). **🚩 `fly_rl.py` default still points at inc4 — pass inc7 (or inc6) ckpt explicitly.**
  - **🚩 Retrain footguns:** `peregrine_eval.py`/`offline_rollout.py` DEFAULT to legacy flat plant — all evals must run map-ON; `/scratch/network/fl3689/peregrine_repo` is FILE COPY, not git clone; parity gate CLEARED job 3270602 (was "not run since S16").
  - **S2 architecture decision pending:** decomposed plan-line + RL-tracker vs monolithic — decide post-live-transfer; TOGT structural-gap datum (35.3 s → 8.3 s twin → 4.27 s TOGT) + style-envelope cost (2.3 s/lap) feed it.
  - **Perception model (VISION-PKG2):** world-fix σ≈[0.73,0.47,0.29] m (N,E,D), range-flat to ~24 m; acceptance ~47% race-window frames, leak 0.53%; twin fix-cov model = Stage-2 twin input. Detail [[project-phase2-rl-vision-decisions]] §VISION-PKG2.
  - **STACK-REVIEW-VQ2 (2026-06-10):** architecture AFFIRMED (MonoRace validates our class). META gaps ①② CLOSED; ③ vision-only (case C) readiness UNBUILT (KF rewind buffer · range-anisotropic R · in-loop latency never measured). Detail [[project-phase2-rl-vision-decisions]] §STACK-REVIEW.
  - **Vision for VQ2:** detector→robust-PnP→KF chain. ✅ VISION-FRAME-FIX (8d7b0b3): east bias eliminated. 🚩 sigma_theta=1.4° re-fit queued. [[project-estimator-robustness]].

## Durable strategy & directives
- **Track A = VQ1 floor (BANKED) + VQ2 baseline.** Estimator = linear pos/vel KF. Detector = YOLO-pose → IPPE/robust-PnP. MVP = min-snap line + slow VALID finish > fast invalid.
- **Overfit GEOMETRY, randomize APPEARANCE.** NEVER fine-tune detector on clean VQ1 frames.
- **Planning + speed = VQ2 differentiator; vision = bounded threshold.**
- **RL = COMMITTED VQ2 path** now VQ1 is banked. [[project-master-plan]] C1.
- **User directives:** speed > keeping gate in view; vision must be excellent; determinism is per-track; offline between-runs processing LEGAL; be a good citizen on Adroit (own dirs, SLURM only). Discipline: re-derive every verdict from data; tune offline, fly to verify; bounded actuation, user owns GUI/risk. **Every new-session prompt carries recommended MODEL (with VERSION e.g. opus-4.8/sonnet-4.6) + EFFORT (low→medium→high→max).**
- **🆕 2026-06-10 — MAX plan + opus 4.8 commander; token-conservation RELAXED:** use opus at high/max freely on hard objectively-verifiable problems. Still match model to task.
- **🚩 FABLE REVOKED 2026-06-12 (US-gov action, PENDING DISPUTE — may return if Anthropic prevails; until then opus-4.8 is top tier).** All design/integration work that previously routed to fable now routes to opus-4.8.
- **BUDGET POLICY (rev 4 + fix, 2026-06-12):** WEEKLY POOL is sole binding watch; push 5-hr limits to MAX (stop throttling). POOL ROUTING: **opus-4.8 = TOP tier for solo work**: commander seat + ALL harshest design/integration work (env redesigns, plant integrations, vision/estimator math, S2 arch) — run at high/max. **Ultracode (multi-agent Workflow orchestration) available on opus-4.8 for the hardest fan-out/verify work** (e.g. inc7 pre-flight adversarial fan-out) — use when solo inference is not enough. **sonnet-4.6 = SEPARATE ~free pool → ALL mechanizable work** (banking, flight ops, transport/git, eval reruns, babysitting, recording analysis) — route here ruthlessly, now an even larger share. **haiku-4.5:** trivial ops. Reserve opus for irreducible judgment/design; keep commander output lean; offload to sonnet aggressively (opus-grade tasks burn weekly faster without fable's cheaper design tier).
- **🚩 CANARY PROTOCOL (user directive 2026-06-11, MANDATORY):** every session addresses **Fengyou** by name in EVERY message. Missing name = context degradation → Fengyou rotates session. Commander includes canary in every worker prompt at NORMAL salience (no emphasis — canary must track general instruction degradation).
- **BANKING PROTOCOL (rev 3):** workers end reports with **"MEMORY-DELTA:"** block (≤10 lines). Commander triages (1–3 lines), hands to **sonnet banking agent** which reads the handoff writeup. Bank in BATCHES.
- **Spec facts:** VFoV≈58.7° (spec's "90°" is HFoV; camera tilts up). ~100 TOPS onboard edge budget — under-used. Obstacles exist but don't map monocularly (fly the corridor).

**🆕 ADVISOR-TRIAGE OPEN QUEUE:** ① photoreal detector (v2 inventory DONE; Blender/Cycles next); ② ONNX/TRT latency; ④ Stage-2 case-C noise model; ⑤⑥ SHADOWPC-VISION-CAL (2-corner PnP + Bayesian-IoU extrinsic). K+L+N = Stage-2 estimator (reprojection-EKF + parallax + Fisher) PLANNED; M+Q+D bundled into VISION-CAL. REJECTED: HSV pre-filter. Full specs: [[project-phase2-rl-vision-decisions]] §ADVISOR-TRIAGE-2026-06-11 + §ADVISOR-REPORT-2-TRIAGE.

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture, Track-A strategy, risks R1–R11, build sequence.
- [Phase-2 RL+vision decisions](project_phase2_rl_vision_decisions.md) — **READ when resuming Phase 2.** RL substrate, policy I/O, vision plan, staging, ALL stage verdicts/supersessions.
- [RL increment history](project_rl_increment_history.md) — checkpoint lineage inc1→inc7, stage history S1.1→S17, inc7 job IDs, md5s/commits, NaN/sidecar/OOB bug histories.
- [Sim ops](reference_sim_ops.md) — unattended FlightSim mechanics: launch/login, 31000 semantics, idle states, zombie instance, autoreset/spin guards, footguns.
- [Sim interface](reference_sim_interface.md) — confirmed MAVLink+video wire spec; all 5 must-verify items.
- [CTBR control + sysid](project_ctbr_control_sysid.md) — flyable stack: gains/signs, offline twin, Gate-0 saga, alt-relay.
- [Detector pipeline + PnP](project_detector_training_pipeline.md) — Adroit YOLO-pose training; v2/v3; weighted-PnP; adroit-connector ops.
- [AI Grand Prix context](project_ai_grand_prix.md) — competition overview.
- [Competition materials](reference_competition_materials.md) — spec facts, rules/FAQ, VQ1 mechanics, open unknowns.
- [Hardware](project_hardware_constraint.md) — laptop (dev) · ShadowPC (sim) · Adroit (GPU training).
- [Adroit cluster](reference_adroit_princeton.md) — Slurm/GPU; summer access confirmed.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries + 8-paper ledger.
- [Tooling eval](project_tooling_recommendations_eval.md) — 3rd-party triage; keepers ADRC/GTSAM/min-snap/Isaac.
- [Estimator robustness](project_estimator_robustness.md) — adaptive R, innovation-gate, map-avg, SEARCH state, optical-flow.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must be full VQ2 stack under-tuned.
- [Red-team passes #2+#3](project_red_team_pass_2.md) — historical triage; kept to avoid re-litigation.
- [Checkpoint transfer](feedback_checkpoint_transfer.md) — small RL checkpoints go in git; ShadowPC pulls.
