**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-27.)

## NOW · 🎯 GATE-SIDE STRIKE (the standing target) — SSOTs [[gate-strike-last-3m-2026-07-27]] · [[failure-census-2026-07-27]] · [[velocity-channel-2026-07-27]]
- 🎯 **THE TARGET (Fengyou): the drone flies into the SIDE of the gate with vision fine.** 🟢 Lateral loop is sound on approach (slope |lat@8 m|→|lat@2 m| = **0.000 ± 0.03**, R² = 0.004) ⇒ **the tail is created LATE** — see the blind-1.5 m bullet, which now supersedes every earlier mechanism. 🛑 My "8→5 m capture gap" = **SELECTION ARTIFACT**. 🛑🛑 **0.75 m IS THE WRONG APERTURE — `0.75 − body_radius`, r~U[0.28,0.38] ⇒ 0.37–0.47 m**; vs 0.45 m, **~30% of ALL approaches exposed**. → [[gate-strike-last-3m-2026-07-27]]
- 🛑🛑 **EIGHT PREMISES WITHDRAWN IN THREE DAYS — every one an INSTRUMENT error, NOT ONE a wrong number.** Record → **[[velocity-channel-2026-07-27]]** + [[aim-cohort-and-loop-rate-2026-07-27]]; craft → `COMMANDER.md` §2. rotation contamination · pitch coupling · classifier RULE ORDER · gate-seam teleport · **I read the LAUNCHER, not the run** · n=5 direction count · unmatched-range comparison · a norm blamed for a scatter problem. 🛑🛑 **ADJUDICATE FROM `.hydra` + THE STAGE DICT, NEVER LAUNCHER SOURCE.** 📏 Training velocity error = **0.28–0.30 m/s** (not 0.01). 🚩 **Parity gap SURVIVES: corrected KF (training) vs uncorrected (deploy) ≈ 5×** — structural; the fix needs a MAP, deploy is map-free.
- 🚩🚩 **FRAME GOTCHA — misread ⇒ every log analysis inverts:** logged `rel_flu` is TRUE body FLU **UNFLIPPED** (neg lat = gate RIGHT), but `obs[0:3]` vel / `obs[5:8]` rates / `obs[11:14]` rel_pos are **VIRTUAL-FLIPPED** (`diag(-1,-1,1)`, `ego_obs.py:36,:124-127`) ⇒ **true v_left = −obs[1]**. Two conventions in ONE log line.
- 🟢 **CENSUS N=572** (`ed6de9f8`, SSOT [[failure-census-2026-07-27]]): at-gate **AXIS-UNDETERMINED 33.7%** (the real #1) · blackout 11.2% (14.4%→3.9% after the emission fix, p=3.5e-04) · lateral 7.7–22.2% · vertical 4.4–7.2%. 🚩 **A CLASSIFIER'S RULE ORDER IS A CAUSAL CLAIM** · ranking by EXPOSURE always ranks gate 0 first.
- 🛑 **RELEASE-DIVE METRIC HAS NO DEMONSTRATED PREDICTIVE VALIDITY — and it gated v1.8/v1.9/v2.0** (died −1.379 vs passed −1.378; arm corr **+0.338 WRONG SIGN**; z=−0.76) ⇒ **the Q-arm rejection is suspect.** 🚩 v2.0 prices NO pitch direction/angle ⇒ release pitch is an **unpriced weathervane**.
- 🛑🛑 **TWO INVISIBLE OBSTACLES: ~14.5 m BEFORE gate 4 and before gate 5**; gates 0-3 clean; survival RECOVERS at g6 ⇒ a discrete WALL, not policy decay. 🛑 **The gate-5 dodge's at-gate deaths are NOT caused by the dodge** (10–20 m of recovery; the SHORTEST dodge still died at the gate). 🚩 **Position-free obs never knows WHICH gate ⇒ a per-gate dodge is UNLEARNABLE by the policy; the deploy layer is the ONLY place it can live.** 🚩 **Fengyou names gates 0-BASED = log `gate_index`.** → [[failure-census-2026-07-27]]
- 🏁 **v2.0 SHIPPED** (`ego-ckpts-v20-2026-07-26`, v20Vs0): vert env **5.884 vs v19Ws0 4.893 (+20%)**, flat 5.973 vs 5.880 ⇒ **v19 course-FRAGILE, v20 course-ROBUST**. A legitimate lead; its "release-dive regression" is not a safety signal (above).
- 🟢🟢 **POST-IMPACT RECORDER WORKS (7/7, then every cohort) — first impact data in the project**; **`COLLISION id 1001 = GATE / 1002 = ENVIRONMENT`** (`race_outcome.py:11`) ⇒ kill axis is now read **AT THE IMPACT** (`scripts/postimpact/`), and an "ENV only" death = an invisible obstacle, not a slam. Gate-0 deaths at 2.2 s are **gate strikes, not a launch window**.
- 🟢🟢 **RATE 30 CONFIRMED — TRUE SINGLE-VARIABLE (metas differ on `rate_hz` ALONE, 31 min apart, same v19Ws0): pooled n=18 mean 3.78 vs 40 Hz 1.14**, ZERO g0 deaths, first gate-8; Fisher ≥g3 **p=0.041**; fresh-fix 69–91% → **83–99%**. 🚩🚩 **CAUSE, BY CONSTRUCTION: knob `rate` help says "training dt = 30", default 30.0, but `_V1_RECIPE` PINS `rate: 40.0` (`pilot_panel.py:427`) and `rate` IS recipe-managed ⇒ EVERY MODEL-PICK PUTS A 30 Hz POLICY ON A 40 Hz LOOP.** 🛑 **After setting `rate` 30, DO NOT re-pick the model.**
- 🚩 **LEFT-SHIFTED PASS DISTRIBUTION (real, but NOT the kill mechanism):** confirmed passes pooled rate-30 **n=68 mean +0.193, t=+3.13, 48L/20R, p=0.0009**; vertical centred ⇒ LATERAL-ONLY; **survives removing the instrument** (raw unlevelled +0.238). 🛑🛑 **"ALL 5 LATERAL KILLS ARE LEFT (p=0.031)" WITHDRAWN** — the next 9 flights put 3 of 4 on the RIGHT; pooled **6L/3R, p=0.51**. **n=5 direction counts are worthless.** 🛑 **"9° yaw boresight" WITHDRAWN** — closing-in shrinkage is just the loop working. 🛠 **LATERAL TRIM = ZERO CODE:** `ego_aim_offsets` `0:0.2,0;…;8:0.2,0` + **`ego_aim_release` 0.0** (release gates on `rng <= release`). 🛑 **One global release ⇒ lateral trim and the gate-4/5 vertical dodge are MUTUALLY EXCLUSIVE**; `ego_gate_z_bias` has **NO lateral counterpart**.
- 🟢🟢🟢 **ROOT CAUSE FOUND — THE VISION HORIZON IS FIELD-OF-VIEW GEOMETRY (SSOT [[vision-horizon-fov-2026-07-27]]).** **0 of 899 confirmed passes EVER got a fix inside 1.19 m** (last-sighted range p10 1.48 / med **1.78** / p90 2.80). Camera is **HFOV 90° but VFOV only 58.7°**, axis **+20° UP** (`frames.py:243-258`) ⇒ **vertical clips first**. Solving `|el| + atan(0.75/R) = VFOV/2` per pass predicts **1.39/1.76/2.78** vs observed **1.48/1.78/2.80** — **median error +0.04 m**; `|el|+half` = 28.9° vs the 29.4° frame edge; overflows vertically **47%→70%** one tick later; **CONTROL: last-fix |az| 13.1° vs 5.2° all-fresh.** 🛑 Per-pass corr is only +0.113 — the DISTRIBUTION match + overflow + azimuth control carry it, not the correlation. 🚩 **MY FIRST VERSION WAS CIRCULAR** (per-tick binning uses the PROPAGATED range when not seen ⇒ coasts fill the low bins); **the non-circular instrument is per-APPROACH last-sighted range on CONFIRMED PASSES**. ⇒ **The last ~1.8 m (~0.22 s) is flown BLIND BY CONSTRUCTION, so every deploy aim/trim knob acts BEFORE the interval that kills.** ✅✅ **AND THE OBVIOUS ARM IS ALREADY REFUTED — DO NOT RUN IT.** `gate_visibility.py:225` projects **8 keypoints through the SAME `racer.frames` intrinsics** and needs **4 in-frame**: training's horizon is **1.72 m at the flown +24° pitch, 1.78 m at +10°, 1.19 m when 0.3 m high** — **the wire's median 1.78 and minimum-ever 1.19, to centimetres.** The policy ALREADY trains on this blackout, and deploy vision is AT training parity (median observed−predicted +0.04 m). ⇒ terminal scatter is **the policy's real precision limit on a blind commit**, not a mismatch. Levers now: **reward/termination pressure on terminal L-inf SCATTER** · shrink the blind interval in TIME (approach speed) · partial-gate detection (training must move too, or it creates a NEW mismatch) · belief propagation · FOV/mount.
- 🛑🛑 **AT THE LAST SIGHTED FIX, PASSES AND GATE-STRIKES ARE NEARLY THE SAME DISTRIBUTION** (SSOT [[aim-cohort-and-loop-rate-2026-07-27]]): **|lat| AUC 0.582**, |vert| 0.676, **L-inf max(|lat|,|vert|) 0.736** = the real discriminator; **blind metres 0.42, speed 0.53, body rates ~0.5 = NO information**. **40% of CONFIRMED PASSES sit outside 0.45 m laterally and pass anyway**; strikes include |lat| 0.02–0.16. 🟢 The lateral trim **DOES actuate** (range-matched **−0.303 ± 0.067 at 5–8 m**) **but only −0.096 by 1–2 m** ⇒ authority dies with the vision. ⇒ **knobs move a MEAN; what kills is the SCATTER. TRAINING problem, not deploy.** 🛑 **SELF-CORRECTION: "L-inf pricing is the v2.1 defect" is UNDERMINED — `cross_offset = pass_linf` prices EXACTLY the best-discriminating quantity.** The defect is scatter MAGNITUDE, not the norm.
- 🚩 **PILOT (Fengyou 07-27): takeoff assist turned OFF deliberately — the transition OUT of assist SINKS the drone right before a gate.** That is the `ego_assist_thrust` drift from 05:26; a real handoff-transient defect, sits next to TERMINAL DIVE. 🚩 **META PROVENANCE HOLE: `recipe_drift` names knobs whose VALUE the meta never records** (`ego_assist_thrust`) ⇒ a drifted assist silently confounds a cohort. Trivial fix.
- 🚩 **DE-INJECT THE AIM OFFSET BEFORE ANY GEOMETRY:** logged `rel_flu` INCLUDES it ⇒ `rel_true = rel_logged + [0, +aim_lat, −aim_vert]`. 🚩 **RANGE-MATCH OR BE FOOLED** — the n=16 pass-point statistic said the trim moved the drone the WRONG WAY; it was mixing ranges. Tick-level, range-binned, it was unambiguous.
- 🛠 **DEPLOY BRANCH = `aim-offsets-pin-2026-07-27`** — the WHOLE stack (vertical fix-gain + D1 + recorder + aim offsets). Obstacle dodge = `ego_aim_offsets` `4:0,3;5:0,3`, release 12.0, fade 0.0. **`+vertical = UP`** (OPPOSITE sign to `ego_gate_z_bias` = camera +Y = DOWN, different frame). 🛑 **ORDER MATTERS — PICK THE MODEL FIRST, THEN TYPE THE OFFSET** (`_V1_RECIPE` pins it `""`, so a model-pick CLEARS it — deliberate).
- 🧪 **V vs C IS A TRUE SINGLE-VARIABLE TEST** — recorded overrides differ by **exactly 3 lines** (`course_drop_lo/hi=-10/10`, `course_gates_ceiling=16.0`); same warm start/updates/cadence ⇒ isolates vertical course structure alone (the old vert-vs-flat head-to-head could NOT). Eval sbatch at `rl/v20_VvsC.sbatch` (4 policies × 2 envs, `--time=02:00:00` for backfill). **C = job 3323864, v20C_s0_v100, ~17.1k/18k at last check.**
- ✅ Base `deploy-consolidated-2026-07-27` (`7da19a2b`) = D1 (OFF) + recorder (ON) + vertical fix-gain (OFF); superseded by the aim branch. 🛑 **NO TRAINING ARM IS CURRENTLY JUSTIFIED** — v2.1 correctly NOT launched, `ego_faithful` already armed. Next candidate = **L-inf lateral pricing**. 🛑 MIRRORED-COURSE PROBE DEAD — course is FIXED.
- 🏁 **v1.8 / v1.9 SHIPPED** (`ego-ckpts-v19-2026-07-24` = v19Ws0, the SAFE lead); v1.9 = roll-rate penalties on action **ch1** + **`ROLL_EVAL`**. 🛑 **EMA REFUTED** — the swept knob never touched the obs. 🟢 **GATE-1 RESOLVED: v16-SPECIFIC (launch-untrained/OOD)**; vertical target is **pitch-coupled body-frame and CORRECT** (only coarse SECTOR obs[9:11] is gravity-leveled); z_bias = a CONSTANT band-aid.
- 🚩 **ROLL is the UNGUARDED axis:** census gates YAW+PITCH but NEVER ROLL (fix: `ROLL_EVAL`). 🛑 **NO roll ANGLE fence** — backfires on the 60° turns; rate penalty ONLY. 🛑 **TERMINAL DIVE (open defect):** the fence blocks **nose-DOWN only** (`fly_rl.py:766` `max(cmd,0)`) ⇒ `pitchcmd==+0.000` = a BLOCKED dive; inside 6 m ~100% fenced with the gate ABOVE. LOAD-BEARING, masks training.
- 🚀 **CLUSTER OPS:** `gpu-medium` = **2 CONCURRENT GPUs**. 🚩 **18k-update wall: A100 ~4:18 · MIG ~6:00 · V100 ~7:02** ⇒ **`--time` ~6 h for backfill.** 🚩 **`--time` is the ONLY start-time knob**; **Node Weight h11g1=100/h11g2=2/h11g3=1 ⇒ A100 is fastest AND most SHELTERED** (launcher's "A100 queues ~13 h" is STALE; its `--exclude=adroit-h11g3` blocks V100). 🛑 **`/scratch/network/fl3689/peregrine_repo` IS NOT A GIT REPO** ⇒ establish provenance laptop-side before any submit. → [[velocity-channel-2026-07-27]]
- 🛑🛑 **PITCH CLAMP IS BIMODAL, NOT A DIAL:** resting `obs[4]` = **−0.3107 rad = −17.80°** (n=100); fence = `obs[4] <= −clamp_rad` (`fly_rl.py:757`) ⇒ **clamp ≤17.8 = fence ON AT REST · ≥17.9 = OFF**. Every clamp result in project history is a fence-on/off dichotomy.
- 🚩 **det_hold owns the ADVANCE** (0.2 → true-gate lock ×8/8; 0.3 → WRONG far lock ×6/6) · **stale_horizon owns the CLOSE-IN blind meter** (~0.18 s OPEN-LOOP) · **deploy default 0.2/0.6** · **RE-FLY RULE = ‖gyro‖>2.5 for ≥3 CONSECUTIVE ticks** · **A RECORD IS NOT A BASELINE**; map cohorts NULL (p=0.63).
- 🗺 **MAP SURVEY INSTRUMENT = FLIGHT PATH, never the camera.** 🚩 **TWO RANGE CAPS:** `max_valid_range_m` 30 vs **`max_acquire_range_m`=22 (NO panel field)**; `continuity_reject` ~38× bigger.

## 📁 STANDING OPERATIONAL DURABLES → **[[durables-operational]]** (flight/recipe ritual · run-selection · training-diagnosis · settled verdicts · Track A framing + reference constants)
- 🎯 Goal still standing: **BEAT vpeffs0 on the full 20-gate course.** Thread history → [[track-a-beat-vpeffs0-2026-07-13]] · [[replay-ratchet-2026-07-17]] · [[trackA-stall-forensic-2026-07-13]] · [[ego-deploy-contract-2026-07-09]] · [[audit-ego-inc9-2026-07-09]] · [[vq2-rl-commander-2026-07-05]] · [[now-archive-2026-07-05]].

## Operating directives — ALWAYS APPLY
- 🚩 **`COMMANDER.md` (repo root) = boot prompt + self-improvement loop.** Read FIRST on boot; EDIT LAST before handoff. MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL rev 4 (Fengyou): vision commander DEPRECATED — RL commander = SOLE commander, owns the deploy repo/branches DIRECTLY** (read+edit+push; NO relay boxes; Fengyou = pilot + hands on ShadowPC). Laptop → Agents/Workflow; Adroit → adroit-connector `serve`/`x`. → [[index-strategy-meta]] 🚩 **CANARY:** address **Fengyou** by name in EVERY message (missing name = context degradation → rotate).
- **BUDGET rev 5:** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. 🚩 FABLE REVOKED (pending dispute).
- **BANKING rev 5 (thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages, banks IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`. **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices — each carries SESSION + MODEL + EFFORT + MEMORY-DELTA rule + escape hatch + report path + **its own worktree**.
- 🚩 **DELEGATE BUILDS TO AGENTS (Fengyou):** multi-file implementation = spawn agents; commander = judgment/diagnosis/review only. → [[feedback-delegate-builds-to-agents]]
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; SLURM only on Adroit; concurrent sessions use separate worktrees. 🛑 **NO-SPIN = HARD REQUIREMENT, enforce by CONSTRUCTION** (fatal spin aborts + realized-yaw clamp), NEVER incentive shaping; NO energy penalties → [[feedback-no-spin-hard-requirement]]. 🛑 **NO-GT IN ACTOR OBS: actor sees ONLY estimator-faithful state**; GT legal in critic/reward/terminations only → [[feedback-no-gt-actor-obs]].
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · Adroit contact · fresh recording).
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly; the C6/POC hold is a SEQUENCING gate, not risk. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]
- 🚩 **TAILS NOT MEDIANS (Fengyou):** verdicts lead with per-flight worst-events (max/p99, tail counts, single-event kill attribution) — one strong roll kills a flight with a perfect median. → [[feedback-tails-not-medians]]
- 🚩 **GATE CONTACT = INVALID RUN.** · **FLIGHT CURRICULUM: close the loop SLOW first, ramp after** → [[feedback_slow_is_smooth]] · **MOONSHOT: championship-fast, not just finishing** (Swift, Nature 2023) → [[project_moonshot_speed]] · **BLENDER VQ2 = HIGH-FIDELITY ONE-LOOK, not broad DR** → [[project_blender_vq2_data_pipeline]]
- 🛑🛑 **NEVER READ BODY-FRAME GATE GEOMETRY WITHOUT REMOVING THE DRONE'S OWN ATTITUDE/ROTATION** — two withdrawn root causes came from this one error class (gyro de-rotation; pitch coupling calls **66.2% of CONFIRMED PASSES a vertical strike**, levelled → 6.0%). **INSTRUMENT = `scripts/failure_profile/vg.py`** (on `lateral-capture-refute-2026-07-27`). **true roll = −obs[3], true pitch = −obs[4]; R_level = Ry(pitch)@Rx(roll); q[2] += z_bias·cos20°.** 🚩 **Calibrate any strike classifier on CONFIRMED PASSES.** → [[failure-census-2026-07-27]]
- 🛑 **THE GATE SEAM HAS TWO FORMS — `gate_index` guards NEITHER.** (a) **ADVANCE seam** (`rel_flu` jumps to the next gate; alone gave AUC 0.723 vs seam-free 0.565) — kill **by construction**: a fixed RANGE band, both edges before the plane, never a time window. (b) **MIS-LOCK seam** (seeker re-locks a different gate under the old index) — **GATE IT AS A DISTANCE, NOT A SPEED: reject a consecutive-fix lever jump > 1.5 m** (leak/reject 0.14%/3.72% vs 1.45%/9.76% for a 20 m/s speed gate). 🚩 **Exclude the aim-offset RELEASE tick — its 3 m step trips this guard.** 🛑 **A SWEEP CANNOT RESCUE THE WRONG VARIABLE**; **NEVER gate on the VARIABLE UNDER TEST.** 🚩 **BY CONSTRUCTION > RIGHT VARIABLE > SWEPT > HOLDS AT YOUR CUT.**
- 🚩 **REPRODUCING A NUMBER IS NOT VERIFYING IT** — I re-derived the −0.110 slope with the *same confounded instrument* and called it confirmed. **Name the artifact that could produce this exact sign and magnitude, then stratify by that artifact's own size** — an independent mechanism, not a recompute. **Suspect the instrument first.**
- 🚩 **A PROBE THAT RUNS ≠ A PROBE THAT MEASURES.** Hand-built "at rest" obs are **OOD** (tell: every arm returns **~zero collective**) — launch-window questions go through `replay_sweep_v18.py` over real flights, never a synthetic. **Decode channels from the wire path**: ego action = **`[thrust, roll, pitch, yaw]`** ⇒ `act[2]` is pitch; with virtual_flip `rate_frd[1]=+act[2]` ⇒ nose-DOWN = `rate_frd[1]<0`.
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]]
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) → stream `SET_ATTITUDE_TARGET` BODY-RATE (mask 0b10000000, FRD, thrust [0,1]) ≥30 Hz; `fly_rl.py` drives. Use `DroneState.gyro_body`, NOT `angular_rate_body`.
- 🚩 **`[daemon] unauthorized` = TWO daemons, not bad auth.** Windows `SO_REUSEADDR` lets a restarted `adroit.py serve` bind 8765 while the stale one answers with its old token — `netstat -ano | findstr 8765` → **kill the OLDER**.
- 🚩 **CRITIC FOOTGUN: use `algo=appo`** (ppo leaves the privileged critic unconnected = seed collapse; critic input_dim **37**) → [[index-rl-training]]. **Judged runs use `rl/submit_rl.py`**; `fly_rl.py` default ckpt retired → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG = self-consistent alias — DO NOT "fix"** · **ODOMETRY quat is R_y(π)-CONJUGATED** — run `scripts/frame_residual_report.py` after EVERY live session → [[index-control-sim]]. 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG) · **BORESIGHT bake = METRIC TRANSLATION** (vert_offset_m −0.25) → [[index-vision-estimator]].
- 🚩 **`main` CANONICAL · 1091 tests** · `*.pt`/`*.pth` + ckpt JSONs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`gh release download`), NEVER git-add → [[project-fullstack-burn]]. 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.**
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]]
- 🚩 **WORKTREE HYGIENE:** auto-worktree per session = isolation; **ONE WORKTREE PER CONCURRENT AGENT** (sharing moves HEAD under a live agent and can wipe uncommitted edits); commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` RECYCLES — never manually prune; only `git worktree prune`. → [[index-strategy-meta]]

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator verdict + gate-relative, VISION-PKG2, detector pipeline, case-C, robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition rules, hardware, prior-art, tooling, feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🚀 **[[deploy-emission-2026-07-22]]** — v1.8 adjudication, vision-fix deltas, flicker, patch 3, terminal dive.
- 🧠 [[vq2-rl-commander-2026-07-05]] · 🗄️ [[now-archive-2026-07-05]] · 🛰️ [[velocity-channel-2026-07-27]] · 💥 [[failure-census-2026-07-27]] · 🔧 [[durables-operational]]
