**UPDATE (2026-06-02 — FIRST CONTACT SUCCEEDED on ShadowPC; branches UNIFIED; read first):** Official sim is in hand: `AI-GP Simulator v1.0.3364.zip` = `FlightSim.exe` + an official `PyAIPilotExample` reference client (extracted on the laptop at `C:\Users\Fengy\Downloads\AIGP_sim\`). **Sim host is now ShadowPC** (full Windows+GPU cloud PC the user drives directly; **TensorDock retired**; the laptop can't run the sim — Intel iGPU only). User is installing Claude Code ON ShadowPC to run first contact there (co-located client; fresh session = empty memory unless synced). **Cross-checked our client ↔ the sample ↔ spec and ALIGNED it — full wire interface + the 5 must-verify items in [[reference-sim-interface]].** **Strategy-relevant findings (VERIFY LIVE):** the sim's MAVLink sends `LOCAL_POSITION_NED` + `ODOMETRY` ⇒ **position+velocity may be GIVEN** (contradicts 'vision-only' — keep vision→KF in-loop regardless, walking-skeleton); it PROVIDES a `TRACK_INFO` **gate map** + `RACE_STATUS` (active_gate_index/timing) + `COLLISION` feedback + `MAV_CMD 31000` reset (all via repurposed `ENCAPSULATED_DATA`); control offers pos/vel + CTBR + **direct-motor** (`SET_ACTUATOR_CONTROL_TARGET`, the sample's default); video header `<IHHIIQ` matched us byte-for-byte. **Code:** `mavlink_client` now parses all of it (+`DroneState.reset_counter`, `send_sim_reset`, `send_actuator_control`, `parse_race_status`/`parse_track_info`); `jpeg_receiver` hardened (non-contiguous chunk_id guard); **`MessageRateTracker`** added (per-type Hz + heartbeat<2Hz/camera<25fps flags, wired into `session_lifecycle`+`record_session` — answers 'capturing at the right rate?'); **211 tests green.** **Git: `red-team-tier-a` = 5501d43 pushed; `main` brought up to date = cfe7d82** (default GitHub view now reflects reality; was stale at the PR-#1 merge). Detector eval/weighted-PnP thread COMMITTED `fc29287` — v2 chosen as deployment detector, v3 `--hard` a negative result ([[project-detector-training-pipeline]]). **FIRST CONTACT DONE 2026-06-02 — 5 must-verify items RESOLVED: position+vel GIVEN (baro=NaN→z from position); ATTITUDE pitch SIGN-INVERTED → orientation from ODOMETRY quat (this WAS the 35.6° "bias", NOT an ESKF trigger); gate map = 2.72 m OUTER / ~1.5 m inner, broadcast at level-load to already-connected clients; fly on CTBR (acro position cmd → 57 m runaway); arm = plain p1=1. Full detail [[reference-sim-interface]]; ShadowPC notes in `handoff/shadowpc-firstcontact-2026-06-02/`. Branches UNIFIED (main = red-team-tier-a). NEXT: apply handoff TODOs (jpeg frame_id dedup · re-test pos/vel in ANGLE mode · system-ID on a running race), wire navigator, bank a VQ1 completion run.**

**UPDATE (2026-05-31, late — DETECTOR TRAINED on Adroit + WEIGHTED-PnP; read first):** **The detector is trained and the autonomous-Adroit pipeline WORKS.** Detail: [[project-detector-training-pipeline]]. **(1) Detector v1 DONE** — yolo11s-pose, mixed L1/L2/L3 curriculum (`gen_synthetic_dataset.py --mix`, weights [1,2,2,3,3]; de-stacked albumentations `OneOf` = one primary effect/img, user's instinct was right), 100 epochs on an Adroit MIG A100 slice (`gpu:3g.20gb`) → **box mAP50 0.995 / pose mAP50 0.995 / pose mAP50-95 0.991**; weights `models/gate_yolo11s_curriculum.pt`. Local demo through the REAL SENSE seam (GateDetector→GateObservation→estimate_gate_pose) = 100% detect L1/L2/L3 incl. chaos, corners 3–8 px, 3-corner P3P fallback fires on clips. **(2) Tail diagnosed with DATA** (`scripts/eval_detector.py` distribution + `scripts/diagnose_tail.py` per-corner): **median corner err ~2 px (excellent)**; the TAIL = "one corner off ~50–70 px, others ~5 px" from (a) corners near a CLIPPED frame edge + (b) clutter/occluder rectangles grabbing a corner; PLUS a rare (~1%) corner-IDENTITY swap confined to HIGH ROLL (mean 38–40° vs ~20°). **My swap hypothesis was largely WRONG — checking first saved a wasted "fix". LESSON: diagnose the tail with distributions + per-corner patterns before prescribing.** **(3) Multi-gate generator BUILT** (`synthetic.render_scene`: 1–3 gates/frame, painter's-order near→far occlusion via `_in_ring` point-in-poly, one label row per visible gate; doubles as clutter-hardening — real-gate vs gate-coloured-rectangle). **(4) v2 RETRAIN** = multi-gate + 2× volume (6000/800), `curriculum_11s_v2`; pose mAP50-95 plateaued ~0.986 (NOT comparable to v1's 0.991 — v2 val set is harder/multi-gate; the FIXED-SET eval on v1 AND v2 is the real apples-to-apples). **DON'T analyze v2/v3 yet — user wants to do it together tomorrow.** **(5) v3 = TARGETED oversampling** (`gen ... --hard` → `sample_gate_pose(high_roll_prob/edge_prob=0.3)` = the swap-prone + near-edge tail configs), `curriculum_11s_v3`, submitted right after v2 to run overnight on the freed slice. **(6) OFF-FRAME-keypoint idea REJECTED with evidence** (user's good instinct, framework-blocked): ultralytics `Instances.clip()` (called in RandomPerspective/Mosaic/LetterBox — every training image) ZEROS visibility + clamps any keypoint outside the frame ⇒ can't train YOLO to predict off-frame corners. **Off-frame reasoning belongs in the geometric SOLVER, not the detector.** **(7) WEIGHTED PnP BUILT** (`gate_pose._refine_pose`, default-on): robust confidence-weighted Gauss-Newton over all corners — each weighted by YOLO keypoint-confidence (→1/σ²) × an ANNEALED Tukey biweight (redescending → a confident-but-WRONG corner gets ZERO weight = fully rejected; Huber only down-weights, failed the gross-outlier test). Replaces the hard-drop→P3P cliff with a smooth robust solve; yields an ANALYTIC pose covariance (Fisher info) for the KF (confidence-aware; replaces the Monte-Carlo path); `_refine_ok` guard falls back to IPPE on divergence. `GateObservation.corner_confidence` already existed + the detector already populated it. **195 tests green** (+6 weighted-PnP, +5 multi-gate/oversampling synthetic). **(8) DEFERRED → now ON the first-contact list: relax the detector hard `kpt_conf_thresh=0.5`** so marginal corners (0.2–0.5) flow through to the now-robust weighted solver instead of hard-dropping to 3-corner — SAFE now (Tukey rejects bad ones) but the right cutoff needs the REAL confidence distribution. **adroit-connector improvements (uncommitted, `C:\Users\Fengy\Downloads\Projects\Adroit\adroit-connector`):** `run_remote` 4 KB→256 KB recv (big pulls crawled + wedged the single-threaded daemon for minutes), `pull_file.py` (sha256-verified CHUNKED base64 pull — a single ~100 MB framed response blows the 300 s socket timeout), `watch_job.py` (SLURM poller → notify-on-finish), getpass passphrase (no plaintext, asked at runtime), `serve`/`x` token-framed localhost daemon = one Duo covers many commands. **GOTCHA: run connector CLIs via PowerShell, NOT Git Bash — MSYS mangles a bare `/scratch/...` arg into `C:\Program Files\Git\scratch\...` (broke pull_file twice before I caught it).** **SYSTEM RESPECT (user directive): ALL work in `/home/fl3689` + `/scratch/network/fl3689`, compute via SLURM only, NEVER touch other users' files; be a good citizen.** All Anduril changes uncommitted (multi-gate + oversampling generator, weighted PnP). **DONE 2026-06-02: v2 WINS the fixed-set eval; v3 `--hard` REGRESSED (the ~0.7% high-roll swap = geometric aliasing → solver/prior, not data). Shipped v2, committed `fc29287`. NEXT: `racer/navigator.py` integration.** **First-contact battle plan + the exhaustive "what to extract from the sim" list: `docs/first_contact.md` (rewritten tonight).**

**UPDATE (2026-05-31) — read first:** Evaluated a 3rd-party tool/library recommendation list (~22 items: VIO, detectors, planners, RL/sim, edge-compile) — full triage in [[project-tooling-recommendations-eval]]. **Lens: the list assumes real-hardware FPV (Jetson+STM32/motor-RPM/fisheye/sim-to-REAL); our virtual-quals reality = MAVLink-setpoint sim (CTBR floor, <100 Hz), attitude GIVEN, pinhole cam, cloud GPU, no HW until Sept ⇒ most of it is one phase ahead + one level below us.** **SPIKED: TOPP-RA speed-profiling** — `src/racer/speed_profile.py` (`time_optimal_profile`→`Trajectory.setpoint_at`; pure-numpy fwd/bwd time-optimal w/ curvature-coupled accel budget; 8 tests, **184 green**). The `toppra` lib is Cython/C++ and FAILS to build on Windows+py3.13 (no Windows SDK — acados-class pain) ⇒ added as a Linux-only `[planning]` extra; the numpy impl is the Windows default + the RACE speed rung (replaces naive α time-scaling, seeds the RL reference line). **KEY INSIGHT (user's catch) — perception-noise-model is a HARD RL requirement:** a state→action policy trained on PERFECT sim state CRASHES on real YOLO-pose→PnP→KF at speed; must inject a MEASURED perception noise/latency/dropout model (from first-contact recordings) into the policy's observations + asymmetric actor-critic (privileged critic) ⇒ perception fidelity = the RL SPEED CEILING. Raw-pixels vision-in-loop REJECTED (appearance gap + kills parallel non-rendered dynamics-sim training; SWIFT avoided it). **QUEUED post-VQ1/VQ2 (user: 'everything else after vq2'):** actuator/dynamics randomization (motor τ±30%/thrust±20%/latency → RL sim-to-sim spec) · adaptive state-based cropping (deferred VQ2 long-range perception lever; banned from the VQ1 floor — couples detect→estimator) · TVM/TAO (Sept physical-round edge-deploy). **PARKED AS FALLBACK:** VIO family (VINS-Fusion/OpenVINS/LARVIO/VID-Fusion) — retain IF first-contact shows attitude missing/biased/low-quality. **INSPIRATION-only (interface mismatch):** G&CNet (motor-RPM, can't command; arch = our RL policy) · TinyMPC (no FC to flash) · Gate-fused-EKF (→ offline mapper) · LMPC (→ iterate-the-line loop). **Offline between-runs processing is LEGAL (our determinism strategy, NOT a cheat) + the latency-free lane for heavy tools; confirm submission/attempt mechanics at sim drop.** Branch `red-team-tier-a` unchanged; `speed_profile.py` uncommitted.

**UPDATE (2026-05-29, eve of sim drop) — read first:** Red-team **pass #3** triaged against the real code → **7 Tier-A fixes APPLIED on `red-team-tier-a` (169 tests green, +10; NOT yet committed):** **A2** controller tilt-clamp vertical over-thrust (sharpest — a hard turn/brake kept full thrust magnitude at the clamped 45° → rocketed skyward; now altitude-priority) · **1.1** mission gate-plane-crossing advance **+ 1.2** planner velocity-disambiguation (a *compounding* crash mode: a fast fly-through skips the 1.0 m sphere → gate_index sticks → planner U-turns back through the gate) · **B3** takeoff relative-to-start-altitude (baro-MSL-bias safety) · **A5** localization attitude lever-arm covariance (~0.35 m @20 m/1° — KF was over-trusting distant fixes vs the baro) · **B2** KF dt upper-clamp (sim-reset; the negative-dt "detonation" was ALREADY guarded) · **4.1** sysid quadratic thrust-curve fitter (first-contact tool). **5 claims REJECTED with reasoning (don't re-litigate):** **A6** additive-eps "fix" would UNDO the correct pass-#2 noise-floor fix (sub-px reproj diffs are noise) · **B5** sideslip-yaw (camera is body-fixed; nose-at-gate CENTERS it; the fix is backwards) · **A4** IPPE "death spiral" (candidate separation ≫ 2–3° ego-rotation) · **A3** pure-pursuit "diagonal" (carrot-beyond-gate already IS the centerline LOS) · **1.3** IPPE frontal tiebreak (risky + the position fix is insensitive to the near-frontal tilt ambiguity). **DEFERRED** (unbuilt navigator wiring / first-contact, not the night before the drop): 3.2 ESKF bias (trigger+instrumented) · 2.1/B4/A1 IMU-rate predict + async vision (already the documented architecture; seam = `on_message`) · B1 pitch-vs-FOV (racing-line constraint) · 2.2 out-of-order frames (→ delayed-vision ring buffer). Pass-3 edits uncommitted pending user OK. Full triage + per-claim reasoning: [[project-red-team-pass-3]].

**UPDATE (2026-05-30):** Pre-sim-drop red-team pass #2 triaged against the real code → **Tier A correctness fixes APPLIED on branch `red-team-tier-a` (154 tests green, +9):** P3P behind-camera→`None` (was feeding impossible poses to the KF at transit — sharpest catch) · IPPE ambiguity-ratio additive-eps (the `else inf` sentinel bypassed the prior on frontal/low-noise gates) · `_reproj_rms` Z-guard · controller 2 geometric-law singularity guards (antipode clamp-bypass + `b3∥x_c` NaN) · KF `inv(S)`→`solve` (Joseph form already present ⇒ hygiene not catastrophe) · jpeg `select()` over the Windows-15 ms `sleep` busy-poll. **Tier B folded:** 3-corner soft-coast (`localization.P3P_FIX_COV_INFLATION`). **DEFERRED + noted (build on first-contact evidence):** ESKF bias-state · delayed-vision ring buffer (VQ2-speed) · hard-coast + innovation-gating (navigator/mapper). Several report claims were overstated/INVALID (replace()-race, "KF blows up", MTU, time_boot_ms) — logged so we don't re-litigate. Branch NOT yet merged. Same session: **first-contact instrumentation GAPS filled** (to settle the deferred decisions with DATA tomorrow) — `scripts/clock_probe.py` (video↔IMU clock-align + MTU/packet-loss health) · attitude-bias check in `session_lifecycle` (`firstcontact.sample_attitude_bias`) · `JpegUdpReceiver.metrics` · `docs/first_contact.md` runbook (ordered sweep + observation→action table); 159 tests. Full triage + instrumentation map: [[project-red-team-pass-2]].

**STRATEGIC DECISION (2026-05-29) — RL is now the COMMITTED VQ2 path (was "parked stretch"):** the moment VQ1 is banked, switch HARD to RL — even before VQ2 opens (use the gap as training runway). Belief: higher ceiling + user's RL strength + ~100 TOPS makes the policy free to deploy + Adroit gives the compute. **Architecture = SWIFT's:** KEEP the vision→KF perception; a learned **state→action** policy replaces planner+controller behind the `Setpoint` seam (NOT end-to-end pixels). **Train on a massively-parallel twin** (Isaac Lab on Adroit / any drone sim — user is open) whose DYNAMICS are matched to the system-ID'd competition plant → Elodin spec-camera oracle → fine-tune on the official sim (close the sim-to-sim gap with system-ID + domain randomization + a residual step). **The deterministic pre-VQ1 work (system-ID + recorder/replay) IS the twin's dynamics spec — capture everything.** Model-based stays the banked VQ1 floor + RL baseline + safety net. Sequencing FIRM: deterministic until VQ1 passes, then immediate switch. C1 un-parked — details in [[project-master-plan]] C1.

**SIM HOST PROVISIONED (2026-05-29):** TensorDock **RTX 3090 (24 GB)** / 12 vCPU / 32 GiB / Win10 Pro N / public-IP RDP:3390 — instant, no GPU-quota wait. Full stack validated on it (Python 3.13.13 + branch `red-team-tier-a` + `[dev]` → **159 tests green**); 24 GB ⇒ sim+detector GPU-contention moot. **N-edition gotcha:** `import cv2` needs the **Media Feature Pack** (elevated `Add-WindowsCapability` + reboot) + VC++ redist. `red-team-tier-a` pushed to origin (NOT merged to main) so the box could pull it. Co-locate sim+client here tomorrow (localhost). Details: [[project-hardware-constraint]].

**STATUS (2026-05-28) — read first:** BUILD phase underway (plan validated via a red-team+research pass — see the **"Review validation (2026-05-28)" layer atop [[project-master-plan]]**, the single source of truth; it overrides older conflicting sections). **The full SENSE→ESTIMATE→THINK→ACT chain is BUILT + tested offline — 125 tests pass on Python 3.13.2:** contracts (7 frozen data contracts incl. Gate map; GateObservation now allows 3-or-4 corners + corner_ids) · frames · mavlink_client (send_command all 4 modes; **sim_time_ns driven by HIGHRES_IMU ONLY — clock isolation**) · jpeg_receiver (eviction-leak fixed; idle-timeout) · gate_pose (IPPE + **3-corner P3P fallback**; **ambiguity-gated prior**; corner-order LOCKED) · localization + Gate · LinearKF state-estimator (+ **attitude-error Q inflation**) · elodin_adapter · atmosphere · synthetic YOLO-pose dataset-gen (label order verified PnP-consistent). **The sense→estimate loop closes end-to-end with NO sim** (project→PnP→localize→KF), and blind-segment drift is de-risked (1 s vision blackout <0.3 m, recovers). **THINK→ACT skeleton BUILT (2026-05-29):** `contracts.Setpoint` (planner→controller seam) · `controller.py` (POSITION/VELOCITY floor that leans on the sim stabilizer + geometric ATTITUDE path; throttle scale = placeholder pending `innerloop_step` system-ID; CTBR body-rate = next) · `planner.py` (ReactivePlanner LOS pure-pursuit through gates; min-snap line = next, same Setpoint seam) · `mission.py` (Mission state machine IDLE→TAKEOFF→RUN→FINISHED/ABORT + `run()` driver; arm/land handshake deferred to first contact). A closed-loop point-mass test flies a 3-gate course to FINISHED. **Detector inference adapter BUILT (2026-05-29):** `vision/detector.py` — YOLO-pose output → GateObservation in the LOCKED canonical corner order; low-confidence keypoints → a 3-corner obs + corner_ids → P3P; `ultralytics` lazy-imported (core stays installable without the `[detector]` extra); pure core tested end-to-end through PnP. **SENSE seam now complete in code** (frame→detect→PnP→localize→KF). Remaining for a REAL closed loop: trained YOLO-pose weights (Adroit, now unblocked), wiring `Mission.run`'s navigator, and a sim (Elodin). **HARDENED per 2026-05-29 external code-review** (6 anomalies fixed: clock jitter · IPPE prior blind-trust · 4-pt-PnP-too-strict at transit · attitude-Q · frame-eviction leak · smoke-timeout hang; ultralytics now a `[detector]` pyproject extra; see [[feedback-walking-skeleton-no-vq1-crutches]]). Git: **`main` = a011336, pushed to origin (github.com/Hat000/Peregrine), 125 tests green** — review-hardening (f7ab593) + THINK→ACT skeleton (ae5d147) + detector adapter (a011336) all fast-forward-merged into main 2026-05-29 (linear history, 9 commits). The `review-hardening` + `think-act-skeleton` branches are now merged/redundant (local + origin; can be deleted). **ENV: `.venv` on Python 3.13.2** (stable PyTorch CUDA; 3.14 nightly-only until torch 2.10; `requires-python >=3.13,<3.14`). **🚨 OFFICIAL SIM DROPS 2026-05-30 (user-confirmed 2026-05-29) — pivots near-term priority to FIRST-CONTACT READINESS** (see NEXT). Sim host = Azure NVadsA10 v5 Windows GPU VM (confirmed); **co-locate the Python client ON the same VM** (localhost MAVLink/UDP — a remote/internet client blows the <50 ms latency budget); **FILE THE NV-series GPU QUOTA REQUEST NOW** (new Azure subs ship with 0 GPU quota; approval takes hours–1 business day, so it gates VM creation). **Elodin Apache-2.0 surrogate** now DEMOTED from dev-rig to (a) offline estimator eval-oracle + (b) future RL training surrogate only (Linux/WSL; no Windows wheel; adapter BUILT; patch camera `far=0.65` if used). NOT yet built: detector TRAINING/weights (synth data + `vision/detector.py` inference adapter DONE; train YOLO-pose on Adroit), mapper/orderer, logger/replay, first-contact toolkit (planner/controller/mission-loop now have FLOOR versions; min-snap line + CTBR body-rate = upgrades on the same seams). 20 academic papers triaged (8 signal).

**Current strategy:** Track A (model-based) = VQ1 floor + VQ2 baseline; leans on the sim's built-in stabilizer ⇒ little hand-rolled control. MVP backbone = min-snap line + conservative completion + simple tracker (a slow VALID finish beats a fast invalid one). **Estimator = LINEAR pos/vel KF first** (attitude GIVEN; baro=z, mag=yaw; only x,y is vision-only); ESKF = fallback. Detector = **YOLO-pose corners** (pre-train on SYNTHETIC/Blender data NOW; full-frame, ROI deferred) → IPPE/map-based PnP. Control = SET_ATTITUDE_TARGET (attitude→CTBR); system-ID the Betaflight-style stabilizer+airframe. **Speed-UPGRADE (recommended, user-fit): RL-on-surrogate**; MPCC = classical alternative. Auto-label flywheel feeds the detector. **Effort priority: planning (racing line + speed) = the VQ2 differentiator (where rank is won); vision = bounded THRESHOLD but **PRESERVE the domain-randomization advantage: overfit GEOMETRY (track layout/poses/line — deterministic per track), NOT APPEARANCE. VQ1 is clean/desaturated/aids-on; VQ2 is photoreal/cluttered/aids-off ⇒ fine-tuning the detector on clean VQ1 frames would COLLAPSE DR right before the ranked round. TRAIN IN CHAOS (albumentations: JPEG artifacts/motion-blur/occlusion/glare/channel-dropout); the flywheel donates hard real GEOMETRY with photometrics randomized away; validate on chaos not clean. ByteTrack stabilizes detections behind the GateObservation seam (gap-fill/stable-IDs/false-pos-reject, ~0 TOPS)**; estimation = a speed cap w/ safety valve; control mostly given by the stabilizer. Adroit: heavy→RL policy, bounded→detector. Chain mental model: must-not-BREAK (validity) vs sets-your-RANK (time).**

**Spec-verified / research-resolved (don't re-assume):** no position in telemetry & velocity-unconfirmed ⇒ derive both, **vision = primary position source** (but baro→z, mag→yaw likely free; check LOCAL_POSITION_NED for position!); **VFoV=90° is mislabeled — it's HFoV; VFoV≈58.7°** (camera looks UP); obstacles exist but DON'T map monocularly (fly the gate corridor, tighten only where proven safe); determinism PER-TRACK; track not pre-known but FIXED/shared ⇒ map-once-then-race; VQ2 timed; **attempts likely ~UNLIMITED, best valid time counts (verify)**; **onboard edge compute ~100 TOPS (FAQ) = a NN-inference budget we currently under-use ⇒ size up perception, add learned depth, deploy a learned policy (see master-plan "Edge-compute budget" layer)**.

**NEXT:** P0 — [DONE: env on 3.13; data contracts; mavlink_client/jpeg_receiver refactored; gate_pose (IPPE); elodin_adapter + atmosphere; LinearKF state-estimator (validated; blind-segment drift de-risked); localization glue + `Gate` map contract; synthetic YOLO-pose dataset-gen (`vision/synthetic.py`, procedural, PnP-consistent labels); THINK→ACT skeleton (`contracts.Setpoint` + `controller.py` + reactive `planner.py` + `mission.py` state-machine); detector inference adapter (`vision/detector.py`); — 125 tests pass; corner-order LOCKED; ultralytics=[detector] extra; Elodin runbook docs/elodin.md; 2026-05-29 review-hardening + THINK→ACT skeleton done] **🔥 CRITICAL PATH for the 2026-05-30 sim drop (build NOW, sim-independent): (1) first-contact toolkit — ✅ `msg_audit` (baked into record_session.py), ✅ **`session_lifecycle`** (`scripts/session_lifecycle.py`: arm/disarm handshake via COMMAND_LONG + COMMAND_ACK/STATUSTEXT/heartbeat-metadata capture + clock/TIMESYNC + R1 audit; SAFE — no setpoints), ✅ **`control_mode_probe`** (`scripts/control_mode_probe.py`: arms then streams tiny bounded setpoints per mode [velocity/position/attitude/body_rate], reads baro-climb/attitude/gyro to find which interface is LIVE + whether position easy-mode works; force-disarms on exit). MavlinkClient gained `arm()/disarm()/send_command_long()/wait_command_ack()/wait_armed()` + COMMAND_ACK/STATUSTEXT/autopilot-type-custom_mode capture; `racer/firstcontact.py` shared helpers (all spec-standard MAVLink, **Elodin-AGNOSTIC by directive** — Elodin is the open-source surrogate, NOT the sim source of truth; reused by Mission). ✅ **`projection_check`** (`scripts/projection_check.py`: OFFLINE validator — confirms VFoV≈58.72° [the spec's 90° is the HFoV], elevation band [-9.4,+49.4]°, + 5 frames.py sign/convention checks ALL PASS [tilt-up sign trap verified: horizon at v≈296]; `--frame`/`--from-recording` overlays the computed horizon on a real frame; added `frames.horizontal_fov_deg/vertical_fov_deg/camera_elevation_band_deg`), ✅ **`innerloop_step`** (`scripts/innerloop_step.py`: arms + thrust-sweep → hover_thrust + thrust→accel slope + TWR [calibrates the `Controller.hover_thrust=0.5` PLACEHOLDER], attitude-step → delay/τ/overshoot; pure fitters in `racer/sysid.py`; writes JSON; SAFE force-disarm). **First-contact toolkit COMPLETE** (msg_audit · session_lifecycle · control_mode_probe · projection_check · innerloop_step; `racer/firstcontact.py` shared helpers incl. `stream_setpoint`). 145 tests pass; (2) ✅ **DONE (2026-05-29)** logger/recorder = `racer/recording.py` (`Recorder` writes off the hot loop via a queue+writer-thread; `RecordingReader` replays) + `scripts/record_session.py` (records BOTH streams, self-heals on stream gaps, prints a built-in msg_audit: MAVLink type-histogram + which hedge fields appeared → resolves R1). On-disk: `mavlink.tlog` (standard pymavlink tlog, MAVExplorer/QGC-readable; sim clock rides inside frames) + `video.bin`+`video_index.jsonl` (bit-exact JPEG, seekable) + `meta.json` (monotonic↔unix clock bridge + stats). Added `Frame.jpeg_bytes` (raw bytes; receiver populates) + a `MavlinkClient.on_message` raw tap. 6 round-trip tests. RUN IT FOR EVERY CONTACT; (3) wire `Mission.run`'s navigator to a real MAVLink transport for a first gate-pass. Be ready for the Betaflight-UDP-vs-MAVLink wrinkle (spec=MAVLink is authoritative; elodin_adapter = RC-style reference if it's actually Betaflight). PARALLEL (Adroit, no sim needed): generate synthetic dataset (`scripts/gen_synthetic_dataset.py`) + train a YOLO-pose baseline.** Then: **detector: USER CHOSE YOLO-pose path** (synth data DONE) → ultralytics YOLO-pose training (Adroit/Azure) + inference→GateObservation adapter (emit canonical order; 3-corner+corner_ids when clipped); mapper/orderer; min-snap racing line + CTBR body-rate (reactive+attitude FLOOR done, same seams); stand up Elodin on Linux/WSL + solver glue (adapter BUILT; Elodin=callback not MAVLink; patch camera far=0.65) + wire it behind `Mission.run`'s navigator; logger/replay; first-contact toolkit incl. `control_mode_probe` (position-easy-mode test), `projection_check`, `session_lifecycle`, `innerloop_step`, + baro/mag/clock audit. **Bank a VQ1 completion run before its deadline.** User action items: confirm attempts+submission-process+VQ1-deadline w/ organizers (info@theaigrandprix.com) + Azure path. **Adroit access CONFIRMED 2026-05-29 ⇒ detector-training track unblocked (free GPU; pre-train YOLO-pose on the synthetic data NOW).** Directives: Azure cost NOT a constraint (size up to full A10); speed > keeping gate in view; vision must be excellent.

- [Master plan (LIVING)](project_master_plan.md) — architecture + execution plan (current-state + decision-log): Track-A strategy, components, risks R1–R11, build sequence, codebase state. Read first when resuming.
- [AI Grand Prix project context](project_ai_grand_prix.md) — Anduril autonomous drone racing competition, May–Nov 2026, user is competing for fun.
- [Hardware constraint](project_hardware_constraint.md) — three-machine plan: laptop (dev) + Azure NV-series (sim) + Adroit Princeton (detector training).
- [Competition source materials](reference_competition_materials.md) — spec PDF facts, verified rules/FAQ, open unknowns, MAVLink links.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries (license/reuse verdicts) + the 8-paper academic ledger.
- [Walking-skeleton directive (feedback)](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must BE the full VQ2 stack under-tuned (vision+KF in-loop), never a crutch; from the 2026-05-29 review.
- [Red-team pass #2 (2026-05-30)](project_red_team_pass_2.md) — triage of the pre-sim-drop report: Tier A fixes applied (branch `red-team-tier-a`, 154 tests), Tier B soft-coast folded, ESKF/ring-buffer deferred, overstated/invalid claims logged so they aren't re-litigated.
- [Red-team pass #3 (2026-05-29)](project_red_team_pass_3.md) — 3rd report (eve of sim drop): 7 fixes applied (169 tests) incl. the A2 tilt-clamp skyward-launch bug + the 1.1/1.2 fast-fly-through→U-turn crash mode; 5 claims rejected (notably A6, which would have undone pass #2); 3.2/2.1/B4/A1/B1/2.2 deferred to navigator wiring / first-contact.
- [Tooling recommendations eval (2026-05-31)](project_tooling_recommendations_eval.md) — triage of a 3rd-party tool list: TOPP-RA SPIKED (`speed_profile.py`); perception-noise-model = a HARD RL requirement (else policy crashes on real perception); actuator-randomization/adaptive-cropping/TVM-TAO queued post-VQ2; VIO family parked as fallback; G&CNet/TinyMPC inspiration-only (interface mismatch); offline-between-runs is legal, not a cheat. ROUND 2 (2026-05-31): ADRC + GTSAM (offline mapper, cov→R) + min-snap (port rpg) + Isaac-native RL lib (rsl_rl/rl_games/SKRL) = keepers; PVNet + SymForce park contingent; Crocoddyl = classical-alt-not-taken (ILC survives cheaply).
- [Estimator robustness — drift vs noise (2026-05-31)](project_estimator_robustness.md) — distinguishing TRUE drift from gate-PnP noise/bias in the KF: 3 layers (adaptive R [built; extend to blur] · Mahalanobis innovation-gating [the right form of the user's dot-product idea; deferred] · map-averaging + relative-at-transit for bias [mapper, not built]); a low-pass is the KF, don't double-filter; bias ≠ zero-mean noise; set thresholds from measured noise. The design home for the innovation-gate + blur-adaptive-R when wiring the navigator. + NO-GATE/"headless-fly" robustness (2026-05-31, user catch): a SEARCH/acquisition state for "no gate visible" (esp. lap start — doctrine exists in Failure-handling, `mission.py` code doesn't) + a light optical-flow→velocity KF assist (the right-sized re-open of "no VIO" for LONG blind gaps, not the transit blink); data-gate both via measured inter-gate visibility gaps.
- [Sim interface — confirmed from PyAIPilotExample (2026-06-01)](reference_sim_interface.md) — the REAL MAVLink+video wire interface vs the thinner spec: repurposed ENCAPSULATED_DATA (gate map / race status / collision) + ODOMETRY + the full control surface (incl. direct-motor) + MAV_CMD 31000 reset; the 5 first-contact must-verify items (is position real, TRACK_INFO inner-vs-outer, rates, actuator scaling, arming).
