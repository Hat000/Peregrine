**Peregrine** — our entry in the Anduril **AI Grand Prix** autonomous drone-racing competition (May–Nov 2026; user competing for fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. This index is the orientation layer; detail lives in the topic files below.

## READ FIRST — current state (2026-06-11)
**🏁 VQ1 PASSED** (dashboard-confirmed). The **model-based CTBR stack threads the full 6-gate course** — valid sim-recognized finish ~35.3 s, in-plane miss `[0.37, 0.05, 0.06, 0.03, 0.10, ~0.71]` m (all < 0.75 half-opening). Built by the **offline-twin discipline** (tune offline against a faithful twin, fly only to VERIFY — this is what got us unstuck; keep it). Now pivoting to **Phase 2 = RL + VQ2 vision** (see NEXT).

**✅ 2026-06-09 — SIM BUILD 1.0.3364 REGRESSION FIXED:** 4× deterministic 6/6 @ 34–35 s, race_outcome-certified. Root cause one-tick attitude step at takeoff→RUN → rate-clamp tumble; fix = `launch_ramp_s=0.6 s` sim-clock-keyed authority ramp + `--rate 100` pinned (commits 260972e+883b725+57f287f; detail [[project-ctbr-control-sysid]]). **VQ2 open items (NOT VQ1):** (A) "yaw-spin at ±180° tilt" — **✅ DISSOLVED (characterize-sweep 510da24): NO open-loop sim anomaly** (= unmodeled super-rate gain + Euler yaw-flip artifact + a gate-post COLLISION; the "spin" was contact dynamics — [[project-phase2-rl-vision-decisions]] §CHARACTERIZE-SWEEP); (B) decel into gates; (C) ~20 km/h cap — both RL/racing-line levers.

**🚩 2026-06-10 — UNATTENDED SIM CONTROL ACHIEVED (durable capability, the user's standing ask):** the fable S1.2 session **cold-launched FlightSim.exe and raced with NO human interaction.** Mechanics: **MAV_CMD 31000** (`client.send_sim_reset()`) restarts the race once a race context exists (fresh ~3 s countdown) — **NO-OP from HOME** (no telemetry there); from HOME, **Win32 `SetForegroundWindow`** on the `AI-GP` window (`AppActivate` alone returns False) + **Enter ×2** (home → waiting room → race+countdown). `rl/fly_rl.py --flights N` chains attempts (auto-reset, never into a ticking countdown). **Removes the manual-reset bottleneck for ALL future sim work.** **🚩 THIRD idle state** (~90 min idle post-race): 31000 NO-OP while telemetry still streams (stale `started=True`, frozen sim_time); recovery = same Win32 focus + Enter ×2 — wired as automatic escalation in `scripts/rate_sysid.py` (which also has `--vel-damp`/`--pos-pull`, `--mode anomaly`, `--mode profile` JSON phase schedules). **🚩 ZOMBIE DUAL-INSTANCE mode:** two sim instances both streaming to 14550 → send-peer flaps → drone ARMS but IGNORES commands; killing the zombie kills BOTH → relaunch via the login chain (twin-falsify WRITEUP §0). **🚩 Measurement footgun:** quaternion-finite-difference rate ALIASES against the LPN/ODO stagger (false "rate gain +20% at airspeed" — refuted); use raw ODOMETRY rate + euler-slope cross-check (WRITEUP §6).

**The flyable stack (durable — and the RL training substrate):**
- **Sim interface** (all 5 must-verify RESOLVED; detail [[reference-sim-interface]]): position+velocity GIVEN pristine (LOCAL_POSITION_NED 97 Hz + ODOMETRY 75 Hz; baro NaN → z from given pos); **🚩 ATTITUDE.pitch sign-inverted → orientation from the ODOMETRY quaternion**; gate map = 6 gates, 2.72 m OUTER / ~1.5 m inner opening (feed inner to PnP), broadcast once at level-load (`capture_track_map.py`, map-once-reuse); course DESCENDS 26 m; clocks INDEPENDENT (align on recv-monotonic); video re-sent ~14× → dedup by frame_id; sim PAUSES physics off-race.
- **Fly on CTBR** (body-rate + explicit collective via `SET_ATTITUDE_TARGET`; sim runs ACRO — easy-mode/ANGLE velocity path = World A, closed). Plant system-ID'd → faithful OFFLINE TWIN (`src/racer/twin.py`/`twin_fit.py`, the RL plant): **rate_gain [2.50,2.50,2.23], physics rate_sign [+1,+1,−1] (only YAW cmd inverted), τ=0.019 s, hover=0.2656, linear_drag≈0.21/s**. **🚩 rate_gain is amplitude-DEPENDENT (sweep 510da24): STATIC "super-rate" map `g(|c|)=G0/(1−s·min(|c|,π)/π)`, s≈0.30 ⇒ real full-stick authority ~11 rad/s, NOT 7.85** (flat-2.5 stays valid small-signal); **✅ map INTEGRATED in all three plants (S14, parity-gated twin→rl_plant→torch; defaults None = bit-identical legacy; `faithful_config(super_rate=True)` = measured map-ON)** — detail [[project-phase2-rl-vision-decisions]] §CHARACTERIZE-SWEEP+§S14. **🚩 TWIN-FALSIFY (2026-06-11, ShadowPC; predictions committed pre-flight): TWO MORE FALSIFICATIONS — ① linear_drag WRONG: real drag QUADRATIC, body-direction-dependent (c2≈0.052/m; 2.2× wrong at 9 m/s ⇒ policies learned ~2× under-braking); ② linear collective map WRONG: thrust curve CONVEX, full-stick ≈8 g (the banked "3.77 g ceiling" was a linear-model artifact).** Survivals: rate map airspeed-invariant, control-rate invariant, no battery sag, determinism SD ~0.03 m/s. `CandidatePlant` ready → **S16 parity-gated integration PENDING** — detail §TWIN-FALSIFY + `handoff/shadowpc-twin-falsify-2026-06-10/WRITEUP.md`. **🚩 PHYSICS≠TELEMETRY: the ODOMETRY quat REPORTS roll inverted + raw rate inverted on roll+pitch (reporting artifact, NOT physics); the controller `body_rate_sign=[1,1,−1]`/`odo_att_sign=[−1,1,1]`/`odo_rate_sign=[−1,−1,1]`/`ff_gain=2.5` undo them.** Arm = `MAV_CMD_COMPONENT_ARM_DISARM` p1=1. Live config = `FAITHFUL_TUNED_GAINS` (`kp_alt 3.0/kd_alt 1.75`, raw vz) + `−0.4 m alt_offset` + `launch_ramp_s=0.6 s`. Detail + Gate-0 saga + alt-relay + the 1.0.3364 fix: [[project-ctbr-control-sysid]].
- **🆕 RL DEPLOYMENT RECIPE (S1.2, fable — corrects 3 sonnet S1.1 bugs; the verified-correct way to fly a DiffAero actor live, in `rl/fly_rl.py`):** ① **tanh(mean) → rescale** (thrust [0,5], rates ±3.14); ② FLU→FRD action sign **[1,−1,−1]**; ③ collective obs init **0.0**, rescaled feedback; ④ training rate **30 Hz**; ⑤ training resets **at rest** 1 m before a random gate. **🚩 "OOD-at-start = root cause" RETRACTED** (artifact of sonnet's wrong transfer function; reset saturation is normal). Full recipe + verified-correct list: [[project-phase2-rl-vision-decisions]] §S1.2.
- **🆕 RATE SIGN SETTLED (S1.2):** raw ODOMETRY `angular_rate` → TRUE rate = **[−1,−1,1]**; the two prior values were each self-consistent (different references). **`state.velocity_ned` is PRISTINE** (quat+body-twist reproduce world velocity to 0.00 m/s). Evidence: [[project-ctbr-control-sysid]] 06-10 update.
- **Altitude balloon is OURS not the sim** (in CTBR we own thrust). Alt loop limit cycle = a **delay-driven relay (~40 ms loop delay), ALWAYS-ON but VALIDITY-HARMLESS**; pure-PD descent-vs-hover conflict fundamental → clean-up deferred. **🚩 the offline twin under-models live latency ~25% even calibrated — do NOT trust offline "holds" claims** (the LESSON is the keeper). [[project-ctbr-control-sysid]].
- **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`) — do NOT reintroduce:** twist is BODY-frame, was stored as world; now rotated via `frames.world_vec_from_body_quat`. Was the phantom "KF lags 4×"; fixed KF tracks truth to 0.05 m/s.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate) — won the fixed-set eval. v3 `--hard` tail-oversampling = a documented NEGATIVE result (high-roll corner-swap is geometric aliasing → solver/temporal prior, not training data). [[project-detector-training-pipeline]]. **✅ Catastrophic-tail fix (4673517, 2026-06-09):** raw |fix|≥3 m 46%→6.3% via NEW `association.py` (wrong-gate kill) + depth-sanity gate — **key insight: the "frontal depth flips" were wrong-scale detector boxes, not solver flips; geometry consistency is the kill.** `PNP_FIX_COV_INFLATION` CLOSED. **✅ VISION-PKG2 COMPLETE (2026-06-10): measured covariance model SHIPPED** — `ATTITUDE_NOISE_STD_RAD=1.4°` + `FIX_COV_FLOOR_STD=0.40 m` floor + `vision_max_range_m` 40→32; good-fix over-rejection **15.7%→1.2%**, catastrophic leak **1.06%→0.53%**, KF-accepted 147→171. **🚩 the ~3.6°/range YAW BIAS is NOT a fixed calibration — REFUTED** (depth-scale artifact + both-sign per-gate offsets + flight-specific roll wander ⇒ COVARIANCE, not calibration; re-measure roll-wander on the next fresh 6/6 with `--dump-extras`). **🚩 REAL BUG FIXED (37e7ab1): `corner_to_center`'s gate frame was 180° in-plane vs the detector convention** — PnP disambiguation prior was anti-aligned. Vision workstream (b) COMPLETE for VQ1+Stage-1. Full numbers: [[project-phase2-rl-vision-decisions]] §VISION-PKG2 + `handoff/shadowpc-vision-pkg2-2026-06-10/`.

**Git / env:** **`main` is CANONICAL** (the `red-team-tier-a` + merged feature/backup branches were retired 2026-06-07 in the pre-Phase-2 cleanup; `scripts/` pruned to the 17-file live+twin+vision+sysid stack). **457 tests green**, `.venv` Python 3.13. `*.pt` weights + `data/runs` recordings are gitignored (move via the adroit-connector). Project memory is mirrored repo `memory/` ↔ `~/.claude` — the cross-machine sync vehicle; ShadowPC passes findings via `handoff/`, not by editing `memory/`.

## NEXT — Phase 2 (RL + VQ2 vision); VQ1 is banked
**🚩 VQ1 = PASS/FAIL "navigate the course" (spec §8, 8-min cap), NOT a ranked time (that's VQ2's fastest-valid-time).** The submission is our **Python stack that THEY run unattended in a controlled eval** (interface "coming weeks", emailed to registered teams) → it must be **FULLY AUTONOMOUS end-to-end** (auto-arm→fly→finish, no human GO/disarm; §7: human interaction = DQ). Unlimited attempts, window May→~mid/late July. **Adroit summer access CONFIRMED (2026-06-08).** **User actions: confirm registration is active + email info@theaigrandprix.com for — ① (architecture-defining) does VQ2 stream `LOCAL_POSITION_NED`/`ODOMETRY` (given pose) or is position vision-only? ② the submission interface spec + ③ the VQ1 deadline + 🆕 (stack-review 2026-06-10) ④ can the stack carry state (map, tuned line) BETWEEN attempts in the controlled eval? (decides whether track-randomized generalization is mandatory vs insurance) + ⑤ the eval-hardware compute envelope — does the submitted stack get a GPU? (gates detector sizing; do NOT size to 100 TOPS for virtual quals).** Detail + open items: [[reference-competition-materials]].
- **3 known VQ1-stack issues** (`handoff/shadowpc-reverify-2026-06-07/`): **✅ ① start transient FIXED** (launch_ramp, 260972e); **② aggressive descents** (peak sink 9.3 m/s) + **③ always-on alt relay** (validity-harmless) remain VQ2/RL targets.
- **RL = COMMITTED VQ2 path** (VQ1 banked): SWIFT-style learned state→action policy behind the `Setpoint` seam (NOT pixels), trained on a massively-parallel twin of the system-ID'd plant + the MEASURED perception model. [[project-master-plan]] C1; **ALL stage detail, verdicts and supersessions: [[project-phase2-rl-vision-decisions]]** (§Staging, §S1.2, §CHARACTERIZE-SWEEP, §S14, §TWIN-FALSIFY, §TOGT-BOUND). **🚩 CRITICAL PATH: S1.4/S15 env-coherence redesign (reward+termination designed TOGETHER; COLLISION-based crash-termination, NOT tilt-threshold; + procedural track randomization with VQ1-as-held-out-eval) → S16 AERO integration (quad drag + convex collective, parity-gated twin→rl_plant→torch, same recipe as S14) → retrain map-ON+aero → live transfer.** Any pre-S16 checkpoint under-brakes ~2× at speed and trains under a phantom 3.77 g ceiling (real ~8 g). Ledger:
  - **Substrate = DiffAero on Adroit (CHOSEN over Isaac; bake-off done):** plant injected via `src/racer/rl_plant.py` + `rl/diffaero_dynamics.py`, parity BIT-IDENTICAL to the twin (V100 gate DIV 4.4e-16); 2048-env racing-PPO ~88.9K steps/s; launcher monkeypatches `DYNAMICS_ALIAS` (zero clone edits).
  - **Inc-1 ✅ (0.97 success, `stage1_inc1_actor.pth`)** — but 🚩 NO plant DR (numpy backend silently ignored it; overfit-to-nominal) and aggressive (l_ep~1.9 s). **S1.2 ✅ live deployment: pipeline VERIFIED, checkpoint NOT transfer-ready** — real failure = the **"backflip-diver"** style (rolls 104–126° pre-gate; live divergence = the then-unmodeled super-rate gain; "yaw-spin anomaly" DISSOLVED = sweep); tail-first spawn fixed by a **virtual π body-z flip** in fly_rl.py. Detail §S1.2 + `handoff/shadowpc-s12-rl-live-2026-06-10/`.
  - **S1.3 retrain ✅ in-twin:** 3.765 ceiling + standing-start (fixed a real OOB-spawn bug) + ATT=2.0/JERK=0.5 → 100% 6/6, peak roll 65° (backflip-dive GONE in-twin) + latency/rate-gain DR, parity 2.22e-16. Deliverable = 2000-update checkpoint `rl/checkpoints/stage1_inc3_actor.pth` (md5 ea2bf8a2…, commit c05362f); the 5000-update run NaN'd, no checkpoint. 🚩 S1.3 + inc-1 trained on the WRONG flat-2.5 plant → retrain gating; **S13-LIVE transfer test DEMOTED to optional cheap datum.**
  - **🚩 CHARACTERIZE-SWEEP (510da24) — STRUCTURALLY SUPERSEDES the cc6921d 2nd-order/PI-windup model:** the inner loop = a STATIC super-rate gain map + fast ~critically-damped loop; **PI-windup DISPROVEN**; the S1.2 "9.7 vs 7.85" = unmodeled DC gain. **NO open-loop sim anomaly** ⇒ env/reward redesign UNGATED; crash-termination = collision-based. DR spec (s∈[0.25,0.35], τ∈[0.015,0.03], alpha_max∈[200,320]) **✅ SHIPPED in S14**. Detail §CHARACTERIZE-SWEEP + `handoff/shadowpc-characterize-sweep-2026-06-10/`.
  - **✅ S14 static-map integration (critical-path step 1):** map+slew in all three plants + torch DR, V100 gate worst 1.8e-15 PASS, tests 376→409. **🚩 CAVEAT: `peregrine_eval.py`/`offline_rollout.py` still DEFAULT to the legacy FLAT plant — S1.4 retrain + all future evals must run map-ON** (DR forces it). **🚩 ADROIT OPS: `/scratch/network/fl3689/peregrine_repo` is a FILE COPY, not a git clone** (repo private; stage a deploy-key clone if sessions grow). Detail §S14 + `handoff/laptop-s14-staticmap-integration-2026-06-10/REPORT.md`.
  - **Pending S2 architecture decision:** decomposed plan-line + RL-tracker (backflip structurally impossible) vs monolithic — decide on the post-static-map retrain; the TOGT k=1.85 structural-gap datum feeds it (§TOGT-BOUND).
  - **🚩 Perception model for Stage-2 (MEASURED, VISION-PKG2-updated):** world-fix σ≈[0.73,0.47,0.29] m (N,E,D), bias [−0.4,+0.06,−0.28] m, range-flat to ~24 m; acceptance ~47% of race-window frames, leak 0.53% of solved; twin fix-covariance = K2·analytic + 1.4° lever + 0.40 m floor + 32 m cap — THE Stage-2 twin input (§VISION-PKG2).
- **🆕 STACK-REVIEW-VQ2 (2026-06-10, fable): architecture AFFIRMED** — the 2025 A2RL champion (MonoRace, TU Delft) validates our class; NO component needs replacement. Exposure = **3 META gaps**: **① single-track training vs unseen VQ2 course (potentially BINARY, rank-impact #1** — fix = procedural tracks + VQ1-held-out, folded into the env redesign); **② ✅ CLOSED by the TOGT BOUND (2026-06-11): ceiling ~4.27 s (4.13 s if the full square counts), ROBUST ~4.3–4.7 s under corrected aero — THRUST BINDS (~84% of lap), rates barely matter; our 35.3 s is 8× off; twin-TRACKABLE lap ≈8.3 s (valid only at time-dilation k=1.85 — the 4.55→8.3 s gap is STRUCTURAL → informs S2). REFERENCE LINE SHIPPED `rl/reference_line_vq1.json` + `rl/reference_line.py` progress() for the RL reward. 🚩 Queued ShadowPC probe: live corner-pass (does race_outcome accept ~1.06 m corner clips?).** Detail §TOGT-BOUND + `handoff/laptop-togt-bound-2026-06-10/WRITEUP.md`; **③ vision-only (case C) readiness unbuilt** (delayed-fix KF rewind buffer · range-anisotropic R ∝ r² · in-loop perception latency NEVER measured — ShadowPC torch is CPU-only; the yaw-calibration sub-piece ✅ RESOLVED-AS-REFUTED). Distilled + the examined-and-REJECTED ledger: [[project-phase2-rl-vision-decisions]] §STACK-REVIEW-VQ2; full report `handoff/stack-review-2026-06-10/REPORT.md`.
- **Vision for VQ2** (aids-off, photoreal): the detector→robust-PnP→KF chain. **🚩 the ~3.6°/range "yaw-bias" is NOT a calibration target — REFUTED (VISION-PKG2, 2026-06-10):** depth-scale artifact + both-sign per-gate offsets + flight-specific roll wander → covered by the shipped covariance model (1.4° + 0.40 m floor), not calibrated out. Irrelevant to model-based given-pose nav. Estimator robustness levers: [[project-estimator-robustness]].

## Durable strategy & directives
- **Track A (model-based) = VQ1 floor (BANKED) + VQ2 baseline**, leaning on the sim's stabilizer (CTBR). Estimator = LINEAR pos/vel KF (attitude given). Detector = YOLO-pose corners → IPPE / robust weighted-PnP. MVP = min-snap line + conservative completion (a slow VALID finish beats a fast invalid one).
- **Overfit GEOMETRY, randomize APPEARANCE.** VQ1 is clean/desaturated/aids-on; VQ2 is photoreal/cluttered/aids-off ⇒ NEVER fine-tune the detector on clean VQ1 frames (would collapse the domain-randomization edge). Train in chaos, validate on chaos. ByteTrack sits behind the GateObservation seam.
- **Planning (racing line + speed) = the VQ2 differentiator (where rank is won); vision = a bounded threshold.** Chain mental model: must-not-BREAK (validity) vs sets-your-RANK (time).
- **RL = COMMITTED VQ2 path** (the moment VQ1 is banked — now): learned state→action policy behind the `Setpoint` seam, twin-trained with a measured perception/latency/dropout model. [[project-master-plan]] C1.
- **User directives:** speed > keeping the gate in view; vision must be excellent; determinism is per-track; offline between-runs processing is LEGAL (not a cheat); be a good citizen on Adroit (own dirs, SLURM only). Discipline that worked: re-derive every agent verdict from data; tune offline, fly only to verify; bounded actuation, user owns the GUI/risk. **🆕 Every new-session prompt carries a recommended MODEL (with VERSION, e.g. opus-4.8 / sonnet-4.6 / fable — not just the family) + EFFORT (low→medium→high→ultracode).** **🆕 2026-06-10 — user upgraded to the MAX plan + switched the commander session to opus 4.8 → token-conservation pressure is RELAXED ("turn up the heat"):** use **opus/fable at high freely** on hard, objectively-verifiable problems (RL deployment, reward shaping, adapter+parity work). Still match model to task (no opus for `git status`). **🚩 FABLE SUNSET ~2026-06-22 (12 days from 2026-06-10):** Fable access ends; it is the strongest model we'll have for this project → **MAXIMIZE Fable in this window**: front-load all hard/design-heavy/objectively-verifiable work (plant integration, env+reward redesign, retrains, S2 architecture decision, vision/estimator math, detector DR pipeline) into Fable sessions NOW, run parallel Fable lanes across the three machines, and bank findings/handoffs extra-rigorously so post-Fable models (opus 4.8 and below) inherit a complete written state. Routine ops (flights, evals, git, light tuning) can wait for lesser models. **MEASURED HEADROOM (2026-06-10): a full commander day (3 parallel worker lanes, multiple fable sessions, background agents) used only 39% of 5-hr / 12% of weekly quota — long autonomous sessions are the DEFAULT, never trim scope for budget; the user posts periodic usage updates to recalibrate.**
- **Spec facts:** VFoV≈58.7° (the spec's "90°" is HFoV; camera tilts up). ~100 TOPS onboard edge budget — under-used (room to size up perception / deploy a learned policy). Obstacles exist but don't map monocularly (fly the corridor).

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture + execution plan, Track-A strategy, components, risks R1–R11, build sequence. The durable gameplan; read when resuming. ([[reference-sim-interface]] is the live sim truth where they differ.)
- [Phase-2 RL+vision decisions (2026-06-08)](project_phase2_rl_vision_decisions.md) — **READ when resuming Phase 2.** RL substrate landscape (DiffAero/Crazyflow > Isaac + the bake-off), RL policy I/O+rewards, the vision 2-workstream plan + real-frame perception characterization (`scripts/characterize_perception.py`), gate-map/SLAM cases, the authoritative collision signal, depth/100-TOPS flywheel, staging (Stage 0/1/2).
- [Sim interface — confirmed wire spec + first contact](reference_sim_interface.md) — the REAL MAVLink+video interface; FIRST CONTACT 2026-06-02 resolved all 5 must-verify items (position given · ATTITUDE sign → ODOMETRY quat · gate map · CTBR · arming · clocks · frame-dedup).
- [CTBR control + inner-rate system-ID](project_ctbr_control_sysid.md) — the FLYABLE control stack: rate-loop gains/signs · the plant-matched decoupled CTBR controller · the faithful offline twin + live config · the Gate-0 saga (ODOMETRY frame bug, roll-report inversion, map anchor) · the VERIFY alt-relay finding (delay-driven, validity-harmless).
- [Detector training pipeline + weighted-PnP](project_detector_training_pipeline.md) — autonomous Adroit YOLO-pose training; v2 SHIPPED / v3 `--hard` negative result; multi-gate synthetic; robust weighted-PnP; adroit-connector ops (run connector CLIs via PowerShell, not Git Bash).
- [AI Grand Prix context](project_ai_grand_prix.md) — the competition (Anduril + DCL, May–Nov 2026; user competing for fun).
- [Competition source materials](reference_competition_materials.md) — spec PDF facts, verified rules/FAQ, the VQ1 submission mechanics (pass/fail, stack-submitted, autonomous), open unknowns.
- [Hardware constraint](project_hardware_constraint.md) — three machines: laptop (dev; Intel iGPU, can't run sim) · ShadowPC (sim host) · Adroit Princeton (free GPU, detector training).
- [Adroit cluster notes](reference_adroit_princeton.md) — Princeton's Slurm/GPU cluster (`ssh <netid>@adroit.princeton.edu`, `/home`+`/scratch`); the detector-training compute. Confirm summer access.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries (license/reuse verdicts) + the 8-paper academic ledger.
- [Tooling recommendations eval](project_tooling_recommendations_eval.md) — 3rd-party tool triage: perception-noise-model = a hard RL requirement; offline processing legal; keepers ADRC · GTSAM mapper · min-snap (rpg) · Isaac RL libs.
- [Estimator robustness](project_estimator_robustness.md) — adaptive R · Mahalanobis innovation-gate · map-averaging · no-gate SEARCH state · optical-flow assist; the home for these when hardening the navigator for VQ2.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must BE the full VQ2 stack under-tuned (vision+KF in-loop), never a crutch.
- [Red-team passes #2 + #3](project_red_team_pass_2.md) — historical pre-sim-drop triage logs; kept so claims aren't re-litigated. (#3: [project_red_team_pass_3.md](project_red_team_pass_3.md))
- [Checkpoint transfer workflow](feedback_checkpoint_transfer.md) — small RL policy checkpoints go in git; ShadowPC pulls instead of manual copy
