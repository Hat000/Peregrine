**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`.

## NOW (2026-06-18)
- 🚩 **COMMANDER HANDOFF (Gen 3→4, 2026-06-18) — full briefing: handoff/commander-handoff-2026-06-18.md.** 🚩 LIVE, do NOT disturb: ShadowPC `/goal` = vision training (SEPARATE clone, MAYBE on its own main). ✅ laptop `/goal` system-id **DONE+MERGED bc182f9** (9 numpy↔torch pairs, 86 pins; **#1 obs-emul gap = BYTE-FAITHFUL → POLICY-GAP → MEASURE-IN-TORCH**; full record handoff/system-id-2026-06-18/REPORT.md on main). 🚩 **`main`=bc182f9 LOCAL-ONLY (ahead of origin 54) — do NOT push / rewrite history** (ShadowPC main may have diverged → reconcile via origin deliberately). σ_p0 gate-4 number = ONE Adroit run: `rl/inc8_sigmap0_torch.sbatch` merged+reviewed (diffaero laptop-absent → Adroit-only; `--lookat yaw` cross-check ~0.177 FIRST, then `--lookat auto`).
- **VQ1 PASSED. inc7 LIVE-CONFIRMED = current best** (standing 5/5, gate-3 barrier gone). Phase 2 = RL + VQ2 vision.
- 🚩 **2-DAY BURN (~2026-06-18): CENTER OF GRAVITY = the full stack** (inc7/inc8 ⊕ C2 estimator ⊕ VQ2 detector). **SPIKE CLOSED THE LOOP (95dcc93):** frame→slice→PnP→case-C Navigator→obs[0:20]→inc8 actor = finite in-bounds action (golden tuple 7/7, green_gate 947). **#37 DE-RISKED:** deploy obs ≡ trained obs (Δ≤9.2e-9) → emul S-ladder selection FAITHFUL at the encoding level. 🚩 **LIVE CARRY-FORWARDS:** (a) 8-kpt best.pt NOT staged = binding-CPZ (real-detector ACCURACY arm BLOCKED); (b) promote `deploy_confidence_triple`→estimator_obs.py — on promotion **÷√2 the NavState.nav_inplane_sigma before the d5 clip** (NavState exports sqrt(P_E+P_D) = √2× the emul's sqrt((P_E+P_D)/2); else c_inplane off ≤0.29; pinned tests/test_sysid_production_obs.py); (c) σ √2 FOOTGUN; (d) cold-start range-consistency. Full spike chain + carry-forward detail + artifact-pipe + saturate-queue → [[project-fullstack-burn]].
- 🚩 **BINDING VQ2 RISK = ESTIMATOR** (not planner/policy). Case-C absolute world-frame nav = NO-GO at any speed; **FIX = gate-relative obs** (SOLID: map bias drops EXACTLY — rel E_bias −0.000 vs abs/submap +0.176; RMS 0.139). Build gate-relative REGARDLESS (Q① only decides load-bearing-vs-insurance). → [[index-vision-estimator]]
- 🚩 **GATE-4 CLOSURE REDUCES TO ONE NUMBER (2026-06-15, three analyses converged): terminal CENTERING σ_p0_lat ≲ 0.08 m (r=0.30; ≲0.05 r=0.38; ≲0.04 tight-VQ2), delivered by inc8 per-fix accuracy + band fix-DENSITY @~12 m** (miss p99 ≈ 3·σ_p0). Coast-drift RESOLVED (informed coast <0.02 m over 0.4 s = NOT the blocker); **VIO / ESKF / velocity / speed ALL ruled out** (VIO #70 ×9-margin, blind to centering); best gate-4 speed = 30 m/s; sim BLUR-FREE so at-speed σ = a PHYSICAL-DRONE item only. **4 reward-WEIGHT iterations ALL NO-GO ⇒ it's reward ARCHITECTURE not weights** (pointing→fix is sparse/all-or-nothing → PPO can't discover it from a non-pointing start at ANY weight) → 🚩 **ARCHITECTURE PIVOT (Fengyou-approved 2026-06-16).** The **LOOK-AT PRIMITIVE is the load-bearing fix** (ENGINEER the pointing → break the exploration wall PPO can't cross, instead of rewarding a proxy) + a dense terminal-σ_p0 CENTERING reward as the honest objective (≈ range-weighted err_ip, already computed). **inc8 LADDER — S0→S3, warm-start, the obs-gap→policy-gap saga (course-completion was NEVER a GO criterion; the ladder optimized a POINTING proxy on a non-flying policy, success≡0 lineage-wide): full episodic detail → [[project-rl-increment-history]] §inc8.** ROOT-CAUSE FIX BUILT+MERGED (9cecf64): gated `through_centering_reward` = R1-to-centre's CROSS-TRACK lateral component (hypot(y,z) dist-to-center-line delta, whole approach; ORTHOGONAL to arc-Γ = no forward double-count; OFF byte-id, green_gate 940); sbatch `peregrine_inc8_recenter.sbatch` = 3 FRESH seeds RW_TC=10.0 + look-at + **course-completion GO + auto early-stop after seed-0**, rw_centering OFF (single-var; S3-cliff driver, layer later). 🚩 **RECENTER FLEW 3/3 (job 3276449, 2026-06-18) = FIRST FLYING inc8** — success_rate ~0.50-0.54, n_passed ~3.0, single-var (RW_TC=10.0) → the through-approach-centring restore IS the root-cause fix (reproducible; lineage was ≡0, died at gate-0). Pointing rebalanced DOWN (intended trade — budget→course-completion). 🚩 **σ_p0 EVAL saga — YAW-INJECTION BUG FIXED (merged ec4cb03, green_gate 947): the binding blocker was NEITHER look-at omission (ported 44617ce) NOR pitch.** Matched-state trace PROVED t_cam numpy==torch (Δ=0) + the PITCH (body-Y) injection ALWAYS bit-faithful; the real bug was YAW — the eval reconstructed the FLU action via the training adapter `_FLIP=[1,-1,-1]`, but `policy_step` emits FRD through the LIVE wire map `_ACT_FLU_TO_FRD=[1,-1,1]` (the eval plant runs `_RATE_SIGN_LIVE=[1,1,1]`, NOT trained-world [1,1,-1] — 🚩 **a prior report's [1,1,-1] was WRONG + is what misled the pitch hypothesis**); the two involutory maps differ on YAW ONLY → the look-at's yaw realized rate was sign-flipped. FIX = invert/re-apply `_ACT_FLU_TO_FRD` at contact_true_eval.py:365,367 (inc7 byte-id; inc8 yaw-only DID change — yaw was the buggy axis — but flight/σ unchanged, reach ~0.67). 🚩 **2-axis σ_p0 STILL UN-MEASURABLE via the numpy-emul instrument:** rc1 flies 2-axis in TRAINING (torch) but 0/200 on the numpy-eval obs — the aggressive g_pitch=3 look-at + the **#37 obs-fidelity gap (torch-train emul ≠ numpy-eval emul OBS)** destabilize it (monotonic in g_pitch, both signs, dies with velocity, low-noise KF crashes even yaw-only). Best-available FAITHFUL σ_p0 = yaw-only seed0 **0.177 m (bias −0.095, p99 0.375) → NO-GO vs 0.08** (pessimistic — no elevation pointing → gate-4 fix-rate≈0). 🚩 **NEXT = FIRE `rl/inc8_sigmap0_torch.sbatch` on Adroit** (torch-env GT-crossing instrument BUILT+MERGED dceaeba, merge-reviewed + **LAUNCH-AUDITED GREEN 2026-06-18** (5-way read-only: cfg-keys/crossing-math/yaw-absence/AUP all PASS; one fix = sbatch default **CKPT_SUB=best NOT periodic**, d2fdaf4 — periodic = a weaker non-monotonic-PPO snapshot ⇒ wrong σ_p0); diffaero laptop-absent → Adroit-only; **fire with `best`**; `--lookat yaw` cross-check vs 0.177 FIRST, then `--lookat auto` = the 2-axis number). The system-id `/goal` **RESOLVED #37 (DONE+MERGED bc182f9):** numpy↔torch obs emulators are BYTE-FAITHFUL (obs[0:17]≤9.5e-7, obs[17:20]≤2.9e-8, no range/fix/stale blow-up) → the 0/200 numpy-grader death is a **POLICY-GAP**, answer = **MEASURE-IN-TORCH** (RNG-order: numpy draws fix-noise conditionally, torch unconditionally; distributionally identical — so the torch instrument IS the faithful path, and the yaw cross-check's 0.04 *band* is the right tolerance). Then RW_TC sweep / MonoRace idea#1 tighten; seed-1/2 + real-8-kpt (best.pt unstaged) remain. VQ2 still governs case-C priority; warm-start training-stability win (cliff/appo-dodge) STANDS. 🚩 **SIGN FOOTGUN (LIVE for launch): `++env.lookat_g_yaw=-3.0 ++env.lookat_g_pitch=3.0` (DOUBLE-plus; both empirical, OPPOSITE signs, analytical WRONG both times) + `+env.lookat_warmup_updates=0` + `+env.rw_centering=1.0` + per-seed `hydra.run.dir`.** σ_p0 = per-fix σ_lat (band fix-density; pointed 0.075–0.11 → CLOSES w/ margin; raw at-speed 0.13–0.17 → knife-edge) ⊕ σ_b (boresight bake −0.25 DEPLOYED 5764291). 2nd lever = near-field GATE estimator (lower the 12 m floor). r=0.38 NEVER; "fix-rate ≥0.50 through the last 6 m" GEOMETRICALLY UNACHIEVABLE (the 12 m PnP floor). Full saga → [[index-vision-estimator]] §TERMINAL-LOCK · [[index-rl-training]] §CONVERGENCE RUN. C6/POC HELD. → [[index-control-sim]]
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** d1/d2 spec PROSE "estimator delivers −L" is wrong (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py (+L 4.77e-7 / −L control breaks 24 m). Future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- ✅ **C2-ESTIMATOR-CHAIN DONE & MERGED (05ed750, 723 green, 0 regressions).** RewindKF (productionized OOSM, horizon=0.5, assert_horizon_gt guard) + gate_relative_inplane_fix (+L, anisotropic cov) + relinnov gate χ²(2,.999)=13.82 + estimator_obs seam + NavState cov export. Gated by use_rewind_kf + use_gate_relative (BOTH OFF → VQ1 byte-identical). G3: rel E_bias −0.000 / RMS 0.131 / p90 0.197. → [[index-vision-estimator]]
- 🚩 **OBS CONTRACT FROZEN (d5 layout, 2026-06-13): obs_dim=20.** [0:17] unchanged bit-exact; [17] c_inplane, [18] c_along = clip(σ_ref/σ_hat,0,1) bounded confidence ratios (σ_ref≈0.05 m); [19] age_norm = clip(t_since_last_fix/TAU_STALE,0,1) TAU_STALE≈0.10 s. Critic get_state 33→36. Deploy: fly_rl gates on checkpoint sidecar obs-dim (inc7 17-dim unchanged). → [[index-vision-estimator]] · [[index-rl-training]]
- **S2 DECIDED = `staged_monolithic_then_decomposed`.** Speed gap = TILT ENVELOPE, not architecture. → [[index-rl-training]]
- **CRITICAL PATH:** σ_p0 GO (Adroit, above) → POC flight (case-C self-localizing stack on VQ1 slow = the emulator-fidelity check, #37) → deploy. DONE+MERGED: C2 estimator chain (05ed750), boresight ε_vert −0.25 (frames.BORESIGHT), L3 / δ_map (REFUTES L3-close) / fix-surrogate / P1-vision-accuracy. C6/POC HELD (Fengyou's gate). → [[index-control-sim]] · [[index-vision-estimator]]
- 🚩 **SPEC CAMERA REVIEW (2026-06-14, §3.8):** Camera 20° is SPEC-EXACT (no tolerance). Mount FIXED → mount-uptilt lever DEAD; elevation fix-rate is POLICY-only. VFoV=90° in spec is MISLABELED (it is HFoV); true VFoV≈58.7° — ALWAYS use fy=320. Boresight ε_vert ≈ 0.56° = calibratable convention/projection seam (vision-extrinsics audit pending); NOT a wrong mount angle. → [[index-vision-estimator]] §Spec facts
- ✅ **PHASE-2 PATHS DONE/LAUNCHED (2026-06-14):** P1 vision-accuracy (boresight+ESKF, c740743), P2 inc8-RL (+fix_surrogate ffb2c74), P3 sim-ops, P5 VQ2 detector (d64e1dc). 🟢 **P5 RESULT — DEPLOY Round-1 YOLO11s best.pt** (sim-to-real 39/40; legacy-crush 47/46% vs 28% at scale; ~4 ms ⇒ ACCURACY not size; Round-2 aug NEUTRAL on clean data; TensorRT-INT8 validate on-target first). 8-kpt LOCKED (permanent/YOLO11s/no-nano/no-re-render); hard-condition DATA re-render DEFERRED (Fengyou's ShadowPC call, ~VQ2 arena); eval-tooling `0b99157` UNPUSHED. Full → [[project-parked-backlog]] #30/#58 · [[index-vision-estimator]]. **POC** (held) = inc7 on the merged estimator stack at VQ1 slow, case-C, full lap.

- 🚩 **COMPETITION INTEL (COWORK-1, 2026-06-14; spec VADR-TS-002):** (1) VQ eval = Windows + ~8 GB-VRAM GPU → our 15–25 ms GPU path ≪ 50 ms (CPU-latency risk RETIRED; "~100 TOPS" = the physical Neros drone, not the qualifier box). (2) NO position on the scored wire (§4.3 = HEARTBEAT/ATTITUDE/HIGHRES_IMU/TIMESYNC) → SELF-LOCALIZE REQUIRED → gate-relative pivot VALIDATED. (3) MonoRace (arXiv 2601.15222, TU Delft, won A2RL 2025) = near-1:1 prior art, READ FIRST. (4) Round 2 = real 3D-scanned env → appearance gap REAL → P5 photoreal justified. → [[reference-competition-materials]]
- 🚩 **WIRE-SPEC (CORRECTED 2026-06-17, Fengyou):** ShadowPC = the **OFFICIAL VQ1 eval sim** (the "twin" is our diffaero model, not ShadowPC). Official VQ1 **STREAMS LOCAL_POSITION_NED + ODOMETRY → our VQ1 PASSES USED GIVEN POSITION (case-A, NOT a case-C self-loc demonstration).** Spec §4.3 says the SCORED wire has NO position → DEPLOYED stack must SELF-LOCALIZE; LPN/ODOMETRY GT tooling = offline-calibration-only, may not transfer. 🚩 **VQ2 honest wire expected ~2026-06-18 (TOMORROW) = THE resolver** (decides case-C = LOAD-BEARING vs INSURANCE) — **WAIT for it before concluding (Fengyou).** → [[reference-sim-interface]] · [[project-parked-backlog]] A1


- 🚩 **DOCTRINE (2026-06-14): inc8 = CASE-C DEPLOYABILITY (self-localizing racer), NOT a big speed jump** (~9.76→~8 s; binding gate-4 speed ~30 m/s [not 37–55] → closure EASIER; the old ~4.6 s "bound" was the rate-infeasible TOGT optimum). Reward spine = explicit arm A (2-axis terminal-lock). **Gate-4 is VERTICAL-σ-dominated** (σ_vert 0.28 ≫ σ_lat 0.10 — same axis as boresight). → [[index-rl-training]]

- 🚩 **SINGLE-COMMANDER MODEL (2026-06-14, Fengyou):** the overall commander writes ONE prompt per pathway → Fengyou pastes each into an ULTRACODE session that self-orchestrates via Workflow; the commander keeps SOLE memory authority + merge gate + SSOT and does NOT drive Workflow directly. → [[index-strategy-meta]]

- 🚩 **CAMERA-POINTING VALIDATED ON REAL DATA (P3 head-on, 2026-06-14): head-on fix-rate 67–77% vs inc7 1.4% (~50× fix density).** The inc8 2-axis terminal-lock thesis is empirically confirmed, not just the surrogate. ε magnitude −0.25 m @ 23 m pinned; FORM RESOLVED = METRIC (→ +L lever). → [[index-vision-estimator]] §P3 BORESIGHT HEAD-ON

- ✅ **P1 VISION-ACCURACY DONE (c740743):** boresight dual-form infra + ESKF re-scope design (gated OFF → inc7 byte-id); ε drops in via frames.BORESIGHT once P3's FORM lands. Full → [[index-vision-estimator]] §P1.

- ✅ **inc8 TORCH PORT MERGED (b0b322e):** PeregrineRacingInc8 (obs 17→20; OFF==inc7 byte-id; S1–S4 torch==numpy ≤1e-4). 🚩 **PARITY-CATCH (do NOT re-chase): the 'R_camera_from_body sin-sign flip' was a bug in the worker's OWN torch reimpl (inc8_estimator_emul.py), NOT main's frames.py (CORRECT).** → [[index-rl-training]]


## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read it FIRST on boot (role + the generalizable commanding doctrine: selective-merge default, sole-memory-writer, empirical-over-analytical signs, seed discipline, diagnose/bracket/risk-order, relay-hazard); EDIT it LAST before handoff (add the lesson you earned, prune the stale, log your generation in its Lineage). Each commander improves the next — forever. This MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **CANARY (rev 2, 2026-06-14):** the **OVERALL COMMANDER persistent session** addresses **Fengyou** by name in EVERY message (missing name = context degradation → rotate session). **Sub-commanders and workers do NOT need the canary** (Fengyou, 2026-06-14).
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work** (≈0.5× fable — the affordable design tier); **ultracode on opus** for hardest fan-out/verify; **sonnet-4.6 = low-stakes mechanizable** (banking, flight ops, git, reruns); haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index structure):** workers end with **MEMORY-DELTA** (≤10 lines); commander triages; sonnet banking agent banks IN BATCHES. **Route detail to the topic file + its domain sub-index; touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use the /consolidate-memory skill (Fengyou, repeated 2026-06-15) — maintain the thin index BY HAND: MEMORY.md = layer-1 index → domain sub-index → topic file; when MEMORY.md grows over its limit, route the bloated/superseded detail DOWN a layer (into the sub-index) and leave a one-line `[[pointer]]` — do NOT run a consolidation skill.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt carries SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA requirement + escape hatch + report path.
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees.
- 🚩 **ANTI-FORGET (2026-06-14):** review **[[project-parked-backlog]]** at session start + whenever a trigger fires (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Sub-commanders/workers surface NEW parked items UP in their MEMORY-DELTA; the commander files them in the register — NEVER re-scatter parked items back into topic files.

- 🚩 **SIM = NO PHYSICAL RISK (Fengyou, 2026-06-14):** measurement/non-POC sim flights carry no risk — the commander AUTHORIZES them directly (no risk-escalation/AskUserQuestion ceremony). The C6/POC hold is a SEQUENCING/milestone gate (Fengyou's), NOT a risk gate; only the explicit POC unfreeze needs him. Validity rule (zero gate contact) + sim time = the only flight constraints. ALSO: every commander↔sub-commander relay through Fengyou = ONE copy-paste code box (not prose). → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN** (zero-contact is THE validity rule).
- 🚩 **inc8 CRITIC IS SYMMETRIC (obs-input 20), NOT the asymmetric/privileged 36-dim critic the SSOT implied (worker finding 2026-06-16, STRONG — version-independent launcher evidence):** GuardedPPO extends diffaero's symmetric `PPO`; `GuardedPPO.build` passes obs_dim ONLY (no state_dim); `algo=ppo` ⇒ `get_state`(33→36)/`state_dim` are NEVER consumed in training (the critic sees the 20-dim obs + shares hidden_dim w/ the actor; inc7 ran the same). The "critic 36 / truth-seeing asymmetric-critic anti-damping" claims describe the get_state DESIGN, **not the active training path.** → **ROOT-CAUSE of the seed collapse — CONFIRMED ×2 (static launcher evidence + the S3 rw=1.0 cliff-collapse 2026-06-17)** (the stabilizer was never connected). CONFIRM on Adroit: print `agent.agent.critic.critic.input_dim` (==20 ⇒ symmetric). To actually get the privileged critic = `algo=appo` + GuardedPPO(AsymmetricPPO) + env `state_dim=36` (separate decision; the `critic_hidden_dim` width knob works either way). → [[index-rl-training]]
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7, NO `MAV_CMD 31000` on the wire). `fly_rl.py` default ckpt = retired inc4 → pass inc7 explicitly if bypassing submit_rl.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG is a self-consistent alias — DO NOT "fix".** → [[index-control-sim]]
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session (internal consistency cannot catch a conjugation). → [[index-control-sim]]
- 🚩 **`main` CANONICAL · 947 tests collected (.venv; prior 723/884/933 stale; +14 spike) · `*.pt`/`*.pth` + `rl/checkpoints/*.json` + `data/runs` gitignored.** Pre-merge gate = `scripts/green_gate.py` (GREEN/RED; stack-guard + load-bearing invariants + sentinel baseline 933 → bump to 947). 🚩 **ARTIFACT PIPE:** binaries (ckpts/weights/recordings) ship as GitHub release assets (`burn-artifacts-*` tags, `gh release download --repo Hat000/Peregrine`), NEVER git-add — recipe + gh gotchas → [[project-fullstack-burn]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** The d1/d2 spec PROSE "estimator delivers −L" is wrong (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py (+L 4.77e-7 / −L control breaks 24 m). The future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- 🚩 **CONTACT RADIUS 0.38 = EMPIRICAL CRASH HALO, NOT GEOMETRY.** Rigid-body chassis geometry caps at 0.2135 m (3D half-diagonal; tilt adds only +0.015 m). 0.38 is the DR upper tail of a posture-matched steep-crash band [0.28,0.38]. DO NOT "fix" it as a geometry bug. Central reporting radius = **0.30 m** (band 0.26–0.33); 0.38 stays as worst-case stress knob. → [[index-vision-estimator]]
- 🚩 **BINDING MARGIN FACTOR = VERTICAL BORESIGHT BIAS ε_vert ≈ 0.215 m (~0.56° camera-pitch mismatch) + ATTITUDE/ACCEL BIAS, NOT CONTACT RADIUS.** δ_map discriminator (d7c592e) PROVES no map offset — ε_vert is a genuine perception/attitude sighting bias that +L does NOT cancel. cold@1.4°-bias p90 = 0.338 m fails at EVERY physically admissible radius. Radius correction is real but does NOT close the binding case. Path to close = boresight calibration (CALIBRATABLE, ~0.56°) + ESKF attitude-bias estimation. CANNOT-SETTLE-OFFLINE SURVIVES. → [[index-vision-estimator]]
- 🚩 **GATE-0-NEAR-LEVEL ANCHOR IS WRONG POSTURE FOR GATE-4.** Corner-pass probe (r_eff ~0.18–0.20 m) is gate-0, ~3 m/s CTBR bridge, near-level. Do NOT use that as the gate-4 radius. Gate-4 posture-matched data = gate-3 steep crashes (tilt ~55°, 12–18 m/s) → r_eff ≥ 0.26–0.38 (lower bounds). → [[index-vision-estimator]]
- 🚩 **CAMERA MOUNT FIXED AT 20° (SPEC §3.8, EXACT) — mount-uptilt lever DEAD.** Elevation fix-rate is POLICY-only (inc8 pitch/pointing). VFoV=90° in spec is MISLABELED — it is HFoV; TRUE VFoV≈58.7° (fy=320, H=360). ALWAYS decode vertical from fy=320. Boresight ε_vert≈0.56° is a calibratable convention/projection seam, NOT a mount-angle error. → [[index-vision-estimator]] §Spec facts
- 🚩 **COMMON-MODE-ACROSS-GATES ≠ PROOF OF +L CANCELLATION.** A vertical (or lateral) offset seen identically at multiple gates is consistent with BOTH (a) a track_map representation offset (corner_to_center half-height-lift) that +L CANCELS [→ margin closes] AND (b) a detector/camera-EXTRINSIC sighting bias that +L does NOT cancel [→ margin fails] — because one camera/detector sees every gate, an extrinsic bias is also common-mode. Only the DIRECT map-vs-sim-truth pin distinguishes them. **The δ_map discriminator (d7c592e, 2026-06-14) CHOSE (b): δ_map_vert ≈ 0 across all 6 gates → ε_vert ≈ 0.215 m = genuine perception bias → L3 "CLOSE" REFUTED.** → [[index-vision-estimator]] §δ_MAP VERTICAL DISCRIMINATOR

- 🚩 **ADROIT ACCEPTABLE-USE = SUSPENSION RISK (#62 RESOLVED 2026-06-14 — use PERMITTED with rules):** NEVER compute on login nodes (SLURM only); compute nodes have NO INTERNET (pre-download git/pip/HF/weights on login/vis FIRST); output → /scratch NOT /projects; accurate --mem + 1-core-serial (over-alloc / multi-core-serial → ACCOUNT SUSPENSION); zero-GPU-util jobs killed at 2h; run `checkquota` routinely. Full AUP → [[reference-adroit-princeton]]. Bake into every P2/P5 SLURM worker prompt.

- 🚩 **WORKTREE HYGIENE (2026-06-14):** the harness AUTO-creates one isolated worktree per spawned session — that ALONE satisfies non-interference. Workers MAY make their own worktrees freely (rev 2, 2026-06-14 — Fengyou's call: isolation > clutter, and enforcing no-add is needless friction). The overall commander OWNS consolidation — merge EVERY worker branch into main at the gate, then prune MERGED branches/worktrees so the list stays manageable. The `.claude/worktrees/*` pool is harness-managed + RECYCLES (observed: an "empty" worktree was reclaimed as oneoff-regsuite) — NEVER manually prune it (racy, can knife a launching session); only `git worktree prune` (gone dirs) + remove confirmed-DONE manual worktrees. Code-workers REPORT their branch name UP; the overall commander keeps the branch↔role ledger, merges, and prunes BRANCHES on merge. → [[index-strategy-meta]] §worktrees

- 🚩 **BORESIGHT FORM-DISCRIMINATION NEEDS WIDE-SEPARATED RANGES (P1, 2026-06-14):** the head-on ε test must use **≥2 ranges with Δrange ≥ ~10 m** (e.g. 12 m & ≥24 m) to separate ANGULAR (∝range) from METRIC (flat) — "≥3 ranges" alone is INSUFFICIENT (closely-spaced 20/22/24 = DEGENERATE → pins magnitude, NOT form). g2/g4 at ~4 m only if per-fix σ_vert ≤ ~0.005 m (heavy frame-averaging). FORM is load-bearing: it sets WHETHER −ε goes in the extrinsic rotation (angular) or the +L lever (metric), AND the ESKF watchdog (rotational δB can't absorb a metric offset). Bake site = frames.R_camera_from_body (audit: no code bug). → [[index-vision-estimator]] §P1 BORESIGHT AUDIT + CALIB-V2

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, S2 decision, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness, advisor-triage.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops (unattended mechanics), autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling eval, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 banked-but-parked threads, triaged F/T/D with revive-triggers. **REVIEW each session + when any trigger fires** (COWORK-1 organizer answers = cluster key un-gating 8 threads).
