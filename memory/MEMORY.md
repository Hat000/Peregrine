**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026; fun). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. Orientation index; detail in topic files.

## READ FIRST — current state (2026-06-12)
**🏁 VQ1 PASSED** (dashboard-confirmed). CTBR stack threads 6-gate course — valid finish ~35.3 s, in-plane miss `[0.37, 0.05, 0.06, 0.03, 0.10, ~0.71]` m (all < 0.75 m). **Offline-twin discipline** (tune offline, fly to VERIFY) got us unstuck — keep it. Phase 2 = **RL + VQ2 vision**.

**✅ LAPTOP-FRAME-AUDIT RESOLVED (2026-06-12, laptop fable; commits 93023cf fix + 1bfb936 writeup). ONE ROOT CAUSE explains all three mirrors: sim ODOMETRY quat is attitude in an R_y(π)-conjugated frame pair. q_true = q_raw·[1,−1,1,−1] (roll+yaw Euler negated, pitch intact); ω_true = −w_raw (all axes); live cmd→rate sign = [+1,+1,+1] — vanilla CTBR, NO inversion. Every historical "roll/yaw inversion" = this single telemetry alias. Conjugation is a proper rotation → passes ALL internal consistency checks (quat-FD, twist, level flight); ONLY external invariant (FD of pristine vel_ned) discriminates (corr +0.97…+0.99 conjugated; −0.84 as-is at bank, 28k ticks). ✅ S18 LAPSE VOIDED — artifact of mirrored projection (true-attitude refit ratio ≈1.0–1.1; no plant gap ≥~2 m/s² in any regime, mixer plant). ✅ INC6 SALVAGED — training world internally self-consistent; deploy matrix REBUILT 16/16 (fixed tools); counterfactual 6/6 @ 9.50 s; no retrain needed. DO-NOT-FLY LIFTED. ✅ LIVE CONFIRMED 2026-06-12: bridge 2/2 FINISHED @ 16.24/17.74 s — FIRST RL LIVE FINISHES EVER; 9/9 convention tests pass; mirror canary TRUE +0.97/AS-IS ≤−0.68 all sessions; gate-0 centered both modes (both prior failure modes gone). Standing 0/4 — deterministic gate-3 crash (pos ≈ (−110.6, −5.0, +22.5)) = trajectory alignment gap (~0.4 m south of center), NOT frames; bridge clears gate 3 (+0.4 m north, −0.8 m). 🚩 LIVE 16–17 s vs twin 9.5 s (~70% slowdown) + rate gain ~0.85 at bridge speeds vs ~0.94 standing → LIVE-GAP-ANALYSIS queued (hypothesis: under-tracking rate loop at speed is one cause of both symptoms). QUEUED: VISION-FRAME-FIX (navigator.py:295 pairs REPORTED attitude with true-world pixels — harmless VQ1, wrong at bank; fix before VQ2 banked vision). S19 (mixer_probe2 vs S17 contradiction — sign-invariant, unchanged by audit). R2 ✅ resolved; gen SEED-VOLATILE (0.741–0.982) → ≥3-seed average henceforth.** Full diag + lineage: [[rl-increment-history]] §LAPTOP-FRAME-AUDIT + §INC6-LIVE-CONFIRMED.

**✅ SIM BUILD 1.0.3364 REGRESSION FIXED (2026-06-09):** 4× deterministic 6/6 @ 34–35 s; fix = `launch_ramp_s=0.6 s` sim-clock-keyed ramp + `--rate 100` pinned (root cause + commits: [[project-ctbr-control-sysid]]). **VQ2 open items (NOT VQ1):** (A) "yaw-spin" **✅ DISSOLVED** (contact dynamics, not anomaly — §CHARACTERIZE-SWEEP); (B) decel into gates; (C) ~20 km/h cap — both RL levers.

**SIM OPS (full mechanics [[reference-sim-ops]]):** unattended control PROVEN. 31000 restart NO-OP from HOME → Win32 foreground + Enter×2; three idle states; zombie dual-instance on 14550; sim AUTORESETS on gate contact → guard cuts RL commands instantly (mandatory); fullscreen auto-minimizes → verify foreground; use raw ODOMETRY rate (quat-finite-diff aliases). **🚩 SPAWN-ARTEFACT (2026-06-12):** gate-3 HARD COLLISION full-reset → next respawn hits residual collision geometry → 0-tick crash at 0 gates; creates gate-3-crash → artefact → clean-spawn → gate-3-crash cycle; NOT a policy failure; affects per-batch stats.

## The flyable stack (durable — RL training substrate)
- **Sim interface** (all 5 must-verify RESOLVED; full wire spec [[reference-sim-interface]]): pos+vel GIVEN pristine (LPN 97 Hz + ODOMETRY 75 Hz; baro NaN → z from given); **🚩 ATTITUDE.pitch sign-inverted → use ODOMETRY quat**; gate map = 6 gates, 2.72 m OUTER / ~1.5 m inner (feed inner to PnP), `capture_track_map.py` map-once-reuse; course DESCENDS 26 m; clocks INDEPENDENT (align recv-monotonic); video dedup by frame_id; sim PAUSES physics off-race.
- **Fly on CTBR** (`SET_ATTITUDE_TARGET`; sim runs ACRO). OFFLINE TWIN (`src/racer/twin.py`/`twin_fit.py`, RL plant): **rate_gain [2.50,2.50,2.23], live physics rate_sign [+1,+1,+1] (vanilla — NO inversion on any axis; all prior inversion lore = R_y(π) telemetry alias), τ=0.019 s, hover=0.2656, linear_drag≈0.21/s (small-signal)**. **🚩 rate_gain amplitude-DEPENDENT: super-rate map `g(|c|)=G0/(1−s·min(|c|,π)/π)`, s≈0.30 ⇒ full-stick ~11 rad/s (NOT 7.85); flat-2.5 valid small-signal.** **Plant status: super-rate map ✅ S14 + quad drag/convex collective ✅ S16 + mixer per-motor clip ✅ S17 — all THREE plants complete; V100 GATE_PASS 7.1e-15.** Falsification detail (TWIN-FALSIFY numbers, parity residuals): [[rl-increment-history]].
- **🚩 CTBR/VQ1 LEGACY SIGN CONFIG (VQ1-proven, DO NOT "fix"):** The CTBR controller uses `body_rate_sign=[1,1,−1]`/`odo_att_sign=[−1,1,1]`/`odo_rate_sign=[−1,−1,1]`/`ff_gain=2.5`. These values form a self-consistent level-attitude alias (a roll mirror that is dynamically correct at near-level VQ1 attitudes) — tuned end-to-end, VQ1-proven, unchanged. **They are NOT the true physical conventions.** Arm = `MAV_CMD_COMPONENT_ARM_DISARM` p1=1. **Live config = `FAITHFUL_TUNED_GAINS` (`kp_alt 3.0/kd_alt 1.75`, raw vz) + `−0.4 m alt_offset` + `launch_ramp_s=0.6 s`.** Detail + Gate-0 saga + alt-relay + 1.0.3364 fix: [[project-ctbr-control-sysid]].
- **🆕 RL DEPLOYMENT RECIPE (current, 93023cf — supersedes bcc93f9/S1.2; in `rl/fly_rl.py`):** ① **tanh(mean) → rescale** (thrust [0,5], rates ±3.14); ② attitude from ODOMETRY quat conjugated: `q_true = q_raw·[1,−1,1,−1]` (wxyz); ③ rate sign **[−1,−1,−1]** (`_ODO_RATE_SIGN`; true rate = −w_raw); ④ wire command = `rate_flu·[+1,−1,+1]` (`_ACT_FLU_TO_FRD`); ⑤ collective obs init **0.0** rescaled; ⑥ training rate **30 Hz**; ⑦ resets **at rest** 1 m before random gate. **🚩 "OOD-at-start" RETRACTED** (artifact of wrong transfer; reset saturation is normal). Detail [[project-phase2-rl-vision-decisions]] §FRAME-AUDIT.
- **🚩 TRUE PHYSICAL CONVENTIONS (2026-06-12, 93023cf — supersedes bcc93f9):** ODOMETRY quat is R_y(π)-CONJUGATED (NOT true attitude AS-IS). True attitude: `q_true = q_raw·[1,−1,1,−1]` (negate x,z in wxyz; roll+yaw Euler negated, pitch intact). True angular rate: `ω_true = −w_raw` (all axes; gain 0.999). Live cmd→rate sign: **[+1,+1,+1]** — vanilla CTBR, no inversion. ODO twist (body vel) and accel_body PAIR with the raw quat → keep as-is. `state.velocity_ned` is PRISTINE. **VALIDATION DOCTRINE (strengthened):** internal consistency (quat-FD vs rate, level flight, twist round-trip) CANNOT catch a proper-rotation conjugation — only external invariants (FD of pristine vel_ned vs attitude-derived force) discriminate. Run `scripts/frame_residual_report.py` after EVERY live session. [[project-ctbr-control-sysid]] + [[project-phase2-rl-vision-decisions]] §FRAME-AUDIT.
- **Alt balloon is OURS** (CTBR owns thrust). Limit cycle = delay-driven relay, ALWAYS-ON but VALIDITY-HARMLESS; deferred. **🚩 LIVE LATENCY = 2 ticks (67 ms)** (command-vs-realized cross-correlation, supersedes ~40 ms lore); offline twin under-models latency — do NOT trust offline "holds" claims.
- **🚩 ODOMETRY velocity-frame bug FIXED (`c3b5a8e`) — do NOT reintroduce:** twist is BODY-frame, was stored as world; now rotated via `frames.world_vec_from_body_quat`.

**Detector: SHIP v2** (`models/gate_yolo11s_curriculum_v2.pt`, multi-gate; v3 `--hard` = NEGATIVE, corner-swap aliasing — [[project-detector-training-pipeline]]). **✅ Catastrophic-tail fix (4673517):** |fix|≥3 m 46%→6.3% (`association.py` + depth-sanity); `PNP_FIX_COV_INFLATION` CLOSED. **✅ VISION-PKG2 (2026-06-10):** 1.4° attitude lever + 0.40 m cov floor + range 40→32 m; over-rejection 15.7%→1.2%, leak 0.53%. **🚩 ~3.6°/range yaw bias REFUTED** (→ covariance). **🚩 BUG FIXED (37e7ab1):** `corner_to_center` 180° flip. Detail [[project-phase2-rl-vision-decisions]] §VISION-PKG2.

**✅ GATE MAPPER COMPLETE (2026-06-11; commits 46a4cc4→75fa42a):** `src/racer/gate_mapper.py` — case A (pose-aided robust) + case B (1-lap ~0.6 m) + case C (no pose, 2–22 s); exact `capture_track_map.py` schema, navigator untouched. Detail [[project-phase2-rl-vision-decisions]] §GATE-MAPPER.

**Git / env:** **`main` CANONICAL** (branches retired 2026-06-07). **598 tests green** (589→598 at LAPTOP-FRAME-AUDIT; +9 golden-file handedness pins in `tests/test_frame_conventions.py`), `.venv` Python 3.13. `*.pt` + `data/runs` gitignored. Memory mirrored `memory/` ↔ `~/.claude`; ShadowPC passes findings via `handoff/`.

**🚩 GATE CONTACT = INVALID RUN** (rules-confirmed). Zero-contact is the VALIDITY condition.

**✅ CORNER-PASS PROBE (2026-06-11; REVISED — contact=invalid): pass aperture ≤~0.5 m from center; TOGT 1.06 m corner-clip optimum definitively illegal; shipped reference line (≤0.14 m from centre) is the correct planning artifact. TOGT planning-valid bound = 4.27 s (inscribed-circle, contact-free); 4.13 s contact-tolerant cuts = INVALID.** Detail [[project-phase2-rl-vision-decisions]] §CORNER-PASS.

**🚩 APPENDIX (mixer diag):** gate map EXONERATED (4–9 cm LOW uniformly = CTBR cruise-drag, no fix); VISION-PKG2 +0.3 m vertical NOT map-z — camera-body offset, see ADVISOR-TRIAGE ⑥.

**✅ S17 + INC6 COMPLETE (2026-06-11) + FRAME-AUDIT RESOLVED (2026-06-12). Mixer model: per-motor clip u_i=clip(c+S·d, idle, 1); κ_err=0.073; mixer_probe2 ✅ (all rows; S17 contradiction flagged S19). All three plants; defaults OFF=bit-identical legacy; V100 GATE_PASS 7.1e-15. KEY REWARD LESSON (durable): joint corner tax `w·|thr−mid|·‖rate‖` DECOUPLES smoothness from generalization. Winner c16: VQ1 1.000/9.86 s, gen 0.982, thr_p95 0.061, 0% saturation, max-offset 0.277 m (zero-contact). Inc6 SALVAGED: deploy matrix REBUILT 16/16 (fixed tools, mixer plant, latency 0–3×4 start modes); counterfactual 6/6 @ 9.50 s. S18 LAPSE VOIDED (artifact of mirrored projection — do NOT use --plant lapse or dr_lapse). `fly_rl.py` default still inc4 — explicit --checkpoint required. R2 ✅; gen SEED-VOLATILE → ≥3-seed average henceforth. Detail [[project-phase2-rl-vision-decisions]] §S17 + §FRAME-AUDIT + [[rl-increment-history]] §LAPTOP-FRAME-AUDIT.**

**🚩 STYLE ENVELOPE COST MEASURED — S2 input: ≤~65° roll costs ~2.3 s/lap on real aero; anomaly-avoidance rationale DISSOLVED. Relaxation = 3-step ladder: (1) global rw_tilt 96→48, (2) free-cone 60°→75–80° (binds only on 3 segments: G1→G2/G2→G3/G4→G5; racestart pk 75°), (3) unconstrained (~6.6 s potential); start→G0 cost = global conservatism not the cap; per-segment = contingency only. Gated on gap analysis + gate-3 standing fix.** Detail §INC5 + §TILT-CONCENTRATION.

**🆕 NEXT queue (post-live-confirm 2026-06-12):** ① **LIVE-GAP-ANALYSIS** (offline, new recordings: twin-vs-live divergence, lap-gap decomposition, rate-gain regime dependence) ② **VISION-FRAME-FIX** (navigator.py:295 before VQ2 banked vision) ③ gate-3 standing trajectory work (likely resolves with ①; bridge start = fallback submission posture if zero-contact: ~2× faster than VQ1's 35.3 s) ④ S19 mixer contradiction ⑤ envelope ladder (gated on gap analysis). Bridge laps zero-contact status: verify in WRITEUP.md.

**🚩 POLICY DECISION RATE 30 Hz WAS NEVER A CHOICE — inherited DiffAero racing.yaml default (dt=0.0333);** at VQ2 speeds 20–39 m/s that is 0.7–1.3 m between decisions vs 0.75 m gate margin. **QUEUED after live transfer verdict:** 60/100 Hz retrain (dt override + PPO horizon retune + latency-DR step rescale) vs 30 Hz baseline. **🆕 ANALYTIC SPEED CEILING (ADVISOR F) VERDICT: 30 Hz binds ONLY at (≥30 m/s × last-fix ≤10 m); fixes to 15 m (32 m range cap + KF) → NOT bottleneck; envelope 65°→80° is the bigger lever. 🚩 GATING MEASUREMENT (queued, SHADOWPC-VISION-CAL): per-gate last-accepted-fix distance; ≥15 m → queued; ≤10 m → critical path.** Formula + ceiling table: §SPEED-CEILING-ANALYTIC.

**🆕 PARALLEL ONBOARD SYSTEMS LEDGER (2026-06-11; gated on organizer ①⑤; threads feed 30 Hz loop at tick boundaries only; keystone = KF rewind buffer). Tier 1:** async ~5 Hz full-res detector rewind-corrected into KF; ROI re-detection "zoom" (may crack case-C co-visibility wall); MPC-shadow safety floor + watchdog demotion; critic-as-risk-monitor. **REJECTED (no re-litigate):** in-race map mutation; policy-ensemble voting; wind estimator; in-race policy adaptation. Tier 2 + specs: §PARALLEL-SYSTEMS.

## NEXT — Phase 2 (RL + VQ2); VQ1 banked
**🚩 VQ1 = PASS/FAIL "navigate the course" (spec §8, 8-min cap), NOT ranked time (VQ2 = fastest-valid).** Submission = **Python stack THEY run unattended** → **FULLY AUTONOMOUS end-to-end** (auto-arm→fly→finish; §7: human interaction = DQ). Unlimited attempts, May→~mid/late July. **Adroit summer access CONFIRMED (2026-06-08).** **User actions: confirm registration active + email info@theaigrandprix.com for — ① does VQ2 stream `LOCAL_POSITION_NED`/`ODOMETRY` or is position vision-only? ② submission interface spec ③ VQ1 deadline ④ can stack carry state BETWEEN attempts? ⑤ eval-hardware compute — GPU? ⑥ does a physical round follow VQ2 (timeline)?** (Risk if yes: second sim-to-real layer — scope only if confirmed; detail §ADVISOR-TRIAGE-2026-06-11.) Detail [[reference-competition-materials]].
- **VQ1 issues:** ✅ ① start transient FIXED; ② aggressive descents (peak sink 9.3 m/s) + ③ alt relay remain VQ2/RL.
- **RL = COMMITTED VQ2 path:** learned state→action policy behind `Setpoint` seam. Stage verdicts: [[project-phase2-rl-vision-decisions]]; lineage: [[rl-increment-history]].
  - **Substrate = DiffAero on Adroit** (bake-off done over Isaac): `src/racer/rl_plant.py` + `rl/diffaero_dynamics.py`, parity BIT-IDENTICAL (V100 DIV 4.4e-16); 2048-env PPO ~88.9 K steps/s.
  - **Checkpoint lineage (full history [[rl-increment-history]]):** inc1 (overfit-no-DR) → inc3 (wrong flat plant) → inc4 (aero-blind, RETIRED) → inc5 (mixer-blind, RETIRED — live failure twin-reproduced) → **inc6 = CURRENT BEST / LIVE CONFIRMED** (`stage1_inc6_actor.pth`, mixer+aero+map plant, corner-tax c16; bridge 2/2 FINISHED @ 16.24/17.74 s; standing 0/4 gate-3 barrier; NEXT = LIVE-GAP-ANALYSIS). **🚩 `fly_rl.py` default still points at inc4 — pass inc6 ckpt explicitly.**
  - **🚩 Retrain footguns:** `peregrine_eval.py`/`offline_rollout.py` DEFAULT to legacy flat plant — all evals must run map-ON; `/scratch/network/fl3689/peregrine_repo` is FILE COPY, not git clone; V100 parity gate not run since S16.
  - **S2 architecture decision pending:** decomposed plan-line + RL-tracker vs monolithic — decide post-live-transfer; TOGT structural-gap datum + style-envelope cost (2.3 s/lap) feed it.
  - **Perception model (VISION-PKG2):** world-fix σ≈[0.73,0.47,0.29] m (N,E,D), range-flat to ~24 m; acceptance ~47% race-window frames, leak 0.53%; twin fix-cov model = Stage-2 twin input. Bias + full one-liner: §VISION-PKG2.
  - **🆕 STACK-REVIEW-VQ2 (2026-06-10):** architecture AFFIRMED (A2RL-champion MonoRace validates our class). **3 META gaps:** **① single-track training** (rank-impact #1 → procedural tracks + VQ1-held-out, in env redesign); **② ✅ TOGT BOUND CLOSED:** ceiling **~4.27 s** (contact-free; see CORNER-PASS above), ROBUST ~4.3–4.7 s with corrected aero — THRUST BINDS ~84% of lap; our 35.3 s is 8×; twin-tracked lap ≈8.3 s at k=1.85 = **STRUCTURAL gap** → S2 tracking arch; reference line `rl/reference_line_vq1.json` + `rl/reference_line.py`; detail [[project-phase2-rl-vision-decisions]] §TOGT-BOUND+§CORNER-PASS; **③ vision-only (case C) readiness UNBUILT** (KF rewind buffer · range-anisotropic R · in-loop latency never measured).
  - **Vision for VQ2:** detector→robust-PnP→KF chain. 🚩 yaw-bias REFUTED — in covariance, not calibration. [[project-estimator-robustness]].

## Durable strategy & directives
- **Track A = VQ1 floor (BANKED) + VQ2 baseline.** Estimator = linear pos/vel KF. Detector = YOLO-pose → IPPE/robust-PnP. MVP = min-snap line + slow VALID finish > fast invalid.
- **Overfit GEOMETRY, randomize APPEARANCE.** NEVER fine-tune detector on clean VQ1 frames.
- **Planning + speed = VQ2 differentiator; vision = bounded threshold.**
- **RL = COMMITTED VQ2 path** now VQ1 is banked. [[project-master-plan]] C1.
- **User directives:** speed > keeping gate in view; vision must be excellent; determinism is per-track; offline between-runs processing LEGAL; be a good citizen on Adroit (own dirs, SLURM only). Discipline: re-derive every verdict from data; tune offline, fly to verify; bounded actuation, user owns GUI/risk. **Every new-session prompt carries recommended MODEL (with VERSION e.g. opus-4.8/sonnet-4.6/fable) + EFFORT (low→medium→high→ultracode).**
- **🆕 2026-06-10 — MAX plan + opus 4.8 commander; token-conservation RELAXED ("turn up the heat"):** use opus/fable at high freely on hard objectively-verifiable problems. Still match model to task.
- **🚩 FABLE SUNSET ~2026-06-22 (12 days from 2026-06-10):** MAXIMIZE Fable now — front-load all hard/design-heavy/objectively-verifiable work (plant integration, env+reward redesign, retrains, S2 architecture, vision/estimator math, detector DR) into Fable sessions; run parallel Fable lanes across three machines; bank findings/handoffs rigorously so post-Fable models inherit complete state. Routine ops wait for lesser models.
- **BUDGET POLICY (rev 3, 2026-06-11, supersedes rev 2):** WEEKLY is the binding constraint (26% used with 6 days left ⇒ target ≤~12%/day); 5-hr limits are effectively unhittable — push them freely. POOL ROUTING is the main lever: **sonnet-4.6 draws a SEPARATE ~free pool → route everything mechanizable there** (ALL memory-banking agents, transport/git, flight ops, eval reruns, recording analysis). **fable:** commander seat + the harshest design/integration tasks only; ULTRACODE effort reserved for the very hardest (plant integrations, env redesigns). **opus-4.8:** hard-but-known-recipe. **haiku-4.5:** trivial ops. Commander keeps its own outputs LEAN (its tokens come from the weekly pool). Throttle heuristic: weekly <~10% → fable takes anything critical-path; weekly >~20% mid-cycle → opus absorbs known-recipe work; sonnet always takes wall-clock-bound/mechanical (separate pool).
- **🚩 CANARY PROTOCOL (user directive 2026-06-11, MANDATORY; amended same day):** the commander AND every worker session must address the user by name — **Fengyou** — in EVERY message they send. A message missing the name = sign of memory/context degradation ⇒ Fengyou rotates that session. Every worker prompt the commander writes must include this canary instruction **at NORMAL salience — one plain line among the other instructions, NO flags/bold/MANDATORY emphasis.** Rationale: the canary only indicates general instruction-following degradation if it decays at the same rate as ordinary instructions; over-stressing it makes it outlive the instructions it's supposed to proxy for.
- **BANKING PROTOCOL (rev 3, cuts commander-side cost):** workers end every report with a **"MEMORY-DELTA:"** block (≤10 lines: new facts, supersessions flagged with what they replace). Commander triages (1–3 lines: accept/reject/amend) and hands the delta + triage to a **sonnet banking agent** which READS the handoff writeup itself for detail (sonnet pool is free; commander no longer writes full distillations). Bank in BATCHES per report-wave. Index-size discipline unchanged (MEMORY.md ≤24 KB, compress-as-you-add).
- **Spec facts:** VFoV≈58.7° (spec's "90°" is HFoV; camera tilts up). ~100 TOPS onboard edge budget — under-used. Obstacles exist but don't map monocularly (fly the corridor).

**🆕 ADVISOR-TRIAGE (2026-06-11):** QUEUED: ① VQ2 photoreal detector pipeline — **first-action DONE** (v2 inventory done: label infra reusable, renderer-only replacement; gaps = lighting/backgrounds/edge-crops/pastel gates — see [[project-detector-training-pipeline]] §v2-inventory); full Blender/Cycles build next; ② ShadowPC CPU in-loop perception latency + ONNX/TRT export sanity; ③ tilt-cost concentration **✅ DONE** → see §TILT-CONCENTRATION; ④ Stage-2 noise model case-C magnitudes if VQ2 vision-only; ⑤ **2-corner translation-only PnP fallback** (~1-day; MonoRace analog); ⑥ **Bayesian-IoU pitch+roll extrinsic calibration** + camera-body vertical offset — bundle ⑤⑥ into one SHADOWPC-VISION-CAL session post-inc6. REJECTED (ledger): HSV color pre-filter. BANKED: gate-identity/blind-transits CLOSED (equivalent to MonoRace). Full specs: [[project-phase2-rl-vision-decisions]] §ADVISOR-TRIAGE-2026-06-11 + §MONORACE-DIGEST.

## Topic files
- [Master plan (LIVING, SSOT)](project_master_plan.md) — architecture, Track-A strategy, risks R1–R11, build sequence.
- [Phase-2 RL+vision decisions](project_phase2_rl_vision_decisions.md) — **READ when resuming Phase 2.** RL substrate, policy I/O, vision plan, staging, ALL stage verdicts/supersessions.
- [RL increment history](project_rl_increment_history.md) — checkpoint lineage inc1→inc6, stage history S1.1→S17, md5s/commits, NaN/sidecar/OOB bug histories.
- [Sim ops](reference_sim_ops.md) — unattended FlightSim mechanics: launch/login, 31000 semantics, idle states, zombie instance, autoreset/spin guards, footguns.
- [Sim interface](reference_sim_interface.md) — confirmed MAVLink+video wire spec; all 5 must-verify items.
- [CTBR control + sysid](project_ctbr_control_sysid.md) — flyable stack: gains/signs, offline twin, Gate-0 saga, alt-relay.
- [Detector pipeline + PnP](project_detector_training_pipeline.md) — Adroit YOLO-pose training; v2/v3; weighted-PnP; adroit-connector ops.
- [AI Grand Prix context](project_ai_grand_prix.md) — competition overview.
- [Competition materials](reference_competition_materials.md) — spec facts, rules/FAQ, VQ1 mechanics, open unknowns.
- [Hardware](project_hardware_constraint.md) — laptop (dev) · ShadowPC (sim) · Adroit (GPU training).
- [Adroit cluster](reference_adroit_princeton.md) — Slurm/GPU; summer access confirmed.
- [Prior-art survey](reference_prior_art.md) — drone-racing projects + libraries + 8-paper ledger.
- [Tooling eval](project_tooling_recommendations_eval.md) — 3rd-party triage; keepers ADRC/GTSAM/min-snap/Isaac.
- [Estimator robustness](project_estimator_robustness.md) — adaptive R, innovation-gate, map-avg, SEARCH state, optical-flow.
- [Walking-skeleton directive](feedback_walking_skeleton_no_vq1_crutches.md) — VQ1 must be full VQ2 stack under-tuned.
- [Red-team passes #2+#3](project_red_team_pass_2.md) — historical triage; kept to avoid re-litigation.
- [Checkpoint transfer](feedback_checkpoint_transfer.md) — small RL checkpoints go in git; ShadowPC pulls.
