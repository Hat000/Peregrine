**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`. (Compacted 2026-07-27.)

## NOW · 🎯 GATE-SIDE STRIKE (the standing target) — SSOTs [[gate-strike-last-3m-2026-07-27]] · [[failure-census-2026-07-27]] · [[velocity-channel-2026-07-27]]
- 🎯 **THE TARGET (Fengyou, restated after I drifted off it): the drone flies into the SIDE of the gate with vision fine.** 🟢 **THE LATERAL LOOP IS SOUND TO 3 m** — slope |lat@8 m|→|lat@2 m| = **0.000 ± 0.03**, terminal |lat| on ALL 4–8 m state **R² = 0.004** ⇒ entry error is fully rejected and **THE TAIL IS CREATED INSIDE THE LAST ~3 m**. 🛑 My "8→5 m capture gap" was a **SELECTION ARTIFACT** — a labels-blind linear-Gaussian null reproduces the table and OUT-separates real data at 3 m; conditional-independence β = **−0.37…−0.51** (null sign); the roll sub-claim died too (null matched every bin to 2 s.f. **with no roll axis in it**). 🛑🛑 **0.75 m IS THE WRONG APERTURE — training threads `0.75 − body_radius`, r~U[0.28,0.38] ⇒ 0.37–0.47 m**; against 0.45 m, **~30% of ALL approaches are exposed, 41% at gate 3**. 🛑 **NO REWARD TERM ISOLATES LATERAL** (verified in code): crossing uses `cross_offset=pass_linf` = **L-inf max(|lat|,|vert|)**, centering/corridor price 3-D perp, `rw_passage` is hard-zeroed by `rw_parabola_crossing` ⇒ **priced symmetrically, fails 4.5× asymmetrically** = the leading v2.1 candidate. 🚩 Lateral exposure 4.5× vertical, worst at **gate 3**. 🚩 The sim-vs-wire capture test is **NOT runnable as framed** (sim course RE-SAMPLED every reset ⇒ no gate 1/2/3). → [[gate-strike-last-3m-2026-07-27]]
- 🛑🛑 **FIVE PREMISES WITHDRAWN IN TWO DAYS — every one an INSTRUMENT error, NOT ONE a wrong number.** Full record → **[[velocity-channel-2026-07-27]]**; craft → `COMMANDER.md` §2 (Gen 7). One line each: lateral-velocity "no information" = **rotation contamination** (de-rotated **+0.93**) · "vertical undershoot dominates" = **pitch coupling** (66.2% of CONFIRMED PASSES misclassify) · "launch window 20.1%" = **classifier RULE ORDER** (they die at range p50 **1.50 m** = at-gate at gate 0) · "speed kills at gate 0" AUC 0.723 = **gate-seam teleport** (seam-free 0.565) · "GT leak / 100× gap" = **I read the LAUNCHER, not the run**. 🛑🛑 **ADJUDICATE FROM `.hydra` + THE STAGE DICT, NEVER LAUNCHER SOURCE — "no launcher sets X" is a NULL RESULT.** 📏 **Training velocity error = 0.28–0.30 m/s** (not 0.01). 🚩 **Parity gap SURVIVES: CORRECTED KF (training) vs UNCORRECTED (deploy, 0 fixes/94 991 ticks) ≈ 5×** — structural, the fix needs a MAP and deploy is map-free.
- 🚩🚩 **FRAME GOTCHA — misread ⇒ every log analysis inverts:** logged `rel_flu` is TRUE body FLU **UNFLIPPED** (neg lat = gate RIGHT), but `obs[0:3]` vel / `obs[5:8]` rates / `obs[11:14]` rel_pos are **VIRTUAL-FLIPPED** (`diag(-1,-1,1)`, `ego_obs.py:36,:124-127`) ⇒ **true v_left = −obs[1]**. Two conventions in ONE log line.
- 🟢 **CENSUS N=572** (`ed6de9f8`, SSOT [[failure-census-2026-07-27]]): **at-gate AXIS-UNDETERMINED 33.7%** (the real #1) · blackout 11.2% (**v15/v16 14.4%→v18/v19 3.9%, p=3.5e-04** = the emission fix, visible in deaths) · lateral 7.7–22.2% · vertical only 4.4–7.2%. 🚩 **A CLASSIFIER'S RULE ORDER IS A CAUSAL CLAIM** · ranking by EXPOSURE always ranks gate 0 first (best conditional survival 72.6%, most deaths — all 563 flights attempt it).
- 🛑 **RELEASE-DIVE METRIC HAS NO DEMONSTRATED PREDICTIVE VALIDITY — and it gated v1.8/v1.9/v2.0.** v19 died −1.379 vs passed −1.378 (tails identical); arm-level corr **+0.338 WRONG SIGN**; dose–response n=563 **22.2% vs 25.9%**, z=−0.76. ⇒ **my v2.0 objection was unfounded; the Q-arm rejection is suspect.** 🚩 v2.0 prices NO pitch direction or angle ⇒ release pitch is an **unpriced weathervane**.
- 🛑🛑 **TWO INVISIBLE OBSTACLES CONFIRMED (Fengyou + measured): ~14.5 m BEFORE gate 4 and before gate 5**; gates 0-3 clean. **DOMINANT DEEP-COURSE BARRIER: of 101 flights reaching gate 4, only 9 pass gate 5 (91% attrition);** survival 56/46/**20**/44% RECOVERS at g6 ⇒ a discrete WALL, not policy decay. 🟡 The gate-5 dodge cleared the obstacle (band deaths **0/5 vs 9/23**) but pass rate did NOT improve (2/5 vs 5/23). 🛑 **Its at-gate deaths are NOT caused by the dodge (Fengyou + geometry):** released at 12.2–21.8 m, died at 1.9–3.8 m ⇒ **10–20 m of recovery**, and the SHORTEST dodge (most recovery) still died at the gate. 🚩 Duration was never a rule — he TUNED it 2.00/1.97/0.94/0.70/0.47 s; two reports each generalised a different subset. 🚩 **Position-free obs never knows WHICH gate ⇒ a per-gate dodge is UNLEARNABLE by the policy; the deploy layer is the ONLY place it can live.** 🚩 **Fengyou names gates 0-BASED = log `gate_index`.** → [[failure-census-2026-07-27]]
- 🏁 **v2.0 SHIPPED** (`ego-ckpts-v20-2026-07-26`, v20Vs0): vert env **5.884 vs v19Ws0 4.893 (+20%)**, flat 5.973 vs 5.880 ⇒ **v19 course-FRAGILE, v20 course-ROBUST**. Its "2× release-dive regression" is **NOT a safety signal** (above) and my conditional-release warning was noise; 3/6 wire deaths is p≈0.11. **v2.0 is a legitimate lead.**
- 🎥 **POST-IMPACT RECORDER LANDED** (`c0d3f6ef`): the impact was **IMPOSSIBLE to log** — stop conditions `break` at the TOP of the tick, the forensics row is written at the BOTTOM ⇒ the detecting tick is discarded whole. Median at-gate log ends **1.43 m / 0.222 s short** ⇒ **69% of at-gate deaths have NO determinable kill axis**. 🚩 **`video_index.jsonl` runs 70–130 ms LONGER than `ego_obs.jsonl` ⇒ the impact is probably already ON VIDEO in flights we hold.**
- 🛠 **AIM-OFFSET SETUP (handed to Fengyou):** branch **`aim-offsets-pin-2026-07-27`** carries the WHOLE stack (vertical fix-gain + D1 + recorder + aim offsets). Set `ego_aim_offsets` = **`4:0,3;5:0,3`** (verified to parse to `{4:(0,3),5:(0,3)}`); `ego_aim_release` 12.0, `ego_aim_fade` 0.0. **`+vertical = UP`** (OPPOSITE sign to `ego_gate_z_bias`, which is camera +Y = DOWN, and a different frame). 🛑 **ORDER MATTERS — PICK THE MODEL FIRST, THEN TYPE THE OFFSET:** I pinned `ego_aim_offsets:""` in `_V1_RECIPE`, so a model-pick CLEARS it (that pin is deliberate — it stops a stale offset riding into the next ckpt). Confirm it armed: `aim_off` non-null on `gate_index` 4/5 in `ego_obs.jsonl`. **3 m ≈ 2× the ~4.3° the geometry needs; his flown 10 was ~6× oversized.** VERTICAL ONLY — adding lateral makes it unattributable.
- 🧪 **V vs C IS A TRUE SINGLE-VARIABLE TEST** — the two runs' recorded overrides differ by **exactly 3 lines** (`course_drop_lo/hi=-10/10`, `course_gates_ceiling=16.0`); same warm start, updates, cadence. ⇒ it isolates the vertical course structure alone (the old vert-vs-flat head-to-head could NOT — those envs differed in cadence too). Eval sbatch prepared at `scratchpad/v20_VvsC.sbatch` (4 policies × 2 envs, `--time=02:00:00` for backfill). **C = job 3323864, v20C_s0_v100, ~17.1k/18k at last check.**
- ✅ Base `deploy-consolidated-2026-07-27` (`7da19a2b`) = D1 (OFF) + recorder (ON) + vertical fix-gain (OFF); superseded by the aim branch above, which contains it all. 🛑 **NO TRAINING ARM IS CURRENTLY JUSTIFIED** — v2.1 correctly NOT launched, `ego_faithful` already armed. Next arm candidate = the **L-inf lateral pricing** above. 🛑 MIRRORED-COURSE PROBE DEAD — course is FIXED.
- 🏁 **v1.8 / v1.9 SHIPPED** (`ego-ckpts-v19-2026-07-24` = v19Ws0, the SAFE lead). v1.9 = roll-rate penalties on action **ch1** + **`ROLL_EVAL`**, default-OFF byte-identical; signflips 2.6→2.26 ∧ n_passed 5.72→6.03 ∧ dive IMPROVED. Q arms REJECTED (dive 450/451). 🛑 **EMA REFUTED** — the swept knob never touched the obs at all.
- 🟢 **GATE-1 RESOLVED (07-24):** the wall was **v16-SPECIFIC (launch-untrained/OOD)** — v16 0/8 vs v18Qs1+darkred+z_bias 0.30 = 11/12. Vertical target is **pitch-coupled body-frame and CORRECT** (only coarse SECTOR obs[9:11] is gravity-leveled); z_bias = a CONSTANT band-aid. 🟢 vision-emission fix `b13a07f` WORKED (no-fresh-fix 23.6→7.3%, flicker 8.8→0.7%) ⇒ **the coast backlog was aimed at yesterday's problem**.
- 🚩 **ROLL is the UNGUARDED axis:** the census gates YAW+PITCH but NEVER ROLL (fix: `ROLL_EVAL`). 🛑 **NO roll ANGLE fence** — it backfires on the 60° turns; rate penalty ONLY. 🛑 **TERMINAL DIVE (open defect):** the fence blocks **nose-DOWN only** (`fly_rl.py:766` `max(cmd,0)`) ⇒ `pitchcmd==+0.000` = a BLOCKED dive; inside 6 m ~100% fenced with the gate ABOVE. LOAD-BEARING, masks training.
- 🚀 **CLUSTER OPS:** `gpu-medium` = **2 CONCURRENT GPUs**. 🚩 **MEASURED 18k-update wall: A100 ~4:18 · MIG ~6:00 · V100 ~7:02** ⇒ **cut `--time` to ~6 h for backfill; the 20 h wall never binds.** 🚩 **`--time` is the ONLY start-time knob you control**; **Node Weight h11g1=100/h11g2=2/h11g3=1 ⇒ A100 is BOTH fastest AND most SHELTERED** (the launcher's "A100 queues ~13 h" is STALE; its default `--exclude=adroit-h11g3` blocks V100). 🛑 **`/scratch/network/fl3689/peregrine_repo` IS NOT A GIT REPO** ⇒ on-cluster provenance UNVERIFIABLE; establish it laptop-side before any submit. → [[velocity-channel-2026-07-27]]
- 🛑🛑 **PITCH CLAMP IS BIMODAL, NOT A DIAL:** resting `obs[4]` = **−0.3107 rad = −17.80°** (n=100); fence = `obs[4] <= −clamp_rad` (`fly_rl.py:757`) ⇒ **clamp ≤17.8 = fence ON AT REST · ≥17.9 = OFF**. Every clamp result in project history is a fence-on/off dichotomy.
- 🚩 **det_hold owns the ADVANCE** (0.2 → true-gate lock ×8/8; 0.3 → WRONG far lock ×6/6) · **stale_horizon owns the CLOSE-IN blind meter** (~0.18 s OPEN-LOOP) · **deploy default 0.2/0.6** · **RE-FLY RULE = ‖gyro‖>2.5 for ≥3 CONSECUTIVE ticks** · **A RECORD IS NOT A BASELINE**; map cohorts NULL (p=0.63).
- 🗺 **MAP SURVEY INSTRUMENT = FLIGHT PATH, never the camera.** 🚩 **TWO RANGE CAPS:** `max_valid_range_m` 30 vs **`max_acquire_range_m`=22 (NO panel field)**; `continuity_reject` ~38× bigger.

## 📁 STANDING OPERATIONAL DURABLES → **[[durables-operational]]** (flight/recipe ritual · run-selection · training-diagnosis · settled verdicts · Track A framing + reference constants)
- 🎯 Goal still standing: **BEAT vpeffs0 on the full 20-gate course.** Thread history → [[track-a-beat-vpeffs0-2026-07-13]] · [[replay-ratchet-2026-07-17]] · [[trackA-stall-forensic-2026-07-13]] · [[ego-deploy-contract-2026-07-09]] · [[audit-ego-inc9-2026-07-09]] · [[vq2-rl-commander-2026-07-05]] · [[now-archive-2026-07-05]].

## Operating directives — ALWAYS APPLY
- 🚩 **`COMMANDER.md` (repo root) = boot prompt + self-improvement loop.** Read FIRST on boot; EDIT LAST before handoff. MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **COMMANDER MODEL rev 4 (Fengyou): vision commander DEPRECATED — RL commander = SOLE commander, owns the deploy repo/branches DIRECTLY** (read+edit+push; NO relay boxes; Fengyou = pilot + hands on ShadowPC). Laptop → Agents/Workflow; Adroit → adroit-connector `serve`/`x`. → [[index-strategy-meta]]
- 🚩 **CANARY:** persistent COMMANDER sessions address **Fengyou** by name in EVERY message (missing name = context degradation → rotate).
- **BUDGET rev 5:** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work**; ultracode on opus for hardest fan-out/verify; sonnet = low-stakes mechanizable; haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING rev 5 (thin-index):** workers end with MEMORY-DELTA (≤10 lines); commander triages, banks IN BATCHES. Route detail to topic file + domain sub-index; **touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use /consolidate-memory (Fengyou, repeated) — compact BY HAND: over-limit ⇒ route detail DOWN a layer + leave a `[[pointer]]`.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Each carries SESSION + MODEL + EFFORT + MEMORY-DELTA rule + escape hatch + report path + **its own worktree**.
- 🚩 **DELEGATE BUILDS TO AGENTS (Fengyou):** multi-file implementation = spawn agents; commander = judgment/diagnosis/review only. → [[feedback-delegate-builds-to-agents]]
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; SLURM only on Adroit; concurrent sessions use separate worktrees. 🛑 **NO-SPIN = HARD REQUIREMENT, enforce by CONSTRUCTION** (fatal spin aborts + realized-yaw clamp), NEVER incentive shaping; NO energy penalties → [[feedback-no-spin-hard-requirement]]. 🛑 **NO-GT IN ACTOR OBS: actor sees ONLY estimator-faithful state**; GT legal in critic/reward/terminations only → [[feedback-no-gt-actor-obs]].
- 🚩 **ANTI-FORGET:** review [[project-parked-backlog]] at session start + on any trigger (POC unfreeze · Adroit contact · fresh recording).
- 🚩 **SIM = NO PHYSICAL RISK (Fengyou):** non-POC sim flights = commander AUTHORIZES directly; the C6/POC hold is a SEQUENCING gate, not risk. Validity (zero gate contact) + sim time = the only flight constraints. Relays through Fengyou = ONE code box. → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]
- 🚩 **TAILS NOT MEDIANS (Fengyou):** verdicts lead with per-flight worst-events (max/p99, tail counts, single-event kill attribution) — one strong roll kills a flight with a perfect median. Every adjudication + A/B carries a tail column. → [[feedback-tails-not-medians]]
- 🚩 **FLIGHT CURRICULUM: close the loop SLOW first, ramp after** → [[feedback_slow_is_smooth]]
- 🚩 **MOONSHOT: championship-fast, not just finishing** (north star = Swift, Nature 2023) → [[project_moonshot_speed]] · **BLENDER VQ2 = HIGH-FIDELITY ONE-LOOK, not broad DR** → [[project_blender_vq2_data_pipeline]]
- 🚩 **GATE CONTACT = INVALID RUN.**
- 🛑🛑 **NEVER READ BODY-FRAME GATE GEOMETRY WITHOUT REMOVING THE DRONE'S OWN ATTITUDE/ROTATION.** One error class, **TWO** banked-then-withdrawn root causes (07-27): (a) lever-differencing without gyro de-rotation — 8 m × 0.3 rad/s yaw fakes **2.4 m/s** of lateral motion (real std ~1.7), and since the controller **yaws to correct drift** it is **anti-correlated with truth BY CONSTRUCTION** = the bogus −0.110; (b) raw `rel_flu[2]` at the flown **+23.8°** median pitch calls **66.2% of CONFIRMED GATE PASSES a vertical strike** (levelled → 6.0%). **CORRECT INSTRUMENT = `scripts/failure_profile/vg.py`** (gyro-de-rotated lever ⇒ true trajectory vs the gate, KF-free). 🚩 **Calibrate any strike classifier on CONFIRMED PASSES** — a pass cannot be a strike, so FP rate is directly measurable. → [[failure-census-2026-07-27]]
- 🛑 **THE GATE SEAM HAS TWO FORMS — `gate_index` guards NEITHER fully.** (a) **ADVANCE seam:** `rel_flu` jumps to the NEXT gate at the advance; a straddling time-window gave PASSES −28.8 m/s p10 and alone produced **AUC 0.723** (seam-free 0.565, sign reverses). Kill it **by construction** — a fixed RANGE band with BOTH edges strictly before the plane; never a time window. (b) **MIS-LOCK seam:** the seeker re-locks a DIFFERENT gate while RACE_STATUS holds the old index — **1.57% of same-index pairs move >3 m** (max 29.1 m). **GATE IT AS A DISTANCE, NOT A SPEED: reject a consecutive-fix lever jump > 1.5 m.** Measured leak/reject **0.14%/3.72%** vs **1.45%/9.76%** for a swept 20 m/s speed gate — dividing by `dt` DILUTES a teleport landing across a long detection gap (teleport apparent-speed p5 = 28 m/s), and 1.5 m ≈ the physical ceiling (p99 speed 14.2 × p99 gap 124 ms = 1.76 m). 🛑 **A SWEEP CANNOT RESCUE THE WRONG VARIABLE** — rank: construction > right variable > swept > chosen value. 🛑 **NEVER build a data-quality gate out of the VARIABLE UNDER TEST** (e.g. `|D_vis − D_dr|`) — it keeps the windows where the baseline already agrees and **silently flatters the control arm**. 🚩 **Rank evidence: RULED OUT BY CONSTRUCTION > SWEPT > HOLDS AT YOUR CUT** — and sweeping should show the default sits INSIDE the clean region, with a direction-randomised placebo that LOSES.
- 🚩 **REPRODUCING A NUMBER IS NOT VERIFYING IT.** The commander re-derived the −0.110 slope with the *same confounded instrument* and called it confirmed. **Name the artifact that could produce this exact sign and magnitude, then stratify by that artifact's own size** — an independent mechanism, not a recompute. 🛑 **FOUR root causes were withdrawn in one session** (rotation artifact · pitch coupling · classifier rule order · launcher-not-run-config). Every one was an INSTRUMENT error, none a code bug. **Suspect the instrument first.**
- 🚩 **A PROBE THAT RUNS ≠ A PROBE THAT MEASURES.** Hand-built "at rest" obs are **OOD** (tell: every arm returns **~zero collective**) — launch-window questions go through `replay_sweep_v18.py` over real flights, never a synthetic. **Decode channels from the wire path**: ego action = **`[thrust, roll, pitch, yaw]`** ⇒ `act[2]` is pitch; with virtual_flip `rate_frd[1]=+act[2]` ⇒ nose-DOWN = `rate_frd[1]<0`.
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed):** HIGHRES_IMU fields_updated=63 = accel+gyro only. YAW + ALTITUDE MUST COME FROM VISION (ESKF = roll/pitch leveler only). Any code path assuming mag/baro is DEAD. → [[index-vision-estimator]]
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2):** ARM (MAV_CMD 400 p1=1) → stream `SET_ATTITUDE_TARGET` BODY-RATE (mask 0b10000000, FRD, thrust [0,1]) ≥30 Hz; `fly_rl.py` drives. Use `DroneState.gyro_body`, NOT `angular_rate_body`.
- 🚩 **`[daemon] unauthorized` = TWO daemons, not bad auth.** Windows `SO_REUSEADDR` lets a restarted `adroit.py serve` bind 8765 while the stale one answers with its old token. `netstat -ano | findstr 8765` → two PIDs ⇒ **kill the OLDER**; re-running `serve` only adds a third.
- 🚩 **CRITIC FOOTGUN: use `algo=appo`** (ppo leaves the privileged critic unconnected = seed collapse; critic input_dim **37**) → [[index-rl-training]]. **Judged runs use `rl/submit_rl.py`** (pins inc7); `fly_rl.py` default ckpt retired → pass explicitly.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG = self-consistent alias — DO NOT "fix"** (tail-first training + virtual π flip compensate) · **ODOMETRY quat is R_y(π)-CONJUGATED** — run `scripts/frame_residual_report.py` after EVERY live session → [[index-control-sim]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE;** the d1/d2 spec prose "−L" is WRONG — 24 m flip), pinned by tests/test_obs_sign_faithfulness.py · 🚩 **BORESIGHT bake = METRIC TRANSLATION** (vert_offset_m −0.25) · 🚩 GATE-4 margin footguns parked w/ inc8 → [[index-vision-estimator]].
- 🚩 **`main` CANONICAL · 1091 tests** · `*.pt`/`*.pth` + ckpt JSONs gitignored · pre-merge gate = `scripts/green_gate.py` (sentinel 1091). 🚩 **ARTIFACT PIPE:** binaries ship as GitHub release assets (`gh release download`), NEVER git-add → [[project-fullstack-burn]]. 🚩 **ShadowPC/clones push FEATURE branches, NEVER `main`.**
- 🚩 **ADROIT AUP = SUSPENSION RISK (use PERMITTED w/ rules):** SLURM only (never compute on login); compute nodes NO internet (pre-stage deps); output → /scratch; accurate --mem; zero-GPU-util killed at 2 h; `checkquota` routinely. Bake into every SLURM worker prompt. → [[reference-adroit-princeton]]
- 🚩 **WORKTREE HYGIENE:** auto-worktree per session = isolation; **ONE WORKTREE PER CONCURRENT AGENT** (sharing moves HEAD under a live agent and can wipe uncommitted edits); commander owns consolidation (merge at gate, prune MERGED only). `.claude/worktrees/*` RECYCLES — never manually prune; only `git worktree prune`. → [[index-strategy-meta]]

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator verdict + gate-relative, VISION-PKG2, detector pipeline, case-C, robustness.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops, autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition rules, hardware, prior-art, tooling, feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 parked threads w/ revive-triggers. REVIEW each session + on any trigger.
- 🚀 **[[deploy-emission-2026-07-22]]** — v1.8 adjudication, vision-fix deltas, flicker, patch 3, terminal dive.
- 🧠 [[vq2-rl-commander-2026-07-05]] · 🗄️ [[now-archive-2026-07-05]] · 🛰️ [[velocity-channel-2026-07-27]] · 💥 [[failure-census-2026-07-27]] · 🔧 [[durables-operational]]
