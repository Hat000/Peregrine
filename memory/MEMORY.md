**Peregrine** — Anduril **AI Grand Prix** autonomous drone-racing entry (May–Nov 2026). Repo `github.com/Hat000/Peregrine` at `C:\Users\Fengy\Downloads\Projects\Anduril`. **This file is a THIN INDEX** — detail lives in the domain sub-indices + topic files; follow the `[[pointers]]`.

## NOW (2026-06-30) · 🟢 ACTIVE — VQ2 SLOW-LAP FLIGHT BRING-UP (supersedes the REACTIVATING/DORMANT framing below)
- 🟢 **ACTIVE (2026-07-01) — VQ2 SLOW-LAP FLIGHT BRING-UP is THE current thread.** Footage-verification loop on branch **`claude/loving-galileo-92f020`** (HEAD **`35561c7`**, pushed; NOT merged to main). 🟢 **A15b (pre-warm CONFIRMED — worst-work 2873→~515 ms, live view no longer freezes; observation UNBLOCKED, the #1 blocker cleared).** Dynamically-confirmed diagnosis: **pursuit's vertical handoff commanded a SATURATED +1.5 rad/s NOSE-UP the instant it engaged → drone climbed into the ceiling** (flight 2: pitch −15.5°→+15° over 12 ticks). 🟢 **ROOT-CAUSED + FIXED (`35561c7`, `ff_owns_horizontal`): the decoupled controller's horizontal velocity-damping term `−kd_vel·vel_xy` was chasing the DEAD-RECKONED (fictional, growing ~1.8 m/s) horizontal velocity — carried in via vertical-align's `velocity_ned=[0,0,vz]` — which swamped the +1.2 forward feedforward, flipped `a_h` BACKWARD, and tilted the target nose-UP. Softening (kp_att/slew) couldn't fix it: wrong-direction target, not stiffness. FIX: when a feedforward `accel_ned` owns the horizontal, DON'T apply the velocity_ned term to the horizontal axes (alt-hold still reads velocity_ned[2] for vz). Offline-repro-confirmed +2.701→+0.595, climb thrust preserved; default-off=VQ1 byte-id; green_gate GREEN-except-known.** 🎯 **NEXT = A16 re-fly** — confirm the ceiling climb is gone + pursuit holds nose-down + threads gate 1, THEN finally test the 337c554 yaw un-mirror on the off-axis gate 2 (untested — both A15b flights died at the nose-up before reaching it). 🚩 **TWO standing COMMANDER-investigate items (from A15b):** (a) **FRAME-DROP / worst-work suspect = a per-frame stack step running long (Fengyou's recollection: the "Manhattan / vanishing-point" module — name UNCONFIRMED, verify vs current code)** — prime suspect for the ~19% recorder starvation + the 468–515 ms worst-work ticks; (b) **standing render request: a per-frame "command sent" overlay** (nearest-tick body_rate vs frame timestamp) for cadence-vs-framerate visibility — build into a handoff-dir render tool, NOT the canonical `scripts/render_vision_video.py`. 🚩 **LAUNCH RITUAL (Fengyou, A15b): launch `fly_rl` FIRST → wait for its "Waiting PASSIVELY" banner → THEN GO** (GO-before-launch = a late-join where pursuit never releases; banked to sim-ops). MILESTONES: **A5 = FIRST GATE PASSED**; **A10 = self-localization TRUSTWORTHY (28 s)**; **A15 = recall SOLVED (`valid_empty` 74%→0%) + thrashing gone (337c554 yaw-frame + 8b702e7 softening held)**; **A15b = pre-warm (`12f366b`) confirmed → observation unblocked**. (A15's "sat in start gate" read was RETRACTED — never observable under the warmup freeze; the A5→A15 pre-warm chain detail = [[index-vision-estimator]] §VQ2 SLOW-LAP.) 🚩 **FULL STATE + resource map + seams + the fix chain = `handoff/commander-handoff-2026-06-30.md` (Gen 6→7 boot briefing).** Saga detail → [[index-vision-estimator]] §VQ2 SLOW-LAP. TWO active threads: flight-controller bring-up + vision-training. Pending after A16 (which confirms the `ff_owns_horizontal` fix): does pursuit HOLD nose-down + thread gate 1, then the 337c554 yaw un-mirror on off-axis gate 2 (both were blocked behind the nose-up = the old "Lead 1/blocker #3", now fixed pending A16); frame-drop/Manhattan-VP check; residual perception hardening (Blender scaffold only); loop-rate/choke. 🚩 **Background agents DIED repeatedly this session (process exits) — on a death, CHECK the working tree for recoverable uncommitted work before redoing; go INLINE for load-bearing tasks.** 🚩 green_gate carries ONE known UNRELATED pre-existing RED (`test_diagnose_session`, handoff-slimming fixture gap — don't chase; "GREEN-except-that" = pass). Seams for vq2-only tuning: `deploy_profile.vq2_case_c` `seeker_overrides`→GateSeekerConfig + `controller_overrides`→make_seeker_controller (both default-off, VQ1 byte-id). venv = MAIN checkout `…/Anduril/.venv` (NOT the worktree).

## NOW (2026-06-27) · 🟢 REACTIVATING (activation sprint — understand + clean, NOT building yet)
- 📁 **REACTIVATION (2026-06-27, superseded by ACTIVE above) — repo cleaned + re-onboarded: git topology pruned (branches 22→3, worktrees 7→3), `handoff/` slimmed 93→20 MB, memory reconciled on main. Re-onboarding map = `docs/reactivation-2026-06-27/REACTIVATION_BRIEFING.md`.**
- 📁 **VQ2 STACK DESIGN RESEARCH (2026-06-27, divergent — pre-flight, mostly superseded by the empirical A5→A15 bring-up).** SSOT = [[vq2-stack-research-2026-06-27]] → `docs/reactivation-2026-06-27/candidate-stack-v2.md`. Headlines still worth knowing: asym-privileged-critic = #1 RL fix (symmetric=catastrophic); recon-map MOAT (VGGT+gate-PnP); bootstrap-vision-RL (pure=0%).
- 🟢 **VQ2 SPEC DROPPED 2026-06-24 (VADR-TS-003) — case-C CONFIRMED LOAD-BEARING. 🟢 VQ2 LOAD-DAY LIVE RECON 2026-06-29 (build 1.0.3379): all §9.3 blocks CONFIRMED ON LIVE SIM — ATTITUDE/LOCAL_POSITION_NED/ODOMETRY/GLOBAL_POSITION/GATE_INFO BLOCKED in BOTH training AND competition (byte-identical message sets; the "training may expose more" hedge REFUTED). REMAINING wire = HEARTBEAT(10Hz) + HIGHRES_IMU(117Hz, accel+gyro ONLY — 🚩 NO MAG, NO BARO; fields_updated=63, bits 0-5) + 30Hz JPEG cam (udpin:14550/video:5600, 24-byte header unchanged) + ENCAPSULATED_DATA=RACE_STATUS(4Hz, active_gate_index PRESENT — gate ordering SOLVED on wire) + ACTUATOR_OUTPUT_STATUS(95Hz, 4 motors); TIMESYNC = RESPONSE-ONLY. 🚩🚩 NO MAGNETOMETER/NO BAROMETER = YAW + Z MUST COME FROM VISION (ESKF demotes to roll/pitch leveler; EqVIO becomes estimator SPINE). 🟢 CONTROL-MODE HANDSHAKE SOLVED: BODY_RATE `SET_ATTITUDE_TARGET` (type_mask=0b10000000) + ARM = working CTBR path; earlier tumble was wrong-message (`SET_POSITION_TARGET_LOCAL_NED` ignored in ACRO). TRACK = "Now You See Me, Now You Don't" (dark warehouse, stations 01-20). Full recon → [[index-vision-estimator]] §VQ2-WIRE-RECON-2026-06-29.** ❓RESOLVED: (a) training==competition wire — YES (byte-identical); (b) track NOT randomized per-load (deterministic confirmed); (c) training does NOT expose blocked telemetry. Spec banked at repo root `260624_Technical_Spec_0003.pdf`. → [[reference-sim-interface]] · [[reference-competition-materials]]
- 📁 **PARKED RL WORK (inc8, from the 2026-06-20 DORMANT period — HELD, do NOT prune).** The inc8 RL speed-increment is paused while we bring up the VQ2 slow lap. Preserved: branch **`inc8-deterministic-retrain`** (05809ba) + a **STASHED reach-rate result** (`claude/funny-nash-d07d7b` @ 0edea93 + `handoff/inc8-reach-rate-2026-06-19/`) = "levers #1+#2 lifted det reach 0.467→~0.72 @ r=0.30", HELD pending Fengyou's review. NEXT inc8 lever (when resumed) = RL to lift gate-4 reach/pass-rate. Assets: detector clean-ensemble (282abb9) + C2 gate-relative estimator chain (05ed750, MERGED). 🚩 Footgun: **ShadowPC/other clones push FEATURE branches, NEVER `main`** — reconcile any divergent main via `origin` deliberately, never force. Prior handoffs archived: `handoff/commander-handoff-2026-06-18.md`.
- **VQ1 PASSED; inc7 LIVE-CONFIRMED = current best.** Phase 2 = RL speed (inc8, PARKED above) + VQ2 vision/flight (ACTIVE above).
- 🚩 **COMMANDER MODEL (rev 2, 2026-06-27):** ONE persistent commander that DELEGATES IN THE BACKGROUND; copy-paste relay RETIRED **except ShadowPC** (the ONLY remaining paste; it runs the competition sim). Laptop → spawn Agents/Workflow directly; Adroit → the adroit-connector (`…\Adroit\adroit-connector\adroit.py`, paramiko SSH). Commander keeps SOLE memory authority + merge gate. Retire the last paste = build a shadowpc-connector. → [[index-strategy-meta]] §SHADOWPC-CONNECTOR.
- 📁 **PRE-FLIGHT VQ2 DESIGN + inc8-RL SAGA (mid-June 2026 — mostly SUPERSEDED by the empirical A5→A15 flight bring-up; full detail in the sub-indices, load-bearing footguns duplicated below).** Durable results worth knowing: **C2 gate-relative estimator chain MERGED** (05ed750: RewindKF + gate-relative +L + relinnov gate; gated OFF = VQ1 byte-id); **obs contract FROZEN** (d5, obs_dim=20, [17:20]=confidence+age; critic get_state 37-dim); **VQ2 detector clean-ensemble 282abb9** (training-loss lever, now DEPLOYED via `--seeker-weights` — see ACTIVE); **gate-4 is a REACH/PASS-RATE problem** (real σ_p0 bar ≈0.15 lat; the ≲0.08 "closure bar" was a margin_envelope DOUBLE-COUNT — RETRACTED); **binding margin = vertical boresight/attitude bias ε_vert≈0.215 m** (calibratable, δ_map-proven a real sighting bias, NOT contact radius); **inc8 = case-C deployability** (self-localizing, not a speed jump); **camera mount FIXED at 20° spec-exact** (VFoV mislabeled → fy=320). Full → [[index-vision-estimator]] · [[index-rl-training]] · [[project-fullstack-burn]] · [[reference-competition-materials]].


## Operating directives — ALWAYS APPLY
- 🚩 **COMMANDER BOOT PROMPT + SELF-IMPROVEMENT LOOP = `COMMANDER.md` (repo root).** Read it FIRST on boot (role + the generalizable commanding doctrine: selective-merge default, sole-memory-writer, empirical-over-analytical signs, seed discipline, diagnose/bracket/risk-order, relay-hazard); EDIT it LAST before handoff (add the lesson you earned, prune the stale, log your generation in its Lineage). Each commander improves the next — forever. This MEMORY.md = project STATE; COMMANDER.md = durable CRAFT.
- 🚩 **CANARY (rev 2, 2026-06-14):** the **OVERALL COMMANDER persistent session** addresses **Fengyou** by name in EVERY message (missing name = context degradation → rotate session). **Sub-commanders and workers do NOT need the canary** (Fengyou, 2026-06-14).
- **BUDGET (rev 5):** weekly pool sole watch; push limits to MAX. **opus-4.8 = ALL correctness/judgment-dense work** (≈0.5× fable — the affordable design tier); **ultracode on opus** for hardest fan-out/verify; **sonnet-4.6 = low-stakes mechanizable** (banking, flight ops, git, reruns); haiku = trivial. PUSH models UP. 🚩 FABLE REVOKED (pending dispute).
- **BANKING (rev 5 — thin-index structure):** workers end with **MEMORY-DELTA** (≤10 lines); commander triages; sonnet banking agent banks IN BATCHES. **Route detail to the topic file + its domain sub-index; touch MEMORY.md ONLY for NOW / footgun / directive changes.** 🚩 **NEVER use the /consolidate-memory skill (Fengyou, repeated 2026-06-15) — maintain the thin index BY HAND: MEMORY.md = layer-1 index → domain sub-index → topic file; when MEMORY.md grows over its limit, route the bloated/superseded detail DOWN a layer (into the sub-index) and leave a one-line `[[pointer]]` — do NOT run a consolidation skill.** Agents `git add` ONLY their own paths (never `-A`). Mirror `memory/` ↔ `~/.claude`.
- **PROMPT-EMISSION:** re-emit COMPLETE worker prompts, never splices. Every prompt carries SESSION + MODEL(version) + EFFORT + canary + MEMORY-DELTA requirement + escape hatch + report path.
- **User directives:** speed > gate-in-view; tune offline / fly to verify; re-derive every verdict from data; bounded actuation, user owns GUI/risk; offline processing legal; SLURM only on Adroit; concurrent laptop sessions use separate worktrees.
- 🚩 **ANTI-FORGET (2026-06-14):** review **[[project-parked-backlog]]** at session start + whenever a trigger fires (POC unfreeze · COWORK-1 answers · Adroit contact · fresh recording). Sub-commanders/workers surface NEW parked items UP in their MEMORY-DELTA; the commander files them in the register — NEVER re-scatter parked items back into topic files.
- 🚩 **FLIGHT CURRICULUM (2026-06-29): close the loop SLOW first, ramp speed after.** Slow = vision self-loc works (more frames/m, no blur, yaw+Z from vision). 🟢 **MILESTONE A5 (2026-06-29): FIRST GATE PASSED** (footage-confirmed, map-free, no mag/baro; dead-straight threading). Remaining = post-pass transition (A6 in progress). → [[feedback_slow_is_smooth]] · [[index-vision-estimator]] §VQ2 SLOW-LAP BRING-UP SAGA
- 🚩 **BLENDER VQ2 DATA PIPELINE (2026-06-29): build Blender render harness matched to glowing-red/dark-warehouse VQ2 appearance — HIGH-FIDELITY ONE-LOOK, not broad domain randomization.** Extends learned detector beyond classical red-glow limits. → [[project_blender_vq2_data_pipeline]]
- 🚩 **MOONSHOT (2026-06-29): target = championship-fast, not just finishing.** North star = Swift (Nature 2023). Strategy = slow bootstrap → perception hardening → RL speed ramp. → [[project_moonshot_speed]]

- 🚩 **SIM = NO PHYSICAL RISK (Fengyou, 2026-06-14):** measurement/non-POC sim flights carry no risk — the commander AUTHORIZES them directly (no risk-escalation/AskUserQuestion ceremony). The C6/POC hold is a SEQUENCING/milestone gate (Fengyou's), NOT a risk gate; only the explicit POC unfreeze needs him. Validity rule (zero gate contact) + sim time = the only flight constraints. ALSO: every commander↔sub-commander relay through Fengyou = ONE copy-paste code box (not prose). → [[feedback-sim-no-physical-risk]] · [[feedback-worker-prompt-copy-paste-box]]

## Cross-cutting footguns — forget = disaster
- 🚩 **GATE CONTACT = INVALID RUN** (zero-contact is THE validity rule).
- 🚩 **NO MAG, NO BARO ON VQ2 WIRE (live-confirmed 2026-06-29, build 1.0.3379):** `HIGHRES_IMU.fields_updated=63` = accel+gyro bits 0-5 ONLY; mag (xmag/ymag/zmag) + baro (abs_pressure/temperature) absent/NaN. YAW + ALTITUDE must come from VISION (EqVIO landmark bearings pin yaw+z). ESKF/AHRS = roll/pitch leveler only. Any code path that assumes HIGHRES_IMU carries mag or baro is DEAD. → [[index-vision-estimator]] §VQ2-WIRE-RECON-2026-06-29
- 🟢 **CONTROL-MODE HANDSHAKE SOLVED (VQ2, 2026-06-29):** RECIPE = ARM (MAV_CMD 400, p1=1) then stream `SET_ATTITUDE_TARGET` BODY-RATE (type_mask=0b10000000, FRD rates + thrust [0,1]) ≥30Hz; sim default = ACRO = CTBR; `fly_rl.py` already drives this path. Earlier failure = wrong msg (`SET_POSITION_TARGET_LOCAL_NED` tumbles in ACRO; attitude-QUAT also ignored in ACRO). Deploy: use `DroneState.gyro_body` (HIGHRES_IMU), NOT `angular_rate_body` (ODOMETRY-blocked). → [[index-vision-estimator]] §VQ2-CONTROL-HANDSHAKE-2026-06-29
- 🚩 **inc8 CRITIC SYMMETRIC FOOTGUN (PARKED with inc8):** building inc8 as training does (`algo=ppo`) leaves the privileged `get_state` critic NEVER connected (input_dim==obs_dim==20) = root-cause of seed collapse. FIX WIRED: `algo=appo` (`GuardedAPPO`) → critic input_dim **37** (`get_state` is 37-dim, not 36). Launched job 3289989, then parked for the flight bring-up. Re-apply the 2 hunks to CRLF `main` on resume. Full → [[index-rl-training]]
- 🚩 **Judged runs use `rl/submit_rl.py`** (pins inc7, NO `MAV_CMD 31000` on the wire). `fly_rl.py` default ckpt = retired inc4 → pass inc7 explicitly if bypassing submit_rl.
- 🚩 **CTBR/VQ1 LEGACY SIGN CONFIG is a self-consistent alias — DO NOT "fix".** → [[index-control-sim]]
- 🚩 **ODOMETRY quat is R_y(π)-CONJUGATED;** run `scripts/frame_residual_report.py` after EVERY live session (internal consistency cannot catch a conjugation). → [[index-control-sim]]
- 🚩 **`main` CANONICAL · 1091 tests collected (.venv, verified 2026-06-20; prior 723/884/933/947/1077 stale — 1077 was a runtime PASS-count not a collect) · `*.pt`/`*.pth` + `rl/checkpoints/*.json` + `data/runs` gitignored.** Pre-merge gate = `scripts/green_gate.py` (GREEN/RED; stack-guard + load-bearing invariants + sentinel baseline = **1091**, bumped 933→1091 @ 6480546; `--full` GREEN 1047 passed/44 skipped 2026-06-20). 🚩 **ARTIFACT PIPE:** binaries (ckpts/weights/recordings) ship as GitHub release assets (`burn-artifacts-*` tags, `gh release download --repo Hat000/Peregrine`), NEVER git-add — recipe + gh gotchas → [[project-fullstack-burn]].
- 🚩 **OBS SIGN = +L (CORRECT IN CODE; obs_from_zup:348 = R_w2g@(gate−pos), localization:86).** The d1/d2 spec PROSE "estimator delivers −L" is wrong (would be a 24 m flip). NOT a past code bug — pinned by tests/test_obs_sign_faithfulness.py (+L 4.77e-7 / −L control breaks 24 m). The future C2 estimator→obs MUST deliver +L. → [[index-vision-estimator]]
- 🚩 **GATE-4 MARGIN FOOTGUNS (PARKED with inc8 — detail all in [[index-vision-estimator]]):** contact radius 0.38 = empirical crash HALO not geometry (central 0.30); binding margin = vertical boresight/attitude bias **ε_vert≈0.215 m** (δ_map-proven a real sighting bias, +L does NOT cancel, CALIBRATABLE), NOT contact radius; gate-0 near-level is the WRONG posture for gate-4 (use gate-3 steep-crash data); camera mount FIXED 20° spec-exact (VFoV mislabeled → always fy=320); common-mode-across-gates ≠ +L cancellation (only a direct map-vs-truth pin distinguishes; δ_map chose "genuine bias").

- 🚩 **ADROIT ACCEPTABLE-USE = SUSPENSION RISK (#62 RESOLVED 2026-06-14 — use PERMITTED with rules):** NEVER compute on login nodes (SLURM only); compute nodes have NO INTERNET (pre-download git/pip/HF/weights on login/vis FIRST); output → /scratch NOT /projects; accurate --mem + 1-core-serial (over-alloc / multi-core-serial → ACCOUNT SUSPENSION); zero-GPU-util jobs killed at 2h; run `checkquota` routinely. Full AUP → [[reference-adroit-princeton]]. Bake into every P2/P5 SLURM worker prompt.

- 🚩 **WORKTREE HYGIENE (2026-06-14):** the harness AUTO-creates one isolated worktree per spawned session — that ALONE satisfies non-interference. Workers MAY make their own worktrees freely (rev 2, 2026-06-14 — Fengyou's call: isolation > clutter, and enforcing no-add is needless friction). The overall commander OWNS consolidation — merge EVERY worker branch into main at the gate, then prune MERGED branches/worktrees so the list stays manageable. The `.claude/worktrees/*` pool is harness-managed + RECYCLES (observed: an "empty" worktree was reclaimed as oneoff-regsuite) — NEVER manually prune it (racy, can knife a launching session); only `git worktree prune` (gone dirs) + remove confirmed-DONE manual worktrees. Code-workers REPORT their branch name UP; the overall commander keeps the branch↔role ledger, merges, and prunes BRANCHES on merge. → [[index-strategy-meta]] §worktrees

- 🚩 **BORESIGHT FORM-DISCRIMINATION NEEDS WIDE-SEPARATED RANGES (P1, 2026-06-14):** the head-on ε test must use **≥2 ranges with Δrange ≥ ~10 m** (e.g. 12 m & ≥24 m) to separate ANGULAR (∝range) from METRIC (flat) — "≥3 ranges" alone is INSUFFICIENT (closely-spaced 20/22/24 = DEGENERATE → pins magnitude, NOT form). g2/g4 at ~4 m only if per-fix σ_vert ≤ ~0.005 m (heavy frame-averaging). FORM is load-bearing: it sets WHETHER −ε goes in the extrinsic rotation (angular) or the +L lever (metric), AND the ESKF watchdog (rotational δB can't absorb a metric offset). Bake site = frames.R_camera_from_body (audit: no code bug). → [[index-vision-estimator]] §P1 BORESIGHT AUDIT + CALIB-V2

## Library index (MEMORY → domain sub-index → topic files)
- 🛩️ **[[index-rl-training]]** — RL increments/lineage, training doctrine, S2 decision, inc8 + speed-ladder, plant/sysid-for-RL, retrain footguns, Adroit/DiffAero.
- 👁️ **[[index-vision-estimator]]** — estimator race-speed verdict + gate-relative, VISION-PKG2, detector pipeline, case-C readiness, estimator robustness, advisor-triage.
- 🎮 **[[index-control-sim]]** — CTBR control/sysid, deploy recipe + true conventions, sim interface (wire spec), sim ops (unattended mechanics), autonomy-hardening.
- 📋 **[[index-strategy-meta]]** — master plan (SSOT), competition materials + rules, hardware, prior-art, tooling eval, walking-skeleton + feedback.
- 📂 **[[project-parked-backlog]]** — STANDING register of 70 banked-but-parked threads, triaged F/T/D with revive-triggers. **REVIEW each session + when any trigger fires** (COWORK-1 organizer answers = cluster key un-gating 8 threads).
