---
name: vq2-stack-research-2026-06-27
description: VQ2 monocular-racing stack DESIGN RESEARCH — load-bearing conclusions + pointers to the full lit-review docs (read the docs for detail; do NOT duplicate here)
metadata:
  type: project
---

2026-06-27 activation-sprint research phase (Fengyou-led, DIVERGENT — keep forks open). Digested **~28 papers + 4 Gemini Deep Research scans** on the VQ2 monocular+IMU, no-map, per-load-randomized racing stack. **RESEARCH BREADTH = COMPLETE** (swept: racing canon, VIO/SLAM, perception-aware control, Gaussian-splat, detectors, RL methods, 6-DoF pose, monocular depth/3D, defense/aerospace GNC, patents, dissertations, Chinese-language GNC, high-dynamics estimation). Remaining value = SPIKES + VQ2-load checks, NOT more reading. **FULL DETAIL lives in repo docs — visit them, don't re-derive:**
- `docs/reactivation-2026-06-27/candidate-stack-v2.md` — integrated candidate stack + Tier-1→4 action ladder + spikes.
- `docs/reactivation-2026-06-27/lit-review/2026-06-27-five-paper-synthesis.md` — all digests (8 rounds, paper-by-paper).
- `docs/reactivation-2026-06-27/hybrid-control-design-draft.md` — RL-only vs RL+MPPI/AC-MPC.
- `docs/reactivation-2026-06-27/spike-plans.md` — executable designs for the 3 spikes (gate-4 discriminator, MPPI-at-speed, VGGT-moat) + decision rules + predictions.
- `docs/reactivation-2026-06-27/vq2-load-checklist.md` — load-day checks C0–C10 (ordered, fork-gated) + pre-staged inspector tooling.

**LOAD-BEARING conclusions (promoted so they steer decisions WITHOUT opening the files):**
1. **RECON-MAP MOAT.** Multi-lap-within-a-load turns unsolved "no-map racing" into solved "build-then-exploit-map": recon lap → VGGT feed-forward 3D (offline, A100/H100) → Sim(3)-anchor to gate-PnP → frozen metric map ⇒ localization + gate-ordering (= geometry along recon trajectory) + the known-map regime where proven estimators work. Gated on track-fixed-within-load → VERIFY on VQ2 load. [[index-vision-estimator]]
2. **ASYMMETRIC PRIVILEGED CRITIC = the #1 fix.** 6 independent sources confirm symmetric critic = catastrophic; our inc8 symmetric-critic footgun is exactly this. `algo=appo` + state_dim=36; deploy-unobservable signals → critic only. [[index-rl-training]]
3. **Tier-1 build ladder (all inside the existing RL stack, do first):** ① asym critic ② perception-aware reward (PAMPC image-plane centering + anti-blur) ③ velocity-weighted contact penalty (gate-4 under-braking) ④ curriculum reward-weight annealing.
4. **VQ2 vision policy:** pure vision-RL = 0% → MUST bootstrap (state-teacher [inc7/inc8] → DAgger student → adaptive-RL fine-tune, performance-gated; Xing 2403.12203). Add small GRU actor for gate-occlusion memory.
5. **Perception:** DISCARD PnP rotation (planar-square FLIP ambiguity, worst at distance) → attitude from AHRS/filter; 8–12 keypoints + per-corner covariance → EKF R; IPPE_SQUARE; detection filters (dist 1–13 m, skew>2, occlusion).
6. **AHRS (new VQ2 scope):** learned attitude (RIANN GRU / Brossard gyro-denoise) trained on sim GT, OR invariant/Modified-Polar EKF for high-g covariance stability (no Cartesian collapse).
7. **DEFENSE-GNC EDGE (paywalled, robotics doesn't cite — asymmetric moat):** drone-through-gate ≡ strapdown-seeker bearings-only terminal intercept. Gap-Traversal Guidance (closed-form through-gate from vertex bearings, no PnP), bearing-angle/known-size observability (range without lateral maneuver → fly direct), Modified-Polar-Coord EKF (no high-g collapse), tanh/polynomial FOV-constrained shaping (guaranteed keep-in-view, O(1)), stochastic-projection (≡ ADR-VINS). New control fork D4. [[index-strategy-meta]]
8. **RULED OUT (with evidence):** pixels-no-estimator (track memorized in weights → trap under per-load randomization; mine the analytic-mask + asym-critic only); FoundationPose/MegaPose onboard (RGB-D, too slow 8 GB); GS/NeRF onboard reloc (exceeds every validated envelope → GS = OFFLINE sim tool only); optimal-line MPC tracking as deployed controller (infeasible-reference failure; OC offline for the bound only); IMM filter banks onboard (compute-hostile — MSTF single-filter gets most of it).
9. **ESTIMATOR DESIGN (binding-risk core; both GNC scans converge — A/B vs RewindKF, don't adopt blind):** error-state + **direct reprojection innovation (ADR-VINS)** (1–2 corners, no PnP) + **multiple-fading-factor Strong Tracking** (per-state covariance inflation on innovation spike → trust camera over lagging aero model; ABSENT from Western drone racing = edge; use VB-STF to separate maneuver vs false-fix) + **cubature** (positive-weight, mixed-degree) + **square-root/UD** (32-bit embedded stability, sqrtVINS) + **FEJ2/ROBOCENTRIC** observability consistency (robocentric ≈ our gate-relative pivot; kills hallucinated-observability divergence, 32–47% drift cut). Discard-PnP-rotation, attitude from AHRS (+ vision-aided AHRS: gate-corner pixel innovations can correct tilt). **Terminal blind-zone coast** (gate fills FoV in final fraction-sec → vision→belief-state, pairs with GRU actor). Guaranteed FoV via Barrier-Lyapunov / tanh shaping (vs reward-shaped). [[index-vision-estimator]]

**SPIKES that gate the uncertain forks (designs in spike-plans.md — predictions: gate-4 likely CALIBRATION+REACH not SITT; MPPI likely PARKED at racing speed; VGGT likely YELLOW = corridor+ordering win, rims smear):** gate-4 discriminator (2×2 counterfactual: force_accept=R, bias_off=C, reach-only=P + SITT proxy-KL) · MPPI-at-noisy-state-at-racing-speed (paired, asym-critic prereq, 8 GB VRAM kill switch) · VGGT-on-sim-renders (consider DPVO as cheaper first probe) · VQ2-load checks C0–C10. 🚩 **NEW OPEN GATE (load-bearing): is a Training-derived artifact (pre-baked map / Training-calibrated boresight) LEGAL to ship under "code-audit on submit"? If NO → offline-calib + Training-mapping forks DIE regardless of GT exposure. Resolve on VQ2 load.** 🚩 **Sharper gate-4 fact (unmerged branch inc8-deterministic-retrain): AIRTIGHT NEGATIVE — fix-seating ⊥ deterministic flight (fix_rate=0 at every flyable gain).**

**Research-channel model (Fengyou, 2026-06-27):** Gemini Deep Research = breadth/recall (300+ sources incl. paywalled; Fengyou unblocks) → recommends deep-dives; Claude = depth/verify/design (digest, adversarially verify Gemini's citations — several were future-dated/confabulated, design). Stance memory: [[feedback-divergent-research-stance]].
