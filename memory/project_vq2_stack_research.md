---
name: vq2-stack-research-2026-06-27
description: VQ2 monocular-racing stack DESIGN RESEARCH — load-bearing conclusions + pointers to the full lit-review docs (read the docs for detail; do NOT duplicate here)
metadata:
  type: project
---

2026-06-27 activation-sprint research phase (Fengyou-led, DIVERGENT — keep forks open). Digested **~28 papers + 4 Gemini Deep Research scans** on the VQ2 monocular+IMU, no-map, per-load-randomized racing stack. **RESEARCH BREADTH = COMPLETE** (swept: racing canon, VIO/SLAM, perception-aware control, Gaussian-splat, detectors, RL methods, 6-DoF pose, monocular depth/3D, defense/aerospace GNC, patents, dissertations, Chinese-language GNC, high-dynamics estimation). Remaining value = SPIKES + VQ2-load checks, NOT more reading. **FULL DETAIL lives in repo docs — visit them, don't re-derive:**
- `docs/reactivation-2026-06-27/candidate-stack-v2.md` — integrated candidate stack + Tier-1→4 action ladder + spikes.
- `docs/reactivation-2026-06-27/lit-review/2026-06-27-five-paper-synthesis.md` — all digests (8 rounds, paper-by-paper).
- `docs/reactivation-2026-06-27/hybrid-control-design-draft.md` — RL-only vs RL+MPPI/AC-MPC.
- `docs/reactivation-2026-06-27/spike-plans.md` — executable designs for the 3 spikes (gate-4 discriminator, MPPI-at-speed, VGGT-moat) + decision rules + predictions.
- `docs/reactivation-2026-06-27/vq2-load-checklist.md` — load-day checks C0–C10 (ordered, fork-gated) + pre-staged inspector tooling.

**LOAD-BEARING conclusions (promoted so they steer decisions WITHOUT opening the files):**
1. **RECON-MAP MOAT.** Multi-lap-within-a-load turns unsolved "no-map racing" into solved "build-then-exploit-map": recon lap → VGGT feed-forward 3D (offline, A100/H100) → Sim(3)-anchor to gate-PnP → frozen metric map ⇒ localization + gate-ordering (= geometry along recon trajectory) + the known-map regime where proven estimators work. Gated on track-fixed-within-load → VERIFY on VQ2 load. [[index-vision-estimator]]
2. **ASYMMETRIC PRIVILEGED CRITIC = the #1 fix.** 6 independent sources confirm symmetric critic = catastrophic; our inc8 symmetric-critic footgun is exactly this. `algo=appo` + state_dim=36; deploy-unobservable signals → critic only. [[index-rl-training]]
3. **Tier-1 build ladder (all inside the existing RL stack, do first):** ① asym critic ② perception-aware reward (PAMPC image-plane centering + anti-blur) ③ velocity-weighted contact penalty (gate-4 under-braking) ④ curriculum reward-weight annealing.
4. **VQ2 vision policy:** pure vision-RL = 0% → MUST bootstrap (state-teacher [inc7/inc8] → DAgger student → adaptive-RL fine-tune, performance-gated; Xing 2403.12203). Add small GRU actor for gate-occlusion memory.
5. **Perception:** DISCARD PnP rotation (planar-square FLIP ambiguity, worst at distance) → attitude from AHRS/filter; 8–12 keypoints + per-corner covariance → EKF R; IPPE_SQUARE; detection filters (dist 1–13 m, skew>2, occlusion).
6. **AHRS (new VQ2 scope):** learned attitude (RIANN GRU / Brossard gyro-denoise) trained on sim GT, OR invariant/Modified-Polar EKF for high-g covariance stability (no Cartesian collapse).
7. **DEFENSE-GNC EDGE (paywalled, robotics doesn't cite — asymmetric moat):** drone-through-gate ≡ strapdown-seeker bearings-only terminal intercept. Gap-Traversal Guidance (closed-form through-gate from vertex bearings, no PnP), bearing-angle/known-size observability (range without lateral maneuver → fly direct), Modified-Polar-Coord EKF (no high-g collapse), tanh/polynomial FOV-constrained shaping (guaranteed keep-in-view, O(1)), stochastic-projection (≡ ADR-VINS). New control fork D4. [[index-strategy-meta]]
8. **RULED OUT (with evidence):** pixels-no-estimator (track memorized in weights → trap under per-load randomization; mine the analytic-mask + asym-critic only); FoundationPose/MegaPose onboard (RGB-D, too slow 8 GB); GS/NeRF onboard reloc (exceeds every validated envelope → GS = OFFLINE sim tool only); optimal-line MPC tracking as deployed controller (infeasible-reference failure; OC offline for the bound only); IMM filter banks onboard (compute-hostile — MSTF single-filter gets most of it).
9. **ESTIMATOR DESIGN (binding-risk core; both GNC scans converge — A/B vs RewindKF, don't adopt blind):** error-state + **direct reprojection innovation (ADR-VINS)** (1–2 corners, no PnP) + **multiple-fading-factor Strong Tracking** (per-state covariance inflation on innovation spike → trust camera over lagging aero model; ABSENT from Western drone racing = edge; use VB-STF to separate maneuver vs false-fix) + **cubature** (positive-weight, mixed-degree) + **square-root/UD** (32-bit embedded stability, sqrtVINS) + **FEJ2/ROBOCENTRIC** observability consistency (robocentric ≈ our gate-relative pivot; kills hallucinated-observability divergence, 32–47% drift cut). Discard-PnP-rotation, attitude from AHRS (+ vision-aided AHRS: gate-corner pixel innovations can correct tilt). **Terminal blind-zone coast** (gate fills FoV in final fraction-sec → vision→belief-state, pairs with GRU actor). Guaranteed FoV via Barrier-Lyapunov / tanh shaping (vs reward-shaped). [[index-vision-estimator]]

**SPIKES that gate the uncertain forks (designs in spike-plans.md — predictions: gate-4 likely CALIBRATION+REACH not SITT; MPPI likely PARKED at racing speed; VGGT likely YELLOW = corridor+ordering win, rims smear):** gate-4 discriminator (2×2 counterfactual: force_accept=R, bias_off=C, reach-only=P + SITT proxy-KL) · MPPI-at-noisy-state-at-racing-speed (paired, asym-critic prereq, 8 GB VRAM kill switch) · VGGT-on-sim-renders (consider DPVO as cheaper first probe) · VQ2-load checks C0–C10. 🚩 **NEW OPEN GATE (load-bearing): is a Training-derived artifact (pre-baked map / Training-calibrated boresight) LEGAL to ship under "code-audit on submit"? If NO → offline-calib + Training-mapping forks DIE regardless of GT exposure. Resolve on VQ2 load.** 🚩 **Sharper gate-4 fact (unmerged branch inc8-deterministic-retrain): AIRTIGHT NEGATIVE — fix-seating ⊥ deterministic flight (fix_rate=0 at every flyable gain).**

**Research-channel model (Fengyou, 2026-06-27):** Gemini Deep Research = breadth/recall (300+ sources incl. paywalled; Fengyou unblocks) → recommends deep-dives; Claude = depth/verify/design (digest, adversarially verify Gemini's citations — several were future-dated/confabulated, design). Stance memory: [[feedback-divergent-research-stance]].

## STRATEGIC PIVOT (2026-06-27, Fengyou) — VQ1 BEHIND US, VQ2 IS THE FRONTIER
- 🚩 **VQ1 is no longer the goal.** Today's asym-critic investigation concluded VQ1-clean RL is TAPPED OUT: the privileged critic (algo=appo, wired+smoke-clean) flew recenter 3/3 but only MATCHED symmetric (~0.50–0.55) — insurance, not a lever, on our engineered 20-dim obs. Even the rw_centering=1.0 "cliff" did NOT reproduce on the recenter recipe (control ppo_rwc1 flew at 0.50 — the S3 collapse was recipe-specific; the matched control caught a false premise). Keep appo WIRED (free, byte-id fallback); its payoff is the harder VQ2 obs, not VQ1. σ_p0 A/B eval auto-firing (job 3290044). → [[index-rl-training]]
- 🚩 **LOAD-DAY READINESS = TWIN-FIDELITY VALIDATION (Fengyou's reframe, load-bearing).** The real purpose of load-day inspection is to verify our **diffaero twin faithfully reproduces VQ2's dynamics** — REQUIRED for RL that transfers. **Telemetry availability is the linchpin** (no usable telemetry ⇒ twin validation much harder), so **C5 (does TRAINING mode leak ODOMETRY/LPN/GATE_INFO GT?) is the PIVOTAL load-day check.**
- 🟢 **Twin fidelity DEGRADES GRACEFULLY, not off a cliff:** even worst-case (competitive wire only, no GT), we always have COMMANDS-in + HIGHRES_IMU-out + VISION-out → a commanded-input→IMU-response system-id + vision gate-trajectory match validates the twin (coarser, workable; = the Cioffi "know our own thrust" lever repurposed). Tier: BEST (Training GT → full state residual + plant calib + labeled data) / FALLBACK (IMU+vision+commands → IMU residual + gate-traj match). Catastrophic case = no Training mode AND useless IMU (C5/C8 reveal fast).
- **NEXT BUILD (launched 2026-06-27, sonnet `loadday-kit`):** pre-stage `scripts/vq2_loadday/` inspection layer (`wire_inventory.py` w/ `--diff TRAINING COMPETITIVE` = C5; `imu_profile.py` = C2/C8; `two_load_hash.py` = C3/C4) + a `twin_fidelity_probe_DESIGN.md` (tiered twin-replay-residual harness, builds on system-id #37 + frame_residual_report.py). → [[vq2-load-checklist]]

## OVERNIGHT PROGRESS (2026-06-28, autonomous)
- ✅ **VQ2 LOAD-DAY INSPECTION KIT PRE-STAGED + COMMITTED (8105f2e), 15 tests GREEN.** `scripts/vq2_loadday/`: `wire_inventory.py` (C0/C1 + the pivotal **C5** `--diff TRAINING_JSON COMPETITIVE_JSON` telemetry-exposure probe + C10), `imu_profile.py` (C2 mag / C8 rate-noise-clip), `two_load_hash.py` (C3/C4 moat falsifier, geometry-vs-appearance separated). All degrade gracefully w/o a live sim; `--tlog` replay supported. Tap pattern: wrap `MavlinkClient.on_message` + `MessageRateTracker` (lives in `racer.firstcontact`, not mavlink_client).
- 🚩 **TWIN-FIDELITY HARNESS = SPEC ONLY, BLOCKED ON FENGYOU REVIEW.** `docs/reactivation-2026-06-27/twin_fidelity_probe_DESIGN.md` — tiered (BEST=Training-GT commanded-replay velocity-residual p90≤1.5 m/s GO / FALLBACK=command→IMU accel-residual + vision-PnP traj match), builds on `frame_residual_report.replay`. **5 review Qs before coding:** (a) how commands are captured in Competitive (debug_obs.jsonl vs tlog), (b) which AHRS filter is active at load (LinearKF/ESKF?), (c) anchor spacing, (d) RecordingReader cv2 dep, (e) calibration write-back path. Do NOT autonomously build until reviewed — morning item.
- 🚩 **OPS GOTCHA (banked): the WORKTREE has NO `.venv`** — use the MAIN checkout interpreter `C:/Users/Fengy/Downloads/Projects/Anduril/.venv/Scripts/python.exe` for pytest/green_gate from inside a worktree. (And `cmd | tail` masks the cmd's exit code → don't gate `&& git commit` behind a piped test; check the test exit explicitly.)

## OVERNIGHT WIND-DOWN (2026-06-28 ~03:00, autonomous) — MORNING BRIEF
**Completed + committed overnight (all on branch `claude/loving-galileo-92f020`):**
- ✅ asym-critic PRODUCTIONIZED to main (`0392b64`): GuardedAPPO(AsymmetricPPO)+AGENT_ALIAS[appo]+env state_dim fix; green_gate GREEN (sentinel 1106≥1091; 1 pre-existing unrelated diagnose_session failure = missing gitignored data, passes on main); symmetric path byte-identical.
- ✅ VQ2 load-day inspection kit (`8105f2e`, 15 tests green) + twin-fidelity design spec.
- ✅ Banked: 3-seed asym verdict (matches symmetric = VQ1 insurance), strategic pivot, twin-fidelity pre-answers.
**🚩 KEY FINDING (from twin-fidelity pre-answers):** current `state_estimator.LinearKF` TRUSTS the given ATTITUDE msg — VQ2 BLOCKS ATTITUDE → **NO self-attitude estimator exists; the ESKF is documented-but-unbuilt.** ⇒ building AHRS (ESKF/learned) is a HARD VQ2 requirement, not optional. **T1 AHRS bench = the priority frontier build.**
**BLOCKED until morning:**
- σ_p0 A/B eval (Adroit job 3290044) RAN but is READ-blocked: the adroit daemon's SSH transport idle-dropped (~02:50) and reconnect needs a Duo push (didn't Duo-spam Fengyou's sleeping phone). **MORNING: restart `serve` in the CMD window → read `/scratch/network/fl3689/inc8_rwc1_sigeval.out` (g4_ip_p99 control vs treatment) → bank Track-3 final verdict.** Data safe on cluster.
- twin-fidelity harness + T1 AHRS bench = design-gated (want Fengyou's input on the 2 judgment Qs + AHRS filter choice). NOT built autonomously.
**WHY WIND-DOWN:** Adroit (needed for σ_p0 read + RL/eval/twin-data) is down till Fengyou restarts serve; remaining frontier builds benefit from his design direction; no agents running. Budget-honest to consolidate vs spin. Resume on his morning message.
**SUGGESTED MORNING ORDER:** (1) restart serve + read σ_p0 → close Track 3; (2) decide AHRS filter direction → build T1 AHRS bench; (3) answer twin-fidelity Qs (c)/(e) → build the harness.

## AHRS BENCH = SCAFFOLDED BUT RED (morning debug) — 2026-06-28
T1 AHRS bench first slice committed WIP (RED). Structure complete: `src/racer/ahrs/{imu_gen,eskf,classical,metrics}.py` + `scripts/benches/ahrs_bench.py` + `tests/test_ahrs_bench.py` (21/25 pass). **DO NOT trust the high-g verdict yet.** Morning debug, in order: (1) FIX the geodesic-metric **q/−q double-cover bug** (test_q_and_neg_q_give_zero_distance — suspected missing `abs(dot(q1,q2))`; likely pollutes ALL attitude numbers), then re-run; (2) re-judge ESKF — currently inconsistent (beats classical 9.6× on HIGH_G_PULL but LOSES 0.2× on HIGH_G_RANDOM where Madgwick hits 0.3°) → after the metric fix, debug the ESKF accel-gating/tuning if it still loses; (3) Mahony static/spin tolerances. The framework is reusable; real twin-GT IMU plugs into `imu_gen`'s interface later (Adroit). Learned/invariant filters = stubs, Fengyou's call.

## STRATEGIC PRINCIPLES (2026-06-28, Fengyou) — DETERMINISM + AHRS-EVERYWHERE
- 🚩 **EXPLOIT DETERMINISM (the asymmetric edge our refs can't use):** VQ2 is a DETERMINISTIC sim — same inputs → same outputs. Most prior art is sim-to-real (fights sensor noise / unmodeled dynamics / latency jitter via heavy domain randomization + robustness margins). **We don't have that problem.** ⇒ (1) **TWIN CALIBRATION = the single highest-leverage investment** — exact system-id is TRACTABLE because deterministic (no noise to average out); an exact twin match → policy transfers EXACTLY, no robustness tax. (2) **REDUCE domain randomization** — our recipe's dr_aero/dr_mixer/dr_force_bias/dr_latency is a sim-to-real reflex that BLURS the policy under exact-match → a real fork: **robust-DR vs exact-match** (tighter/faster). (3) AHRS/estimator tune to the EXACT sensor model, not a noise envelope; eventually against EXACT captured VQ2 IMU sequences. (4) Residual hard problem narrows to per-load TRACK randomization (the moat), NOT dynamics robustness. CAVEAT: "exact" assumes we can replicate VQ2's integrator/timestep — that's what the twin-fidelity probe GATES. If not exactly matchable, some DR stays as insurance. This is the concrete form of Fengyou's "don't train in the wrong environment" risk → twin-fidelity validation is the gate, determinism makes passing it tractable.
- 🚩 **AHRS IS FIRST-CLASS IN EVERY FORK (Fengyou directive):** every candidate flight stack MUST name its L1 attitude solution (current LinearKF can't run without the VQ2-blocked ATTITUDE). Options w/ refs: classical (ESKF/Madgwick/Mahony) · learned (RIANN GRU / Brossard — Chen Oxford DPhil'20, EqNIO NeurIPS'24) · invariant (IEKF/EqVIO — van Goor ANU PhD'23; Modified-Polar EKF high-g) · vision-aided (gate-corner tilt fix). T1 AHRS bench is the selection vehicle (fix+IEKF in progress).
- **PHASE SHIFT:** moving from DIVERGENT (list all researched/feasible ways) → CONVERGENT (pick & assemble the optimal stack). Leading strawman assembly: L1 ESKF→eval invariant/learned · L2 YOLO-pose + discard-PnP-rotation + bearing-angle range · L3 gate-rel RewindKF + only twin-residual-demanded menu items · L3.5 online recon-map (pending moat load-check) · L4 RL/CTBR + asym critic + GRU + reduced-DR. → [[master-plan]]

## L1 AHRS DECISION (2026-06-28) — ESKF picked, evidence-backed (T1 bench GREEN, ebacf15)
- **PICK: ESKF as the VQ2 AHRS** (the simplest correct winner). T1 bench (`src/racer/ahrs/`, `scripts/benches/ahrs_bench.py`, 33/33 tests) on synthetic high-g IMU: ESKF/IEKF p90 ~6.5° vs Madgwick 27° / Mahony 82° (HIGH_G_PULL); ESKF 0.67° vs Madgwick 0.88° / Mahony 5.8° (HIGH_G_RANDOM). The bench promoted NOTHING fancier than ESKF — exactly as the min-sufficient-complexity principle predicts.
- 🚩 **KEY MECHANISM (non-obvious):** the high-g win is the **χ²(3,.95)=7.815 INNOVATION gate, NOT accel-magnitude gating.** Ablation (HIGH_G_RANDOM p90): no-gate 55.9° · mag-gate-only 66.7° (useless — 43% of samples have ‖a‖≈g but direction-corrupted) · innovation-gate 0.86° · both 0.65°. Same χ² family as our C2 relinnov gate → estimator consistency across L1/L3.
- **IEKF = a deliberate NULL result (and that's correct):** left-invariant SO(3) (Barrau-Bonnabel / van Goor EqF) CONVERGED (recovers 80° tilt <1 s) but matches ESKF to ~1e-11° on attitude-only — the invariant consistency edge only materializes on the coupled **SE₂(3)/INS-VIO** problem, not attitude-from-gravity. ⇒ IEKF is the GROWTH PATH to EqVIO when we couple attitude+position, not a present win. Cross-validates the ESKF.
- **3 bugs fixed building it:** geodesic-metric input normalization (q/−q now pins ~0), Mahony specific-force-vs-gravity sign (static 30°→0.07°), ESKF Jacobian g-scale (rotation 15°→0.07°).
- **LEFT:** real twin/VQ2 IMU integration via the `imu_gen.IMUSequence` seam (HIGHRES_IMU; GT from ODOMETRY offline — R_y(π) conjugation footgun); learned AHRS (RIANN/Brossard) needs captured data. Determinism: final tuning vs EXACT captured VQ2 sequences, not the synthetic noise envelope. → [[index-vision-estimator]]
