---
name: trackA-stall-forensic-2026-07-13
description: "7-agent forensic (wf_df3f628d) on WHY Track-A 8-gate improvement stalled at ~0.33 success / exit_frame ~0.63 — critique-adjusted verdict + ranked levers + the checkpoint-selection footgun."
metadata:
  node_type: memory
  type: project
  originSessionId: c864e5bb-f53f-4b54-b78a-a2216bb2c906
---

# TRACK-A STALL FORENSIC (2026-07-13, wf_df3f628d-e27; 5 dims → synthesis → adversarial critique)

Data staged local: full TB (5 runs × 66 tags), m8 rollout JSONL (8.8 MB, 24k pass rows), reward/obs/train code from chaum. All single-seed (seed 0) — the audit's ~2.8× variance rule applies to every magnitude below.

**HEADLINE: "0.33" is not one wall — it's FOUR stacked effects, and the two most fixable are things WE did or MISMEASURED.** exit_frame = **95.8% of all deaths** — the ceiling is acquisition/frame-retention, full stop.

## SURVIVING conclusions (robust even at n=1 — corroborated across dims)
1. 🎯 **LATE-TRAINING REGRESSION on ALL 8-gate runs** — they PEAK then decay 24–42%: m8b **0.458→0.326**, rs0 0.396→0.231, m8 0.284→0.217; 2-gate w0/w1 stay flat (−3/−5%). **The published/flyable ckpt = the END-OF-RUN (degraded) one.** `best/actor.pth` (18:50) ≠ `checkpoints/actor.pth` (19:50, = what shipped). Pulled best → `scratchpad/ckpts/vtrackAm8b_best_actor.pth` sha dcbbf753 (final = 481265fb).
2. 🔴 **8-gate-specific CRITIC NON-CONVERGENCE** = the differentiator: critic_loss late10 **141/207/225** (m8b/rs0/m8) vs **36/44** (w0/w1); grad spikes 670–1519 vs 296–467. Return scale ~4× (banked_prog ~100 vs ~23) unnormalized + γ=0.9975 long horizon ⇒ noisy value → noisy advantages → drift off the peak.
3. 🟡 **rs0 = REWARD-HACKING** (seed-independent tell, NOT the invalid value-compare): within-run finish_reward/step **0.072→0.031 (−57%)** while total_reward ROSE 0.655→0.698; exit_frame reverted 0.631→0.733. Culprit = **`rw_exit_align=4.0`** — pays exit-velocity HEADING (a proxy satisfied even when the next gate is never acquired), and it's **not covered by the farm-neutrality `__post_init__` assert**. yaw_dither 0.125 was a correct 1/clamp² rescale (KEEP).
4. ⚪ **Entropy runaway REFUTED as the cause**: entropy_loss rises 2.8→8.3 IDENTICALLY on the successful 2-gate runs (0.85). Red herring for the ceiling (at most a co-driver of drift). The noise_anneal/logstd ceiling (`apply_noise_schedule`) is built but OFF in every run.
5. 📐 **20-GATE MATH (robust)**: best current per-gate ~0.91 ⇒ 0.91²⁰ ≈ **0.06** on 20 gates; 0.9-success needs per-gate **0.9947**. 8-gate episode-success is an exponent-dominated, gate-BLIND selection target ⇒ eventually needs a REGIME CHANGE (per-gate hazard target + mid-course-spawn curriculum), not tweaks.

## CRITIQUE'S CORRECTIONS (where the synthesis was wrong/over-weighted)
6. 🟢 **The ceiling is NOT mainly horizontal-KINEMATICS** (synthesis over-weighted θ_feasible=ω·d/v). Trace split of exit_frame deaths: only ~35% horizontal-edge; **38% IN-frame (perception/estimator dropout)**, **19% pure VERTICAL** (tight ±29.4° V-FOV as the drone pitches down — yaw-immune). And it's all m8-regime (0.35 clamp); the vcap **DID bind** on m8b (mean_speed 9.41→7.66 ⇒ crossing ~9, not 7.6, not 11). ⇒ likely true residual = **PERCEPTION dropout + vertical framing**, not slew-rate.
7. 🚩 **CROSS-OFFSET accumulation = CONTESTED (do NOT act yet).** Synthesis: cross-offset accum drives a declining per-gate curve (0.886→0.705) ⇒ strengthen centering. Critique GUTTED it: r=0.94 over only n=8 gates, a **2.5 cm** mean shift (0.186→0.212) "tripling" hazard, absolute death counts ~FLAT ~500/gate (equally consistent with intrinsic deep-gate difficulty) ⇒ likely a PROXY for a latent (lateral velocity/energy). **Strengthening centering is the lever MOST likely to reward-hack like rs0** (same family, re-touches the exact knob rs0 moved). HOLD until data.

## THE TWO CHEAP EXPERIMENTS THAT RESOLVE THE UNKNOWNS (zero new training)
- **(a) DET_EVAL m8b `best/` vs `checkpoints/` (final)** — is the "+~13 free points" real or a stochastic-noise-max artifact (critique's caveat)? Also decides which to fly. Fengyou A/B'ing best-vs-final in the real sim IS this DET_EVAL.
- **(b) m8b rollout trace at the PEAK ckpt, WITH per-frame detection-confidence + lateral-velocity fields** — adjudicates perception-vs-kinematic-vs-cross-offset at m8b's ACTUAL regime (0.7 clamp, capped), and stops the next reward arm from hacking. The single highest-value missing artifact.

## ACTION LADDER (critique-adjusted; select on exit_frame/per-gate HAZARD, never on value; multi-seed before banking any recipe)
1. **Checkpoint selection** — DET_EVAL best vs final; deploy the better; add rolling-100-best-on-success ckpt tracking. LOW effort, possibly free +pts. (Confirm deployed==final first.)
2. **Stabilize the 8-gate critic** — value/return normalization (running-return std) + tighter critic grad clip. Attacks the robust late-regression. Respects γ=0.9975 (do NOT change γ). LOW.
3. **Keep m8b the CLEAN base** — revert rs0's shaping; no next-gate reward until obs-v2 can represent an out-of-frame next gate; add exit_align to the farm-neutrality monitor. LOW.
4. **obs-v2 (21→23, zero-pad warm)** — continuous signed next-leg bearing MAGNITUDE (course-geometry, available out-of-frame) replacing the sign-only coarse sector; recovers the sharp-turn tail + UNBLOCKS any next-gate reward. Won't break the ceiling alone. MED. = task #41.
5. **Perception/vertical** — if the m8b trace confirms perception dropout + V-edge dominate, lever = rw_perception / less head-down (NOT a cap), watch for the speed double-count. Data-gated on (b).
6. **Regime change for real 20-gate** — mid-course spawn curriculum (spawn at gates 4–7) + per-gate hazard objective. HIGH; needs arrival-state distribution match. Delegate to agents.

🚩 **HELD / do-not-do:** strengthen centering (reward-hack risk, contested causation — needs (b) first); any next-gate reward pre-obs-v2 (rs0 proved it hacks); push yaw >0.7 (horizontal ~closed); select/shape on VALUE.

## REBUILD DIRECTIVE (Fengyou 2026-07-14) — "find bugs, rebuild, retrain, LIMIT VELOCITY TO 5 m/s"
- He could NOT get a valid record run. 🚩 **SPEED RUNAWAY is DEPLOY-WIDE (all models): aw0→~14 m/s, am8→~11 m/s by GATE 1 — both overshoot training (~8.3/~9).** Mechanism: `velocity_cap_penalty` is a SOFT reward penalty, ZERO below v_cap_soft=9 (free band) ⇒ training never slow; it's a REWARD ⇒ absent at deploy; policy eases only on OBSERVED ‖v‖ (obs[0:3]). Runaway = deploy-side: **(a) deploy vel-estimate reads LOW** (doesn't feel fast) and/or **(b) control latency** overshoots. ⇒ **a naive "lower soft cap to 5" will NOT transfer.** Fix = penalize TRUE speed (GT-legal in reward) AND/OR a DEPLOY GOVERNOR (throttle when observed>cap) AND/OR match the vel-KF fidelity. 🎯 DECISIVE DATUM = a fast-flight `ego_obs.jsonl`: observed obs[0:3] speed vs comp-reported true (40–50 kph) = the bias, sizes the governor. 5 m/s = BANK-FIRST slow-but-valid (speed ramps later).
- 🚩 **OSCILLATION is SEPARATE from speed:** 8-gate-specific (survives the deploy coarse-map fix + am8@0.35 oscillates but w0@0.35 does NOT ⇒ the 8-gate GAIT), = the sign-only-map bang-bang yaw (§CRITIQUE #4). Speed runaway is deploy-wide; oscillation is 8-gate-gait. Two problems.
- 🟢 **DESIGN WORKFLOW RUNNING (wcj8x1daj / wf_82fccdab-437):** velocity-transfer design + reward/obs/train bug-sweep → recipe synthesis → adversarial verify. Output → recipe (exact overrides + code + bundle-vs-stage + multi-seed plan). NEXT: verify → delegate build to agents → launch MULTI-SEED (≥3) on Adroit. best/ ckpt staged (`scratchpad/ckpts/vtrackAm8b_best_actor.pth`) for the free A/B.

## EMPIRICAL SHADOWPC LOGS (21 panel_run flights, 2026-07-14) — `scratchpad/analysis/shadowpc_logs/` (from origin branch `claude/ego-deploy-2026-07-09-shadowpc:data/runs/`)
- 🟢🟢 **KEY CORRECTION — the deploy estimator does NOT badly under-read: observed speed ≈ true (k≈1).** aw0 obs-speed MAX ~13 m/s = ~50 kph = matches Fengyou's comp reading. The "policy sees 9 at true 14" (k=0.64) hypothesis (workflow + code-reasoned) is EMPIRICALLY REFUTED. ⇒ **a deploy governor at observed-5 holds TRUE ~5** (the adversarial verify flagged a sign-error assuming k<1; the LOG resolves it: k≈1, soft=5 is right).
- 🟢 **The vcap TRANSFERS:** m8b (vcap soft9) obs-speed ~8 vs no-cap aw0/aw1/am8 ~11–39 (am8 had a 39 m/s runaway). So retraining with a LOWER vcap is a real lever. Runaway cause = no-cap models never learned to brake + NO deploy governor exists (confirmed: fly_rl policy_step has only a max_thrust OOD start-cap + rate/attitude clamps; the sole ‖v‖ read drives the takeoff handoff, not thrust).
- 🚩 **LOOP JITTER severe:** median ~45 Hz but slow-tail (p90 work_ms) ~16–19 Hz, **~45% of ticks over the frame budget.** dt-sensitive (ticks_hi=1) ⇒ partly OOD; feeds wobble + brake-lag. ShadowPC lever = detector-variance / orphan-pilot hygiene. All 21 flights CRASH/STALL, max gate reached = 3.
- 🚩 **OSCILLATION in the data:** commanded-yaw sign-flips/s — 8-gate/shaped rs0 3.4 · m8b 2.8 · am8 2.6 vs 2-gate aw1 **1.3** (aw0 2.8 but crashed <4 s). Analysis script `scratchpad/analyze_shadowpc.py`.

## REBUILD RECIPE = `vtrackAr5` (design wf_82fccdab; CONDITIONAL-GO w/ must-fixes) — full → `tasks/wcj8x1daj.output` + `tool-results/bxg5tb9mk.txt`
- **VELOCITY (layered):** PRIMARY = ✅ **deploy governor LIVE — commit `293ffee` on `claude/ego-deploy-2026-07-09` (pushed)**, `--ego-speed-gov 5,6.5` (soft,hard m/s observed HORIZONTAL ‖v‖; caps collective toward hover, altitude-neutral, default-off per-ckpt; logs `gov`/`gov_engaged`; 11/11 tests). 🚩 ShadowPC on fork `-shadowpc` (80dd3a1) must **merge origin/claude/ego-deploy-2026-07-09** (or cherry-pick 293ffee) to get it → fly any model + `--ego-speed-gov 5,6.5` = caps at ~5 NOW, no retrain. SECONDARY = lower TRUE-speed hinge `rw_v_cap_soft 9→4 / hard 12→5.5` + `rw_finish_time 1.0→0.25` (m8b proves vcap transfers). Governor soft=5 (k≈1 ⇒ holds true~5; NO k-correction needed per the logs).
- **STAGE-1 BUNDLE (clean base, warm vtrackAw0):** governor + **rolling-best-on-`n_passed_gates` ckpt selection + DET_EVAL** (fixes shipped=degraded-final, task: even w0 shipped degraded) + **critic-only grad-clip(10) + return-norm (PopArt)** (fixes 8-gate critic non-conv) + **noise-anneal ceiling ON** (stops logstd 2.8→8.3) + **FULL rs0 revert** (exit_align→0, perception_next→0, perception→0.02, centering→0.0, KEEP yaw_dither=0.125) + **≥3 SEEDS**, select on DET n_passed_gates + hazard NEVER value.
- 🛑 **MUST-FIX before launch (adversarial verify):** (1) `cross_zero_m=4.0` is a BUG — parabola varies only 3.5% across the aperture ⇒ no centering gradient ⇒ near-frame passes that DQ; set to the true gate half-opening (~0.75 = `gate_half_opening_m`, already in code — FALSE blocker). (2) **DE-BUNDLE return_norm** (structural PopArt at an UNKNOWN diffaero-agent site — confirm or use reward-scaling fallback; run ≥1 seed with it OFF to bracket). (3) BRACKET the velocity hinge (≥1 seed at soft=9). (4) governor = HORIZONTAL-only ‖v‖ + instrument speed/thrust/altitude through the band (porpoising/lag-oscillation + cos θ altitude-sag risk). (5) verify realized TRUE mean ≤5 in a smoke before 3 seeds. (6) resolve warm-base (vtrackAw0 best 0.884 vs degraded final 0.851). (7) confirm train-KF vs deploy-KF cov-floor PARITY (symmetric only).
- 🔵 **STAGE B (deferred):** obs-v2 continuous signed turn-MAGNITUDE coarse map (21→23, zero-pad warm, deploy-parity table) = the real oscillation fix — warm from Stage-1 ONLY if it still oscillates at 5 m/s (slower speed may subside it). = task #41.

## 🚩🚩 TRACK-A CODE-BASE FOOTGUN (2026-07-14 — forget = disaster)
- **THE Track-A training code = commit `629d2a2`** ("feat(ego Track A): annealed yaw-dither + 60° att-cap + full-lineage stage") = **byte-matches Adroit `/scratch/network/fl3689/peregrine_repo`** = what m8b/rs0/w0/aw1 ACTUALLY trained from. It HAS the `dual_gate_fullstack_floor_pef_trackA` stage + `v_cap`/`perception_next`/`yaw_dither` reward fields.
- 🛑 **origin `claude/optimistic-chaum-6c893b` HEAD (`f7b1be1`→now `f1a7eed`) is 9 COMMITS BEHIND 629d2a2** — it LACKS the trackA stage + those knobs. The Track-A lineage (629d2a2 + `04a9a63` anti-vel-runaway + `661346b` pefcap + `7be9979` nodither + controls) was committed LOCALLY + deployed to Adroit but **NEVER pushed to origin chaum**. So origin chaum ≠ the real Track-A code.
- 🛑 **ALL Track-A rebuilds/deploys MUST base on 629d2a2** (= Adroit peregrine_repo), NOT origin chaum HEAD. Deploying an origin-chaum-based build to Adroit would DELETE the trackA scaffolding (unknown-stage failure). The FIRST vtrackAr5 build (`f1a7eed`) was on the WRONG base (origin chaum) — do NOT deploy it. Caught BEFORE deploy via sha-match (Adroit ego_reward `34d90efa` == 629d2a2). Deploy-verify rule: sha-match the Adroit peregrine_repo file against the intended base commit BEFORE pushing.
- ✅ **CORRECTED build DONE: `39d3757` on `vtrackAr5-base` (parent 629d2a2), pushed to origin** (Track-A lineage + Stage-1 fixes now ON origin — hygiene closed). All 4 changes ported; tests 159+10 pass. cross_zero_m 4.0→**0.75** via `++env.rw_cross_zero_m=0.75` (rw_-key wins from_cfg precedence; the 4.0 is the trackA stage dict at vq2_ego_curriculum.py:1569); finish_time via `++env.rw_finish_time=0.25`. Deploy = push the delta onto Adroit peregrine_repo (clean, same base) → PRECHECK smoke (≥50 upd, save_freq~25, to see BEST_CKPT) → Fengyou green-light → 3 seeds + hinge9 bracket.
- 🛑🔎 **CORRECTION — TWO recipe tokens were INERT NO-OPS on the real Track-A base (629d2a2), and were inert for m8b/rs0/w0/aw1 TOO** (no read site in 629d2a2): (a) **`course_lateral_offset_lo/_hi`** — the randomized xy lateral shifts were **NOT applied**; the 8-gate course had NO xy variety (contradicts prior memory + task #40). (b) **`ego_coarse_map_live`** — no read site; the "live/rolling coarse map" gating isn't in 629d2a2 (🚩 VERIFY whether the training coarse map is stale-once-at-init or built-at-reset — bears on the oscillation/acquisition diagnosis). ⇒ m8b trained on a LESS-varied course than believed. For vtrackAr5 this is FINE (clean delta = same inert tokens as m8b). If we WANT xy course variety / true live-map, that's SEPARATE CODE (a follow-on, do NOT bundle).

## 🟢 RUNTIME 2026-07-14 PM — DEPLOY DONE, SMOKE PASSED, GOVERNOR REJECTED, MAP-FIX IN FLIGHT
- **DEPLOY (39d3757 delta → Adroit peregrine_repo, content-exact):** rl/{ego_reward,peregrine_train_ego}.py + launch_vtrackAr5{,_hinge9}.sh. 🚩 **DEPLOY-VERIFY LESSON: raw sha256 mismatch was PURE CRLF** — Adroit .py are LF-normalized-identical to 629d2a2 (base confirmed) and the deploy is `39d3757`-content in CRLF (Python-transparent, = every prior trackA deploy); `.sh` land LF via `.gitattributes *.sh eol=lf` (bash-correct). **Deploy-verify must compare LF-NORMALIZED content, not raw sha** (git-archive CRLF-ifies .py on Windows; .sh stay LF). Tools: `scratchpad/push_file.py`/`.s15_ref/pull_file.py`; daemon `x` via **single-quoted PowerShell arg, no `$`/quoted-spaces/`&&`** (PS pre-mangles) — or call adroit.py from Python (push/pull do) to preserve the string.
- **SMOKE PASSED (job 3309251, COMPLETED 0:0, 100 upd, save_freq=25, warm aw0 final):** every Stage-1 fix wired — `[ckpt-select] ON`+best_npg/ dir, BEST_CKPT@25/50/75/100 (roll 0.83→1.05), `[critic-grad-clip] ON`, noise-anneal std_ceil 0.6→0.08 (anneals), all reward reversions+`rw_cross_zero_m=0.75`+hinge live. DET n_passed_gates=2.01 (unconverged smoke, collision~0.97 expected). Real log = `exec >`-redirect to `/scratch/network/fl3689/peregrine_vq2_ego_<RUN_SUFFIX>.out` (NOT slurm-<id>.out, which only holds the sbatch trace).
- 🛑🛑 **SPEED-GOVERNOR (293ffee) REJECTED (Fengyou) + being REMOVED → [[feedback-no-deploy-bandaids]].** Bandaid; empirically hits gate SIDES (collective-starve in turns). ROOT-CAUSE of "slow-train/fast-sim": NO gap for CAPPED models (m8b 7.66→~8 transfers); fast flights = NO-CAP aw0/am8 (`rw_v_cap=0`) + short 2-gate course masking top speed. `rw_v_cap` = soft penalty on **GT/TRUE speed** (ego_reward.py:376-391) via asym critic = correct design; NO speed-clamp flag exists. vtrackAr5 soft4/hard5.5 = the ONLY velocity fix (no governor).
- 🚩 **WARM BASE = aw0 FINAL (0.851):** aw0 has NO recoverable peak (only checkpoints/=periodic/=final + periodic_prev 1-interval-back; predates rolling-best). 0.884 peak was overwritten. Fine — logstd resets, 12k re-optimizes, vtrackAr5 saves its own best_npg/.
- 🚩🔧 **STALE-COARSE-MAP BUG (confirmed by code read) + ckpt-promote path bug → FIX branch `vtrackAr5-mapfix` (agent building):** `_coarse_map = build_coarse_map(gate_pos,spawn_pos)` built ONCE in peregrine_racing_ego.py `__init__` (~L792); `reset_idx` (~L901) resamples the course per-ep + rebuilds the racing line but NOT the coarse map; `ego_coarse_map_live` flag INERT (no read site) ⇒ ep-2+ policies get ep-1's turn hints = wrong acquisition prior (build_coarse_map sector = "where to look for the next gate before visible" ⇒ directly feeds the exit_frame=95.8%-of-deaths acquisition wall). m8b had this too. FIX = rebuild `_coarse_map[env_idx]` in reset_idx (mirror racing-line rebuild, same cfg deadbands). Bundled: ckpt-promote resolves checkpoints/ wrong ("dir not found near RUNDIR" — resolve as sibling of best_npg/). Fixing the map in TRAINING is itself the anti-bandaid move (kills the deploy hand-authored-map crutch). NEXT: verify agent → redeploy delta (adds peregrine_racing_ego.py) → re-smoke → launch 3 seeds+hinge9 (reward-penalty only, no governor).

- ✅ **MAP-FIX SHIPPED + RE-SMOKE PASSED (2026-07-14 PM):** coarse-map rebuild-on-reset + ckpt-promote path fix = `9ff7672`, tests 9+40 pass; DEPLOYED+sha-verified to Adroit. **Sizing: 56% of horiz turn hints (4.48/8) were WRONG per episode pre-fix** — strong support that the stale map drove the 8-lineage yaw oscillation. Re-smoke (job 3309311, COMPLETED): fix runs clean on GPU, promote fix WORKS (`best_npg=2.49 vs final=2.50 → KEEP final`), DET n_passed_gates **2.01→2.50 (+24%)** vs stale-map smoke @100 upd (encouraging, not conclusive). 🚩 **ESTIMATOR STALE-GEOMETRY (2nd, LOW-severity) FOUND+FIXED `4c526f4`:** estimator gate_pos/gate_yaw are LIVE references (main rel_pos nav signal FRESH — `.to()` no-op via `_ego_dtype=gate_pos.dtype` tautology + in-place resample) BUT `R_world_gate` (gate frames from yaw) was built once in `__init__`, never rebuilt on reset → stale after per-ep yaw resample; 2nd-order (only rotates injected perception-noise/bias anisotropy, NOT the nav vector; rel_normal/rel_yaw are dead) ⇒ m8b still flew. 1-line RNG-neutral refresh in `BatchedEgoEstimator.reset_idx`. DEPLOYED+verified. **All 3 fixes now on Adroit; training code clean of stale-geometry.** NEXT (⏳ AWAIT FENGYOU GO): 1-seed smoothness probe (read training yaw-dither @~2-3k vs m8b) → fan to 3 seeds+hinge9 if smooth, else pivot to obs-v2 (#41, sign-only→continuous bang-bang). 🚩 **DEPLOY FLAG BLOAT (Fengyou 2026-07-14): 14 of his ~40 fly_rl flags are pure DEFAULTS (droppable, zero-change); 4 real bandaids (pitch-clamp→rw_perception, sector-map+coarse-map→map-fix/vision, gate-z-bias→bake) vanish as training absorbs them → target ~6 flags.**

## 🟢🟢 BASE PIVOT aw0→vpeffs0 (Fengyou flight-verified 2026-07-14 PM) — new lineage `vpefcap`
- 🚩 **THE YAW-OSCILLATION IS SPEED-MASKED, not absent.** Fengyou flew the models and reported: aw0 with NO pitch-cap "looks smooth" ONLY because it runs away too fast to SEE the oscillation; WITH pitch-cap (slowed) aw0 visibly oscillates yaw. So the pitch-cap REVEALS (by slowing) — it does NOT cause. vpeffs0 is genuinely smooth even WITH cap (smooth when slow). vpefwh2 (boot) oscillates; aw1 (trackA+anti-dither+**yaw 0.7**) is SMOOTH; am8/am8b/rs0(near-gate) oscillate. ⇒ **(a)** anti-dither is NOT the cause (aw1 has it, smooth) — my earlier "anti-dither backfired" was WRONG; the cause is **tight yaw-0.35 clamp + undertrain + the 8-gate multi-gate bang-bang**. **(b)** Our velocity cap (~5 m/s) REMOVES the speed mask ⇒ the base must be genuinely smooth WHEN SLOW ⇒ **vpeffs0** (the only proven one). aw0 imports the (masked) oscillation ⇒ **vtrackAr5 warm-aw0 ABANDONED.**
- ✅ **`vpefcap` recipe (`launch_vpefcap.sh`, deployed):** warm **vpeffs0** (`.../ego_dual_gate_fullstack_floor_pef_seed0_vpeffs0/checkpoints`) · stage **`dual_gate_fullstack_floor_pef`** (vpeffs0's own — NO att-cap/anti-dither/fast-spawn; yaw 0.7; ticks_hi=1; blur off; spin_abort_anneal; logstd 0.18) · add ONLY **velocity cap** `rw_v_cap 3/soft4/hard5.5` (reward-only, never touches yaw) + `rw_cross_zero_m 0.75` + `rw_finish_time 0.25` + Stage-1 infra (ckpt-select/critic-grad-clip 10/noise-anneal/eval_det 600) + **course_n_gates=8** (clean A/B vs m8b) · fixed map+estimator ride along. Overridable env: `UPD`/`SEEDS`/`SAVEFREQ`/`NGATES`/`WARM`. SMOKE=`UPD=100 SAVEFREQ=25 SEEDS=0`. 🎯 the probe TEST = does vpeffs0+cap+8gate+fixed-map stay smooth (Source-2 multi-gate is the only remaining oscillation risk; vpeffs0+cap@2gate is already smooth per Fengyou). DEPLOY: NO pitch-cap (it only masks), NO governor.

## 🟢🟢🟢 RECOVERY PIVOT (2026-07-14 late) — the failure is RECOVERY, not speed
- 🛑 **vpefcap velocity-cap soft4 FAILED (killed @~8.4k upd):** held mean_speed **3.3 m/s** (cap worked, vcap_pen 2.18→0.016) BUT n_passed_gates **0.19** (stochastic TB), collision **99.7%**, exit_frame ROSE **73→91%** as it slowed; the cap STARVED the climb (body-up vel the up-legs need). ⇒ **5 m/s is BELOW the acquisition/regime floor; the "reduce speed to 5" goal is RETRACTED.** (slower = the gate has MORE time to drift out of the ±45°/±29° frame = worse acquisition.)
- 🚩 **Fengyou 386-flight lab analysis:** (a) variance is **CONFIG-INDEPENDENT** — same config ×19 → gates {0×1,1×7,2×4,3×3,4×2,5×2}, std 1.44 = initial-condition sensitivity, NOT a knob. (b) perception/takeoff/detection **RULED OUT** (detector locks gate0 conf 0.99 in BOTH good+bad runs). (c) failure = bank/speed/altitude **ACCUMULATE gate-over-gate (peak roll 22°→70°) → diverge into a wall; 100% collide** ("reaching gate N" = accumulated less first). (d) the course **DEMANDS the aggressive regime**: g2→g3 turn = **50-61° bank (p90 71°)**, up-legs = hard climb, far gates = speed. (e) roll cap 20-25° makes the 61° turn IMPOSSIBLE (dies at gi=3); speed-gov 2.08→1.61 gates (fires @~17° bank, starves climb). ⇒ **CAN'T CLAMP OUT — any fence tight enough to catch the runaway kills the maneuver.** 🚩 **RULE: fence only the axis the course DOESN'T use** — pitch = free discipline (no steep nose-down needed, in recipe); roll/speed = NO.
- ✅ **THE LEVER = train RECOVERY into the regime** (bank-then-LEVEL, climb-then-HOLD, de-accumulate between gates). Reward built: branch `vtrackAr5-recovery-reward` **`9711b4c`** (off 4c526f4, default-OFF, tests 8/8, deployed+sha-verified — ego_reward+peregrine_racing_ego+peregrine_train_ego+vq2_ego_curriculum). **form A** `recovery_roll_penalty` = −rw_roll_recover·roll²·exp(−(θ/θ0)²), θ = **TRAVEL-relative** horiz gate bearing (NOT nose — perception-yaw coupling keeps the nose ON the gate + this lineage flies **TAIL-FIRST**, so nose/body bearing would punish the turn). w→1 lined-up (price sustained bank), w→0 off-axis ⇒ **61° turn FREE by construction (w≈0.018 @60°off)**. **form B** `cross_level_penalty` = per-crossing roll² (off). Knobs `rw_roll_recover`(0)/`rw_roll_recover_theta0_rad`(0.524=30°)/`rw_cross_level`(0), annealed `recovery_anneal`/`recovery_start`/`recovery_hold_frac` (mirrors att_cap). 🚩 arm via EXTRA on the **CLEAN `_pef` stage**, NOT the agent's `_recover` stage (it's trackA-based = att-cap+anti-dither+yaw0.35, muddies the A/B).
- 🔬 **A/B RUNNING (both warm vpeffs0, `_pef` stage, NO cap, 8 gates, differ ONLY in the recovery term):** `3309476` (baseline, no recovery) vs `3309479` (+recovery form A, rw_roll_recover=0.05, θ0=30°, annealed). `launch_vpefcap.sh` env-overridable: `RECOVER`(roll-recover weight, 0=off)/`RWVCAP`(0=cap off)/`VSOFT`/`VHARD`/`NGATES`/`TAG`/`WARM`. First sweep if it works: rw_roll_recover 0.03–0.1, θ0↑~0.6 if the turn is over-taxed. **MONITOR VIA TB, not stdout** (SLURM block-buffers stdout — the .out froze at PRECHECK while the run was fine): snapshot the live event file on Adroit (`cp` — it grows) → `pull_file.py` → `scratchpad/tb_parse.py <ev>` (dep-free TFRecord/protobuf reader, no tensorboard needed). Progress signal = periodic/ saves + TB, NOT the buffered .out.

## 🟢🟢🟢 `vpef8nc` BASELINE DONE = BEST 8-GATE MODEL YET (2026-07-14 night)
- **`3309476` (vpef8nc) COMPLETE 12k:** warm vpeffs0, `_pef` stage, **NO velocity cap**, 8 gates, natural ~9.5 m/s. **DET n_passed_gates 5.33 / 43.6% det-success** (stochastic late: 5.8 gates / 47.7% success, collision 100%→52%, exit_frame 0.94→0.47) — **BEATS m8b (0.33 success, ~5 gates, 7.6 m/s) on EVERY metric AND at higher speed.** ⇒ **THE WINNING RECIPE = NO cap + vpeffs0 base + multi-gate.** The velocity cap was the whole problem (starves the regime); dropping it + the vpeffs0 smooth base + 8-gate chain-pressure = the leap.
- 🔑 **Multi-gate training ALONE (no recovery reward) taught MOST of the recovery:** peak_roll rose to ~70° early then **SETTLED ~60°** (the turn requirement — NOT diverging in training); collision fell 100%→52%. So the chain pressure itself is a strong recovery teacher; the recovery reward is now a **+delta** question, not the whole fix.
- 📦 **PUBLISHED `ego-ckpts-vpef8gate-2026-07-14`** (Hat000/Peregrine; `gh release download`). Deploy = `--ego-yaw-clamp 0.7 --ego-sector-mode map --ego-coarse-map <json> --ego-slot1`, **NO pitch-cap, NO governor, NO roll-cap** (+ optional `--ego-gate-z-bias 0.1` for the climb). Flying HEAD-TO-HEAD vs vpeffs0-2gate — 🚩 **yaw-oscillation watch (Fengyou): does vpeffs0's smoothness SURVIVE 8-gate training, or does 8-gate reintroduce the hunt like on aw0?** Training gross-spin low (exit_spin/spin_abort ~0.04); the fine yaw-hunt only shows in flight = his eyes. If it hunts → add a yaw-flip tracer metric (the recovery term is the roll analog).
- 🚩 **now logs `metrics/peak_roll_deg`** (track the accumulation directly). 🚩 **promote-ordering bug UNFIXED: best_npg is NOT auto-promoted on a fresh run** ("[ckpt-select] FLAG: checkpoints/ dir not found") → PULL `best_npg/actor.pth` MANUALLY for deploy (rolling-best on n_passed_gates; DET verified 5.33 ≈ final since n_passed rose monotonically).
- 🚩 **TOOLING banked:** `scratchpad/tb_parse.py <ev>` = dep-free TFRecord/protobuf TB scalar reader (the main `.venv` has NO tensorboard). Monitor via **TB + periodic/ saves, NOT stdout** (SLURM block-buffers stdout — a live run's .out froze at PRECHECK while it was fine). Snapshot the live TB event file on Adroit (`cp` — it grows mid-pull, md5 fails otherwise) → `pull_file.py`. `launch_vpefcap.sh` env-overridable: `RECOVER`/`RWVCAP`/`VSOFT`/`VHARD`/`NGATES`/`TAG`/`WARM`.
- ⏳ **recovery A/B `3309479` (+recovery reward form A 0.05/θ0=30°) STILL RUNNING** (~3k/12k at last check; ~40% GPU util = slow, ~3h/run): at matched updates it TRACKS the baseline (recovery reward not strangling turns); the recovery weight END-HOLDs (full in last 30%, ~8.4k+) so the +delta vs the 5.33 baseline only shows at convergence. NEXT: pull its TB at ~12k → does recovery DAMP peak_roll + lift success above 5.33? 🚩 **DEPRECATION shift: vpef8nc now SUPERSEDES m8b as the best 8-gate; rs0/m8/aw1 still deprecate.** Governor-removal from deploy branch still queued (default-off, inert).

Links: [[track-a-beat-vpeffs0-2026-07-13]] (run portfolio) · [[feedback-no-deploy-bandaids]] (governor rejected) · [[feedback-reward-exit-align-perception-next]] (the refuted hypothesis) · [[audit-ego-inc9-2026-07-09]] (single-seed 2.8× rule). Full output: `tasks/w0amohlkw.output` (787 lines).
