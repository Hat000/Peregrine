# Parked Backlog Register — Peregrine

STANDING register of banked-but-parked threads — the anti-"banked-then-forgot" mechanism. **REVIEW every session AND whenever a trigger/gate fires** (POC unfreeze, COWORK-1 organizer answers, an Adroit contact, a fresh recording). Sweep origin: 2026-06-14 (70 items surfaced across all memory). Dispositions:
- **F = FOLD-NOW** — actionable; in/into an active path (P1/P2/P3) or a one-off dispatch.
- **T = DORMANT-TRIGGERED** — parked until [trigger] fires; do not spend before then.
- **D = DEAD/SUPERSEDED** — closed; recorded to prevent re-litigation.

🔑 **COWORK-1 CLUSTER KEY — ✅ LANDED 2026-06-14** (handoff/cowork-2026-06-14/competition-intel.md): Q① = NO pose streamed → estimator LOAD-BEARING; Q⑤ = eval is desktop GPU → LATENCY axis CLOSES (#13 ONNX/TRT now lower-urgency for VQ, still the physical-round portability play); Q④ = offline retune BETWEEN runs ALLOWED → #48/#53 between-attempt refinement LEGAL. UN-GATED (blocked→ALLOWED; triage on merit, NOT all critical-path): #12, #47, #48, #49 (vision-only confirmed), #53, #58 (Round-2 3D-scan → appearance gap REAL), #67. #13 demoted (VQ latency closed).

---

## 🔼 FOLD-NOW (F)
- **#3** ✅ **DONE (P1, a1620b3, 2026-06-14):** bake site = frames.R_camera_from_body; NO code bug (round-trip 1.48e-8 m; fy=320/cy=180 consistent render+PnP). The 0.56° = render-vs-decode convention constant. (Closed.)
- **#4** ⏳ **INHERITED by overall commander (2026-06-14):** surrogate σ vs L3 gate-4 σ reconciliation (surrogate pooled σ_vert 0.28 vs L3 gate-4 0.10). P3 confirmed the L3 at-speed recording is DONE → this is ANALYSIS, folded into the boresight-closure + margin-re-run pathway. Full 37 m/s recording TODO (gated on the held inc8 envelope-relaxed policy).
- **#5** bearing-resolve ShadowPC `data/runs/*_l3atspeed_*` via shadow_gate4.py → **P3** boresight-lock alternative.
- **#6 / #28** 30 Hz gating + per-gate last-accepted-fix distance from L3 → **P3** at-speed recording (feeds speed-ceiling analytic + #43).
- **#13** ONNX/TensorRT export of detector v2 + in-loop latency benchmark → **NEW dispatch**; CLOSES the ≤50 ms LATENCY axis if eval=GPU (COWORK-1). Export laptop-doable now; on-target benchmark folds into P3.
- **#15** ✅ **VALIDATED + IN USE (P1, 2026-06-14):** MonoRace IoU-BO recovers pitch+roll 0.000° noiseless / ≤0.008° @0.5 px = the boresight ANGULAR-form estimator. (Closed.)
- **#16** ✅ **VALIDATED + IN USE (P1, 2026-06-14):** level-hover WLS splits intercept(metric) vs slope(angular) = the ANGULAR/METRIC/MIXED form classifier. (Closed.)
- **#22** sim regression-suite promotion (8 scripts handoff/ultracode-substrate-audit → tests/; resolve test_confirmed_cr4_03 slug-collision) → one-off hygiene (prevents silent regressions).
- **#23** CR1-01 + P1-C06 yaw-active capture rider → fold into **P2** inc8 fresh-reset live batch (closes abs-yaw wire sign + obs yaw seam in one capture).
- **#25** ShadowPC simops tooling push to main → **P3 #3** (partly on origin 1b54cb8; verify complete).
- **#36** inc8 reward design (lean banked, NOT frozen) → **P2** prepares ablations; **Fengyou owns freeze**.
- **#37** ✅ **RESOLVED (system-id, 2026-06-18, bc182f9):** the emul obs wrapper is REGISTERED byte-faithful (numpy `EstimatorEmulator` ↔ torch `BatchedEstimatorEmulator` ≤9.5e-7, no condition blow-up) → the 0/200 numpy-grader gap is a POLICY-GAP, answer = **MEASURE-IN-TORCH**; the v* gate-4-approach-speed instrument = the σ_p0 torch eval (built, launch-audited GREEN, Adroit-pending). NOTE: emul↔emul fidelity ✓; emul-vs-REAL (YOLO/PnP) still pends the real-detector spike + POC. (Closed; full record handoff/system-id-2026-06-18/REPORT.md on main.)
- **#39** scipy-SLSQP toy MPCC probe (measures real tracking-k for the decomposed-architecture ceiling) → optional cheap one-off, P2-adjacent.
- **#40** ✅ **DONE (P2 W#2, merged 5a4afa6, 2026-06-14):** rl/reference_line_inc8.json + build_reference_line.py — dead-centre, orientation-fixed, drag-feasible UPRIGHT, R1'-ready. (Closed.)
- **#55** BSR3 spin-gate (spin_rate_abort→9–10, spin_time_abort→3.0 s) → **P2** prereq before ANY inc8 run.
- **#76** 🆕 **(2026-06-27, VADR-TS-003) VQ2 NEW SCOPE — self-attitude estimation (AHRS) + checks to run the moment VQ2 loads.** VQ2 blocks ATTITUDE + ODOMETRY → the deployed stack must estimate ATTITUDE from `HIGHRES_IMU` (gyro-integrate + accel-gravity/mag-correct), then feed the gate-relative localizer. The existing `LinearKF` TRUSTED given attitude (`attitude_noise_std=1.4°`) → this is genuinely NEW work (an ESKF/AHRS, not just a bias-correction; §3.8 body→IMU = identity helps; the dead P1-ESKF-DESIGN branch had a starting sketch). 🚩 **ON VQ2 LOAD, just inspect (Fengyou: no organizer email):** (a) training track == VQ1?; (b) competition track randomized per-load? [would break pre-baked maps → online mapping]; (c) training flights expose blocked telemetry for offline calib/mapping?; (d) photoreal confirmed. → building-phase; [[reference-sim-interface]] · MEMORY.md VQ2 marker.
- **#75** 🆕 **(2026-06-27) inc8 smoke-sbatch REPORTING bugs — FIX before trusting the next smoke output.** Two harness bugs hid the signal: (a) the `RUN=` dir glob returns EMPTY; (b) pointing/fix-rate metrics are grepped from `.out` stdout but only land in the TB event file → smoke reads as signal-less even when training is fine. NOT model bugs. Root-cause + TB-pulled values in `handoff/inc8-gpu-smoke-2026-06-14/REPORT.md` (cherry-picked into main lineage 2026-06-27). Same report's "pointing flat under arm A" = ALREADY RESOLVED (the look-at primitive fixed it); ONNX-export RC=1 = cosmetic (fires after save), documented in [[project-phase2-rl-vision-decisions]]. → fold into **P2** before the next inc8 smoke.
- **#60** organizer Qs (6 open: Q①④⑤ + Q-A/B/C/D) → **COWORK-1** attacks; keep ONE weekly nudge.
- **#62** ✅ **RESOLVED (2026-06-14):** Adroit acceptable-use PERMITTED with rules — Fengyou provided the Princeton RC AUP; banked in reference_adroit_princeton.md §Acceptable-Use Policy + MEMORY.md footgun. (Closed.)
- **#30 / #58** 🟢 **ACTIVATED (2026-06-14) — ONE overnight session DISPATCHED (B CUT as redundant, Fengyou-caught):** (A) VQ2 photoreal Blender/Cycles dataset GENERATOR [ultracode] (bpy, runnable on ShadowPC — intrinsics==K[[320,0,320],[0,320,180]] @640x360 +20° mount, appearance-randomized incl. VQ1-red+wide-hue to kill gambling-red overfit, Cycles photoreal + motion-blur/flares, keypoint-aware albumentations, YOLO-pose labels matching src/racer/vision/{synthetic,detector,gate_pose}.py, 5 scenario presets, viz-verify, OUTPUTS a standard ultralytics data.yaml; handoff/vq2-blender-dataset-2026-06-14). 🚩 **NO new training harness needed — cluster/yolo_train_v3.sbatch IS the harness (standard `yolo pose train` on a data.yaml; ultralytics does val metrics + early-stop; trained v2/v3).** Morning training = copy yolo_train_v3.sbatch, point data= at the Blender data.yaml, drop the gen step (A pre-renders) = ONE-LINE edit. Only genuinely-new residue = sim-to-real eval on REAL VQ1 frames (existing pipeline validates synthetic-only) — SMALL, post-training, not a session. Morning: ShadowPC runs A → upload → reuse yolo_train_v3.sbatch. A reports branch UP for merge. → was [[index-vision-estimator]] P5 photoreal.
  - ✅ **LANDED on main (d437507, 2026-06-15, 28 tests green, pure additions): src/racer/vision/blender_gen/ + 6 presets + VQ2_VISION_TRAINING_PLAN.md.** 🚩 **MonoRace (A2RL'25 winner, near-1:1 match) THESIS: photorealism is the BASE not the lever — the transfer levers are APPEARANCE DIVERSITY + MOTION BLUR + a GEOMETRY-KEYED LABEL + ~30% HARD NEGATIVES; gate color = NUISANCE axis (real DCL gate = SOLID PURPLE frame NOT LED/orange; orange was drone-LED+VQ1-styling); venue = DARK INDOOR ARENA (Sept SoCal + Nov Columbus indoor) not daylight.** Generator: optical-frame PnP-exact labels (<1e-9px vs frames.py; live ≤1px self-check), real-course viewpoints, ProceduralBackend (laptop, no Blender) + BlenderBackend (bpy/Cycles ShadowPC, doc-verified-not-render-verified → intrinsics self-check + viz overlay = live arbiters on 1st ShadowPC run). **COMMANDER ANSWERS (2026-06-15): Q-color = color-as-nuisance CONFIRMED (VQ1-orange anchor retained; hard-negs make it discriminative). Q-arch = EMIT MASKS (cheap hedge, keeps segmentation open w/o re-gen) + keypoint-pose stays BASELINE but segmentation+geometric-corners (MonoRace's appearance-robust path) = FIRST-CLASS A/B in the detector-recipe session (#32/#33, now un-gated; corners feed the same PnP→KF → small downstream cost); sim-to-real eval picks winner.** 🚩 **MonoRace→BORESIGHT loop: MonoRace's biggest accuracy lever = offline IoU-based extrinsic calibration from flight logs = EXACTLY our ε_vert −0.25 boresight (frames.BoresightCorrection ready) → champion validates the approach FOR REAL-WORLD fix-ACCURACY at DEPLOY. ⚠️ CORRECTION (2026-06-15): boresight does NOT touch the inc8 TRAINING conversion gap — that was root-caused as reward-window mis-aim (w_term peaks in the p_accept dead zone), and p_accept=f(range,in_image) ONLY, so boresight is NOT in the training accept path. See index_rl_training §CONVERSION-GAP DIAGNOSED.** 🚩 PROCESS: worker committed to main DIRECTLY (2nd branch-collision instance; landed safe but luck) → reinforce worktree isolation. → [[index-vision-estimator]] · [[reference-prior-art]]
  - ✅ **PHASE-0 RENDER-VERIFIED (2026-06-15, ShadowPC, Blender 5.1.2 / RTX 2000 Ada; branch vq2-blender-render-2026-06-15 @ e3857eb — LOCAL on ShadowPC, NOT YET PUSHED → MERGE PENDING the push):** the bpy/Cycles BlenderBackend (previously doc-verified-not-render-verified) now renders CORRECTLY-LABELED frames — intrinsics self-check **0.0000 px (3.6e-5)** every run; overlay keypoints land on the rendered inner corners; deployed PnP recovers 4.2–28 m; hue-randomized gate + arena lighting + flare + motion blur all visible (samples/ committed). 🚩 **THREE bugs the LAPTOP SUITE STRUCTURALLY CANNOT CATCH (it never imports the bpy leaves → real-Blender validation is MANDATORY for any bpy_* change, not the laptop suite):** (1) bpy_camera double-dot import `from .. import` resolved to racer.vision not …blender_gen → leaf couldn't load (LOAD-BEARING; →single-dot); (2) Blender 5.x renamed BLENDER_EEVEE_NEXT→BLENDER_EEVEE (_resolve_engine per-build); (3) CompositorNodeComposite removed in 5.x → graceful glare fallback (NodeGroupOutput); + embedded-Python strips PYTHONPATH/user-site (render_entry re-honors). **MASKS ADDED** (masks.py gate-ring instance masks via `--masks`, post-augment from known polygons; viz_overlay validator; 30 tests, was 28) + **RUN_GUIDE.md** (blender_python -m pip install --user opencv-python-headless scipy albumentations; set PYTHONPATH to AppData site-packages). **PILOT rendering** (appearance_broad 4k / negatives 2k / terminal 1k, distinct seeds, Cycles GPU OptiX 96-samples; **~1465 frames/hr** = 2.46 s/frame; ~5.3 h; gitignored under handoff/.../pilot/). NEXT (post-merge): train via cluster/yolo_train_v3.sbatch on the data.yaml + the 40-real-frame sim-to-real eval. → [[project-detector-training-pipeline]]
  - ✅ **DELIVERED + MERGED to main (d64e1dc, 2026-06-16; SELECTIVE merge of vq2-blender-render-2026-06-15 — Blender-lane ONLY; branch forked pre-inc8-S0/#74 @ 9b585862 so its stale src/racer + rl/inc8_* + memory/ + throughput/validate8kp scratch were EXCLUDED).** Productionized photoreal generator (bpy_photoreal + assets.py: CC0 Polyhaven HDRI / PBR floor BELOW the line so gates FLOAT / props / **procedural-mannequin** people off-corridor, solid-color gate, dims TRUE inner 1.5 / outer 2.72 / depth 0.26 m proven to the pixel) + 🚩 **8-KPT CONTRACT (contract.py: kpt_shape [8,3], flip_idx [1,0,3,2,5,4,7,6]; inner 0-3 = PnP keypoints BYTE-IDENTICAL in IPPE_SQUARE order → deployed 4-kpt PnP UNCHANGED; outer 4-7 = gate FRAME. FORKS the VQ1 4-kpt synthetic writer — any cross-writer training MUST reconcile kpt_shape)** + Ultralytics 8-kpt pose harness (cluster/vq2_pose_dataset/train/sbatch; on-the-fly AlbumentationsX photometric + Ultralytics geometric/pose-aware aug; smoke-trained) + masks. Render-verify K to 0.000036 px (self-check ABORTS >1px → can't emit wrong labels); 3 real bpy bugs fixed by ACTUALLY rendering; 31 tests green. 2000-img base (1400 all-hue + 600 VQ1-red) + assets LOCAL/gitignored (regen via RUN_GUIDE; transfer to Adroit **/scratch** for training). FULL RECORD = handoff/vq2-blender-render-2026-06-15/COMMANDER_REPORT.md ON MAIN. 🚩 **NEXT = data → Adroit /scratch (transfer worker dispatched) → cluster/vq2_pose_train.sbatch → 40-frame sim-to-real eval = the real transfer verdict. PATH FOOTGUN: the sbatch reads `data/vq2_sets/` (via cluster/push_dir.py) — reconcile the transfer dest + the data.yaml split paths or training dies at epoch 0.** NEW PARKED: **procedural-mannequin people** — no CC0 humans on Polyhaven; fine at distance, convincing close-ups need a different (likely paid) asset source [revive = round-2 real-scanned env / close-up people become load-bearing] (2026-06-16). 8-kpt OUTER-corner visibility is in-frame-only (inner PnP corners carry the full prop-occlusion test).
  - 🟢 **DETECTOR-RECIPE + SIM-TO-REAL session (actions #32/#33; RELAYED 2026-06-16, consistency-checked but NOT yet code-verified — COMMANDER_REPORT.md @ `82f315d` is UNPUSHED, merge-review PENDING):** **Round-1 transfer CONFIRMED 39/40** (the real verdict) — the win was **DATA/RECIPE at CONSTANT model size (both champions YOLO11s)**, not a bigger net (MonoRace thesis holds). **Latency CORRECTED:** the 9.9 ms was eager-overhead; real floor ≈ 4 ms → latency does NOT bind model size → pick the detector on ACCURACY, validate on-target via TensorRT-INT8 (consistent w/ COWORK-1 latency-axis-closes). **Data audit:** 2k imgs, **0% object-occluded corners = THE transfer GAP**; both gates labeled in 2-gate frames. **Aug recipe:** grouped multi-layer (~2.3 effects/img), photometric-ONLY (can't desync labels) + glare/flash + synthetic corner-occlusion; magnitude rule = 'structure inferable, corner may be obscured.' **LOCKED:** 8-kpt permanent · stay YOLO11s (→ m when data grows) · NO nano · NO re-render now. 🚩 **NEXT-RENDER DIRECTIVE (parked):** max **4 gates/frame** (gate-on-gate occlusion) = the real fix for the corner-obscuration gap. **IN-FLIGHT:** expanded-aug retrain **b0yz1s3oz RUNNING** → worker AUTO-RESUMES on completion to run the expanded test (240-frame multi-gate real course_bundle + 200 photoreal+albu) across Round-2/Round-1/legacy; synthetic val is SATURATED so REAL-frame transfer is THE metric. **MERGE-GATE TODO:** get `82f315d` pushed → fetch → substance-review (watch for worker `memory/`-edits + stale base) → selective-merge the report + any recipe/code. New origin branch `vq2-data` also appeared (un-reviewed). (2026-06-16)
  - ✅ **ROUND-2 EXPANDED TEST DONE (b0yz1s3oz; RELAYED 2026-06-16, code/report `0b99157` UNPUSHED → merge-review PENDING):** the beefed-aug retrain is **NEUTRAL on clean data** — Round-2 ≈ Round-1 within single-seed noise on every measurable test (clean photoreal val pose mAP50-95 0.892 vs 0.903; 240-frame multi-gate real detect+assoc 47% vs 46%; range-err median 0.39 vs 0.26 m; degraded-val drop −0.016 vs −0.013). **Round-1 best.pt = CHAMPION on everything measurable.** WHY neutral: the new transforms (glare/flash, gate-corner occlusion, chromatic aberration) target conditions NO available test contains (course_bundle = clean VQ1 sim; degraded-val only ~6% hit) → nothing to bite; payoff is the Round-2 photoreal ARENA (real stage lighting + gate-on-gate), unmeasurable yet. 🟢 **UNAMBIGUOUS WIN at scale: both VQ2 models CRUSH legacy on real detect — 47/46% vs 28%, 6 gates / 240 frames** (the Round-1 39/40 was NOT a 40-frame fluke). **DECISION (commander, detector-lane): DEPLOY Round-1** (YOLO11s, ~4 ms real; TensorRT-INT8 on-target validate before commit). Aug recipe is correct-but-unadoptable-on-current-evidence (a hedge) → keep ready, do NOT adopt on faith. 🚩 **4-gate/gate-on-gate re-render = the real lever** (closes the 0%-occluded-corners DATA gap AND makes the aug A/B meaningful) — **DEFERRED: not critical-path (Round-1 deployable; RL + VQ1-POC are priority), better informed by the official VQ2 arena ~2026-06-29; Fengyou's ShadowPC greenlight.** Committed: COMMANDER_REPORT.md §9 + reusable `eval_real_multigate.py` (240-frame multi-gate PnP) + `eval_photoreal_aug.py` (clean-vs-degraded). Base 2000-img dataset → **`origin/vq2-data`** (data-only orphan branch, bd1956d). **MERGE-GATE TODO: get `0b99157` pushed → fetch → review (watch memory/-edits + stale base) → selective-merge the 2 eval scripts + report §9.** (2026-06-16)

## ⏳ DORMANT-TRIGGERED (T) — [trigger]
- **#38** ⭐ **speed-ladder envelope relaxation** (rw_tilt 96→48 ~1.4 s; free-cone 60→75–80° ~1.5 s) — **THE biggest lap-time lever** (cone tax 6.89→4.72 s) — [POC unfrozen + inc8 live-confirm, one step at a time].
- **#2** fix-rate sweep fold into POC — [POC unfrozen].
- **#7** sigma_theta 1.4° refit — [N≥100 at-speed fixes from inc8-class flights].
- **#10** TIMESYNC epoch reconciliation (navigator.py:426) — [full LIVE OOSM needed; C2 predict-forward fallback ships now].
- **#11** range-anisotropic R (depth ∝ r²) — [>24 m fixes start mattering].
- **#12** K+L+N stage-2 estimator bundle (reprojection-EKF / temporal-parallax / Fisher-info KF) — [Q①=vision-only + stage-2] · COWORK-1.
- **#14** 2-corner translation-only PnP fallback — [terminal close-range fix-drought confirmed] · relevant to TERMINAL gate-lock margin.
- **#17** roll-wander τ_pre re-measure — [fresh uncontaminated 6/6 recording].
- **#19** LK sub-pixel corner tracking (M) + rolling-Mahalanobis innovation monitor (Q) — [SHADOWPC-VISION-CAL].
- **#20** SEARCH/acquisition state in mission.py (yaw/alt scan when no gate visible) — [blind-segment robustness needed].
- **#21** optical-flow translation-velocity assist (long blind gaps) — [inter-gate blind-gap data shows need].
- **#27** hard-coast + map-averaging layer-3 — [mapper built = #64].
- **#29** full-lap case-C sim at ~37 m/s gate-4 — [inc8 envelope-relaxed policy exists].
- **#31** adaptive state-based ROI cropping (extends >32 m range) — [case-C co-visibility wall].
- **#32** heteroscedastic per-corner σ (NLL/RLE detector head) — [next detector retrain = P5]. 🟡 **QUEUED: this + #33 (stronger/newer arch) + a better aug recipe = the "detector-recipe improvement" session Fengyou offered (2026-06-14); FIRES once photoreal dataset A lands (tune the recipe ON that data, not before). Per-corner σ feeds weighted-PnP → fix ACCURACY → estimator + emulation-fidelity (see index_rl_training §EMULATION-FIDELITY). Commander to write the prompt when A's data exists.**
- **#33** YOLO26-pose retrain + A/B — [photoreal v4 = P5].
- **#34** kpt_conf_thresh 0.5 relaxation — [real confidence distribution measured].
- **#41** TOGT re-run with mixer coupling — [post-inc8 bound refresh].
- **#42** inc8 per-tick gate crossings from debug_obs — [rung-2/3 gap attribution].
- **#43** 60/100 Hz retrain — [last-fix ≤10 m at speed confirmed; analytic says likely UNNEEDED].
- **#44** per-segment tilt-cost concentration analysis — [if global rw_tilt step insufficient].
- **#46** learned residual dynamics on twin (parity-gated, off by default) — [experiment desired].
- **#47** MPC-shadow safety floor + critic-as-risk monitor — [Q①/Q⑤] · COWORK-1.
- **#48** between-attempt trajectory refinement (exploits determinism) — [Q④ state-persistence] · COWORK-1.
- **#49** stage-2 case-C noise-model extension (1.8–3.4 m lateral) — [Q①=vision-only] · COWORK-1.
- **#50** runtime validity supervisor / watchdog demotion — [submission mechanics known].
- **#51** multi-frame far-gate PnP (3–5 frame accumulate) — [>32 m gates matter].
- **#52** IMM filter bank (hover/cruise/transit) — [KF divergence on sharp maneuvers observed].
- **#53** ILC-style feedforward correction across laps — [Q④] · COWORK-1.
- **#54** fly_rl.py default ckpt inc4→inc7 — [P4 unfreeze / next fly_rl touch]. DEV footgun only (submit_rl.py pins inc7 on judged path).
- **#57** Adroit non-lapse config-matrix V100 parity gate — [next Adroit contact] (the lapse configs are DEAD — S18 voided).
- **#58** full VQ2 photoreal detector campaign (Adroit, 10–50k scenes) — [pilot #30 justifies + COWORK-1 shows appearance gap].
- **#59** PVNet dense-corner-voting escalation — [P3P fallback failing at gate transit].
- **#61** submission interface details + FLOSS disclosure doc — [organizer email / pre-submission].
- **#63** fully-autonomous submission stack (remove human GO/disarm) — [pre-submission; partly done via submit_rl.py autonomy-hardening].
- **#64** GTSAM offline mapper (per-gate marginal cov → localization R) — [case-B/C mapping needed; scipy least_squares first].
- **#66** min-snap geometry rung (rpg_trajectory_generation numpy port) — [VQ2 quality; folds with #40].
- **#67** async heavyweight detector ~5 Hz full-res → rewind-corrected KF — [Q①/Q⑤] · COWORK-1.
- **#68** TVM / NVIDIA TAO INT8 edge-deploy toolchain — [Sept+ physical round].
- **#69** ⏳ **CONFIRMED LIVE (2026-06-14, inc8 GPU smoke):** peregrine_repo on /scratch IS a stale flat copy (no cluster GitHub creds) → refresh needs a `git archive` tarball of the target commit + a manual upload per code change = real iteration friction (one upload per re-smoke/ladder). Workaround works; PROMOTE to deploy-key git clone (replace /scratch file copy) when Adroit iteration cadence justifies — [Adroit sessions grow / next multi-round GPU phase].
- **#70** VIO/VINS escalation (VINS-Fusion / OpenVINS — local drift-bounded odometry for self-loc + smooth control state + coast; full SLAM loop-closure NOT needed on a single pass) — [**POST-INC8 + CONDITIONAL (Fengyou, 2026-06-15)**: revive IF the terminal-coast drift is too large (the ~12 m blind segment below the ~12 m accurate-fix floor — the coast-drift analysis tests exactly this) OR attitude biased/missing. **ENABLER = VQ2 photoreal texture** (VQ1 sim too sparse → with only the gates as features, VIO degenerates to our current gate-relative+IMU; needs background features to track). Precedent: SWIFT = VIO+gate-fixes; MonoRace (our monocular analog, A2RL'25 winner) = lighter gate-PnP+IMU = our current base. Do NOT divert here pre-inc8. 🚩 **COAST-DRIFT BACK-SOLVE (2026-06-15) → STAYS PARKED, ×9 margin:** trigger = σ_v0 ≳ 0.18 m/s (gate-4); IMU delivers ~0.02 → ×9 headroom. VIO shrinks σ_v0 (already fine) but is BLIND to gate-relative CENTERING (σ_p0 = the real binding term) → WRONG LEVER for gate-4. The centering lever = a near-field GATE estimator (lower the 12 m floor — a gate anchor), NOT odometry.]
- **#71** min-snap vs natural-cubic descent-overshoot trim on the inc8 reference line — [if descent overshoot is shown to hurt; minor] (P2 W#2, 2026-06-14).
- **#72** VQ2 appearance_broad preset: `use_hdri=true` but `hdri_dir=null` → SILENTLY falls back to procedural sky (a real indoor-biased HDRI set would widen the domain materially) — [fetch a Poly Haven indoor HDRI set; when widening the photoreal domain / the full recipe render] (VQ2 Blender render, 2026-06-15).
- **#73** glare/bloom on Blender 5.x feeds via NodeGroupOutput (CompositorNodeComposite removed) — WORKS (bloom visible in samples) but do a deliberate compositor-output check before relying on it QUANTITATIVELY — [if bloom/glare becomes a load-bearing appearance lever] (VQ2 Blender render, 2026-06-15).
- **#74** ✅ **DONE (worker/kf-pfloor-2026-06-15 @ fe4b152, code merged to main; 826 green):** in-plane STATE (P) floor in LinearKF — `_apply_inplane_pos_floor` (state_estimator.py, after update) eigenvalue-floors the horizontal N-E position block to σ_floor² = 0.05² (= localization.INPLANE_POS_FLOOR_STD = σ_ref = σ_b, aligned not invented); world-horizontal/centering eigenvalue only (vertical + along-track + velocity untouched); gated `NavigatorConfig.use_inplane_pos_floor` (default-on) active ONLY in case-C (use_rewind_kf OR use_gate_relative) → VQ1/inc7 + C2-gated-off BYTE-IDENTICAL. 🚩 **KEY FINDING: over-convergence does NOT reproduce with realistic CORRELATED fixes** (RewindKF Q + bounded fix count self-limit σ at 0.074–0.124 m > the 0.05 floor → the floor NEVER BINDS in C2 G3; the coast-drift iid MC OVER-STATED the collapse). So the floor is **SOUND INSURANCE, NOT load-bearing today.** obs[17:20] more honest (pre-clip c_inplane 3.77→1.00); NEES over-confident 11.5→honest ~1.5. **SURFACE-UP: the inc8 TRAINING emul (rl/estimator_emul.py + torch BatchedLinearKF) is UNFLOORED — no train/deploy obs mismatch today (c_inplane saturates identically), but MIRROR the floor into the emul IF a future training config produces a denser-than-G3 fix stream** (coast-drift → #74, 2026-06-15).
- **#75** twin-replay / command-replay localization (deterministic digital twin → propagate OUR OWN commands from a known start → read world position) — [Fengyou idea, 2026-06-15. SAME blindness as VIO #70: gives drone WORLD-position, BLIND to gate-RELATIVE centering σ_p0 (the binding gate-4 term), and the map is ROUGH so world-pos ≠ gate-relative. Also open-loop DIVERGES (model mismatch / cross-machine FP-nondeterminism / likely eval per-run RNG) + strictly WORSE than the sim's own IMU dead-reckoning (the sim IMU IS the sim's true dynamics). ONLY viable if A1+A2+A3 (below) ALL relax. Narrow real uses: a divergence WATCHDOG cross-check (twin vs estimator); a command-conditioned process model on the PHYSICAL drone. Revive: A3 confirmed (deterministic exact sim) OR a watchdog is wanted.]
- **#76** Adroit `serve` daemon idle-drops after long gaps → leaves a WEDGED listener on port 8765 that SHADOWS a fresh serve (token-mismatch "unauthorized") → fix = force-kill the stale PID before a fresh serve (recurred 3×+ over the inc8 GPU phase: 50232, 57056, …; current live = 35244) — [revive: any Adroit serve "unauthorized" / token-mismatch] (inc8 ops, 2026-06-15).
- **#77** ⏳ **deploy COLD-START confidence gap** (deploy-obs20, dc56ce7): pre-first-fix the deploy navigator exports inf sigmas → obs[17:18]=0 (zero confidence) vs the trainer's finite cold P → ~0.05. BOUNDED (age_norm=1 flags stale in BOTH; converges on the first fix; characterized+pinned test_deploy_obs20.py). FIX = give the navigator a finite cold gate-frame sigma (changes deploy obs) — [trigger: POC/flight shows the cold window matters].
- **#78** ⏳ **Objective-E: wire the 20-dim deploy SELECTION into the live loop** (deploy-obs20): `estimator_obs_auto(actor_obs_dim)` dispatches 17/20-dim cleanly in the OBS layer, but `fly_rl.main`/`fly_once` is the case-A wire-position path (hardcoded 17-dim = the JUDGED inc7 path, untouched deliberately). Wiring case-C self-loc + 20-dim selection into the live loop = the POC deploy-integration step — [trigger: POC integration / inc8 case-C deploy].
- **#79** ⏳ **green_gate pre-existing test-infra FLAKE**: `tests/test_measured_aero.py` 4 tests FAIL in a batch / PASS in isolation — a diffaero-absent BaseDynamics stub-ordering collision (`object.__init__` at diffaero_dynamics.py:306). Only bites the DIFF-SCOPED pytest with UNCOMMITTED src/racer|rl changes (post-commit green_gate = GREEN: invariants+sentinel). PROVEN pre-existing on pristine main (same 4 fail, change absent). FIX = force `importlib.reload` after the stub-install in the fixture (chip task_1a79a55a spawned) — [trigger: green_gate diff-pytest flake-RED] (2026-06-19).

## ⚖️ WORST-CASE ASSUMPTIONS → RELAXATION LEVERS (2026-06-15, Fengyou) — we designed for the HARDEST case (case-C: self-localize everything, rough map, no world position). If first real-eval-wire contact shows reality is MORE GENEROUS, these EASIER levers open up.
🚩 **REVIVE-TRIGGER (all of these): the official VQ2 sim drops (~2026-06-29, Fengyou-expected ~2 weeks from 2026-06-15) / first real eval-wire contact → run the A1/A2/A3 checklist; if any relaxed, PIVOT to the easier lever.** (The VQ1 sim is available NOW but is our practice-rig twin — streams position = the discrepancy — so it is NOT the A1/A2/A3 resolver; only the OFFICIAL sim is. Keep building for the worst case until then — the spec confirms it anyway.) Pairs with the MEMORY.md WIRE-SPEC footgun (re-verify the whole wire).
- **A1 — NO drone WORLD-POSITION on the wire** (the entire Phase-2 self-loc pivot + inc8 assumes this). Official spec §4.3 streams no LOCAL_POSITION_NED; but OUR ShadowPC sim DOES (the wire discrepancy). → IF RELAXED (eval streams position): **inc7 (spoon-fed pose) works directly; the estimator burden + the whole gate-4 σ_p0 problem largely DISSOLVE** (known pos + map → gate-relative). ← THE BIG ONE; check it FIRST. 🚩 **SPEC-DIVE CONFIRMED (2026-06-15, _spec.txt §4.3/§4.5): Sim→Client = HEARTBEAT / ATTITUDE / HIGHRES_IMU / TIMESYNC; telemetry = orientation + LINEAR VELOCITIES + status — NO position. So velocity IS given (reinforces velocity-cheap, coast-drift) but position is NOT → A1 relaxation hope is LOW (the spec is explicit); the self-loc commitment is CORRECT, not over-engineering.** Also confirmed: **SINGLE-PASS** (§8.2 start→intermediate→finish gates, NOT a loop → recon is BETWEEN attempts, not an in-run exploration lap; resolves the banked single-vs-loop open question); **max run 8 min** (§8.3, generous); A3 (eval RNG) NOT in the spec → still open (needs the actual sim). 🚩 **UPDATE 2026-06-17 (Fengyou): the OFFICIAL VQ1 eval sim (= ShadowPC, NOT a practice twin) DID stream LOCAL_POSITION_NED — our VQ1 PASSES USED GIVEN POSITION (case-A). So A1 is RELAXED *for VQ1*.** The open question = the VQ2/scored wire: **honest VQ2 truth expected ~2026-06-18 (TOMORROW)** → run the A1/A2/A3 checklist THEN. If VQ2 also streams position → the case-C estimator burden + the gate-4 σ_p0 problem largely DISSOLVE (inc7 spoon-fed works); if NOT (per §4.3) → case-C self-loc is LOAD-BEARING. **WAIT for the VQ2 drop before concluding — Fengyou (don't jump to conclusions).** Inc8 RL (S2/S3) is policy-general (improves terminal accuracy either way) so it is NOT blocked on this; only NEW case-C-specific investment should wait.
- **A2 — gate positions ROUGH** (map approximate → navigate by vision) [FAQ]. → IF RELAXED (precise map): dead-reckoning / odometry / twin + a precise map → gate-relative WITHOUT vision precision; σ_p0 can come from the map, not just the vision fix.
- **A3 — no exact / deterministic eval sim** (physics sims rarely cross-machine-deterministic; eval likely injects per-run RNG to block trajectory-memorization). → IF RELAXED (exact downloadable package + deterministic + no eval RNG): **#75 twin-replay localization becomes viable** (lockstep twin → world position).
- 🚩 **The HARDER directions push the OTHER way** (real-drone sim-to-real blur; VQ2 flips/vertical/stacked gates) → they make the vision-PERCEPTION lever MORE load-bearing, not less. **Unifying invariant: gate-4's binding term is gate-RELATIVE centering (vision); NO drone-world-localization (IMU/VIO/twin) addresses it unless A1 or A2 relaxes.**

## ⛔ DEAD / SUPERSEDED (D) — do not re-litigate
- **#1** G3 re-run at σ=0.10 — SUPERSEDED by margin-closure-envelope (already re-derived at σ=0.10, 7654e99).
- **#8** predict-forward (constant calibrated age) — SHIPPED in C2/P0-b (reconcile_vision_clock=False).
- **#9** full KF rewind/OOSM — PRODUCTIONIZED as RewindKF (C2 05ed750; gated off by default).
- **#18** vision-velocity LSQ channel — REFUTED (honest σ_v 2.81 m/s; P2 insurance only, NOT load-bearing).
- **#24** KF accel_body convention — CR1-01 SETTLED (navigator.py:313 rotate-by-TRUE-conjugated is correct).
- **#26** absolute-loop Mahalanobis innovation gate — largely SUPERSEDED by C2 relinnov gate χ²(2,.999)=13.82 (re-confirm coverage if a wrong-gate-teleport is ever seen).
- **#35** Elodin adapter `orientation_ned_wxyz` assert — unreachable (only if Elodin solver-glue + Navigator eval runner ever built).
- **#45** S19 mixer per-axis κ contradiction — low-impact (policy avoids the saturation regime).
- **#56** S19 mixer-probe row completion (c100/c60/zhov_r31) — same; low-impact.
- **#65** ADRC / Extended State Observer — RL supersedes (retain only as model-based safety-net reference).

---
*Maintenance: when an item is actioned, move it to DEAD with the closing commit/branch. When a trigger fires, move the item to FOLD-NOW and assign an owner. Keep this file the SINGLE register — do not re-scatter parked items back into topic files.*
