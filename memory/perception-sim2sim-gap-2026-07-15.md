---
name: perception-sim2sim-gap-2026-07-15
description: "Deploy yaw oscillation root cause = PERCEPTION sim-to-sim gap (training GT-oracle vs real YOLO); not recovery-reward, not plant. SSOT for the perception-first sysID."
metadata: 
  node_type: memory
  type: project
  originSessionId: c864e5bb-f53f-4b54-b78a-a2216bb2c906
---

# PERCEPTION SIM-TO-SIM GAP — a REAL but SECONDARY gap (2026-07-15)

> 🟢🟢 **RESOLVED (2026-07-15) — the yaw hunt is CAUSED BY 8-GATE FINE-TUNE; `vpeffs0` (ffs0) is the deploy champion; its ceiling = SPEED ACCUMULATION.** 🛑 NOT aw1 (Fengyou name-confusion, corrected — aw1 never confirmed past 5; **anti-dither/att-cap were a RED HERRING, ffs0 has neither**). ffs0 = clean 2-gate + DR, flies 5 gates smooth. **CEILING = speed accumulation** (deep-flight logs `28404fa`: per-gate speed 3→13.7 m/s over gates 0-5; yaw stays SMOOTH 1-4 flips/s; perception recovers) → OOD/overspeed at gate 4-5, banks 96-121° but can't turn → crash/scrape (one run 49 coll). 2-gate training never saw the late-gate high-speed regime. FIX = extend ffs0 → multi-gate (to learn speed-reg) in a DEPLOY-FAITHFUL-HARD sim (40Hz + latency-tail + lost-frame-tail; current DR 1-3 may undershoot deploy's 40-100ms stalls) so the bang-bang yaw 8-gate rewards is punished in TRAINING; curriculum-anneal gates+hardness with yaw-flip watchdog (`scratchpad/launch_yawtest.sh`). 3-way `.hydra/overrides` diff (Adroit):
> - **aw1** (`..._trackA_seed0_vtrackAw1`): course_n_gates=**2**, `rw_yaw_dither`=0.5→0.125 (ANTI-DITHER ON), `att_cap_anneal`=true, DR(dr_latency 1-3 + aero/mixer/force_bias), noise held-ish.
> - **vpeffs0** (base): course_n_gates=**2**, NO anti-dither, NO att-cap, same DR → SMOOTH in deploy.
> - **vpef8nc** (=vpeffs0 warm **+ ++course_n_gates=8** fine-tune): NO anti-dither, same DR, noise annealed → **HUNTS**.
> **Clean causal isolation: vpeffs0(2-gate) smooth vs vpef8nc(8-gate) hunts, only diff = 2→8 gates ⇒ the 8-gate fine-tune broke it.** Multi-gate training added the hunt, not capability (a smooth 2-gate approach chained N times = an N-gate policy). All 3 share latency-DR(1-3) ⇒ latency/plant/perception are NOT the differentiator — the everything-below (perception dropout, 2× yaw-rate, timing jitter) are REAL sim gaps but NOT the hunt cause; the recipe is. **DiffAero DET-metric rank is INVERTED vs deploy (5.33 "winner" vpef8nc = gate-2 crash).** 🎯 PLAN: aw1 = template; find its CEILING (get an aw1 5+ gate log → diagnose why it stalls, likely perception-accumulation/coarse-map/drift, NOT policy) — do NOT retrain on more gates. Fengyou's 40Hz-exact + lost-frame-tail = hardening refinements IF we retrain aw1's recipe. Confirmatory tool ready: `scratchpad/launch_yawtest.sh` (yaw-signflips vs injected `++dynamics.transport_delay_steps`, harness in `rl/peregrine_train_ego.py` `++rollout_only`). Everything below = the (secondary) perception analysis, kept for reference.

---

> 🛑 **DEMOTED AS LEAD (2026-07-15, fixed-vision retest).** Fengyou fixed deploy vision (.pt→engine) + tick 30→40Hz and re-flew vpef8nc (`2aca009` logs). Perception is NOT the yaw-hunt driver: (1) hunt SURVIVES good vision — pose_seen rose to 47% (one run 68%/conf0.84) but yaw still rails + flips 8-9×/s; (2) it hunts **6×/s DURING seen frames** (looking right at the gate). **LEAD PIVOTED → PLANT/COMMAND + TIMING** (Fengyou's original sim-to-sim instinct): achieved body yaw-rate ≈ **2.0-2.3× commanded** (gyro 1.3 vs cmd 0.66 rad/s — confounded by limit-cycle overshoot, needs clean step-response) + **jittery loop timing** (work_ms 14→73 vs 25ms/40Hz budget = variable control latency). Vision sweep NOT resumed as a fix track (geometry-sweep left to finish for documentation only). Plant-gain probe (DiffAero cmd→achieved-yaw gain, expect ~1× vs deploy 2×) RUNNING; VQ2-side command-replay relay HELD pending probe. The perception detail below stays valid as a documented secondary gap.

---

## (original) perception gap analysis

**Fengyou flew the published `vpef8nc` (best-8-gate, `ego-ckpts-vpef8gate-2026-07-14`) on ShadowPC → oscillates left-right + speeds up, crashes ~gate 2-3.** Fengyou's call: this is a **sim-to-sim difference**, run another sysID capturing EVERYTHING, replay same commands in both envs. Greenlit **PERCEPTION-FIRST**; ShadowPC capture → **vision-commander builds+runs** (I spec+relay). This supersedes the recovery-reward framing as the lead lever — see [[trackA-stall-forensic-2026-07-13]] §RECOVERY-PIVOT (recovery reward damps ROLL; the deploy killer is YAW + perception).

## THE DATA (deploy panel logs, commit `c344e9a` on `claude/ego-deploy-2026-07-09`, 07-15 runs)
Parsed `data/runs/*_panel_run_f1/ego_obs.jsonl` (each frame: `obs` 21-dim, `rate_frd` cmd, `pose_seen`, `conf`, `age_s`, `kf_pos_ned`). Scripts in scratchpad: `osc_analyze.py`, `percep_fingerprint.py`.
- **The "oscillation" is the NOSE (yaw), not the body.** vpef8nc: yaw-rate cmd **railed** (mean|.|=0.65 of 0.7 clamp) and flips sign **~7×/s** = ~3.5 Hz bang-bang limit cycle. vtrackAw1 ("doesn't oscillate"): yaw cmd 0.33, **2.7 flips/s**, NOT railed — but rolls HARDER (143° swing). So AW1 banks-and-commits; vpef8nc swings the nose. 8-gate training made vpef8nc yaw-aggressive.
- **Clamp is a bandaid that fails:** yaw 0.7→0.35 does NOTHING (both saturate at their rail, still 7 flips/s). Bounds amplitude, not the hunt. → [[feedback-no-deploy-bandaids]]
- **Gate seen only 33% (vpef8nc) / 54% (AW1)** of frames; `age_s`=0 when seen (no latency — pure availability).
- **Loss-onset (real seen→lost transitions, no zeroing confound): median 4.7m, 12/18 losses ≤6m.** Detection reliably HOLDS at ~9m (seen-frame range median 9.4m) but DROPS on final approach ≤6m. Gaps median 0.22s (coast bridges), tail 4s (terminal crashes).
- 🚩 **ANALYSIS FOOTGUN:** deploy obs ZEROES `obs[11:14]` when gate lost (after ~0.2s coast) → do NOT bin not-seen frames by obs geometry (they fake-pile at range 0). Use loss-ONSET (last-seen frame) or reconstruct geometry from `kf_pos_ned` + gate world coords. (I over-read a "13% near-field" number before catching this.)

## THE GAP (training perception model, mapped by agent — file:line anchors)
Training = **fully synthetic GT-geometry oracle, NO image/detector**: `gate_detectable` (`rl/gate_visibility.py`, ≥4 of 8 corners in 90°H×58.7°V pinhole + center ≤30m) off ground truth; estimator fabricates fix = `true_rel + noise` (`rl/ego_estimator.py:695-702`). Cam constants `rl/inc8_estimator_emul.py:56-79` (fx=fy=320, 640×360, +20° mount). Deploy = real YOLO-pose (`src/racer/vision/detector.py`, score 0.25 / kpt 0.5 / ≥4 kpts) → ~33% hit rate; KF coasts gaps (`src/racer/navigator.py:30-33`).
- **Availability:** training ~90% seen on a framed 10-25m approach vs deploy 33-54%.
- **Dropout STRUCTURE (the real driver):** training = flat **iid 10% single-frame** miss (`rl/ego_estimator.py:129,719-720`) → "gate back next frame." Deploy = **bursty sustained** blackouts concentrated ≤6m (median 0.22s, tail 4s) → 5m blind corridor at the crossing. Policy trained on iid single-frame misses never learned to COAST a sustained gap → hunts yaw.
- **Motion-blur gate OFF in training** (`ego_blur_gate=false`) → hard yaw-slew costs ZERO detection in training but blurs/drops in deploy. vpef8nc's railed slew is free in sim, self-blinding in deploy.
- **Obs-on-miss:** training default **STALE-CLIFF** (`ego_obs_coast=false`, `rl/peregrine_racing_ego.py:369-370`) zeroes obs instantly (rare→benign since misses are iid); deploy KF-coasts then zeroes.
- **Training near-field blackout:** IS emergent from the 20° tilt corner-exit geometry (`rl/vq2_ego_curriculum.py:18-21`) — its RANGE is being measured by the visibility-sweep to overlay vs deploy's ~5m loss-onset.

## FIX HYPOTHESIS — 3 faithful-perception levers (all default-off/mismatched; no bandaid, no-GT — just make sim honest)
1. Replace iid 10% miss with **deploy-faithful bursty near-field blackout** (match loss-onset ~5m + gap-length dist).
2. Enable **`ego_obs_coast`** to hold+decay obs through gaps like the deploy KF.
3. Arm the **motion-blur gate** (calibrate `blur_rate_lo/hi`, currently 2.0/4.0 placeholders) so slew costs detection.
Then the policy learns to COMMIT + coast the last 5m instead of yaw-hunting. Overlay decides which dominates + calibrates numbers.

## 🛑 FOOTGUN: DiffAero DET-metrics currently UNTRUSTWORTHY for deploy
"Best 8-gate ever" (DiffAero DET 5.33) flew badly in deploy → do NOT rank policies by DiffAero success until perception is made faithful. Recovery A/B `3309479` can finish but don't act on it (damps roll, not the yaw+perception killer).

## STATUS / NEXT
- ✅ Deploy fingerprint done (above). ✅ Training perception model mapped.
- 🔄 Training **visibility sweep** running (laptop, `scratchpad/train_visibility_sweep.py`) → P(detect|range) + near-field blackout onset, to overlay vs deploy ~4.7m.
- 🔄 **ShadowPC clean perception-capture relayed to vision-commander** (via Fengyou): slow straight-in single-gate approach ~20m→0 @2-3 m/s, log RAW detector fire (pre-coast) + GT gate-rel geometry → `data/runs/…perception_probe/`. Calibrates the target curve.
- Probe/logging hook: `rl/peregrine_racing_ego.py` `_step_estimator` :1053-1093 / diagnostics :1481-1514 (behavior-neutral); harness `_run_det_eval` (`rl/peregrine_train_ego.py:481`). Geometry helper `batched_geometry` (`rl/inc8_estimator_emul.py:246-270`).
- Plant channel (rate-loop/thrust replay) still worth a cheap check LATER; perception fully explains the observation so it leads.

## 🟢🟢🟢 VISION-LATENCY LEAD (2026-07-15 evening — Fengyou's insight, the likely UNIFYING mechanism)
**Fengyou killed my "motion blur" theory (NO blur in VQ2 either) and proposed: are we modeling vision COMPUTE-TIME latency in DiffAero? That's likely THE key.** Mechanism: the vision pipeline (camera frame→detect→PnP→pose→obs) takes wall-clock time, so the gate pose the policy acts on reflects where the gate was N ms ago. **Staleness error = latency × SPEED.** This UNIFIES the two things nothing else explained:
- **slow keeps the gate / fast loses it** (pitch-capped drone barely moves during latency → stale pose ≈ right; fast drone moved far → acting on outdated bearing). 
- **the hunt persists DURING seen frames** (a *seen* pose that's stale → policy corrects toward where the gate WAS → by the time the command lands geometry moved → overshoot → **delayed-feedback limit cycle**). This is the fact that broke the perception-dropout theory.
**DEPLOY vision latency MEASURED** (from `ego_timing.jsonl`, new 40Hz runs): `work_ms` med **20** / p90 **56** / max **100** ms (synchronous pipeline → this IS the state→policy latency); `fresh_detect`=**78%** of ticks; the spike source = `nav_ms` (PnP) med14/p90 51/max95; infer 11ms, detect 2ms. **At 10 m/s: 20ms=0.20m stale, 56ms=0.56m stale** (huge vs a ~1.5m gate near-field). Plus camera is 30Hz (frame ~33ms old) vs the 40Hz loop.
**DiffAero: likely does NOT model vision-fix latency** — detection computed from CURRENT GT in `_step_estimator` and fed ~same-step; `ego_est_dt_ticks_hi` (recipe=1=none) chokes the ESKF/KF (IMU) channels NOT the vision-fix cadence; `dr_latency`(1-3) = COMMAND latency (action→plant) NOT vision (state→detection). So the policy trains on a FRESH zero-latency gate pose it never learns to lead → deploy latency breaks it worst at speed.
🎯 **NEXT (verify then fix):** (1) launch an agent to TRACE DiffAero's vision-fix timing — is the gate fix delayed/rate-limited (30Hz) before reaching obs, or current-GT same-step? (2) if unmodeled → ADD a vision-processing-latency model (delay the gate fix ~20-56ms + 30Hz camera rate) to DiffAero, calibrated to deploy → the perception-faithfulness fix. This likely matters MORE for transfer than plant tuning. This is the SECOND sysID axis (PERCEPTION), complementing the plant sysID.

## 🛑 VELOCITY-CAP CLAIM CORRECTED (2026-07-15 — Fengyou caught my error)
My "speed cap hurts perception" was WRONG. The deepest deploy runs (`28404fa` 5-gate vpeffs0) were **PITCH-CAPPED at 1.0 rad (~57°)**, roll/floor/gov OFF → SLOW, perception FINE (pose_seen recovered 82-86%), reached gate 5. **SLOW HELPS** (pitch cap → deeper; delayed the speed-accumulation crash). `exit_frame` in DiffAero = camera POINTING (attitude), NOT speed (detection is speed-independent geometry). The `vpefcap` exit_frame 73→91% rise was a velocity-cap-REWARD-TERM artifact (degraded pointing), not a slow-is-bad law. 🚩 REAL velocity-cap retraction reason = accumulation-can't-clamp (386-flight) + reward degradation, NOT perception. Training lever for the slower gate-preserving flight = `rw_perception` (reward gate-in-view), NOT a cap.

## PLANT SYSID (2026-07-15) — DiffAero side DONE, VQ2 side pending vision-commander
**Fengyou directive: full sysID — run same predetermined program on both sims, tune DiffAero, overlay logs (match=good, mismatch=problem).** DiffAero side built+run (agent, CPU/no-GPU): `scratchpad/sysid_program.csv` (1092 rows, raw policy actions, 40Hz — the shared battery: per-axis step/doublet/chirp + thrust) + `sysid_diffaero_log.csv`. **DiffAero nominal plant (DR off): roll/pitch gain +2.5×, yaw −2.23× (SIGN-INVERTED by legacy `rate_sign[z]=−1` alias), τ=19ms, flat-gain** (super-rate off ⇒ under-predicts real ~3.5× at full stick). Action→physical: `normed_thrust=2.5(a+1)`, `rate_flu=3.14·a`, FLU→FRD `[1,-1,-1]`, `a_up=g·normed_thrust`. ⚠️ **OVERLAY RECONCILE: DiffAero TRAINING inverts yaw-sign; live VQ2 does NOT (fly_rl wire-map [+1,-1,+1] bridges)** — account for before comparing yaw. VQ2 side = relay SENT to vision-commander (command-player: run sysid_program.csv through fly_rl production code, log ODOMETRY+HIGHRES_IMU → `data/runs/…sysid/`).

## RETRAIN — HELD for sysID (2026-07-15)
🛑 **4 huntfix arms SCANCELLED** (`3310181-184`), held until sysID validates/tunes the plant+vision (design-review R3 + Fengyou directive: don't train on an unvalidated sim). Smoke canary `3310178` still queued. **Adroit GPUs DOWN (`drng@` reboot-ASAP, A100 h11g1 ~26h out — job 3309315 running).** Audits: crash-safety **GO** (yaw_dither binds/live/annealed/no-assert; `env_loss/yaw_dither_pen` = watchdog), config-precedence **PASS** (resolved arm-A config exact; warm-load loads actor+critic, width matches). **REVISED launch (post-sysID) folds in:** UPD 12k→**18k** (vpef8nc TB: n_passed/success/collision/exit_frame ALL still improving at 12k = under-trained; critic_loss 28→259 = ambiguous, likely value-scale not broken); **one arm → `_recover` recovery/damping reward** (R2, the only directive-compliant tool for the bank/speed-accumulation ceiling — anti-dither alone doesn't touch ffs0's real death); **`yaw_dither_start=1.0`** (R5, ON-from-birth since warming from already-smooth ffs0, not the aw1 late-anneal); **gate arms behind smoke** `--dependency=afterok` (R1); **hard joint yaw-gate selection** (R6, reject ckpt >~4 flips/s regardless of n_passed). vpef8nc has `best_npg/` (promote worked).
🚩 **CONNECTOR TRICKS (learned 2026-07-15):** SFTP `upload` HANGS on Duo; `x`-daemon works no-Duo → PUSH files via `base64 -w0` local then `x 'echo <b64> | base64 -d > /remote'` (daemon shell handles pipe/redirect). `pull_file.py` needs `MSYS_NO_PATHCONV=1` + LOCAL arg in `C:/` form (bare `/scratch` remote mangles to `C:/Program Files/Git/scratch`). Run launcher via `x 'VAR=x bash /abs/path.sh'` (env-prefix passes; no cd needed, $0 resolves SBATCH_FILE). `.sh` must be LF before push (`tr -d '\r'`).

## REWARD AUDIT + HUNT-FIX RETRAIN (2026-07-15 night)
**Fengyou asked: does the reward inadvertently reward bang-bang (e.g. gate-in-frame → shaking farms points)?** Adversarial audit of `rl/ego_reward.py` (`compute_ego_reward` :1132-1327) + resolved `_pef` stage weights (`rl/vq2_ego_curriculum.py` `_COMMON` + overrides):
- 🟢 **NO term rewards shaking.** `rw_perception`(0.02, exp(−δ⁴)) is concave-peaked ⇒ ANTI-rewards osc (sweep-through-center averages < hold-center, Jensen); guarded ≤ rw_time; no detection-onset/reacquire bonus anywhere; `area` coupling OFF in _pef; parabola/finish sparse once/gate; progress/corridor telescope (osc-neutral).
- 🔑 **ROOT = a MISSING SUPPRESSANT, not a positive incentive.** Yaw is UNPRICED in obs (no yaw channel → no gradient) AND no active term costs the jitter: anti-jitter (`rw_rate`,`rw_dact`=1e-3, pinned <1% of progress) is **~100-250× too weak** vs chaining (`rw_progress`=2.0, parabola +20/gate). ⇒ bang-bang is a FREE mode the optimizer wanders into under turn demand (8-gate) but not 2-gate.
- 🔧 **FIX = turn ON `rw_yaw_dither`=0.5** (`−k·Δyaw_cmd²`, :923): NON-farmable (min 0 at steady cmd → preserves gate-tracking yaw + altitude coupling), taxes a rail-flip ~41% of a productive step, smooth turn ~0. **It's ON in `_nodither`/`_recover` but OFF in the `_pef` BASE the 8-gate lineage trains on — THAT omission is the gate.** 🚩 FOOTGUN: weight is calibrated to the 0.35 clamp — anneal it in LOCKSTEP with clamp 0.35→0.7 (aw1's exact schedule 0.5→0.125); a mismatched clamp = 4× wrong tax. Slow-drift escape already closed by `ego_spin_rev_abort` + clamp (armed in _pef).
**RETRAIN (building, `scratchpad/launch_huntfix.sh`): warm from ffs0 + 8-gate (= vpef8nc recipe) + aw1's yaw_dither/clamp discipline. NO roll/att/velocity fence (Fengyou: don't fence the axis the course uses).** Arms: **A** (primary, seeds 0+1), **C** (A + `dr_latency_max_steps=5` to cover deploy's ~100ms/5-step latency tail). Control = vpef8nc (hunts @gate2). ffs0 warm dir = `..._pef_seed0_vpeffs0/checkpoints` (actor+critic ✓). WATCHDOG = yaw signflips/s in periodic eval (success = flips stay 1-4/s while n_passed climbs past 2). 40Hz + per-frame-jitter dropout = 2nd wave (need dt-semantics care).

### LAUNCH STATUS (2026-07-15 ~night) — QUEUED behind Adroit GPU maintenance
Launcher `launch_huntfix.sh` on Adroit `/scratch/network/fl3689/peregrine_repo/rl/` (md5 ccd2030a, 105L, LF; pushed via base64-through-daemon — SFTP `upload` hangs on Duo, `x`-daemon does NOT → **reuse base64-echo-through-`x` for pushes**). **JOBS QUEUED:** `3310178` smoke(UPD100 canary) · `3310181` huntfixA_s0 · `3310182` huntfixA_s1 (both dither 0.125) · `3310183` huntfixC_s0 (0.125 + `dr_latency_max=5`) · `3310184` huntfixA_s2 (dither **0.25** hedge — 8-gate may need >aw1's 2-gate 0.125). All UPD=12000, ffs0-warm, 8-gate, yaw_dither annealed, clamp 0.7. 🛑 **BLOCKER: all Adroit GPU nodes `drng@` (draining for maintenance), 0 idle, no reservation — StartTime=Unknown.** Jobs auto-run when nodes return. Monitor `bbhjz2bjf` armed on 3310178. **VALIDATION GATE when GPUs return:** confirm `env_loss/yaw_dither_pen` is LIVE (nonzero) in the smoke — if inert (the anneal ++keys didn't bind on _pef), `scancel` the arms + debug (the "absent hook ≠ inert" footgun). SUCCESS = yaw_dither_pen converges down + n_passed_gates climbs past vpef8nc's DET 5.33 while signflips→0. Then pull best_npg per arm, pick winner (highest DET n_passed, tiebreak lowest signflips), publish for ShadowPC flight. TB monitor: `scratchpad/tb_parse.py` on the run's event file.

## VISION-LATENCY + OBS-V2 BUILDS + THE 28Hz DEPLOY FINDING (2026-07-15 late) — Fengyou-directed
**Measured the REAL vision-lag distribution from the 40Hz-engine deploy logs** (11 sessions on `origin/claude/ego-deploy-2026-07-09`, vpef8nc, extracted to `scratchpad/vislag/`; scripts `analyze.py`/`join_speed.py`; CDF `work_ms_cdf.json`). Key numbers:
- **`age_s` in ego_obs is ALWAYS 0** (coast=false → fresh-or-zeroed, never held; doesn't book compute-motion). ⇒ **staleness lives in `work_ms`, NOT age_s** — calibrating from age_s would model ZERO latency (near-miss avoided).
- **`work_ms` (state→policy compute)**: med **24.5** / p75 47 / p90 **62** / p99 96 / max **121 ms**; ≈**1 / 2.5 / 5 ticks** @40Hz. `nav_ms`(PnP) is the tail driver (med19/p90 57). **work_ms is SPEED-INDEPENDENT** (flat across speed bins) — but **stale-distance = speed×work_ms scales** (med 0.20m / p90 0.56 / max **1.23m**): THE speed-dependent mechanism (slow keeps gate, fast loses).
- **Camera feed = 28.8 Hz** (video_index sim-timestamps, rock-steady 34.7ms). `fresh_detect` = new-frame-per-tick (confirmed `fly_rl.py:2148` `_fresh_frame = frame_id != last_lever_fid`).
- 🚩🚩 **DEPLOY DECIDES AT ~28-29 Hz, NOT 40 Hz.** Measured policy-decision rate from obs sim_time deltas = pooled ~28Hz (25-37Hz load-dependent), frame-gated. `rate_hz:40` is the COMMAND-STREAM/target; the synchronous PnP-bound loop recomputes ~once per frame. So fresh_detect≈0.83 (impossible under a true 40Hz-over-28.8Hz loop). **2nd sim2sim gap on top of staleness: training steps 40Hz, deploy decides ~28Hz** (40% slower correction loop → less phase margin → plausibly COMPOUNDS the yaw hunt, hypothesis). PnP (`nav_ms`) is the deploy bottleneck (engine fixed infer not nav).

**BUILD 1 — vision-latency model** (`+env.ego_vision_latency`, default OFF, byte-identical; agent-built, tested 12+225 green, NOT committed): reach-back model (per-tick history of the ESTIMATOR fix, show `tick−L`; L~work_ms CDF per-fire) → obs gate pose stale by speed×L·dt. Sub-knobs `ego_vision_feed_hz`(28.8), `_latency_cdf`, `_ring_depth`, `_latency_seed`(dedicated Generator → estimator stream bit-identical, NO-GT preserved). 🚩 **KEEP feed_hz=28.8 (true frame rate) — do NOT bandaid to 33 to chase 0.83; the 0.83 is deploy's ~28Hz control rate, modeled separately.** Agent embedded 7 CDF anchors (couldn't see session-scratchpad file); drop full 101-pt `work_ms_cdf.json` in for the Adroit run.
**BUILD 2 — obs-v2 windowed coarse map** (`+env.ego_obs_v2`, default OFF, byte-identical 21-dim; agent-built, 5+69 tests green, NOT committed): **APPEND-ONLY** (Fengyou chose the safe variant over the reindex — obs[9:11]=sector[tg] PRESERVED for warm transfer). ON → 21→**23**: obs[0:21] verbatim + obs[21:23]=`sector[clamp(tg+1)]` masked [0,0] past last gate (reuses `ego_window_indices` valid[:,1], slides in lockstep). Warm-pad `peregrine_train_ego.maybe_pad_ckpt_for_obs_v2` zero-pads ffs0's 21-dim actor first layer→23 (step-0 bit-identical; critic UNAFFECTED, stays 16). 🚩 **DEPLOY TWIN REQUIRED before flying v2** (documented, not built): `src/racer/ego_obs.py` `ego_actor_obs_np`+`EgoObsBuilder.update` emit obs[21:23]=`map[active+1]` (map mode, [0,0] last gate); `fly_rl.py` `_EGO_OBS_DIM`/width→23 under `--ego-obs-v2`.
**TREE STATE:** worktree `cool-heyrovsky-e08624` has 4 uncommitted changesets — vislag + obs_v2 (`peregrine_racing_ego.py`+`peregrine_train_ego.py`) + my pre-compaction WIP (`diffaero_dynamics.py` `transport_delay_steps` command-latency knob + `peregrine_train_ego.py` yaw-log `_run_det_eval`/`_accum_yaw`/`_emit_yaw_eval`/`_run_rollout_only`) + tests. **Nothing committed** (commit only when Fengyou asks). Backup patch: `scratchpad/wip_before_coarsemap.patch`. NOTE two DISTINCT latency channels now: `transport_delay_steps`=command latency, `ego_vision_latency`=vision latency.
🎯 **DECISION RESOLVED (Fengyou 2026-07-15):** train with **VISION gated at 28.8Hz (hard camera ceiling — NEVER randomize the gate-fix rate faster; only DOWNWARD stalls/drops via the work_ms tail + coast-mask)** while the **policy ACTS every control tick at the sim's native 40Hz** using FRESH IMU channels (vel/rp/rates) → the action INTERPOLATES the vision gaps instead of zero-order-holding. 🛑 **My "decision-cadence action-hold gate" was WRONG (= the ZOH Fengyou wants gone) — RETRACTED, not built.** The vislag build ALREADY implements this exactly (delays ONLY the gate fix; velocity/attitude stay fresh; policy acts every tick). Retrain config = `+env.ego_vision_latency=true +env.ego_vision_feed_hz=28.8 +env.ego_obs_v2=true` + warm-pad, sim control 40Hz. **Fengyou has "done my best compacting that speed"** (PnP compute optimized). 🚩 **DEPLOY IMPLICATION (flagged to Fengyou → vision-cmdr):** for a 40Hz-decision policy to interpolate in REAL flight, deploy must DECOUPLE vision(async 28.8Hz)↔control(40Hz reads latest gate + fresh IMU); measured current deploy = ~28Hz synchronous policy loop + 40Hz ZOH command-resend (the hold to remove). async-detect was pulled "net-negative" — revisit. Awaiting Fengyou's call on pursuing deploy decouple.

## VISION-COMMANDER MULTI-RATE BRIEF — RECONCILED, vislag REWORKED (2026-07-15) — SUPERSEDES the vislag reach-back
**Brief `origin/claude/ego-deploy-2026-07-09:handoff/multi-rate-vision-control-brief-2026-07-15.md` (commit 2302b2e).** 🟢 **The multi-rate machinery ALREADY EXISTS in `rl/ego_estimator.py` and is train↔deploy pinned — REUSE, don't reinvent (verified in-code):** ego-propagation between fixes (L669-683: `rel_new = R_prev_from_cur@rel_old − v_body·dt`), confidence=staleness (L833-836: `conf=clamp(1−t_since_fix/stale_horizon,0,1)` = obs[14]/obs[19]), propagate-then-mask past stale_horizon. `.step(detectable=)` is the control point.
🛑 **My vislag reach-back (BUILD 1) was the WRONG LAYER** — it delayed the estimator OUTPUT + masked between frames, bypassing the live propagation. **REWORKED (agent running):** replace with a **detector-cadence gate on the fresh-fix**: `detectable_eff = geometric_detectable AND is_frame_tick(30Hz clock over 40Hz sim) AND detector_success_draw(p~0.35)` → valid fixes **7-15 Hz** + dark <4.7m (geometric), estimator propagation carries the gaps. Kept a THIN work_ms frame→pose sub-effect (init t_since_fix≈sampled work_ms, Fengyou wants it, but it's 2nd-order: brief says loop tail corr≈0 with gates). Default OFF=byte-identical. `obs_v2` untouched.
🔑 **KEY NUMBER CORRECTION: valid vision refresh = 7-15 Hz (NOT 28.8).** My 28.8 was frame ARRIVAL; the gate POSE only refreshes 7-15 Hz (detector misses most frames + dark near gate). Frame stream = **30.0 Hz** (their 424-flight number, use it, not my 28.8). Loss-onset 4.7m = the blind-approach yaw-hunt locus.
✅ **DECISIONS (Fengyou 2026-07-15):** (1) match deploy `stale_horizon/det_hold`=**0.6/0.3** — **Fengyou changes the training defaults himself** (rework leaves them alone); (2) control **40 Hz** (age is observed → irregular staleness in-distribution; 60Hz=clean 2× was optional, rejected); (3) calibrate detector-miss to 7-15 Hz.
🟢 **DEPLOY DECOUPLE RESOLVED by brief §3-4:** SINGLE control loop, NO threads (async-detect failed 3 ways: single-entry cache race, GPU contention, trained-sync/deployed-async OOD) — IMU every tick, detect on new frame_id, propagate between; control 40-60Hz. **Loop tail (work_ms) does NOT limit gates (corr≈0)** ⇒ STOP treating compute-latency as a primary lever; the dominant lever is the 7-15Hz vision cadence + blind approach. My earlier "deploy runs 28Hz" concern = the CURRENT sync loop; the multi-rate loop runs control faster. No push needed from me.
🎯 **SYSID STATUS (Fengyou asked):** PERCEPTION sysID = effectively DONE (brief gave all numbers). **PLANT sysID = THE OUTSTANDING GATE** — DiffAero side done (`sysid_program.csv`+`sysid_diffaero_log.csv`); VQ2-side command-replay log NOT back yet → overlay+DiffAero-plant-tune pending. **Retrain LAUNCH gated on plant sysID + Adroit GPUs.** Code (vislag rework + obs_v2) built AHEAD so it's ready.

## 🟢 PLANT SYSID RESOLVED — FLAT vs EXPANSIVE rate gain (2026-07-16) — THE g2→g3 wall mechanism
**ShadowPC captured the VQ2 open-loop rate response** (branch `sysid-handoff-2026-07-15` `72f9359`, brief `handoff/sysid-plant-response-2026-07-16.md`; 8 captures, yaw ampsweep is the money run `data/runs/20260716_014656_sysid_yaw_ampsweep_f1`). **RL-commander overlay vs my DiffAero log = DECISIVE:**
- **DiffAero (training plant) = FLAT** cmd→achieved body-rate gain: roll/pitch **2.50×**, yaw **2.23×** (const across amplitude) — because `super_rate_s` defaults `None`/OFF.
- **VQ2 (real) = EXPANSIVE:** yaw gain rises **2.23→2.31→2.48→2.67→2.89×** at |a|=0.1/0.2/0.4/0.6/0.8; shared across axes (roll 2.74 / pitch 2.71 / yaw 2.67 @0.6).
- 🛑 **ShadowPC's "training assumed 1.0×" was WRONG — DiffAero already models ~2.5× base; the gap is FLAT vs EXPANSIVE.** They agree at small/mid cmd, diverge at HIGH cmd: at |a|=0.8 VQ2 gives 2.89× but DiffAero trained 2.5× → policy over-rotates 10-16% (roll) / 30% (yaw) MORE than trained, WORST at the aggressive g2→g3 50-61° banks = the wall + yaw overshoot. Confirms my earlier "super-rate OFF, flat under-predicts real ~42% at full stick."
- 🚩 **THE CALIBRATION (RL-commander fit to VQ2, yaw ±0.2%; scratchpad/overlay/fit_superrate.py):** model `gain=rate_gain/(1−s·min(|cmd|,π)/π)`; faithful nominals **rate_gain=[2.222,2.198,2.164] (roll/pitch/yaw SMALL-SIGNAL) · super_rate_s=0.315 · alpha_max_rps2=[260,260,80] · rate_sign=[+1,+1,−1] UNCHANGED** (yaw −1 = known legacy alias, DO NOT touch). model+constants ALREADY EXIST in `src/racer/rl_plant.py` (`SUPER_RATE_S_MEASURED=0.30`, `ALPHA_MAX_RPS2_MEASURED`) but default OFF; `diffaero_dynamics.PeregrinePlantDynamics` uses `params or PlantParams()` (flat default).
- 🛑 **FOOTGUN caught: do NOT just flip `super_rate_s=0.30` with existing rate_gain [2.501,2.504,2.231]** — roll/pitch 2.50 is the FLAT-MID fit not small-signal → super-rate on top OVERSHOOTS roll/pitch +13% (teaches under-bank). Must recalibrate rate_gain to small-signal [2.222,2.198,2.164] TOGETHER with s.
- 🟢 **BUILD DONE + VERIFIED (2026-07-16; agent stopped mid-report, commander adjudicated from tree + ran tests):** retrain enable = **`++dynamics.faithful_rate=true`** (installs rate_gain=[2.222,2.198,2.164]+super_rate_s=0.315+alpha_max=[260,260,80]). Constants `SUPER_RATE_S_FAITHFUL`/`RATE_GAIN_SMALLSIGNAL_MEASURED` in `src/racer/rl_plant.py`; wired in `diffaero_dynamics.py` (~L363, scalar overrides win, torch caches). `tests/test_faithful_rate_plant.py` **10/10 PASS**: reproduces VQ2 curve ≤2%, roll/pitch NO overshoot, footgun-test (flat-mid overshoots >10%), **default-OFF byte-identical (torch==numpy==rl_plant legacy <1e-12)**, **torch↔numpy super-rate parity bit-exact <1e-12 incl. |a|>1 clamp** (production path safe). NOT committed. rate_sign untouched.
- 🎯 **TWO OPEN FOLLOW-UPS (ShadowPC re-flies):** (a) roll/pitch small-signal [2.222,2.198] inferred from ONE doublet point each + shared-s — a roll/pitch AMPSWEEP (like yaw) would confirm directly. (b) 🚩 **AERO is ALSO default-OFF** (`quad_drag_c2`/`coll_map` MEASURED, off; docstring: legacy "under-brakes 2.2× at 9 m/s, under-predicts full-stick thrust 2.1×") — a real plant-fidelity gap. 🚩 DIRECTION CARE: under-modeled DRAG → deploy would slow MORE (mitigates over-speed, not causes it); but under-predicted THRUST 2.1× → deploy OVER-delivers thrust for the same command → plausibly feeds deploy over-acceleration/over-climb (the thrust axis, NOT braking, is the candidate speed-accumulation contributor). Net speed effect unclear without data ⇒ do NOT claim aero fixes the accumulation ceiling; close it for FIDELITY. ShadowPC's fresh thrust-curve sysID is PARTIAL (crashed ceiling, missed 3g+free-fall; offered a free-fall-first re-fly). ⇒ recommend re-fly thrust + enable faithful AERO for a fully-faithful plant. Then retrain.

## 🟡 RATE CALIBRATION CORRECTED + AERO DATA (ShadowPC round 2, 2026-07-16; branch `0e0e690`)
🛑 **My shipped faithful rate constants [2.222,2.198,2.164]/s=0.315 were WRONG on roll/pitch (base 6-7% LOW).** The direct roll/pitch AMPSWEEPS (round 2) authoritative over my single-doublet inference: roll [2.47,2.46,2.65,2.92,3.07], pitch [2.47,2.46,2.64,2.90,3.04], yaw [2.23,2.31,2.48,2.67,2.89] at |a|=0.1-0.8. **CORRECTED refit: `rate_gain=[2.359,2.363,2.163]`, `super_rate_s`=per-axis `[0.296,0.284,0.316]`** (roll≡pitch; yaw ±0.2%, roll/pitch ~1.5% flat-toe residual). 🛑 **RETRACTED my "naive-enable overshoots roll/pitch +13%" FOOTGUN — it was an artifact of the creep-suppressed doublet (2.74); real roll@0.6=2.92, naive gives 3.05 = only ~4% high, NOT a footgun.** (`scratchpad/overlay/refit_v2.py`.) 🚩 **CREEP:** the plant rate drifts +4-9% WITHIN a hold (0.6: 2.71@0.1s→2.96@0.5s) — a 2nd-order dynamic the static gain model CAN'T capture (roll/pitch=0.3s holds, yaw=0.5s; ~5% residual, inside DR; NOT modeling — racing maneuvers sub-0.3s).
🟢 **THRUST (clean, hover-3g full):** static is CONVEX ≈ **collective^1.5**, col **0.797→5.2g** (~1.7× legacy linear map). + velocity/inflow: **1g at rest → ~0.4g climbing** (qualitative only, no climb-velocity sweep). `coll_map`/`quad_drag`/`lapse` MEASURED constants exist in `rl_plant.py` (default OFF).
🟢 **RATE CORRECTION DONE+VERIFIED (agent, 17 tests):** `RATE_GAIN_SMALLSIGNAL_MEASURED=[2.359,2.363,2.163]` + per-axis `SUPER_RATE_S_FAITHFUL=[0.296,0.284,0.316]` in `rl_plant.py`; reproduces fresh curves (yaw ±0.2%, roll/pitch ~2%/RMSE0.04); false footgun test DELETED; torch parity bit-exact w/ per-axis array. `++dynamics.faithful_rate=true` READY.
🟡 **AERO WIRED but coll_map STALE (agent caught via escape-hatch, refit RUNNING):** flags `++dynamics.faithful_aero` (coll_map+quad_drag+linear_drag=0) · `++dynamics.faithful_plant` (rate+aero) · `++dynamics.faithful_lapse` (default-OFF even under aero) — all default-OFF byte-identical, **torch aero path EXISTS + bit-exact <1e-12**. 🚩 **`COLL_MAP_ACCEL_MEASURED` OVER-predicts thrust +14% @col0.797 (5.93g vs fresh 5.2g), +33% mid-range.** 🛑 **coll_map refit HALTED — never shipped (COLL_MAP byte-identical to HEAD).** The col^1.5/5.2g law is SUPERSEDED by ShadowPC round-3 (`9bb53b9`): the round-2 "static" was CLIMBING-CONTAMINATED (velocity-reduced, read LOW); TRUE static is STEEPER — **exponent ≈col^1.6**, col 0.531→**3.15g** (not 2.2), col 0.797→**~5.4g** (not 5.2), full-stick ~1.8×; 2.5/3g still extrapolated. (Agent independently reconstructed vz + reached the SAME conclusion.) PLUS 🚩 **17° TILT CONFOUND (Fengyou):** every sysID flight launched from the 17°-fwd-tilted pad → forward translation. **RATE curves + THRUST-MAGNITUDE-vs-collective = both ATTITUDE-INDEPENDENT / SOLID (ShadowPC `1f89b2c` confirmed).** Only the INFLOW LAPSE carries the 17° tag: batteries 3-5 were all at ~17° pitch + forward drift ⇒ the lapse is a sample of **thrust=f(collective, full 3-D velocity) in the RACING regime**, NOT f(collective, vertical-vz). ⇒ coll_map refit to the CORRECTED **col^1.6** static (col0.531→3.15g, col0.797→5.4g; high-stick extrapolated) is UN-confounded → OK to do for **v2**; the LAPSE needs axial-body-z-velocity abscissa (recompute from attitude, or accept the racing-regime 17° sample) — defer to v2. ShadowPC RETRACTED the "level lapse steepest" claim (was a mislabeled 32° fly). 🚩 **PITCH SIGN pilot-confirmed: `a_pitch=−0.10 → nose UP/level, +0.10 → down/forward`** (pose-blind wire: integrated attitude is a DEFENDANT, human eyes = ground truth; 2nd time this paid off after gyro-polarity A9). Clean level-start fly now OPTIONAL (nice-to-have pure-axial lapse), NOT required. ⇒ **`faithful_aero` HELD** (wiring installs whatever module constants hold → auto-picks a future clean static, no wiring change; caveat comment in `diffaero_dynamics.py` says don't refit until clean static). 🟢 **TRAINING SPAWN ALREADY MODELS THE TILT:** `peregrine_racing.py:876` applies `VQ1_SPAWN_PITCH_RAD=-0.31` (−17.8° measured) at standing spawn — NOT a gap. Quad-drag + faithful_rate UNAFFECTED/ready. 🎯 **ASKED ShadowPC (relay via Fengyou): one CLEAN LEVEL-START thrust+inflow fly** (level off pad first → vertical collective sweep so world-vz=axial; through-zero statics 2.5/3g) — kills the confound + nails the extrapolation = last plant data.
🎯 **RETRAIN PLAN (decouple settled from churning):** **v1 = `++dynamics.faithful_rate=true` + vision-cadence + `+env.ego_obs_v2=true` + huntfix** (rate is the CONFIRMED wall mechanism + tilt-safe; doesn't wait on aero). **AERO → v2** after the clean level-start static lands.
🚩 **INFLOW LAPSE OFF** — legacy floor 0.78 under-models fresh 0.4g-climbing; qualitative only ⇒ needs a **climb-velocity thrust sweep** (ShadowPC re-fly, ASKED Fengyou). Other tightening = SKIP. Retrain enable: `++dynamics.faithful_rate=true` READY (+ faithful_aero=v2) · **`+env.ego_vision_cadence=true` READY** (vislag REWORKED+VERIFIED — knob RENAMED from ego_vision_latency; 13 tests: OFF byte-identical, valid fresh-fix 7-15Hz band, estimator propagation ACTIVE between frames, dark-near-gate; sub-knobs `ego_vision_frame_hz`=30.0, detector-success p, `ego_vision_cadence_seed`; 2nd spawn `a0f1a8bf` landed it — 1st `af9fb6df` DIED producing nothing/starved-by-3-concurrent-agents; **LESSON: 0-byte agent transcript ≠ dead, check the TREE mtime — af9fb6df file untouched=dead, a0f1a8bf file changed=alive**) · `+env.ego_obs_v2=true` READY · huntfix knobs READY.
🟢🟢 **v1 RETRAIN CODE ASSEMBLED + VERIFIED (2026-07-16). v1 SCOPE (Fengyou: defer confounding vars):**
- **v1 = `++dynamics.faithful_rate=true` + `+env.ego_vision_cadence=true` + anti-dither/8-gate (huntfix)** — the deploy-FAITHFULNESS fixes (make sim match deploy) + the yaw discipline. All default-OFF, all tested (rate 17 tests, vision 13 tests, +57-test ego regression clean), NOT committed.
- 🛑 **`+env.ego_obs_v2=true` DEFERRED → v1.5** (capability-add not faithfulness-fix; changes obs 21→23; ffs0 flies 5 gates without it ⇒ extra variable muddies v1 attribution). BUILT+ready, just OFF for v1.
- 🛑 **AERO (`faithful_aero`) → v2** after a clean tilt-corrected static (coll_map refit to col^1.6 + the 17°-racing lapse w/ axial abscissa). BUILT+held.
- **v1 now gates ONLY on Adroit GPUs.**
🟢🟢 **v1 LAUNCHER ASSEMBLED + DRY-RUN-VERIFIED (2026-07-16). `scratchpad/launch_v1.sh`** (session scratchpad, 208L LF, syntax+both-phase dry-run clean — stubbed sbatch confirmed every token). Successor to `launch_huntfix.sh`. **RECIPE:** BASE_VPEFFS0 + vpef8nc 8-gate block + aw1 anti-dither + the 2 enables `++dynamics.faithful_rate=true` + `++env.ego_vision_cadence=true` (sub-knobs `ego_vision_frame_hz=30.0`/`ego_vision_detect_p=0.35`→~10.5Hz valid; `ego_vision_cadence_seed=20260715+SEED` per-seed decorrelated). **FOLD-INS all in:** UPD=18000 · `yaw_dither_start=1.0` (dither-from-birth) · smoke-gate = **TWO-PHASE** (`MODE=smoke` default submits 1 UPD=100 smoke → adjudicate DONE log → `MODE=arms SMOKE_JID=<id>` releases arms `--dependency=afterok`; escape `SMOKE_JID=none`). **ARMS:** A(seeds 0,1) core · **R(seed 0)** = A + recovery/damping (`rw_roll_recover=0.5` annealed form-A theta0=30°, tunable via `RECOVER=`; recovery tokens PER-ARM to sidestep the anneal both-zero guard) · C(seed 0) = A + `dr_latency_max_steps=5`. **R6 (hard joint yaw-gate select) = POST-HOC deploy-pick** (in-training promote is n_passed-only; `eval_yaw_log=true` emits YAW_EVAL on final+best → reject >~4 flips/s regardless of n_passed, then rank survivors on DET n_passed). **VALIDATION GATE (smoke, absent-hook≠inert): binding is adjudicable from `<rundir>/.hydra/config.yaml`** (grep `faithful_rate: true`/`ego_vision_cadence: true` — force-added `++` keys resolve INTO the tree) + `env_loss/yaw_dither_pen` LIVE via `tb_parse.py` + EGO_PRECHECK_RC=0. 🚩 **PRE-SYNC delta to Adroit (`/scratch/.../peregrine_repo/`) = 5 files or a stale copy silently trains wrong code:** `rl/diffaero_dynamics.py` · `rl/peregrine_racing_ego.py` · `rl/peregrine_train_ego.py` · `src/racer/rl_plant.py` (all UNCOMMITTED v1 code) · **`rl/ego_reward.py` (recovery — committed at HEAD 9711b4c; VERIFY the Adroit copy is ≥9711b4c or arm R's rw_roll_recover is silently INERT)** + the launcher → `rl/launch_v1.sh`. Push via base64-through-`x` (SFTP hangs on Duo); `.sh` already LF. 🎯 **NEXT: pre-stage (Fengyou's connector) + fire on Adroit GPU return** (still `drng@`); monitor TB not stdout. obs_v2 held for v1.5, aero for v2.
🚩 **CRAFT LESSON (banked, 2× now): on a POSE-BLIND wire, MY integrated attitude/estimator telemetry is a DEFENDANT; the human pilot's eyes are ground truth** — caught gyro-polarity A9 + the pitch-sign/17°-tilt. When a sign/label is uncertain, ASK the human observation. 🚩 **BACKGROUND-AGENT DEATH: a 0-byte agent transcript ≠ dead — check the TREE (file mtime + grep the mechanism). af9fb6df (file untouched)=dead/starved-by-3-concurrent-agents; a0f1a8bf (file changed)=alive. Don't run >2 heavy agents concurrently. Agents that stop "waiting on a background suite" = adjudicate from the tree + run the test yourself.**

## 🛑🛑 CENTRAL DIAGNOSIS CORRECTION (2026-07-16, adversarial-verify + DR-code audit) — "FLAT PLANT = THE WALL" IS WRONG
**The training plant was NEVER flat.** `diffaero_dynamics.py:617` — under `dr=true` (which ALL ffs0/vpef8nc/v1 training uses) the DR **initializes `_dr_s = SUPER_RATE_S_MEASURED=0.30` and resamples [0.25,0.35]**, and the torch step reads `_dr_s` (`:838`), NOT `params.super_rate_s`. So the **static super-rate (expansive gain) is ALWAYS ON under DR at ~0.30, regardless of `faithful_rate`** — committed `b14ca2e` **2026-06-10, a MONTH before ffs0/vpef8nc trained (Jul 11-14)**. Verified quantitatively (yaw ampsweep, `scratchpad/plant_val.py`): the ACTUAL training nominal (default G0=2.5 + s=0.30) is EXPANSIVE (gain 2.31→2.57→2.76), tracks VQ2 (2.13→2.33→2.52), and **OVER-predicts VQ2 gain ~8% = the SAFE/under-rotate direction**; per-tick lag-2 yaw-RMSE = **0.40 (DR-nominal) vs 0.39 (faithful) = only ~2% better**; the "0.87/−29%" I first reported was vs DR-OFF flat (0.68 aligned) = a baseline **training NEVER used**.
- 🛑 **⇒ `faithful_rate` under DR = ONLY a ~5% base-gain (G0 2.5→2.36) reduction** (its per-axis super_rate + alpha are IGNORED — DR draws its own). It's a marginal, correct-direction fidelity polish (G0=2.36 IS the best VQ2 small-signal match) — **KEEP it (free, safe), but it is NOT the wall/oscillation fix.**
- 🛑 **⇒ THE WALL / OSCILLATION IS NOT A RATE-GAIN PROBLEM.** The un-faithful training plant over-predicts gain (→ policy under-commands → under-rotates in deploy = SAFE), the OPPOSITE of the observed over-rotation wall. The real drivers are the NON-PLANT levers (consistent with the earlier "8-gate fine-tune ADDED the yaw hunt" + "ceiling=speed accumulation" finding): **8-gate recipe yaw-hunt → anti-dither · vision staleness → ego_vision_cadence · speed accumulation/over-bank → recovery arm.** v1's VALUE rests on THOSE three; faithful_rate was over-credited.
- ✅ **UNCERTAINTY CLOSED (2026-07-16, RL-cmdr boot): ffs0 PROVABLY trained EXPANSIVE — airtight 3-link chain.** (1) The ACTUAL run's resolved `.hydra/config.yaml` binds `dr: true` (+ dr_aero/mixer/force_bias, latency 1-3). (2) The `/scratch` `diffaero_dynamics.py` in place at train time (mtime **Jul 11 02:14**, provably untouched after — its 3 neighbors got Jul-14-19:21 mtimes in the huntfix update, it kept Jul 11; preserved as `*.pre-v1.bak`) CONTAINS the super-rate DR block (`SUPER_RATE_S_MEASURED=0.30` :127, `_dr_s` init :515, step-read :736, resample :975). (3) ffs0 initialized Jul 11 **22:19:17** (.hydra mtime; actor.pth done Jul 12 01:49) = 20 h AFTER that code landed (sacct shows the sync-then-submit at 02:14:31). **Retraction FINAL; faithful_rate = ~2% correct-direction polish; NO more plant tuning.**
- 🟢 **v1 DECISION UNCHANGED IN COMPOSITION** (faithful_rate + ego_vision_cadence + anti-dither + recovery arm) — the three real levers are intact and correctly targeted; just RESET the expectation that faithful_rate fixes the wall. Adversarial-verify synthesis (`wf_aad4bf35-b75`) independently reached "do NOT credit faithful_rate with the oscillation fix; faithful_rate alone is not a defensible v1" via the completeness/direction lenses — this DR-code audit is the mechanism WHY.

## 🟢 CLOSED-LOOP PLANT VALIDATION vs REAL VQ2 LOGS (2026-07-16, Fengyou-directed pre-deploy check) — log inventory [[reference-vq2-flight-logs]]
**Fengyou: "does our setup make sense? run a sysID test — same commands both sims, watch accumulating errors."** Built a per-tick IMU-match validation (criterion he set: same accel/gyro every tick = accurate; pose-blind wire ⇒ NO position GT, do not chase it). Harness `scratchpad/plant_val.py` (sysID replay flat vs faithful, gyro + specific-force accel) + `scratchpad/panel_val.py` (racing). Frames: world NED (Z down, g=+9.80665 +Z), body FRD; specific force f_body = R_wb^T(a_world − [0,0,g]); yaw −1 alias via per-axis sign detection.
- 🟢 **RATE CHANNEL — faithful_rate CONFIRMED on real data (yaw ampsweep, the clean stepped run).** VQ2 realized gain (achieved/cmd) RISES **2.13→2.33→2.52** with |cmd| (EXPANSIVE, on real flight data, independent of the fit); **flat plant is dead-constant 2.20** (13% low at high stick = the over-rotation source); faithful tracks the rise (2.25→2.72). Per-tick yaw-rate RMSE **flat 1.22 → faithful 0.87 (−29%)**. Yaw = the deploy-oscillation axis AND where flat was most wrong ⇒ the fix earns its place on the failure that matters.
- 🟢 **DIRECTION IS SAFE:** where faithful errs it reads HIGH (sim gain ≥ real) → policy UNDER-rotates in deploy = recoverable; flat reads LOW (yaw) → OVER-rotates → the wall. Faithful strictly moves to the safe side.
- 🟢🟢 **NO EXTRAPOLATION + benefit is TARGETED at the failure tail (command-amplitude check on all 6 racing panels):** commanded |rate_frd| roll p90 1.13 / p99 1.79 / **max 2.46** rad/s, pitch max 1.32, yaw **clamped 0.70**. **0% of ticks exceed 2.5 rad/s (|a|=0.8)** = faithful NEVER extrapolates beyond its measurement envelope. Yaw clamp ⇒ faithful yaw only used at |a|≤0.22 (2.33 vs VQ2 2.31, flat 2.20 = ~6% low, modest). **ROLL carries the aggression (max |a|=0.78 at the banks) = exactly where flat under-predicts ~17% → over-rotation → the wall, and faithful corrects.** So the fix is small at the moderate median and DECISIVE at the aggressive-bank tail that crashes — targeted correctly, zero extrapolation risk.
- 🟡 **ROLL/PITCH: faithful ~neutral in RMSE, slightly TOO STEEP at high |cmd|** (faithful 2.78–2.80 vs VQ2 continuous-sweep 2.47–2.52) — bounded by the known creep (~5–8%); fit from creep-inflated stepped holds. LEAVE as-is for v1 (safe-high); candidate v1.5 super_rate refit. (τ + stepped roll/pitch chars being verified via roll_dyn/pitch_dyn/fill.)
- 🛑 **RACING PANELS CONFOUNDED for plant gain** — closed-loop lag/phase/timing dominates per-tick |achieved|/|cmd| (VQ2 roll "gain" 2.94→1.59 DECREASING = artifact); faithful reads "worse" but the comparison is INVALID. **Plant verdict rests on the CONTROLLED sweeps, NOT the panels.** (1-step-reinit salvage being tested.)
- 🛑 **ACCEL/AERO not yet cleanly read** (thrust-curve accel confounded by IC/velocity); the thrust gap is KNOWN (legacy under-predicts full-stick ~2.1×) but coll_map HELD → **v1 stays RATE-ONLY + recovery-arm stopgap**; clean-thrust re-fly = priority follow-up. (accel channel being BUILT by a verify agent.)
- 🔬 **ADVERSARIAL-VERIFY WORKFLOW RUNNING** (`wf_aad4bf35-b75`, 6 lenses: reproduce+frames · direction-safety-attack · racing-confound-salvage · accel-aero-build · rollpitch-τ · completeness → synthesis). Verdict pending; will confirm/refute C1–C7 + decide final v1 scope + any recal. 🚩 KEY DATA LESSON: `race_wire.jsonl` (GitHub) = outcome-only NOT suitable; `sysid_vq2_log.csv` (sysid-handoff branch) = the SELF-ALIGNED cmd+IMU instrument; panels = confounded. → [[reference-vq2-flight-logs]].

## 🟢 v1 LAUNCH STATE (2026-07-16, RL-cmdr Gen-2 boot session)
- **v1 code is COMMITTED + PUSHED** (supersedes the "UNCOMMITTED" flags above): `13857dc` (4 code files + 3 test files) + `e257b72` (handoff + harnesses + memory sync) on `vtrackAr5-recovery-reward`; worktree `cool-heyrovsky-e08624` clean, local==origin. Handoff §4.1 recycle-risk CLOSED.
- **Pre-staged to Adroit, md5-VERIFIED (all 6):** the 5 code files @ `e257b72` + `launch_v1.sh` → `/scratch/network/fl3689/peregrine_repo/{rl,src/racer}/`; pre-push originals preserved as `*.pre-v1.bak` (the Jul-11 diffaero .bak = the ffs0-provenance evidence above). Pusher = direct-socket chunked (`push_v1_files.py`, session scratchpad): 24k-char chunks through the serve daemon — single-shot `x 'echo <b64>…'` FAILS >32K Windows argv; SFTP still hangs on Duo.
- **Phase-1 smoke: job 3312029** (`v1smoke_s0`, UPD=100, seed 0, 8 gates; chain 3310987→3311965→3311968→3312029). 2026-07-16: A100 h11g1 RETURNED ~16:26; 4/4 GPUs contended all evening (fairshare gap ~740 vs the field). Resubmit lessons: `SBATCH_TIMELIMIT=01:00:00` (launcher byte-unchanged) → auto-QOS **gpu-short** (P-class 100; **QOS is walltime-auto-mapped by the submit plugin — explicit `--qos`/`SBATCH_QOS` OVERRIDDEN, `scontrol update QOS` = admin-only; gpu-test unreachable**). 22:40 (Fengyou caught the over-spec): smoke re-issued **GENERIC `gres/gpu:1`** via `launch_v1_anygpu.sh` (2-line sed dropping the CLI `nvidia_a100` pin; sbatch header was already generic) → eligible for the MIG (8×3g.20gb) + V100 (4×) pools the instant h11g2/g3 return from their reboot-timeout brick — those land empty-queued. Risk accepted (fail-loud, smoke-only): MIG 20GB VRAM unproven for 2048 envs (OOM→resubmit typed); V100 torch sm_70 unvalidated (backlog #57). **Phase-2 after adjudicating the COMPLETED smoke log: `MODE=arms SMOKE_JID=<passing-smoke-jid> bash launch_v1.sh` (ORIGINAL typed-A100 launcher) with `SBATCH_TIMELIMIT=07:00:00`** (18k upd ≈ 5.5h; do NOT use 4h/gpu-short for arms — TIMEOUT risk beats the priority gain).
- 🟢🛑 **SMOKE #1 (3312029) VERDICT (2026-07-17 10:44, ran 2m29s on the MIG pool — the generic-gres catch paid off):** TRAINING = ALL-GREEN (S1-S8, S12: every binding live, warm-start+logstd green, **dither-from-birth confirmed at update 0 scale=1.000**, arm-A pure, PRECHECK_RC=0) but **DET_EVAL: FAILED — `accessing private attribute '_w' is prohibited`**: `_accum_yaw` (train_ego:640) read `env._w` BARE through DiffAero's RecordEpisodeStatistics `_`-guard (and its clamp getattr SILENTLY fell back to 0.0 through the same guard). S9/S10 unmeasured → arms HELD. **FIX `a589211`**: drill via the existing `_unwrap_env_with` (the anneal hooks' own pattern; the L16 family strikes again — this time the EVAL side); +tests/test_accum_yaw_unwrap.py (guard repro + clamp-value + flip-count through 2-level chain); 34 tests green + feature regression green; hot-pushed to /scratch md5-ok. **Smoke #2 = 3312166** (same generic launcher). 🚩 Same-rundir rerun: adjudicate the NEWEST event file (the old run's TB file persists beside it); .out is exec-truncated fresh. MIG VRAM question CLOSED (2048 envs fine); V100 sm_70 still unproven.
- 🛑→🟢 **SMOKE #2 (3312166) FAILED IDENTICALLY — fix v1 (`a589211`) was INSUFFICIENT:** the fixed file provably ran (md5+marker on /scratch) yet `raw._w` still hit the guard ⇒ the drill for `"_w"` MISSED: **`_w` is NOT an instance attr — it resolves via the diffaero BASE class** (PeregrineRacing reads but never assigns it), so it's in NO `__dict__` on the chain; my `or env` fallback reproduced the crash. 🚩 **Lesson: the v1 unit test PASSED because it modeled `_w` as instance storage — model the REAL attribute topology (smoke #2 = the integration truth).** **Fix v2 `8e771f4`:** anchor the drill on the racing env's OWN instance attrs (`_egorw` :835 / `_estimator` — the anneal-proven anchors) → plain lookup for `_w` on that holder; test now models `_w` as a class property + pins drill("_w")→None. 35 green; pushed (origin) + hot-pushed (md5 d05b5b42).
- 🟢🟢 **SMOKE #3 (3312167) = FULL PASS (2026-07-17 12:5x, MIG, 3m30s, exit 0): ALL 13 POINTS GREEN.** Evals RAN: `DET_EVAL[...v1smoke_s0] n_ep=8325 thread=0.0040 collision=0.9622 n_passed_gates=1.877` (warm-ffs0-on-8-gate+cadence DET baseline; smoke=critic-only, NOT lever evidence) · **`YAW_EVAL signflips_per_s=2.473` — S9 STOP-CONDITION CLEAR** (<4.0; frozen champion does NOT hunt under the v1 env; cmd_absmean 0.23, roll_swing 57.6°). Resolved .hydra binds faithful_rate/cadence(0.35, seed 20260715)/dither(0.125, start 1.0)/dr:true/eval_yaw_log:true ✓; dither-from-birth at update 0 ✓; PRECHECK_RC=0 ✓; arm-A pure ✓.
- 🟢🟢 **ARMS RELEASED 2026-07-17 ~13:0x: A_s0=3312168 · A_s1=3312169 · R_s0=3312170 · C_s0=3312171** (typed-A100 launcher, `SBATCH_TIMELIMIT=07:00:00`, afterok:3312167 pre-satisfied; gpu-medium 2-concurrent ⇒ ~11h wall once flowing; A100 wall churns tonight). Monitor per A-watchlist (explicit tb_parse tags; yaw_dither_pen DOWN; arm-R quiet till ~upd 6000; spin exits flat as fence tightens). NEXT: review + rehearse `wf_selection/v1_yaw_sweep_cluster.sh` on the smoke's checkpoints/ while arms queue/run (W2).
- 🟢 **MONITOR PASS 1 (2026-07-17 21:03): A-pair HEALTHY at 90% (upd 16,17x/18,000), pace 1.19 s/upd ⇒ 18k ≈ 5.9 h on a MIG slice** (≈A100-class for this workload — the slice is not the bottleneck). Live tail: loss ~−0.7, l_episode 7.5, success_rate 0.21 (training-rollout metric, NOT the DET verdict), fps ~28k, mem 2.57 GiB steady, 0 tracebacks. 🚩 **Live .out shows ONLY the tqdm bar (stderr, unbuffered); the anneal/hook prints sit in python's stdout buffer — L16 again: absent prints on a LIVE job ≠ inert; adjudicate from the COMPLETED log + TB.** ETAs: A-pair final evals (v2-fixed → YAW_EVAL prints) ~21:40 EDT; R_s0+C_s0 auto-start on cap-free, done ~03:40. Then: per-arm completed-log adjudication → W2 sweep rehearsal → morning 4-arm deploy-pick (rank ONLY in the 1200-step sweep). Worktrees: `wt-fix` (branch @ 8e771f4, pushed) · `wt-ratchet` (@ 3389f68, pushed) — both clean, keep. Daemon pattern: transport idles out between check-ins → one Duo tap revives (heartbeat offer open with Fengyou).
- **Huntfix leftover job 3310178** (submitted Jul 15 10:31 = the huntfix launcher's minute) = SUPERSEDED by v1 → ✅ **CANCELLED 2026-07-16 13:27 (Fengyou-authorized; 00:00 elapsed = never ran, zero GPU-hours lost).**
- Quota healthy (scratch 67/93 GiB). ffs0 warm ckpts confirmed present (`actor.pth`+`critic.pth`). `tb_parse.py` recovered from the predecessor's session scratchpad (was NOT in the preserved handoff dir).

## 🟢 PRE-FLIGHT AUDIT of the v1 submission (2026-07-16, Fengyou-directed) — VERDICT: GREEN, queued job is the audited combo
- **sbatch↔launcher contract VERIFIED line-by-line** (was only ever stub-dry-run): RUNTAG→`exec > peregrine_vq2_ego_<RUNTAG>.out` (exact checklist path) · STAGES/UPD_<stage>/SEED/PRECHECK consumed · `hydra.run.dir` = exactly the checklist RUNDIR · `${EXTRA}` appended LAST so its `++` wins (proven by ffs0's resolved .hydra carrying its EXTRA values) · precheck runs WITH EXTRA (arm wiring self-guarded) · `--gres` CLI overrides header · huntfix leftover **3310178 CANCELLED** (never ran).
- 🛑 **SIBLING-DRIFT CATCH (the audit's keep): 7 of 8 sbatch-imported files on /scratch ≠ branch, and /scratch is AHEAD — blind branch-sync would have REGRESSED the proven July runtime.** Adjudication: `ego_estimator`/`vq2_ego_curriculum`/`peregrine_racing` = **CRLF-only** (content==e257b72; old pushes carried Windows CRLF — my pusher uses git blobs=LF); `inc8_warmstart` = scratch has `reset_actor_logstd` the branch LACKS, pushed train_ego has dual-path import+fallback (`:84/:98`), double-fire idempotent; `peregrine_course` = scratch has Track-A `lateral_offset_m` jog, **byte-identical OFF** (v1 never sets it); `inc8_tb_trace` = smarter FLIGHTCHECK (standing-class + min_step 100), awk field names kept; `inc8_estimator_emul` = internal refactor+latency additions, ego runtime imports ONLY `quat_xyzw_to_matrix_torch`+`_RZ_PI_BODY_NP` (identical both versions). **Rule: /scratch siblings = uncommitted-AHEAD states; NEVER sync-down without content diff.**
- ✅ **21/21 v1 knobs verified READ by the pushed blobs** (faithful_rate→diffaero_dynamics; cadence×4→racing_ego; dither×4 + recovery×6 + eval_yaw_log + critic_grad_clip + ckpt_select_metric→train_ego/ego_reward; rw_finish_time/rw_v_cap/rw_cross_zero_m→ego_reward).
- ✅ **MIRROR-TEST: 35/35 (new features) + 80/80 (ego_reward incl. recovery/dither) tests PASS against the EXACT cluster combination** (git-archive e257b72 + the 4 real-drift scratch siblings overlaid). The pattern: validate the combo that will RUN, not the branch tree.
- 🚩 **QOS `gpu-medium` = MaxTRESPU gres/gpu=2** (no submit cap) → arms trickle 2-concurrent, ~11 h wall for 4 arms (~5.5 h each @18k, ffs0 scaling). 🚩 **afterok is WEAKER than it looks:** the sbatch has no `set -e` — a MAIN-stage python crash still exits 0 (only a precheck failure exits nonzero) → afterok would release arms after a crashed smoke. **The completed-log manual adjudication is the real gate — never skip.**
- 🛑 **A100 node `adroit-h11g1` reboot TIMED OUT** (`drained; reboot issued : reboot timed out`) — needs RC-staff intervention, ETA unknown. Contingency if >~24 h dead: resubmit `--gres=gpu:tesla_v100:1` (V100 ~2× slower, node also draining). Smoke 3310987 stays queued (auto-start).

## 🟢🟢 ADVERSARIAL PRE-FLIGHT WORKFLOW `wf_db82218c-9ce` (2026-07-16, Fengyou-directed; 7 agents/1.4M tok) — **GO-WITH-WATCHES, ZERO blockers**; risk mass = post-hoc SELECTION, not the queued job
Six lenses (semantics/empirics/warmstart/cadence-data/selection/deploy-contract) + max-effort synthesis; synthesis independently re-verified protocol claims in MIRROR; commander spot-checked the 3 scariest (clamp default, eval-censor+association-flip code, tb_parse filter) — all CONFIRMED. Deliverables: session scratchpad `wf_selection/v1_yaw_sweep_cluster.sh` (the pick harness), `wf_warmstart/rehearsal_output.log`, `wf_empirics/empirics_v1_report.json`, `wf_cadence/*`. Watchlists folded into tasks; key durables:
- ✅ **Levers DOUBLY confirmed live** (schedule-import probes + step-level empirics): dither 0.125 FROM UPDATE 0 (`start`=scale-at-birth, never a start-time; inversion refuted); bang-bang @0.7 rail = 0.243/tick vs theory 0.245, smooth tracking ~0; tax = 18.6% of progress at deploy-rate dither (livable bleed, un-farmable); recovery ramps 0→0.5 over front 70% (INERT AT BIRTH BY DESIGN — arm R shows no delta before ~upd 6000, do NOT false-alarm); spin fence never tighter than configured, never disabled; faithful_rate under DR = G0-only 2.50→2.36 (−5.7%) — attribution stance re-confirmed.
- ✅ **Warm-start rehearsed GREEN on the real ffs0 weights** (37 PASS): actor (256,21)→(128,256)→(4,128)+LN, logstd (1,4); critic in=16; no-sidecar path clean; **ffs0 ships std≈0.03 → the 0.18 logstd reset is LOAD-BEARING**; double-fire idempotent.
- ✅ **Deploy contract element-EXACT** (20k-trial obs parity 0 mismatches; action pipeline: wire FRD yaw == training clamped ch3 exactly; obs_v2 append-only verified, OFF=21-dim bit-identical).
- 🛑🛑 **DEPLOY FINGERPRINT CORRECTION — the old numbers are STALE for the champion era; do NOT grade v1 against them.** Champion vpeffs0 @28404fa (post vision-fix): valid-fix **19.4 Hz** pooled (18.0–20.9), per-frame q=0.802, gaps med **42 ms** / p99 ~200 / **max 438 ms**, gap-mass>0.5 s = **0%**, loss-onset **1.5–2.8 m**. The old "7-15 Hz / 0.22 s median / 4 s tail / dark <4.7 m" came from the earlier mixed/oscillator-era batch; the 1–2 s outages are vpef8nc's SELF-induced FOV loss while yaw-hunting (q<3m=0.072) — an effect training reproduces mechanistically and anti-dither attacks at the source. Near-field: training geometric blackout onset 1.3–2.4 m ≈ deploy 1.5–2.8 m = **MATCHED** (the 4.7 m prose is produced by nothing).
- 🟡 **Trained cadence reality: net per-frame 0.315 (p=0.35 × estimator miss 0.10) → 9.45 Hz** (not the advertised 10.5); the 30 Hz frame clock is VACUOUS at dt=0.0333 (fires ~99.9% of ticks — throttle is really per-tick Bernoulli); model is **~2× pessimistic vs champion (burstier, longer gaps) = CONSERVATIVE for v1**, teaches coast — correct direction. **v1.5 recal:** range-dependent detect_p (q≈0.95 mid / 0.63 far / 0.41 near) or single-knob p≈0.72; fix stale prose (racing_ego :649-655 "40 Hz", launch_v1.sh:13 "4.7 m").
- 🛑 **SELECTION PROTOCOL (the real residual risk, all fixes protocol-side):** (W1) `eval_det_steps=600` = 20 s window right-censors slow-clean 8-gate finishers (≈27-53 s laps) — biased against arm R exactly if it works → **rank the deploy-pick ONLY within the 1200-step `++rollout_only` sweep, never mix with in-run 600-step lines**; (W2) rollout_only NEVER yet executed → **rehearse the sweep sbatch on the SMOKE's ckpt before arms finish**; (W3) YAW_EVAL association flip: if `BEST_CKPT_PROMOTED` fired, the plain `YAW_EVAL[<run>]` line describes NON-shipping weights — gate on `[<run>/best]` (path-based sweep sidesteps); (W4) snapshot inventory THIN ({checkpoints,best_npg,periodic,periodic_prev}) + best_npg's "rolling" mean is ~cumulative (deque 2.048M) → best≈final often; optional mid-run periodic_prev/ harvest; signflip metric VERIFIED 9/9 (per-second, dt-invariant, deadband 0.05, per-env MEAN — 4.0 = REJECT-only, a 40%-duty hunter reads ~2.8; prefer <1 among survivors).
- 🛑 **S9 = the ONE smoke stop-condition:** frozen-ffs0-under-v1-env YAW_EVAL >~4 flips/s ⇒ the v1 env itself induces hunting in the smooth champion (from-birth tax would bite a dithering policy from update 0) → HOLD Phase 2. Everything else anomalous-looking is pre-explained (smoke=UPD 100 lies entirely inside critic_warmup=100 ⇒ actor grads ZEROED all smoke, trains critic only, do NOT read smoke n_passed as lever evidence; `no best_npg/ snapshot to promote` EXPECTED since save_freq 500>100).
- 🚩 **Deploy pick-flight flags (fly_rl defaults BITE):** `--ego-yaw-clamp` **defaults 0.0=OFF** (help text itself: MANDATORY 0.7 for clamp-trained ckpts — spin door at 4.5× authority otherwise) · `--ego-sector-mode map --ego-coarse-map <f7f44a3>` load-bearing (auto ⇒ horiz always 0) · sweep `--ego-det-hold {0.2,0.35,0.5}` (v1 = FIRST ckpt trained to exploit ego-propagated obs in the 0.2–0.5 s staleness band that deploy det_proxy age<0.2 s zeroes) · `--ego-stale-horizon 0.5` · governor OFF.
- 🛑 **W8 v1.5 LANDMINE:** `maybe_pad_ckpt_for_obs_v2` gates on ROOT `cfg.ego_obs_v2` (train_ego.py:166) while docs/call-site/env define `+env.ego_obs_v2` → arming v1.5 as documented skips the pad → 21-dim ckpt strict-loads into a 23-dim net = loud RuntimeError. Fix before v1.5 (backlog #83).

## A-PAIR ADJUDICATION (2026-07-17 ~22:00 EDT, from COMPLETED logs — L16-compliant)
- **Both A arms CLEAN end-to-end**: `VQ2_EGO_CURRICULUM_DONE` + FLIGHTCHECK=FLEW both; 0 Tracebacks/CUDA/NaN (the 2 grep hits = the precheck banner's own "(wiring/NaN/dim)" text); **0 HAZARD lines ⇒ no fatal spin-abort exits anywhere in training** (no-spin-by-construction held at 8 gates); `BEST_CKPT_PROMOTED` count 0 both ⇒ **shipped checkpoints/ = FINAL 18000-upd weights; plain YAW_EVAL[run] line IS the shipped ckpt** (W3 moot for A).
- **A_s0 (3312232, 6:04+)**: precheck DET n_passed 1.9428/flips 2.448 → final **n_passed 3.9419, thread 0.2174, collision 0.7736, miss 0.0090, flips 6.204, cmd_absmean 0.665, roll_swing 57.7** (600-step LIVE-env window).
- **A_s1 (3312233, 5:54:17)**: precheck 1.8754/2.452 → final **n_passed 3.3767, thread 0.1280, collision 0.8670, miss 0.0052, flips 5.503, roll_swing 55.1** (vs precheck 58.3 — no instability signature).
- **Read**: gates DOUBLED both seeds (thread up 37×/53×); seed spread ~17% on n_passed (way under the historical 2.8× — multi-seed doctrine looks satisfied for A). **Yaw: both finals >4 flips/s in-run — a FLAG, not a verdict**: at 2× gate throughput legitimate tracking flips rise; but per-gate-normalized dither is ALSO up (~1.26 precheck → ~1.6) so some real added dither exists. Judge = the 1200-step uniform sweep (W1), tiebreak roll_swing; deploy truth = pick-flight with clamp 0.7.
- Baselines cross-node consistent: precheck flips 2.448/2.452 ≈ smoke S9 2.473 ✓.
- **R 3312234 auto-started ~21:28, C 3312235 ~22:05 (QOS cap chaining worked; SMOKE_JID=none escape hatch validated). ETA both ~03:40-04:15.** Rehearsal 3312320 (gpu-short) queued Resources behind them — runs when any slice frees; morning fallback fine.

## R/C + REHEARSAL ADJUDICATION + SWEEP FIRED (2026-07-18 morning, COMPLETED logs)
- **R_s0 (3312234, 5:56, exit 0)**: precheck 1.9428/2.448 (== A_s0 baseline, same seed ✓) → final **n_passed 3.9122, thread 0.2089, flips 6.012, roll_swing 52.81 (best of portfolio)**, collision 0.784. Recovery reward COST NO GATES vs A_s0 (3.94) and bought roll stability. 0 errors/HAZARD/promotions.
- **C_s0 (3312235, 6:09, exit 0)**: precheck **1.5872**/2.505 — lower baseline because precheck runs under C's OWN latency-5 env (ffs0 trained ≤3; expected) → final n_passed 3.2727, thread 0.119, **flips 7.130 (worst)**, roll 57.7. 🚩 C's in-run numbers are on its HARSHER env; the uniform sweep (latency 1-3 BASE) is its fair read. 0 errors/HAZARD/promotions.
- **REHEARSAL 3312320 PASS (ran 21:53-21:56 on A_s1's freed slice, 2:41 total)**: `[rollout-only] LOADED`, 1200-step census, YAW_EVAL parseable: **flips 2.449 ≈ in-run S9 2.473 ✓; n_passed 1.9946 @1200 vs 1.877 @600 (censoring relief confirmed)**; symlink double-eval = bit-identical numbers (free determinism check). Banner prints `yaw_cmd_clamp=?` (cosmetic — env has the clamp, banner can't read it). **This line = the ffs0-baseline ROW for the ranking.**
- **SWEEP 3312393 SUBMITTED** (4 arm rundirs verified; queue empty ⇒ immediate start; ETA ~25-45 min; each run evals {checkpoints,best_npg,periodic,periodic_prev} × 2 symlink paths). **DECISION TENSION queued: ALL arms' in-run flips 5.5-7.1 > the 4.0 hard gate.** If the sweep confirms >4 across the board: earlier snapshots ≈ final (rolling best monotone), so the fallback = pick-flight empirically tests the top-n_passed ckpt (sim flights free; zero-contact + gates = the ultimate judge) vs ship-nothing. Per-gate-normalized dither + roll_swing inform but do NOT override the flight test.

## SWEEP 3312393 VERDICT + RELEASE (2026-07-18): PICKS = A_s0@17500 + R@17500
- Sweep 41:44, exit 0, all 16 evals green, symlink-pairs bit-identical. Dedup table (1200-step): **A_s0@17500 n_passed 4.6515 / flips 6.389 / roll 55.69** (beats its final 4.37) · **R@17500 4.3085 / 6.024 / 52.20** · C 3.94-3.96 / **8.8-8.9 REJECT** · A_s1 3.42-3.73 / 5.5 dominated. ffs0 baseline row (rehearsal) 1.9946 / 2.449. 17500 `periodic_prev` snapshots beat the 18000 finals for A_s0 and R (anneal-endpoint cost).
- 🛑 **4.0 flips/s hard gate OVERRIDDEN (explicitly flagged to Fengyou):** per-gate-normalized dither = A_s0 1.37, R 1.40 vs champion's own 1.23 ⇒ champion-class smoothness at 2.2× throughput; deploy clamp 0.7 caps realized yaw; **pick-FLIGHT (zero-contact + gates) = the binding judge.** If deploy shows oscillation → fall back to champion; release stays published either way.
- **RELEASE `ego-ckpts-v1-2026-07-18`** (A_s0 md5 fb5605ec…, R md5 1e71e0cc…, both pulled chunked via daemon + verified). Notes = full pick-flight recipe: REPORT4 ground truth (TRT M-engine, assist 1.3, LOG-ECHO map side file, det-hold 0.2) + clamp 0.7 MANDATORY + settled GO ≥3 s + det-hold sweep {0.2,0.35,0.5} + `--ego-pitch-clamp 30` experiment arm + governor OFF. Pull tooling: `scratchpad/pull_v1_ckpts.py` (reverse of push, chunked b64, md5-gated).

## PICK-FLIGHT VERDICT (2026-07-18 @6bd989e): FAILED — sim yaw metrics transfer 1:1; my gate-override was WRONG
- 5 flights via panel pick-wiring 394eb33 (= full champion recipe INCL. pitch 1.0; log-echo map now FROZEN at configs/vq2_coarse_map_champion_logecho.json): **v1pick_A 5.5s/1 gate/cmd-flips 4.4/absmean 0.640 · v1pick_R best 7.6s/1/5.2/0.671 · R×3 ~2.6s/0 gates/6.1-6.8/0.60-0.63 · champion (same pipeline) 10.8s/5 gates/2.2/0.151.**
- **Transfer table: sim YAW_EVAL → wire is ~1:1** (v1 flips 6.3/6.0→4.4-6.8, cmd 0.66→0.60-0.67; champion 2.45/0.23→2.2/0.15; champion n_passed 1.99→deploy median 2 ✓) **but v1 n_passed 4.65→~1 ✗** ⇒ hot-yaw + high sim throughput = sim-gap exploitation (trained Bernoulli cadence ≠ real burst/gap detector; pitch frozen at deploy vs FREE in training removes a framing channel v1 uses → yaw compensates). Anti-dither 0.125-from-birth did NOT produce deploy-quiet yaw: v1 cmd amplitude ~4× champion (pegged at clamp ~constantly).
- **Selection doctrine (updated):** flips ≤4.0 = HARD (reinstated; per-gate normalization is NOT a deploy predictor) + **cmd_absmean ≤~0.3 = NEW HARD gate** (sharpest separator, 4×) → then rank n_passed. New-lineage pick-flights fly TRAINING action space → [[feedback-training-faithful-deploy]].
- **v1.5 direction:** training-side yaw-QUIETNESS: amplitude/duty shaping (penalize |yaw_cmd| duty above threshold, not just sign-jitter) + reward gate-CENTERED perception (quiet framing must beat scanning under cadence) + cadence realism recal (range-dependent detect_p, W-item) + obs_v2 (#83 pad fix first). Optional cheap diagnostic first: 1-arm pitch-FREE re-fly of A@17500 (tests the freeze-amplifies-hunt mechanism; does NOT unblock v1.5).

## REWARD-LEDGER FORENSICS (2026-07-18, agent, code @7b c5305 — full report scratchpad/REWARD-LEDGER.md)
- **Effective arm-A ledger:** progress 2.0/m (segment-ARC POTENTIAL, telescoping — total course credit FIXED; 39 m/s clip never binds) · time −0.02/step · perception 0.02·exp(−δ⁴) CAPPED ≤rw_time (~4% of progress @8 m/s — negligible) · centering 0.4 · corridor 4.0 · yaw-dither 0.125 (−0.04/step avg ≪ +20/gate) · rw_v_cap=0 · contact parabola 20/0.75/−100 **and frame strike KEEPS banked gates — death is CHEAP** · finish 20+0.25·t_left · γ=0.9975 ⇒ 400-step/13.3 s horizon.
- 🛑 **RUNAWAY MECHANISM (corrects my per-step-speed-pay story): it is the DISCOUNT, not speed income.** Progress telescopes (no per-step speed pay); but shaving 1 s moves ALL downstream gate bonuses + finish earlier ⇒ +12–27 reward per second shaved; only opposition = discounted crash (~204 incl. banked / OOB 200) ⇒ **break-even crash-prob ≈5–13% per second shaved ⇒ argmax = ride the controllability EDGE with zero margin. The edge is PLANT-DEPENDENT: sim edge ~12 m/s; the expansive deploy plant fails earlier ⇒ sim-optimal speed = deploy-fatal. That IS the sim-gap exploitation, formalized.**
- **Pitch-down:** perception term too weak (4%) to hold the nose up — nothing prices attitude until the gate exits FOV and terminals hit. **Brake punished 3 ways CONFIRMED** (progress 0/neg + time/discount bleed + nose-up drops gate out bottom FOV → slot0 HARD-masked, obs_coast=False ⇒ blind).
- 🛑 **CORRECTIONS to my claims:** velocity IS in obs (obs[0:3] = estimator body velocity — observable, just UNPRICED; "add velocity to obs" = no-op) · **obs_v2 +2 dims = next-gate SECTOR, not velocity** (#83 context updated) · yaw hunt reads as **current-gate bearing-null LIMIT CYCLE, not next-gate scanning** (perception_next=0, slot1 masked-zero, no map prior ⇒ scanning has weak value; 8-gate = 7 hard re-point handoffs trained the aggressive null).
- **Agent Rx (pre-synthesis):** (1) overspeed episode-ABORT by construction (no-spin precedent; ‖v‖ GT in termination = legal; velocity observable ⇒ learnable) · (2) SATURATE progress credit at v* (clip Δs credit; no penalty ⇒ no vel-cap-stall risk; partial — discount lever survives, PAIR with 1) · (3) dither 0.125→0.4–1.0 + duty/jerk form (preserves gate-tracking) — agent also suggests deploy clamp →0.35 ⚠ conflicts [[feedback-training-faithful-deploy]] unless v1.5 TRAINS at 0.35 (open design point) · DECLINES accel-cap (accel unobservable; energy-penalty-like; doesn't bound top speed).

## FLIGHT FORENSICS (2026-07-18, agent — scratchpad/FLIGHT-FORENSICS.md + flight_forensics.py, numbers cross-validated vs my v1pick_yaw_diag)
- **YAW = latency LIMIT-CYCLE CONFIRMED; scanning REFUTED; self-induced-perception-loss REFUTED.** v1 bearing→yaw gain ~2.0 rad/s @10° err = **12-16× champion (0.14)**; **clamp-saturation duty 68-90% vs champion 0%**; ~6 Hz bang-bang; sweeps track ACTIVE gate 76-84% (slot1 53%=chance); flips independent of slot1 staleness; det-loss after sweeps ≈ baseline. **Champion is quiet purely by LOW GAIN (2-gate luck)** — same bearing geometry, same slot1 freshness. Deploy flips == training flips ⇒ the >4 training flag was REAL instability (my throughput-artifact reading wrong twice over).
- **RUNAWAY = LINEAGE-WIDE, NO learned brake anywhere**: champion ramps 6→15 m/s and dies AT 15; p03_r1 peak 20.5; v1_A 12.4; longest brake streak 0.34 s, ineffective. Confirms ledger argmax in data.
- 🛑 **v1_A pick-flight flew pitch_clamp=0.0 (FREE)** (R flew fenced 1.0) ⇒ we ALREADY have the pitch-free diagnostic: **A = the dive** (median −29°, min −65°, 47% of flight <−30°, still accelerating at −38°, camera-down FOV loss %see0 0.58) ⇒ **the deploy fence was MASKING the dive, not curing it**; pitch-free re-fly = MOOT. R deaths: at g0, off-aligned arrival → gate contact, yaw saturated at death + det lost (v1 signature ≠ champion-pop's quiet-yaw/fresh-det g2 deaths).
- **V1.5 PRESCRIPTION (twin-forensics synthesis): warm from FFS0, NOT v1** (v1's 12-16× yaw gain is poisoned; ffs0 = the low-gain prior) + 8-gate + cadence + **(1) overspeed episode-ABORT ~12 m/s by construction** (no-spin pattern; velocity IS obs[0:3] ⇒ learnable; GT ‖v‖ in termination legal) + **(2) progress-credit SATURATION at v*≈7-8 m/s** (clip Δs credit ⇒ deletes discount income above v*; NO penalties ⇒ no vel-cap-stall repeat) + **(3) yaw AMPLITUDE/DUTY + Δcmd-jerk pricing** (flip pen 0.125→0.4+; sustained re-points stay cheap, constant saturation expensive) + pitch FREE + trained clamp stays 0.7 (champion proves 0%-saturation tracking possible at 0.7; don't fence the re-point). **NEW selection gates: flips≤4 ∧ cmd_absmean≤0.3 ∧ clamp-saturation-duty≤~10%** (sharpest separator; add duty to YAW_EVAL print). S9-analog smoke gate: frozen ffs0 under v1.5 env stays quiet. Death-pricing (crash keeps banked = cheap) = LATER lever, not now (fear→stall risk).

## V1.5 SMOKE ADJUDICATION + ARMS LAUNCH (2026-07-18 ~15:50)
- Build @ca9da6c (5 mechanisms default-OFF, 18+342 tests) + @191b58f (my review patch: recovery ANNEAL restored — from-birth positive shaping = farmable; R validated WITH anneal). 4 files pushed md5-exact. SMOKE 3312707 (full A100 h11g1, ~3 min).
- **CHECKLIST 5/5 PASS**: (1) .hydra keys all live incl. recovery_anneal:true ✓ (2) DONE+FLEW, 0 errors ✓ (3) evals print max_speed + satur_duty appended ✓ (4) TB tags ×4 present (10 events each) ✓ (5) FREE-BAND PROOF: frozen-ffs0 flips 2.414/2.435 ≈ baseline 2.45 ✓, cmd_absmean 0.207 < 0.25 band ✓, **satur_duty 9.2% ≠ ~0 — ACCEPTED DEVIATION**: the tail (re-point bursts at clamp) not the mean; tax ≈0.013/step ≈ 2.5% of progress, ~10% of a re-point's gate bonus — desirable pressure, not tracking suppression.
- 🛑 **oob 0.7979 (was 0.0010 under v1 env) @ max_speed 12.67-12.78 = the OVERSPEED ABORT consuming the runaway baseline**: frozen ffs0 hits 12 m/s in ~80% of episodes within 600 steps — the lineage-wide runaway made fatal at birth = the designed curriculum gradient (no-spin precedent: fatal aborts from birth worked for despin). Watch: if overspeed_abort_rate is NOT declining by ~upd 3000, intervene (raise abort to 13-14 or check saturation wiring). n_passed 0.85 frozen-baseline = expected collateral (episodes truncated).
- **ARMS: 3312711 Q_s0 RUNNING full-A100 h11g1 (expect FASTER than 1.19 s/upd MIG pace ⇒ ~4-5 h) · 3312712 Q_s1 (Resources) · 3312713 W_s0 (Priority).** Launched MODE=arms SMOKE_JID=none (afterok-purge footgun, smoke manually adjudicated). Launcher's own monitor doctrine: TB not stdout; duty/jerk/forfeit converge DOWN, abort_rate → ~0, n_passed climbs.
- 🚩 CONNECTOR PROTOCOL (from source, replaces my 3 wrong theories — daemon serve loop SERIAL + run_remote UNBOUNDED; `ran:` prints POST-completion; Duo-reauth on dropped transport BLOCKS the loop; Windows can double-bind two serves on 8765): `x "__ping__"` = loop-health (no SSH) · command_history.log (local) = completion ground truth · ONE short command per call · `timeout N` on every /scratch command · check history before RESENDING (double-exec hazard) · ONE serve window only.

## V1.5 Q_s0 MID-RUN PULSE (upd ~10.1k/18k, 2026-07-18 ~18:20, TB via tb_parse15.py = the LOCAL substring-filter variant pushed to /scratch/network/fl3689/tb_parse15.py; cluster rl/tb_parse.py is a DIFFERENT KEYS-list tool)
- **THESIS WORKING: overspeed_abort_rate → 0** (peak 1.1% in first ~1k upds, then flat 0 — vs frozen baseline 80%; speed discipline learned ~immediately) · **yaw_duty_pen 0.069→0.016 (4×↓), jerk_pen 2×↓** · prog_sat_forfeit ~0.028 (brushes v* lightly).
- **Cost: n_passed U-shape** 0.38@770 → 0.21@6.2k → 0.36@10k (rebuilding); l_episode ~2.7, collision ~0.95, postfix success 0.00 — conservative phase; back half (noise anneal from 50%) = where v1 historically climbed. NO intervention (the intervene-condition was abort-rate-stuck-HIGH; opposite happened). Adjudicate COMPLETED log only.
- Q_s0 pace 0.87 s/upd full-A100, ETA ~19:45 EDT · Q_s1 3312712 + W_s0 3312713 PENDING (cluster Priority).

## 🏆 Q_s0 FINAL ADJUDICATION (3312711, DONE 4:18:38 exit 0, 2026-07-18 ~20:15)
- **n_passed 4.1006** (> v1-A 3.94; 4.8× the truncated frozen baseline 0.85) · thread 0.1385 · collision 0.857 (censored-window class) · miss 0.0037 · **oob 0.0013 (abort EXTINCT — was 79.8% frozen)** · **max_speed 12.20** (self-held under the 12 abort; transients only).
- **YAW: flips 1.230 (HALF the champion 2.45, 1/5 of v1 6.2) · cmd_absmean 0.1937 (< champion 0.207) · satur_duty 0.0109 (limit cycle GONE)** · roll_swing 47.8. ALL THREE selection gates pass with margin; in the "prefer <1" zone for flips.
- 0 Tracebacks/CUDA · 0 PROMOTED (shipped=final 18k) · DONE+util logged. U-shape resolved: 0.36@10k → 4.10@18k (back-half anneal, per lineage history).
- **Meaning: first checkpoint whose SIM yaw metrics (1:1 wire-transfer per the v1 lesson) PREDICT champion-or-quieter deploy while beating v1 gates — the /goal sim-side trifecta (threading ≥, runaway structurally gone, yaw quiet) holds in one policy.** Deploy = the remaining judge: TRAINING-FAITHFUL recipe (pitch FREE, yaw 0.7, settled GO, log-echo map, TRT M-engine).
- NEXT: Q_s1 3312712 + W_s0 3312713 RUNNING (~4.3 h each, staggered ~57/36 min in) → same adjudication → **1200-step sweep needs V15 ENV KEYS: adapt v1_yaw_sweep_cluster.sh EXTRA_COMMON += V15_SHARED (abort 12/vcap 7.5/duty 0.15/band 0.25/jerk 0.05/recover 0.5+anneal) + dither 0.4; satur_duty prints automatically (train_ego on /scratch = v1.5 code)** → release → pick-flights.

## V1.5 FULL PORTFOLIO ADJUDICATED (2026-07-19 ~00:20) — ALL CLEAN, SEED-REPLICATED, SWEEP FIRED
- **Q_s1 (3312712, 4:24:18, exit 0): n_passed 4.1709 / thread 0.1957 (portfolio-best) / flips 1.309 / cmd_absmean 0.1420 / satur_duty 0.0009 / max_speed 11.97 (NEVER touched the abort; oob 0.0000) / roll 47.95.** Seed pair Q = 4.10/4.17 — tight replication vs the historical 2.8× curse.
- **W_s0 (3312713, 4:14:50, exit 0): flips 0.595 (¼ of champion) / absmean 0.1302 / satur_duty 0.0000 / roll_swing 36.12 (project-record calm) / n_passed 3.3288 / max_speed 12.08** — the strong-pricing arm bought yaw-silence + roll calm for ~0.8 gates. If deploy yaw quality dominates the pick, W is the safety candidate.
- Both: 0 PROMOTED (shipped=final), 0 errors. TB (mid-run reads): abort-rate flat 0 both; duty/jerk down 15-18×.
- 📁 Sweep 3312994 fired ~00:20; queued overnight, ran 07-19 morning (34:12, exit 0) → adjudicated below.

## 🏁 CENSUS 3312994 ADJUDICATED + RELEASE PUBLISHED (2026-07-19) — MODEL READY FOR FLIGHT
- **Census** (uniform seed 0 + cadence seed 20260715, 1200 det steps, abort-honest; dedup: best_npg=checkpoints=periodic=final): **Q_s1 final 4.459 g / 1.329 flips / 0.143 absmean / 0.09% satur** · Q_s0 final 4.432 / 1.239 / 0.195 / 1.1% · **W_s0 periodic_prev 4.282 / 0.660 / 0.142 / 0.01% / roll 33.4 — beats W's own final (3.726) by +0.55 g** · Q@17500s ~4.24 · smoke control 0.862 / 2.412 flips / 9.1% satur (honesty row confirms the full recipe matters). Census reads HIGHER than in-run finals (1200 vs 600 det steps un-censors laps). **Snapshot verdict: finals beat @17500 for Q (anneal completed clean — v1's snapshot-wins pattern NOT repeated), but W's periodic_prev wins (heavy W dither/duty weights over-damp by the end).**
- **Q_s1 promotion flag benign:** log L7726 "cannot promote" (runner-save race) — census proves best_npg ≡ final (6 metrics identical under det eval) ⇒ the final WAS the best-by-n_passed.
- **RELEASE [`ego-ckpts-v15-2026-07-19`](https://github.com/Hat000/Peregrine/releases/tag/ego-ckpts-v15-2026-07-19) PUBLISHED** — 2 actors (162,903 B each, chunked-b64 pull md5-verified): `v15Qs1_final_actor.pth` e3bcd9ab (LEAD) + `v15Ws0_periodic_prev_actor.pth` 29f493e2 (quiet fallback). Notes carry the flight contract: **pitch FREE (no clamp — the v1 lesson), yaw 0.7, NO cross-release mixing, settled GO ≥3 s, log-echo map, TRT M-engine, assist 1.3.**
- 📁 Pick-flight box relayed → FLOWN 07-19, deep-read below.

## 🔬 V15 WIRE FLIGHTS DEEP-READ (2026-07-19; 20 sessions @ `fb794e5` on ratchet-arrestor branch; script `scratchpad/v15_flight_analysis.py`)
- **WINS CONFIRMED ON WIRE: yaw quiet transfers 1:1** (absmean 0.10–0.22, flips 0.3–2.7 vs v1's 0.64/4.4-6.8) · **runaway CLOSED** (max 8.2–11.0 under the priced 12) · **pitch-free g0 = 1.97–2.27 s** (clamped lineage 2.94–3.00). Scores: vision-cmdr Qs1 {2,1,2} Ws0 {3,2} (Ws0 f1 = FIRST settled g2); Fengyou Ws0 {5-once,3,3} Qs1 {3,2,2,2}. Fengyou pilot-read: **Ws0 smoother, Qs1 pitch-vibrates near gates.**
- 🛑 **NEW WALL QUANTIFIED = POST-PASS DETECTION BLACKOUT (descend legs):** g2→g3 (sometimes g1→g2) slot0 dark **0.72–5.81 s** vs training's ~0.1 s expectation (flat det_p 0.35@30 Hz, no range dependence). **slot1_when_blind = 0.00 in ALL 20 sessions** (`--ego-slot1` WAS on + slot1 live in view — but `detect_next_gate_lever` needs a frame containing gates ⇒ dark exactly during the blind turn). Policy flies its trained post-pass arc open-loop = "flings off to the side."
- 🛑→✅ **CAMERA MOUNT GAP OVERTURNED (v16-builder, numeric probe):** the +20° up-mount is **ALREADY baked in sim** — `R_CAMERA_FROM_BODY_NP` (inc8_estimator_emul.py:64-79, a=20°), consumed by ALL detection paths; net optical axis +20.000°, band (−13°,+45°); `test_gate_visibility` pins it. My "0° in `_cam_R_wb`" read was a MISDIAGNOSIS (`_cam_R_wb` = z-flip only; mount lives downstream in R_cb). Descend blackouts are a MODELED phenomenon both sides; remaining detection gaps = the flat-0.35-vs-deterministic law (close-in cutoff, 0.99 plateau, far rolloff), which v1.6 fixes. New knob `ego_cam_mount_pitch_deg` = ABSOLUTE override (None=baked 20°; launch's 20.0 = zero-delta byte-identical; lever if true wire mount ≠ 20).
- **Qs1 pitch vibration quantified:** 3.8–11.6 pitch flips/s cruise, p2p to 2.2 rad/s near gates (Ws0 0.6–3.5) — suspect amplified by **`ego_rate_scale` 1.2 flown** (+20% all-rate gain; champion-recipe residue in MODEL_DEFAULTS @394eb33; plant sysid'd at 1.0; **my box let it ride — same error class as the v1 pitch clamp**). A/B = re-fly at 1.0.
- **MAP SPLIT:** scored six flew canonical logecho map (g1 [-1,1], g2 [0,-1]); Fengyou's panel flew `configs/vq2_coarse_map.json` (survey map: g1/g2 = [0,0]) — his scores NOT worse ⇒ sector prior not binding on early gates; unify on canonical.
- **Ws0 g0-crashes (Fengyou's ask):** 2–3 of ~6 = unsettled-start contact storms (coll 57–82 within ~2 s — the fresh-boot tell); rest = climb-through frame clips arriving at vz +4.3–4.8 ~2 s in. NOT next-gate blindness.
- **Pointing rewards ground truth:** v15 stage `dual_gate_fullstack_floor_pef` = rw_perception **0.02 farm-neutral, CURRENT gate only**; `rw_perception_next` EXISTS (pefcap stages, 0.014/0.004 split, curriculum:1778) but **NOT in the v15 recipe** ⇒ nothing ever paid pointing-at-next-gate; anticipation relied on slot1 preview + sector, both dark/2-bit during wire blackouts.
- **Ws0 VARIANCE TAXONOMY (batch 2 @824b343, 17 Ws0 flights total, ALL rs=1.2):** every flight locks TRUE g0 at t0 ([−10,0,+4.1], r≈11) ⇒ NO spawn mis-locks. Death dice: **(A) gate frame-clip while climbing vz +3.7–5.6 at arrival** (≈6× at g0, 2× at g2 — THE dominant killer + the zero-contact blocker; every flight coll ≥1) · **(B) climb-overshoot → gate drops below wire FOV → blind climb** (060301 peak 6.3 m vs gate 4.1 — the mount gap at g0) · **(C) unsettled contact-storms** (coll 57–200, ≤1.4 s, one tick-0 rel_x SIGN-FLIPPED — the boot tell) · **(D) mid-flight false re-snap to ~30 m far gate at conf 1.0** (060432 rmax 29.6, 0 advances — the "Station 2" pathology; `--ego-det-range-cap` exists, suggest ~25 m). **5-gate run = all dice pass + max blind 0.08 s. Policy-side metrics TIGHT across all 17 (yaw 0.10–0.22, v 7.5–11) ⇒ v1.5 made the policy a CONSTANT; variance is arrival-vz + detection events.** Map-file note: `vq2_coarse_map.json` CONTENT changed between Fengyou's batches (g1/g2 [0,0]→canonical [-1,1],[0,-1]; same filename) — sector values NOT implicated in the deaths either way.
- 🏆 **RS=1.0 A/B CONFIRMED (batch 3 @737a3cf, 24 flights, all rs=1.0):** unclamped {5,4,3,3,3,2,1,1,0×4} — median up vs rs1.2, 5-gate no longer unique. **Fengyou pitch-limit sweep (`ego_pitch_clamp`, panel units ~deg/s): 1→frozen crawl (0 g, 27 s) · 10→{4,3} · 15→{5,5,5,4,4,3,3,0} = CONSISTENT 4–5 GATES.** Mechanism: caps arrival-vz/agility = speed governor by proxy ⇒ gate-clip die stops firing. Fengyou verdict "reduce our speed" = MEASURED (consistency lives ~6–7 m/s) → v1.6 lever = rw_progress_vcap 7.5→~5 + pitch-dynamics shaping so the policy flies clamp-15-like UNCLAMPED (training-faithful, not deploy clamp).
- 🛑🛑 **THE GATE-5 FLIP = FIXED UNLIT OBSTACLE ON THE g4→g5 LEG (4/4 five-gate runs, log-proven):** all end **13.7–15.3 m short of gate 5** (~5–7 m past g4, 1.0–1.5 s after the pass) at conf 1.00, cmds ≤0.33, gyro ≤0.5, mild attitude — then log stops (impulse < one 40 Hz tick). Contrast gate-clip deaths (conf degrades + roll rises + gyro builds −1.4..−2.1 over ~10 ticks). Speeds 6.8 AND 10.6 both die there ⇒ NOT speed/policy. "env collision" verdict is LITERALLY CORRECT — invisible in the dark viewport, doesn't occlude gate-5 glow. **NOTHING in project history has passed gate 5 — champion's lone 5 likely died at the same spot (CHECK its death range).** Asks relayed: last-20-frames eyeball (video blobs ShadowPC-side) + champion death-position check + survey; cheap trick: hand-edit map row 5 vert bias to lift/route the leg (map IS hand-authorable).
- 🏆🏆 **BATCH 4 (@6655f31, 43 sessions, accurate 7-row map + clamp 15/17 + rs 1.0): FIRST 6-GATE RUN (074221, clamp 17, coll 1)** + histogram transformed {0:3,1:1,2:2,3:14,4:14,5:8,6:1} (mode 3–4, was 0). Map re-index fixed an off-by-one ([1,-1] row 4→row 3). **OBSTACLE LOCALIZED BY SURVIVOR-vs-DEATHS ALTITUDE SPLIT: 9 cumulative death-zone kills at horiz_r 11.8–16.1 m from g5 ALL at leveled dh +0.2..+1.2 (AT/ABOVE gate-5 line); the 6-gate survivor crossed the same zone at dh −0.6..−0.9 (BELOW the line); 3 more flights got through and died as gate-5 frame clips instead ⇒ OVERHEAD structure over the g4→g5 leg at ~gate-5-height+0..1.2 m, FREE AIR BELOW.** 1-line experiment relayed: accurate-map row 5 [0,0]→[0,−1] to bias the population low. **Gate 6 = +5 m HIGH gate (steep climb leg, vz +3.5-3.9 sustained); 074221 died clipping g6 arriving hot (vz +2.9) + off-center (|y| 1.6–2.2) + close-in conf drop 0.53 + tracker re-snap to far gate at the last tick** — same arrival physics as every gate death; v1.6 tight-thread/vcap items = the medicine.
- **FLOOR HYPOTHESIS REFUTED (Fengyou raised; leveled-frame math):** 5-gate deaths at leveled vz −0.6..+0.7 (two CLIMBING), KF alt 1.6–4.9 m, time-to-floor 2.8–20 s vs abrupt log end, and **drone AT gate-5 altitude (leveled dh ±1.7 m) when killed 13.6–16.1 m short** ⇒ level cruise into a ~2–5 m-tall structure. Fengyou: "does not show up on screen" ⇒ possibly UNRENDERED collision geometry (collider w/o visible mesh — sim artifact class). Deaths cluster ~3 m laterally, same side.
- 🔬 **"WILLINGNESS DECAYS" = REAL, IT'S SPEED NOT FATIGUE (feedforward policy can't tire):** per-leg med max|rollcmd| 0.57→1.11→**1.57(g2 peak)**→0.99→0.61→0.33 while speed rises 4.0→6.8→7.0→7.2→7.5→10.5; arrival |y| off-center 0.13–0.20 m (g1/g2) → 0.47 (g3) → **0.79 m (g4 = the "plops onto the side")**. v1.5's pricing taught high-speed-gentle; speed accumulates (no brake — triple-punished per forensics) ⇒ sharpness fades exactly as speed climbs. Fix = keep the whole flight in the sharp regime (vcap 5) + steepen passage centering.
## 🔬 FLIGHT-MINER VERDICT (opus agent, 2026-07-19; 92 v15 + 35 historical sessions; scripts scratchpad/pglib.py + an_*.py)
- 🚩 **DEPLOY CONFIG = rs 1.0 + pitch_clamp 15** (mean 3.88 g, P≥4 0.69, P≥5 0.38, n=16; clamp inverted-U peaks at 15; 17 NOT better, p=0.32; both ≫ 0, p≈0.005). **Mechanism = cuts nose-down pitch 0.31→0.09 ⇒ prevents descend-dive FOV blackout (the +20° up-mount), NOT turn authority.**
- 🚩 **DETECTION near-DETERMINISTIC in-view, NOT Bernoulli(0.35): P_det(r)=0.99/(1+exp(−(r−2.2)/0.40)); plateau ~0.99 @4–22 m; ~0 below 1.3 m; far rolloff ~26 m; 0 misses in 14,938 in-view frames.** Failure = binary FOV visibility. Signal decode: `age_s==0` = fresh; `conf` = STALENESS CLOCK clamp(1−age/horizon), NOT quality; `fresh_detect` = frame-processed only.
- 🚩 **Descend legs = the blindness: ALL 8 long blackouts on descends, 7/8 fatal; g2→3 leg 12.6% blind; climb legs NEVER blackout. Long blackouts only at clamp 0/1 (0/41 at ≥15).**
- 🚩 **vcap 5.0 REJECTED: roll-authority PEAKS 8–9 m/s (p90 1.05, knee ~9–10); survival sweet spot ~7 ⇒ KEEP rw_progress_vcap 7.5.** (My per-leg "sharpness decays with speed" was confounded by leg geometry — late legs are straight sectors; real speed penalty starts >10.)
- 🚩 **Aperture calibration: survived crossings radial 0.28 m med/0.57 p90 vs clips 0.47/1.42 ⇒ train to radial ≤0.5 m, LATERAL 2× weight** (lat 0.19→0.40 vs vert 0.13→0.19). Trim >10 m/s or >2 m/s-vz arrivals.
- 🚩 **DEATH ZONE REVISED: g4 AND g5 die ~13.7 m short at dh +0.5 AND ~3 m LATERAL offset on straight sectors. CHAMPION DOES NOT DIE THERE — reached 1.1 m of gate 5 (face-clip @12.7 m/s, dead-center)** ⇒ the structure sits off the true line; v15's 3-m-offset line hits it — possibly BECAUSE of:
- ✅ **MAP OFF-BY-ONE RESOLVED (2026-07-19, myself — vision cmdr deprecated): CODE CLEAN** — `src/racer/ego_obs.py` latches `coarse_map[active_gate_index]` at gate change (clamped tail), NO +1. The FILE was destination-authored ("row g = where gate g is"). **Fixed @`00db7af` on ratchet-arrestor: canonical file = the FED sequence `[[-1,1],[0,-1],[1,-1],[0,0],[-1,0],[0,1],[0,0]]`** (Fengyou hand-survey g0→g6; row 6 = gate-7 direction UNSURVEYED placeholder); `_doc` now carries the authoring self-check (FIRST row must = gate-1's direction, this course `[-1,1]`). Batches 1–3 flew the mis-authored variant; batch 4 accidentally flew the correct sequence = part of why it was best. Local wt: `C:\Users\Fengy\Downloads\Projects\wt-mapfix` (branch mapfix-local tracks ratchet-arrestor).
- **Ops: RE-FLY RULE — abort if max‖gyro‖ >2.5 rad/s in first 0.75 s (1.70 vs 3.38 mean gates, corr −0.43; latency/g0-vz useless).** Timing healthy ~38 Hz (not a driver). Slot1: median 0.26 m accurate + ~10% gross re-snap tail ⇒ model as mixture. Qs1 ≈ Ws0 at rs 1.2, NEVER flown at rs 1.0 ⇒ 5-flight retest warranted. One estimator velocity runaway seen (064328 → 58 m/s).
- ➡️ **v1.6 SPEC (rev 3 — miner-calibrated): warm from Ws0** + camera-mount parity (+20°, parameterized `ego_cam_mount_pitch_deg`) + honest acquisition = **mount+FOV binary visibility + close-in sigmoid P_det(r)=0.99/(1+exp(−(r−2.2)/0.4)) + far rolloff ~26 m + conf=staleness-clock parity** (replaces flat detect_p 0.35 as the model) + arm rw_perception_next + **vcap STAYS 7.5 (5.0 rejected by miner)** + **APERTURE-MARGIN CURRICULUM (target radial ≤0.5 m, lateral 2× weight) (Fengyou call: brute-force "shrink the gate" > reward steepening — by-construction doctrine; implement as annealed pass-margin/body-radius so the VISUAL gate stays deploy-faithful; miss-rate watchdog)** + **PITCH-CHANNEL DEFENSE (Qs1 lesson: its near-gate pitch vibration = same disease class as v1 yaw — high-gain limit cycle on the UN-GATED axis; census measured yaw only ⇒ (a) PITCH_EVAL selection gates ~flips≤3.5 calibrated Ws0-pass/Qs1-fail, (b) pitch duty/jerk penalties w/ WIDE free band — pitch is the primary control axis, overdamp = the clamp-1 crawl, (c) Qs1 never retested at rs 1.0 — miner adjudicates)** + fly rs=1.0, no deploy clamps.
- 🛑 **Qs1 RETIRED (retest @20d02f8, 22 flights rs1.0+clamp15+correct map): {0:6,1:5,2:4,3:6,4:1} — strictly worse than Ws0 at every config.** 🔬 **BLIND-LEG MECHANISM QUANTIFIED (billboard course; NOTE Fengyou counts gates 1-BASED — his "gate 4" = index 3, the [1,-1] sharp lower-left):** legs 2→3: **Qs1 5/6 FULLY BLIND (conf 0 entire 2 s, never acquires; search yaw ~0.1 rad/s = the "slight left look"), 1/6 advanced. Ws0 46/51 INSTANT-LOCK (blind 0.00), 31/51 advanced** — same detector, same map ⇒ **map-trust pays BEFORE the pass: Ws0's committed pre-turn while chasing gate 2 opens the billboard sightline AT the 2→3 advance; arrive flat (Qs1) = occluded = weak search = dead.** Occlusion never appears as a blackout for committers — it appears as geometry. ⇒ v1.7 levers doubly confirmed: sector deadband 0.2→~0.4 rad (label = "commit") + occlusion windows (undetectable until r_reveal 8–15 m) so pre-turn commitment is selected for, not seed luck. **g−1 "arrival prior" (Fengyou endorsed): +2-dim append re-feeding row tg−1 so a blind leg keeps its own bearing — candidate v1.7 arm B; requires #83 obs_v2-pad fix + deploy ego_obs.py append (mine now).** Panel @193cfc9 now always flies the editor map.
- 🏆 **v16Q_s0 FINAL (3313525, 4:16:32, clean DONE): n_passed 5.45 @600-step det eval UNDER THE TIGHT margin (v1.5 same-eval loose-aperture finals were 4.10–4.17 ⇒ +1.3 gates on a HARDER criterion)** · yaw 0.99 flips/0.151 abs/0.7% satur · **PITCH_EVAL 1.75 flips/0.285 abs/0.00 satur (healthy band; the duty 0.1 worked)** · margin END-HOLD 1.000 honored · BEST=final upd 18000. All pick gates pass w/ margin. Q_s1 RUNNING ~2:09, W_s0 PENDING. Census note: rollout-only sweep evals at margin-anneal START (loose) = comparable to v1.5 census AND to the wire's physical aperture — fine as-is; optionally add a v16-faithful row set.
- 🟢🟢 **v1.6 STAGED+SMOKED+ARMS RUNNING (2026-07-19 eve):** 6 files md5-pushed (racing da0c8689 · reward 95f9f15f · racing_ego 7c6479d9 · train_ego 46845622 · curriculum a731e2f6 · launch_v16 98e15d34; CRLF→LF normalized). **SMOKE 3313524 PASS 5/5** (2:34; all knobs resolved; margin anneal 1.677→1.000 END-HOLD exact; PITCH_EVAL live — warm base 1.33 flips/0.238 abs/0 satur; **n_passed 5.77 UNDER the tight margin at upd 100** — no threading collapse; TB keys all present, pitch pens FALLING, overspeed ~0). **ARMS: v16Q_s0=3313525 · v16Q_s1=3313526 · v16W_s0=3313527** (18000 upd, afterok satisfied, ~4.5 h each + queue). NEXT: census sweep w/ JOINT yaw+pitch hard gates (launch_v16 header has the full pick protocol) → release → training-faithful fly box (rs 1.0, pitch free, yaw 0.7). Awaiting Fengyou GO + mount constant. (Data note: the "massive batch" = 737a3cf's 29 sessions + the two prior batches — ALL landed and fully analyzed above; no video .bin blobs in git, frames stay ShadowPC-side; @4d8cf66 argv-cap fix was preventive, my "batch didn't land" read was WRONG.)
- 🏆 **v16Q_s1 FINAL (3313526, 4:13:26, clean DONE — adjudicated 2026-07-19 eve): n_passed 5.4148 @600 TIGHT margin (SEED-REPLICATES Q_s0's 5.45; the +1.3-on-harder-criterion result is REAL, not seed luck)** · yaw 0.769 flips/0.1527 abs/0.08% satur · PITCH_EVAL 1.584 flips/0.320 abs/0.00 satur · margin 1.000 held · 0 HAZARD · terminals: collision 0.587 dominant, margin-miss ~0 (0.0015 — the policy genuinely centers at tight margin), oob 0.0031, max_speed 12.05 (abort brushes) · TB n_passed_max 6.23 mid-run · "cannot promote" runner-race flag again (benign class; census dedups it). **Precheck row = v15W warm parent on the v16 env @600 loose: 5.81** ⇒ census beat-the-parent bar.
- 🚀 **v16 CENSUS FIRED (2026-07-19 eve): script `/scratch/network/fl3689/v16_yaw_sweep_cluster.sh` (v15 census adapted: STAGE pef16 + full CORE/Q key set from launch_v16 defaults incl. range-detect + margin keys; EDIT-1 1200 steps + EDIT-2 rollout_ckpt kept; uniform seed 0 + cadence seed 20260715; VERIFIED in code: `_pass_margin_current` init = START = full physical aperture @racing_ego:1031-33 + rollout returns pre-anneal ⇒ census = LOOSE margin = v1.5-comparable + wire-faithful; in-run finals were TIGHT ⇒ expect census ≥ in-run).** Jobs: **3314534 = Q census RUNNING** (Q_s0 + Q_s1 + v16smoke control + **v15W parent row under the v16 env** — the beat-the-parent read) · **3314551 = W census PENDING afterok:3313527** (self-fires when W training lands; W_s0 was ~37 min in, ~4 h ETA). Ops lesson: daemon argv STRIPS inner double quotes ⇒ script takes comma-separated SWEEP_RUNS (`SWEEP_RUNS=a,b,c bash ...`); pusher `scratchpad/push_sweep16.py` (md5 fb92dad4). Readout: `grep -e YAW_EVAL -e PITCH_EVAL -e DET_EVAL /scratch/network/fl3689/v16_yaw_sweep_<jid>.out` → joint gates (yaw flips≤4/abs≤0.3/satur≤10% + pitch healthy vs Q_s0-ref 1.75) → rank n_passed → picks → release `ego-ckpts-v16-*` → fly box rs 1.0/pitch FREE/yaw 0.7/editor map/settled GO.
- 🔬🔬 **v16 WIRE-MINER VERDICT (opus agent, 07-19 late — 83 v16 + 114 v15 sessions; scripts scratchpad/v16-forensics/; v15 baseline REPRODUCED 0.29/0.54 = method valid):** (1) **z_bias 0.25 = SELF-INFLICTED LOW-AIM** (seeker LOWERS emitted gate, gate_seeker.py:678; drone nulls the PERCEIVED gate ⇒ bias lands 1:1 in TRUE miss: crossings +0.01→+0.30 low, deaths −0.01→+0.45 low; NO clip benefit 72≈73%) — **my boresight-calibration story FALSIFIED; drop to 0** (Fengyou's "z0+clamp tops-clip" = unmeasured tail on the NEW 32 — agent follow-up). (2) **pitch fence ON = biggest gate lever** (free 0.89 g vs fenced 1.75–1.88; free = pitch_cmd −0.99 from k0 → −53° dive → g0 out frame-top → re-locks FAR gate). (3) **yaw 0.35 NOT the clip driver — my theory WRONG** (median desire 0.08, clamp idle 88%; side clips = roll-lever LATE, full authority unused; 231540 = commanded-too-late + aimed-low). **NEW: v16 heavy yaw TAIL raw p99 2.66 vs v15 0.57, fires DURING BLACKOUTS = blind-coast panic — v1.7 training item; clamp correctly suppresses.** (4) **map = SECOND-ORDER** (batch flew MIXED 55 stale/36 canonical; stale scored BETTER 1.49 vs 1.19 confounded — my map-alarm overweighted). (5) det_hold 0.3 > 0.2 (clip 56% vs 77%). (6) emit-gap 24.3% vs v15 18.8%, **91% MID-TRACK** (continuity-reject class; UNPROVABLE — video_index = JPEG index only, no detector/keypoint logs, no nav_estimate.jsonl, seeker _last_none_reason NOT logged ⇒ WP0 instrumentation). (7) **re-acquire path SKIPS the 22 m acquire cap** (231220 locked 26.7 m far gate: admissible-empty → fallback-to-ALL after mid-flight loss = the bug). (8) advance-early ~10% both cohorts, ZERO sustained mid-approach wrong-gate snaps in aggregate (wrong-gate = transient, flight-specific). (9) v16 shakier EVERYWHERE (roll-axis, gyro-peak 4.61 vs 2.91), not a crossing impulse. Seeker constants: max_acquire 22 / max_valid 30 / range_jump 6 / bearing_jump 0.35 rad / ema 0.5 / coast 8 ticks. kp_persist 0 ALL sessions (untested; training-parity warning if raised). v15 obs schema: rel_flu=[-obs11,-obs12,obs13]; act_raw[3]=pre-clamp yaw desire.
- 🛑 **FENGYOU 2×2 RE-TEST (32 flights, config CLEAN yaw0.7/z0/canonical-map): v16Qs0@17 mean 2.0 g ≪ morning v15Ws0@17 ~4.1 SAME config = v16 WIRE REGRESSION vs parent stands even config-clean** · v16Qs0-free 0.75 (dive persists = policy property) · v15Ws0-free 1.6 · vpeffs0@17 control coll 6–38 vs v16-era 1–2 (**quietness/contact transfer = REAL win**). Labels lie (panel_run/v15pick) — trust meta.ego_ckpt. Morning 6-gate run = v15Ws0@17, yaw 0.7, NO z_bias knob (didn't exist), canonical-fed.
- 🩹✅ **PATCH-1 SHIPPED @`7e54bca` on ratchet-arrestor (2026-07-19 ~22:20, opus builder #2 — builder #1 killed by app restart, tree-clean relaunch; reviewed diff+reran tests myself, 95/305 green, pushed c4237fe..7e54bca):** WP1a re-acquire cap (`_track_ever_locked` latch; re-acquire w/ no in-cap candidate → None "reacquire_range_reject", coast on the propagated hold — **231220 far-lock regression-tested FIXED**; cold-start A5 fallback retained incl. post-advance until patch-2 promote) · WP1b hint (`EgoObsBuilder.slot0_hint_frd()` body-FRD conf>0-gated → `detect_gate_lever(hint_rel_body_frd=)`; veto >0.6 rad off-hint "reacquire_hint_reject"; inert cold/continuity; STALE on long blackouts — WP1a is the 231220 fix, hint = short-gap net) · WP0 `seeker.jsonl` per tick (reason/n_cand/track_range/coast/emit, slot0+1, joins ego_obs on k/sim_time_ns/frame_id; in-memory, single write at exit) · WP5 `_V1_RECIPE` pins z_bias 0.0 (yaw 0.7 already) · WP6 model-pick repins recipe-managed knobs + meta gains coarse_map_rows/seeker_constants/recipe_drift + flight-start drift warning. ⚠️ Known UX: picking a BARE model (vpeffs0/trackA) blanks detector/weights/map to defaults — re-select after; refine scope in patch 2. 231540/231941 explicitly unchanged by patch 1 (cold-start/continuity paths byte-identical).
- 🛑🔬 **PATCH-1 BATCH VERDICT (07-20, 25 sessions @b71c3b9, config clean, seeker.jsonl LIVE — Fengyou also patched panel @df369d4 to commit it): bottleneck MOVED FORWARD to the gi=2 advance.** Clamped {1,1,1,2×6} = 7/9 reach gi=2 (was 3/8) then ALL lock the SAME WRONG GATE: ~17 m UP-RIGHT rel [13-15, −4..−10 R, +4..7 U] while canonical row1 says next = DOWN; 4 die mid-air 6-13 m short (scenery/true-gate frame), 2 clip-in. **seeker.jsonl tick-narrative (040510 k133-140): slot0 track SURVIVES the MAVLink advance** (nothing resets it in the RL path — only slow-lap _begin_pass does) **→ 6 ticks continuity-rejecting the new leg vs the stale 3 m track → coast-expiry → locks the ONLY candidate (n_cand=1 — true big gate UNDETECTABLE at advance = descend-leg blackout) → continuity defends the wrong lock.** Free block {0×4?,1×7,2×2...} mean ~0.8 = dive unchanged (v1.7's job). Fengyou pilot-read confirmed w/ twist: his "bigger gate should take precedence" = right but the big gate is INVISIBLE at lock-time ⇒ precedence must fire on APPEARANCE (supersede), not at acquisition. Fengyou gate names in prose ≈ overlay 0-based labels.
- 🚀 **PATCH-2 BUILDER LAUNCHED (opus, 07-20): WP2a advance reset+promote (`on_gate_advance`; slot1→slot0 seed w/ range[3,22]+prior sanity, else cold) · WP2b arrival-prior acquisition gating (map row g−1 direction cone; wrong-side veto ~15°, [0,0]=generous; "prior_reject" ⇒ better DARK+sector-trust than wrong-lock; 040510 case must veto) · WP2c nearer-supersede (Fengyou's size-precedence: <0.65× range + bigger area + prior-pass, 3 consecutive frames, "supersede_nearer") · WP2d gyro-fed track prediction (rotate predicted bearing by −ω dt; kills the 91% mid-track reject class) · WP2e log reasons. Named regressions: 040510 advance sequence + promote + supersede-debounce + gyro-propagation. Commander reviews+pushes on completion.**
- 🏆🏆🏆 **PROJECT RECORD: 7 GATES (07-20 `20260720_044014_v16pick_Qs1_f1` @patch-1): Qs1 (the benched seed!), det_hold 0.2/stale 0.4 (Fengyou's own knob change), clamp 17, 17.7 s, 3 contacts, died 1.10 m from GATE 8 — lateral clip 0.6 m off-center + roll-rate tail spike −3.6 rad/s.** Trace: killer gi=2 leg won via 6.9 m first-lock on the TRUE gate (vs Qs0 batch's 16-18 m wrong-locks; dark only 0.175 s) — contributors (n=1, unattributed): Qs1's line · shorter hold = ghost dies faster ⇒ earlier dark ⇒ earlier sector-trust turn · flown map row1=[0,0] (his vert-zero edit; rest canonical). **Legs gi=3/gi=4 "passed" while slot0 tracked ~20 m far locks (end_dist 21.1/19.5) = wrong-lock disease SUCCEEDING on straight geometry — luck not skill; patch-2 makes honest.** Subsequent same-config flights crashed g0 side (NOT pushed yet — ask) = start-dice + possibly shorter-hold interaction at the dive. **det_hold 0.2/0.4-vs-0.3/0.6 = A/B in the next patch-2 batch** (miner's 0.3>0.2 verdict predates patch-1; WP1b hint window = conf>0 ⇒ stale 0.4 shortens it too). 🗺 **MAP ROW 6 SURVEYED FROM FLIGHT: gate 7 (his gate 8) = UPPER-LEFT of the gate-6 arrival ⇒ row 6 = [+1, +1]** (rel [11.5, +4.3 L, +4.6 U] at gi=7 first lock; body-frame at pitch −0.43 — Fengyou to eyeball-confirm on overlay). 🎯 **Fengyou call ACCEPTED: v1.7 tightens aperture — pass_margin_final 1.0 → ~0.75** (this death = 0.6 lat ≈ the margin-1.0 boundary; miner survivable radial ≤0.5) + spawn realism + blind-abort + centering-multiplier.
- 🏆 **DET-HOLD GRID SETTLED (07-20, Qs1 @patch-1, clamp 17 — Fengyou's discovery, 3 cells): WINNER = `det_hold 0.2 / stale 0.6` {5,4,4,3,3,1} mean 3.3, ZERO g0 deaths in 6** vs dh0.2/st0.4 {7,4,3,0,0,0} mean 2.3 (3 close-in blind clips at 0.5–1.3 m, conf collapsed mid-plane; clean starts, gyro 1.5–1.8) vs dh0.3/st0.6-Qs0 mean 2.0. 🔬 **MECHANISM PROVEN — the two knobs decouple cleanly: `det_hold` (emit/ghost lifetime) drives the ADVANCE, `stale_horizon` (confidence decay) drives the CLOSE-IN.** Killer-leg gi=2 first-lock is BIMODAL by det_hold alone, no overlap: **dh0.2 → 6.9–8.4 m TRUE big gate (n=8/8)** vs **dh0.3 → 16.4–18.3 m WRONG far gate (n=6/6)** — short hold kills the ghost fast ⇒ dark ⇒ sector-commit turn ⇒ true gate appears; long hold steers the stale point into the far-lock. Restoring stale 0.6 recovers the last-blind-meter guidance the 0.4 cell lost (detection cuts ~1.3 m) WITHOUT touching the advance win. **Deploy default NOW dh 0.2 / stale 0.6** (supersedes the miner's 0.3>0.2 verdict — that predates patch-1 + wasn't stale-decoupled). Qs1 = the deploy-lead ckpt (7-gate record + this grid); Qs0's advantage was sim-census only. Residual: gi=3/4 legs still first-lock ~20–22 m (far-lock succeeding on straight geometry — patch-2 makes honest); tail gyro 4.7–15.8 rad/s at deaths.
- 🩹✅ **PATCH-2 SHIPPED @`bffedd2` on ratchet-arrestor (07-20; agent commit 7dab4ac cherry-picked onto tip, I re-ran 131 tests green + reviewed API, pushed 84773c4..bffedd2):** `on_gate_advance(prior_dir, level_rp)` = reset+**promote** live-sane slot1→slot0 (`advance_promote`/`advance_reset`; kills the pre-advance continuity-reject storm) · **arrival-prior veto** (`prior_reject`, gravity-LEVELED, cones wrong 0.26/zero 0.50 rad, bites nonzero rows only) · **nearer-supersede** (`supersede_nearer`, <0.65× locked range + prior-compliant × 3 consecutive frames; GatePose has NO size field ⇒ range is the proxy) · **gyro `propagate()` every tick** (rotates track bearing by exp(−[ω]x dt), `track_gyro_propagate` default ON) · WP2e log fields. Defaults conservative: no level_rp/prior ⇒ byte-identical to patch-1.
- 🛑🔬 **MY 040510 PREMISE WAS WRONG — agent re-derived from logs + I verified independently: (1) the flown arrival row was `[0,0]` (Fengyou's editor edit), not `[0,-1]`; (2) the wrong candidate is gravity-leveled ~−8° DOWN, NOT "+4..7 m UP" — body-frame rel_flu up-component read +15.2° while the drone was pitched −36.2° NOSE-DOWN on the descend leg.** ⇒ **the prior veto would NOT have fired; the operative fix for that flight is SUPERSEDE (slot1 was ALSO tracking the same far gate ⇒ promote wouldn't catch it either).** 🚩 **DURABLE FOOTGUN: body-frame elevation FLIPS SIGN under pitch — any bearing compared against a coarse-map bucket MUST be gravity-leveled** (`_leveled_from_body` pinned == `ego_obs.leveled_from_body` by test). Same error class as my camera-mount misdiagnosis: a number read without checking the frame it lives in.
- 🚩 **ENV HAZARD (agent-discovered, cost 2 wipes): the pilot-panel process auto-runs `git reset --hard origin/ratchet-arrestor-2026-07-18` on `mapfix-local` every few minutes** — it wipes uncommitted work AND local-only commits. **Agents working that worktree MUST commit to a side branch** (pattern: `patch2-mapfix-backup`) **+ save a scratchpad `.patch`; commander cherry-picks onto tip and pushes** (pushing also resolves it — the reset then pulls the work IN). Bake into every deploy-worktree agent prompt.
- 🔬🛑 **PATCH-2 BATCH ADJUDICATED (07-20 overnight, 11 sessions @67d4227, all dh0.2/st0.6 Qs1; CONFOUNDED by 3 pitch clamps — 15/17/20 — so treat means as soft): mechanisms ALL FIRE** (supersede_nearer ×4, prior_reject ×7, advance_promote ×5, advance_reset ×2) **and gyro-propagation WORKS: slot0 continuity_reject 32/session (patch-1) → 17/session.** 🎉 **053427 = FIRST ZERO-CONTACT FLIGHT EVER (coll=0)** (1 gate; validity rule met). 🛑 **BUT: `advance_promote` = NET HARM, defaulted OFF @`a654ff3` (pushed 67d4227..a654ff3, 132 tests green, mechanism kept behind `advance_promote_enabled`).** Evidence: slot1 is index-blind and at the gi=1→2 advance is routinely locked on the FAR gate (true gate still occluded when the wire increments) ⇒ promote INSTALLED a confident wrong lock at **17.5/19.6 m in 2/5 gi=2 advances where truth was 7-8 m** (supersede undid it 2-3 ticks later); patch-1's dark-then-reacquire got the TRUE gate 5/5 at 6.9-8.4 m. **Promote's upside was ~nil: on legs where it was right (gi=1 ~17 m, gi=3 ~11 m) cold re-acquire found the same gate at the same range anyway. Going honestly DARK is the mechanism det_hold 0.2 buys — do not shortcut it.** Clamp-17 subsets patch2 {1,1,3} n=3 vs patch1 {5,4,4,3,3,1} n=6 (suggestive regression, n too small alone; promote is the identified cause). Also: meta `seeker_constants` NOT updated for patch-2 keys (writer has a fixed list) — cosmetic, fix opportunistically.
- 🏁🔭 **v1.7 BUILT + PUSHED @`1ca0e3b` on `v16-mount-acq-margin` (07-20 overnight; 4 commits, 35 new tests, 364 affected-suite green; 2 full-suite fails PRE-EXISTING at HEAD, stash-verified).** 🚩🚩 **ROOT CAUSE OF THE START DIVE — MEASURED, and I re-probed it independently through `_r_camera_from_body_np()` (my own frame-claim rule): the VQ1 PAD TILT (−17.8°) CANCELS THE +20° MOUNT. Camera optical-axis elevation = +2.20° on the training pad vs +20.00° at the wire's LEVEL handoff.** ⇒ a gate 10.5 m out / 1.5 m up sits **+5.9° ABOVE** the axis in training but **−11.9° BELOW** it at the wire. The policy's learned "gate low in frame ⇒ pitch down" law has literally NEVER fired from rest in training; at the wire it fires at t=0 and runs to −53°. **The dominant OOD term is ATTITUDE, not distance** ⇒ M1 re-POSES the spawn (level), it doesn't just re-sample the course band. This supersedes my "handoff state is OOD" framing with a number.
- 🛑 **SPEC CORRECTION (builder, code-enforced): `rw_perception` CEILING IS 0.02 — `EgoRewardWeights.__post_init__` asserts `rw_perception + rw_perception_next ≤ rw_time` (rw_time 0.02, never overridden). My 0.03–0.05 ask RAISES the guard.** v1.7 ships the max legal 0.016+0.004; going higher needs rw_time up = MORE speed pressure = works against the very failure modes v1.7 targets ⇒ correctly rejected. **M3's multiplier is the real lever; the additive term never was.**
- 📋 **v1.7 mechanism detail (all DEFAULT-OFF + equivalence-proven; no `_pef17` stage — sbatch passes EXTRA last so `++` beats the stage dict):** M1 `handoff_spawn_frac` 0.0→arm 0.33, range 9.5–11.5 m, dz ±1.5, yaw jit 0.25, att jit 0.3, `handoff_min_agl_m` 0.5 (relative geometry NOT absolute AGL — obs is position-free; hover thrust was ALREADY seeded by `dynamics.reset_idx`) · M2 `blind_abort_s` 0.0→arm 1.2, range 15 m, folded into `oob_full` (pays terminal_oob, KEEPS banked progress) + `exit_blind` class + `blind_abort_rate`/`blind_clock_mean` TB; **scoping = clear `acquired` on ADVANCE ⇒ post-pass blackout is STRUCTURALLY unable to fire (exact event grace, not a tuned timeout); blindness = GEOMETRIC mask never detector cadence (don't price dropout the policy can't control); `& ~gate_passed` so a slow clean thread isn't killed** · M3 `rw_progress_frame_mult` 0.0→arm 1.0, floor 0.5, scale 0.5 rad, exp 2.0, reuses `rw_perception`'s own `cos_view`, positive-credit-only, f≤1 ⇒ can only shrink income (no pump); TB `frame_factor` (climbing→1.0 = healthy, flat-low = coupling failed, REPORT don't tune) · M4 PMFINAL 0.75 = **pure LATERAL tightening** (|lat| 0.5→0.375 m; vertical bound inactive at 0.75 aperture — the right axis for the 7-gate record's 0.6 m lateral death); Rayleigh σ≈0.25 fit ⇒ downgrade rate 4-7% → 14-19% (3×, not collapse). Launch: `bash rl/launch_v17.sh` → `MODE=arms SMOKE_JID=<jid> bash rl/launch_v17.sh`; warm = **v16Q_s1 checkpoints (DEPLOY-proven 7-gate lead, NOT census-lead Q_s0)**. Watch on smoke: `exit_back` (M1 spawns can land ≤3.5 m behind the pad), `blind_abort_rate` high-AND-FLAT ⇒ raise `blind_abort_grace_s` 0.5-1.0, `miss_rate` COLLAPSE ⇒ PMFINAL back to 0.85.
- 🛑 **CONNECTOR DAEMON DEAD (07-20 ~overnight, ConnectionResetError on `__ping__` = local process gone) ⇒ v1.7 SMOKE BLOCKED; Fengyou restarts `adroit.py serve`, then fire immediately.** 📌 PARKED: `test_v15_terms::test_yaw_eval_appends_satur_duty_after_old_fields` stale since v1.6 `2798702` (whole-module `str.index` now straddles PITCH_EVAL/YAW_EVAL lines; invariant still holds inside the YAW_EVAL f-string) — task chip filed, do NOT "fix" by editing the expectation blindly.
- 🚀 **v1.7 BUILDER LAUNCHED (opus, 07-20 overnight, repo wt-fix @0a8b9b8): M1 handoff-spawn realism** (wire handoff = ~0 m/s, level, ~4 m, gate 10.3-11.2 m — measured on EVERY deploy flight; `handoff_spawn_frac` ~0.33; the dive is OOD-extrapolation, NOT a pricing failure — v1.6's pitch duty/jerk did not prevent it) · **M2 blind-abort by construction** (`blind_abort_s` ~1.2 within ~15 m, had-then-lost scoped so post-pass descend gaps DON'T fire; the pitch-cap REPLACEMENT — velocity caps can't bind a from-rest dive, which is why the cap was still needed) · **M3 centering-MULTIPLIER on progress** (f ∈ [0.5, 1.0] framing factor multiplying progress credit — farm-proof by construction, nonzero floor to avoid the clamp-1 freeze mode; + additive rw_perception 0.014→0.03-0.05) · **M4 aperture 1.0→0.75** (Fengyou's ask). All DEFAULT-OFF w/ equivalence proof; arms Q_s0/Q_s1/W_s0 warm from **v16Q_s1** (deploy-proven lead, not census-lead Q_s0).
- ➡️ **DEPLOY PIPELINE WP PLAN (2026-07-19, answers Fengyou's detector→emit ask; commander owns, agent-built, offline-validated, deploy-branch push):** WP0 seeker decision logging (_last_none_reason + candidates + track state per tick → seeker.jsonl; unlocks reject-vs-miss proof) · WP1 re-acquire hardening (enforce 22 m cap on re-acquire — kill fallback-to-ALL after loss; seed re-acquire from ego-propagated held point) · WP2 slot1→slot0 promote-on-advance (train parity; kills cold-start gaps + advance wrong-locks) · WP3 gyro-fed track prediction (+rate-adaptive bearing_jump — jumps/discontinuity class; validate with WP0 data) · WP4 kp_persist=2 A/B (training-parity caveat) · WP5 z_bias=0 + kill silent default (+ agent re-measure of new-32 high-tail) · WP6 panel guards (model-pick re-pins recipe, meta echoes map rows + seeker constants, drift warning). Sequence: WP0+1+5+6 patch #1 → Fengyou pulls+flies (every flight then self-documents) → WP2+3 patch #2 → kp A/B. v1.7 training defenses: blind-coast yaw-panic tail · g0 start-state OOD · terminal lateral limit-cycle.
- 🏁🏁 **Q CENSUS 3314534 ADJUDICATED + RELEASE PUBLISHED (2026-07-19 eve).** Census (35:10, all RC=0, dedup best_npg≡checkpoints≡periodic=final both arms): **Q_s0 final 5.687 g / 0.982 flips / 0.153 abs / 0.8% satur / roll 48.3 / pitch 1.70 / max_speed 11.67** · **Q_s1 final 5.649 / 0.757 / 0.154 / 0.07% / 45.9 / 1.57 / 12.18** · finals beat own @17500 prevs on gates AND flips (both seeds) · **v15W PARENT (periodic_prev = released Ws0) = 5.888 / 0.663 / roll 37.4** · v15W final 5.138 · smoke row BIT-IDENTICAL to parent (critic_warmup=100 froze the actor for all 100 smoke upds ⇒ free determinism check ✓). 🛑→✅ **PARENT-BEATS-CHILDREN ASTERISK ADJUDICATED (ship): loose n_passed counts rim-scrapes; Q holds 5.41-5.45 @TIGHT margin (parent untrained there; parent's wire ledger = coll ≥1 EVERY flight = zero valid runs, clips at radial 0.47/1.42) ⇒ −0.2 loose sim gates = the deliberate price of centering; JUDGE = wire contact counts.** Watch-item: Q yaw slightly hotter than parent (0.76-0.98 vs 0.66 flips) + roll_swing 46-48 vs 37 — if wire shows contact-free but rougher flight, W (heavier duty) is the temperament candidate. **RELEASE [`ego-ckpts-v16-2026-07-19`](https://github.com/Hat000/Peregrine/releases/tag/ego-ckpts-v16-2026-07-19): v16Qs0_final 1747ae2e + v16Qs1_final 3a708498 (162,903 B each, md5-verified both sides; same arch = deploy stack unchanged).** Fly recipe in notes: **rs 1.0 · pitch FREE (the clamp-15-retirement A/B — if v16 needs clamp 15, pitch shaping failed) · yaw 0.7 · det-hold 0.2 · slot1 on · editor map · settled GO ≥3 s · re-fly rule gyro>2.5 in 0.75 s · scoreboard = beat Ws0 clamp-15 {5,5,5,4,4,3,3} UNCLAMPED + coll counts (zero-contact = the point).** Pull tooling `scratchpad/pull_v16_ckpts.py`. W chain unchanged: 3313527 ~1 h left → 3314551 self-fires → W pick appended tonight if gates-pass.
