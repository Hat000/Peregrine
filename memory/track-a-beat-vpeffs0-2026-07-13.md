---
name: track-a-beat-vpeffs0-2026-07-13
description: "Track A SSOT — BEAT vpeffs0 via FROM-SCRATCH full-lineage retrain (warm-from-vpeffs0 FAILED): constraints annealed in during training, preserving yaw+altitude. Recipe ground-truth, control knobs, deploy tooling, run portfolio."
metadata: 
  node_type: memory
  type: project
  originSessionId: c864e5bb-f53f-4b54-b78a-a2216bb2c906
---

**Track A GOAL (Fengyou /goal 2026-07-13):** a policy that MATCHES vpeffs0's threading quality but adds — no velocity runaway (holds ~8 m/s, no ~12 m/s blowup), NO deploy walls (intrinsic roll/pitch/floor/turn — no clamps or hand-authored-map crutch), ZERO yaw oscillation, and VERY HIGH success on the FULL 20-gate course with per-gate randomized distance + xy-plane shifts.

## 🚩 DIRECTIVES (Fengyou 2026-07-13)
- **BEAT vpeffs0 via a FROM-SCRATCH full-lineage retrain — NOT warm-from-vpeffs0.** 🛑 Fengyou 2026-07-13 CORRECTION (I mis-read once): warming the FINISHED vpeffs0 + fine-tuning the walls away is the approach that ALREADY FAILED (nodither 96.9→0.16%, Track B →9.6%; "a short warm-start fine-tune is still re-shaping a CONVERGED policy"). The fix = retrain the WHOLE lineage (boot → fullstack) with the constraints ANNEALED IN from 0 so the policy learns to thread INSIDE the walls from the start. BOTH the reproduction runs (vta0/vta40) AND the warm-from-vpeffs0 beat-runaway (vbrun0/1) were WRONG → canceled. → [[feedback-beat-dont-reproduce]]
- **PRESERVE the yaw → gate-in-view → altitude coupling (the crown jewel).** No absolute-altitude sensor; altitude is held ONLY by keeping the gate in view (gate-relative vertical), and yaw keeps the gate in view. Any control must PROTECT this: anti-dither penalizes only high-freq yaw JITTER (not sustained gate-tracking yaw); attitude cap is a LOOSE backstop (pitch's real lever is rw_perception, NOT the cap — head-down gate-loss sits inside any 60° cone). → [[feedback-preserve-yaw-altitude-hold]]

## vpeffs0 RECIPE — GROUND TRUTH (from both .hydra, 2026-07-13; CORRECTS prior ambiguity)
Lineage = 3-gen WARM CHAIN, all 21-dim, all **ticks_hi=1** (NOT 4 — the stage-dict default 4 was OVERRIDDEN to 1 at launch):
`vwhb0 (GT/percept boot) → vpefwh2 pef-boot (4k, noise=0, warm from vwhb0) → vpeffs0 fullstack (12k, real noise, warm from vpefwh2-boot)`.
- vpeffs0 ckpt (warm-base for beats): `/scratch/network/fl3689/diffaero/outputs/train/ego_dual_gate_fullstack_floor_pef_seed0_vpeffs0/checkpoints` (actor.pth+critic.pth; NO actor.json sidecar — bounds hardcoded in loader).
- **EXTRA overrides that define the recipe (NOT in the stage dict — must pass via sbatch EXTRA):** `++env.ego_blur_gate=false ++env.ego_yaw_cmd_clamp_rad_s=0.7 ++env.spin_abort_anneal=true ++env.spin_abort_scale_start=2.6 ++env.spin_abort_hold_frac=0.25 ++env.ego_est_dt_ticks_hi=1 ++warmstart_reset_logstd=true ++warmstart_reset_logstd_std=0.18`. Stage renders blur=true/clamp=0.35/ticks_hi=4; Hydra LAST-WINS so EXTRA overrides them.
- Stage `dual_gate_fullstack_floor_pef`: 2 gates, spawn 8–15 m, floor_at_spawn, racing_line, corridor 4 + centering 0.4 + parabola_crossing(zero=4), rw_progress=2.0/rw_passage=5.0, rw_altitude_hold=0.0, rw_tilt_free_rad=60°, real noise 0.12/0.03/0.5, n_substeps=5, capture_specific_force, γ=0.9975, DR on.
- sbatch launch mechanics: `EXTRA`/`STAGES`/`UPD_<stage>`/`RUNTAG`/`SEED`/`PRECHECK` are env vars carried by `--export=ALL`. Single-stage run → NO auto +init_from (that only fires for CHAINED stages) → pass `+init_from=...` via EXTRA. `--gres=gpu:nvidia_a100:1` forces the full A100 (else MIG h11g2 = 3–4× slower). scratch sbatch = `/scratch/network/fl3689/peregrine_repo/rl/peregrine_vq2_ego.sbatch` (NOT in git — scratch-only).

## CONTROL KNOBS — BUILT + DEPLOYED (629d2a2, all default-OFF byte-identical, armed via +env. EXTRA)
- **Velocity cap (anti-runaway, 04a9a63):** `rw_v_cap`(0)/`rw_v_cap_soft`(9)/`rw_v_cap_hard`(12) — one-sided quadratic on ‖v‖, EXACTLY 0 below soft, ‖v‖ observable (obs[0:3]=body vel). SAFE improvement (guardrail, doesn't fight core). + **fast-spawn** `spawn_vel_frac`(0)/`spawn_vel_max`(15) in base reset_idx (velocity toward gate0 — exposes the high-speed regime training is too short to reach).
- **Attitude cap (629d2a2):** `rw_att_pitch`(0)/`rw_att_roll`(0), `att_pitch_limit_rad`/`att_roll_limit_rad` (default **60°**), anneal via `att_cap_anneal`/`att_cap_start`/`att_cap_hold_frac`. Per-axis. Roll 60° = the load-bearing self-limit; pitch 60° = loose backstop (lean on rw_perception).
- **Anti-dither (629d2a2):** `rw_yaw_dither`(0) — penalizes yaw-command JERK (Δ post-clamp yaw action, NOT accumulated rotation), anneal via `yaw_dither_anneal`/`yaw_dither_start`/`yaw_dither_hold_frac`. RISKY (touches yaw) — arm gentle + watch yaw+altitude.
- Stage `dual_gate_fullstack_floor_pef_trackA` (629d2a2) = _pef fullstack + attitude cap (roll/pitch 60°) + anti-dither, BOTH annealed 0→target over the fullstack (yaw_clamp stays 0.35, yaw-dither calibrated for it). **RUN FROM SCRATCH: `STAGES="dual_gate_boot_floor_pef dual_gate_fullstack_floor_pef_trackA"` (fresh boot → constrained fullstack). Do NOT `+init_from vpeffs0`.** ticks_hi override to 1 + blur=false + spin_abort_anneal via EXTRA (== vpeffs0 recipe, MINUS the yaw_clamp override). 🚩 fresh-boot despin is UNPROVEN (vpeffs0's boot warmed from vwhb0) → hedge with a variant warming the fullstack from the DESPIN BOOT (vpefwh2's rough boot, NOT vpeffs0) if the fresh boot can't fly.

## DEPLOY TOOLING (scratchpad)
- `push_file.py <local> <remote>` (base64-push over the daemon `x`, sha256-verified; **CHUNK=24000** b64 chars — Windows CreateProcess cmdline cap ~32767). `adroit.py upload` is UNUSABLE non-interactively (getpass passphrase). Deploy = git-archive the changed files from the COMMIT (agent-edit-safe, NOT the dirty worktree) → tar → push_file.py → `tar xf` on scratch. Deploy set 8055acd→HEAD = rl/{ego_reward,peregrine_course,peregrine_racing,peregrine_racing_ego,peregrine_train_ego,vq2_ego_curriculum}.py (KEEP the proven scratch sbatch).

## 🔑 ROOT-CAUSE DIAGNOSIS — m8 per-gate rollout trace (2026-07-13, THE pivot finding)
Flew the FINISHED vtrackAm8 ckpt deterministically on the 8-gate course (`rl/ego_rollout_trace.py`, 5159 eps). Validated vs TB (deterministic 23% full-8 / 4.7 gates > stochastic 11.5% / 4.1 = right direction). **VERDICT = KINEMATIC ACQUISITION LIMIT, not a map or speed-ramp problem:**
- **Map POINTS RIGHT: `map_h_ok` 96–97% at EVERY gate depth, `map_consist`=100%** (live rebuild works, NO staleness). ⇒ NOT a nav/stale-map error. The coarse-map deadband retune is a REFINEMENT, not the threading fix — don't expect it to move success much alone.
- **Speed does NOT ramp** — plateaus at **~11.2 m/s by gate 1** (gate0 9.2 from standing start), flat after. Turn sharpness also stationary (`az_next_p90` ~48–51° every gate). My earlier "accumulated speed by gate 4" story was WRONG. BUT crossing speed ~11.2 (p90 12.7, MAX 15) is HIGH — well above the 8.6 episode-MEAN (mean dragged down by accel phase) and into runaway ⇒ vcap still goal-aligned.
- **Death hazard RISES with depth: 8.7%→10.7→11.5→12.8→13.7→19.2→23.3→29.2%** (gate0→7). The one accumulating signal = crossing offset (`x_off` 0.186→0.212) — enters each gate slightly more off-line, compounding the next acquisition angle. `exit_frame`=73.5% of deaths (floor 1.2%, spin 1.9%, oob ~0).
- **Mechanism (quantified):** next-gate bearing p90 ~49° ≈ JUST past the camera ±45° H-frame-edge ⇒ for the sharp-turn tail the next gate is already OUT of frame at crossing; at 11.2 m/s with the **0.35 rad/s yaw ceiling**, slewing 49° takes ~2.5 s = ~28 m but gates are 10–20 m apart ⇒ overflies the acquisition window → `exit_frame`. **High map_h_ok + high exit_frame = "map's right, can't slew."** ⇒ yaw-rate + speed are the levers (m8b), reward-shaping the trajectory is the next (rs0).

## CAMERA FOV + geometry (from `rl/inc8_estimator_emul.py`, for the deadband/FOV math)
640×360, fx=fy=320, cx=320 cy=180, mount **+20° pitch-up**. **HFOV=90° (edge ±45°), VFOV=58.7° (edge ±29.4°).** Forward-flight pitch ~nulls the 20° up-tilt ⇒ symmetric-about-level buckets OK (Fengyou 2026-07-13). Old coarse deadband 0.20 rad ≈ 11.5° ≈ equal-thirds of the FOV (center too tight — flagged in-frame gates as turns).

## COARSE-MAP RETUNE — BUILT+staged (2026-07-13), NOT yet launched
`build_coarse_map` deadbands now cfg-configurable: `ego_coarse_horiz_thresh_rad`/`ego_coarse_vert_thresh_rad` (getattr defaults **0.20** = byte-identical), threaded into init build (~L811) + reset_idx live rebuild (~L958) of `rl/peregrine_racing_ego.py`; test `tests/test_coarse_map_thresh.py` (4 pass + 60 regression). **Fengyou rule = "outer ⅓ of half-FOV or beyond = turn", SYMMETRIC:** activate `++env.ego_coarse_horiz_thresh_rad=0.5236 ++env.ego_coarse_vert_thresh_rad=0.3418` (30°/19.6°). Staged (not deployed) — keep as a SEPARATE arm (map already directionally right).

## REWARD DESIGN — full set + the next-arm levers (Fengyou 2026-07-13)
Reward = r_prog(2.0) + r_cross(parabola 20@center, zero@4m) + r_center(0.4) + r_corr(4.0) + r_perc(0.02,exp4) + r_att(pitch/roll 0.2@60°) + r_yawdith(0.5) + r_cone(tilt 4@60°/cap70°) + r_smooth(rate/dact 1e-3) + r_time(0.02) + r_fin(20) − terminal(floor100/oob200, progress-scaled) [+ r_vcap on m8b]. Passage(5.0) REPLACED by parabola (⇒ no per-gate depth increment active). **OFF in m8:** area_dist(=0), gate_vhold, align, vel_smooth, exit_align, perception_next. 🎯 **DECISION (next arm): turn ON `exit_align` + `perception_next`; do NOT reward area** — area IS in the obs (slot0 conf+area for current gate, slot1 for next) so the policy LEARNS it as a confidence signal; a reward would fight the angled entry line. `exit_line_reward` fires ONCE per pass = rw_exit_align·cos(exit-vel, curr→next bearing). 🚩 **FARM-NEUTRALITY assert: perception + perception_next ≤ rw_time(0.02)** ⇒ must SPLIT the 0.02 budget, not add. → [[feedback-reward-exit-align-perception-next]]

## RUN PORTFOLIO (2026-07-13, updated)
- ✅ **vtrackAw0** = FIRST wall-free flyer, DET 0.846 ~8.3 m/s (despin-boot → constrained fullstack). THE warm base for all m8-line arms. ckpt `.../ego_dual_gate_fullstack_floor_pef_trackA_seed0_vtrackAw0/checkpoints`. 🚩 RECIPE LOCKED: despin-boot NOT fresh boot (vtrackA0 fresh boot FAILED despin).
- ✅ **vtrackAw1 DONE** (yaw 0.35→0.7 gap-closer) — converged DET **0.843–0.861 = TIES vtrackAw0**. ⇒ yaw authority was NOT the 2-gate DET gap. Clean (speed 8.1, tilt 61°@cap, spin 0, dither smoothed 0.021→0.005).
- ✅ **vtrackAm8 DONE + TRACED** (8-gate + live coarse map, yaw 0.35, no vcap) — avg 4.1 gates (stochastic) / 4.7 (deterministic), full-8 11.5%. The trace above is on THIS ckpt.
- ✅ **m8b=3308205 DONE (12k upd) — WIN over m8, kinematic fix CONFIRMED.** m8 recipe + **yaw 0.35→0.7 + velocity cap (rw_v_cap=3, soft 9/hard 12)**, warm vtrackAw0. Matched-training-metric head-to-head (late10 = last-100-upd mean):

  | metric (late10) | m8 (clamp 0.35, no cap) | m8b (0.7 + vcap) | Δ |
  |---|---|---|---|
  | success_rate | 0.217 (pk 0.284) | **0.326 (pk 0.458)** | +50% / +61% pk |
  | n_passed_gates | 4.60 (pk 5.21) | 4.98 (pk 5.66) | +0.37 |
  | exit_frame (death share) | 0.737 | **0.631** | −10.6 pts |
  | exit_spin | 0.0247 | **0.0067** | −73% (spin DOWN) |
  | spin_abort_rate | 0.0248 | 0.0069 | −72% |
  | mean_speed | 9.41 | 7.66 | −1.75 |
  | peak_tilt_deg | 73.9 | 63.4 | −10.5 |
  | cross_offset_m | 0.202 | 0.197 | ~flat |
  | finish_time_s | 12.7 | 16.2 | +3.5 (vcap trade) |

  🚩 **Mechanism = the trace's prediction, EXACT:** `vcap_pen` collapsed early 1.24 → late 0.009 (policy INTERNALIZED the cap, not fighting it) → slower+calmer envelope → 0.7-rad/s yaw gets time-to-slew onto the next gate → exit_frame down → gates up → success up. Spin FELL at the higher clamp (calmer flight, not incentive) — NO-SPIN honored. CAVEATS: (a) moves 2 levers (0.35→0.7 AND vcap) ⇒ combined-fix verdict, not a yaw-isolation, but vcap is what MAKES 0.7 usable (matched by design); (b) **0.326 is a FLOOR** — yaw_dither=0.5 un-recalibrated (pen 0.0385 = 2nd-heaviest term) still taxes the acquisition turns; rs0's 0.125 should exceed it; (c) exit_frame 63% ⇒ acquisition STILL the bottleneck (5 gates chained, not 8). Event dir `.../ego_dual_gate_fullstack_floor_pef_trackA_seed0_vtrackAm8b/...__0/`.
- 🛑 **vtrackArs0=3308426 DONE (12k) — REGRESSED vs m8b, reward-shaping bundle did NOT help.** TUNED arm (warm vtrackAw0; m8b recipe **+ `rw_exit_align=4.0` + perception split `0.012`/`0.008` + `rw_yaw_dither 0.5→0.125` + `rw_centering 0.4→0.3`**). late10 vs m8b: success **0.326→0.231** (peak 0.458→0.396), exit_frame **0.631→0.733** (BACK UP to m8-baseline), exit_spin 0.0067→0.0134 (~doubled), gates 4.98→4.92, speed/cross ~flat. 🚩 Every knob fired mechanically as designed (exit_reward collected 0.051, perception_next 0.006, yaw_dither_pen dropped 4× to 0.010 = recalibration WORKED, vcap internalized) — but the NET erased m8b's edge. **Fengyou's hypothesis (exit_align + perception_next → better acquisition) REFUTED at n=1: rewarding exit-heading + holding k+1 did NOT cut exit_frame, it slightly worsened it.** CAVEATS: (a) 4-knob bundle ⇒ can't finger the culprit; (b) single-seed (audit variance ~2.8× ⇒ not a death sentence, but did NOT beat m8b). 🔑 INVERTED LESSON: the earlier "m8b's dither too strong, MASKS yaw-0.7" caveat is BACKWARDS — recalibrating dither DOWN coincided with MORE spin + worse frames ⇒ m8b's stronger dither was STABILIZING, not masking. → **m8b is the confirmed Track-A 8-gate winner; rs0 shelved.** To salvage exit_align/perception_next = single-variable isolation + multi-seed (deferred; BANK-FIRST says fly m8b now).

## REWARD-TUNING PASS (Fengyou "mull the rewards, minor touch-ups" 2026-07-13; folded into rs0)
Per-step budget (m8 late): **progress +0.65** (rw 2.0) · crossing-parabola +0.38 (peak +20/gate) · perception +0.019 · finish +0.05 | penalties: **centering −0.215** (rw 0.4, the 2nd-biggest term) · terminal −0.035 (fwd-scaled, forfeits ~94 banked) · time −0.02 · yaw-dither −0.014 · cone −0.009 · corridor −0.006 (PBRS ~net0). Net +0.79.
- 🔧 **TUNE 1 — `rw_yaw_dither` 0.5→0.125 at clamp 0.7 (a real fix, not a preference).** Penalty = weight·(Δyaw_cmd)²; a rail-flip's Δ DOUBLES when the clamp doubles ⇒ identical dither costs 4× more at 0.7 ⇒ over-taxes the fast turn-initiations the wider clamp was meant to enable. 0.125 keeps the per-rail-flip cost = the proven 0.35/0.5 setting (matches vtrackAw1). **RULE: rescale a jerk/Δ-penalty by 1/clamp² whenever you change the action clamp.**
- 🔧 **TUNE 2 — `rw_centering` 0.4→0.3.** Centering is a DENSE tax on off-line flight across the whole approach = fights the racing-line/angled-entry `exit_align` unlocks. Trim 25%; corridor PBRS still homes (no standing tax) + parabola keeps the CROSSING centered (trace x_off ~0.19). Coherent with the exit_align direction.
- LEAVE ALONE: progress 2.0 (core driver), terminal (γ-tuned dominance), vcap soft 9 (under test, keep comparable). SECONDARY (flag not touch): `cross_zero_m=4.0` very flat (frame-strike≈clean thread, clip_terminal off) — tighten toward aperture only if trimming centering widens crossings; TWO overlapping tilt penalties (free-cone 60° + att-cap 60°) both fight the ~74° sharp-turn banks; no per-gate depth increment (parabola replaced passage — completion rests on banked-progress forfeit).
- Build lineage: anti-runaway 04a9a63 → attitude/dither 629d2a2 → course-variety f6c07f1 → coarse-map-thresh + tracer (staged, chaum, NOT pushed). m8b/rs0 are CONFIG-ONLY on f6c07f1 code.
- ✅ COURSE SPAWN VERIFIED (verify_course.py/plot_course.py → course_verify.png): varied pos/yaw/elev, non-degenerate; drop-biased to floor (cosmetic for position-free). obs-v2 (23-dim) DEFERRED (#41).
- 🚩 STALE-COARSE-MAP BUG (fixed by `ego_coarse_map_live=true`, on for m8/m8b/rs0): `_coarse_map` was built once at init, never rebuilt on per-reset resample → stale sector ep-2+.

## SHADOWPC FLIGHT DOCKET (published releases, Hat000/Peregrine)
- 🆕 **`ego-ckpts-tracka-2026-07-13`** (published 2026-07-13, verified 2×162903 B): **vtrackAw0_actor.pth** (wall-free flyer, DET 0.846, deploy `--ego-yaw-clamp 0.35`) + **vtrackAm8_actor.pth** (8-gate chainer, deploy `--ego-yaw-clamp 0.35`). Pulled via `pull_file.py` (sha-verified), released via `GH_TOKEN=<git-credential-fill> gh.exe release create`. 21-dim ego, load_ego_actor, ≥30 Hz.
- 🆕🆕 **`ego-ckpts-tracka-yaw07-2026-07-13`** (published 2026-07-13, 3×162903 B, all verified `uploaded`; Fengyou "push every model"): **vtrackAm8b_actor.pth** (★ 8-gate WINNER, sha 481265fb, `--ego-yaw-clamp 0.7`) + **vtrackArs0_actor.pth** (reward-shaped arm, REGRESSED vs m8b — comparison only, sha 25b7b2c0, 0.7) + **vtrackAw1_actor.pth** (2-gate 0.7 flyer, ties w0, sha 0eba0485, 0.7). 🚩 ALL clamp 0.7.
- **`ego-ckpts-vpef-2026-07-12`**: vpefwh2_actor.pth (PRIMARY 0.969) + vpeffs0_actor.pth (backup ~0.86), deploy `--ego-yaw-clamp 0.7`. Deploy code branch `claude/ego-deploy-2026-07-09` (origin c851802). Detector = `vision-m-2026-07-06`. 🚩 clamp PER-CKPT: vpef=0.7, trackA=0.35.
- ShadowPC: `git checkout claude/ego-deploy-2026-07-09` → `gh release download <tag>` → `fly_rl.py --ego-ckpt <> --seeker-weights <> --ego-yaw-clamp <> --no-bridge --flights 1`. Ritual: kill orphan pilots → fly_rl banner → GO; zero contact = validity. ✅ m8b/rs0/w1 ALL PUBLISHED (yaw07 release). 🚩 clamp per-RELEASE: vpef=0.7, tracka(0.35 release)=0.35, tracka-yaw07=0.7. GT-only diagnostics (vwhb0 0.775, vwff0 0.828) INTENTIONALLY NOT published — they fly garbage on real sensors (no-GT directive). Full deploy contract → [[ego-deploy-contract-2026-07-09]].

## TOOLING (2026-07-13)
- `rl/ego_rollout_trace.py` (+`.sbatch`) — per-gate rollout tracer: imports `peregrine_train_ego`, overrides `TrainRunner.run`, loads ckpt via `maybe_warmstart`, deterministic rollout, logs per-gate speed/map_h_ok/x_off + death histogram + exit breakdown. Launch: `sbatch --gres=gpu:1 --export=ALL,NENVS=256,TAG=<run> .../ego_rollout_trace.sbatch` (INIT_FROM defaults to m8). 🚩 **FOOTGUN: diffaero's `RecordEpisodeStatistics` wrapper `__getattr__` REFUSES private (`_`-prefixed) attr access — read GT off the RAW env `self.env.env`, not `self.env`.** Deployed to scratch.
- `push_file.py <local> <remote>` (base64-push over daemon `x`, sha256-verified, CHUNK=24000). `adroit.py upload` UNUSABLE non-interactively (getpass). TB scalars: `scratchpad/tb_dump.py <eventfile>` via `x "echo B64|base64 -d|python3 - PATH"`.
