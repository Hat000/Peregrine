GOAL: Train a YOLO11-pose 8-kpt gate detector that BEATS the "Round-1" champion on every metric of eval_goodfix.py, with CI confidence -- crown it if found. Run all night; ShadowPC drops and kills jobs repeatedly -- survive, resume, keep grinding. Full brief on disk (read first): handoff/vq2-overnight-goal-2026-06-18/GOAL_BRIEF.md. Every lever we tried this week (more data, more/less aug, tighter loss) LOST; the champion is stubborn. A win must clear the Wilson CI; +2% on 40 frames is noise -- don't fake it.

BAR (champion = runs/pose/runs/vq2_pose_8kp/weights/best.pt; eval = handoff/vq2-train-shadowpc-2026-06-16/eval_goodfix.py over 40 task2_frames + 204 course_bundle): oracle good-fix <0.5m 80% / <1m 95% / <2m 98%; course valid-fix 76%; deployed-IPPE <0.5m 62%. Beat = strictly higher <0.5m AND course, no regression elsewhere, CI clears. 40 frames is thin (+/-12pp) so the 204-frame course metric is the credible win; <1m/<2m are near-ceiling (match them).

SANITY GATE first (mandatory): retrain the champion recipe on data_base2000.yaml -> must hit ~80% (best ep~26, early-stop ~46). If not, the pipeline is broken -- STOP and report; do not waste the night.

DEAD LEVERS, do NOT repeat (proven losers): (a) remove/lighten sensor aug -> 22% (VQ stream is JPEG-over-UDP; flat-9 aug is load-bearing -- sweet spot none22 / flat9 80 / heavy55). (b) more clean data -> caps ~62% (dilutes precision even with identical images + correct split). (c) tighten OKS sigma on the OLD poisoned 4k -> 32% (confounded by poison; sigma-on-CLEAN is untried). (d) pick checkpoints by mAP (a 0.835-mAP run scored 28% good-fix).

LIVE LEVERS (one change per run, champion data+recipe, highest-EV first): 1) CAPACITY: yolo11m-pose then yolo11l-pose (just --model) -- best shot, latency is free (8GB-GPU desktop, 15-25ms vs 50ms budget), untested. 2) LOSS: tighter inner-4 OKS sigma ON CLEAN data + an un-normalized L1 keypoint term (fixes the near-gate area-norm blind spot); patch ref cluster/vq2_pose_train_precision.py; watch for gradient saturation. 3) Inner-4-only head (outer kpts clip off-frame near range, may dilute the precision corners). 4) HP tuning (lr/close_mosaic/optimizer), single-variable. 5) TTA / checkpoint ensembling (no retrain).

METHOD: ONE variable per run. Deterministic seed 0. Judge EVERY run on eval_goodfix.py (never mAP). Pick the deploy checkpoint by good-fix across the save_period snapshots, not best.pt. Keep a live markdown leaderboard (change -> good-fix+CI -> verdict vs champion) so a closeout never loses the night.

SURVIVAL (critical): ShadowPC powers OFF on disconnect and kills the run -- happens repeatedly. Every run: save_period=10 (last.pt each epoch). cluster/vq2_pose_train.py has NO --resume -- ADD one (YOLO(last.pt).train(resume=True), re-pass augmentations=) or a watchdog that relaunches from last.pt on death. ON DEATH: resume from last.pt, never restart. One GPU -> serialize (check nvidia-smi + python procs first).

ENV: USE the venv C:\Users\Shadow\vq2yolo-venv\Scripts\python.exe (torch 2.6.0+cu124, ultralytics 8.4.69) -- NOT the base python. Trainer cluster/vq2_pose_train.py. Champion data FROZEN: .../sets/data_base2000.yaml (never regenerate). NEVER overwrite the champion weights or touch the deployed model. Own branch off vq2-stack-audit-2026-06-18.

DELIVER -> handoff/vq2-overnight-goal-2026-06-18/REPORT.md: leaderboard; verdict (NEW CHAMPION with weights+recipe+CI proof, OR "Round-1 holds" + levers exhausted + why); selected checkpoint + reproduce command; partial wins. Go beat 80% -- or prove cleanly it can't be.