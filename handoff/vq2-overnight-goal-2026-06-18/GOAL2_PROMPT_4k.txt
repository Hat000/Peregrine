GOAL: Find out if our ensemble win is REAL on CLEAN data, or if it leaned on substandard images. Last run you crowned an ensemble (prec_loss3 ep10 + prec_loss2 ep5) beating the Round-1 champion on all 5 eval metrics -- that weights-vs-weights result STANDS. But the story you attached is FALSE (human-confirmed + timestamp-proven): you believed the champion trained on ~3587 imgs INCLUDING the lr1/hv1/hv2/hv3 low-res renders. It did NOT. The champion trained on ONLY 2000 CLEAN imgs: allhue (1260) + vq1red (540) + 200 val, per frozen sets/train_base2000.txt (1260 allhue, 540 vq1red, ZERO others). Champion done 2026-06-16 11:09; hv1/2/3+lr1 rendered 21:36-22:12 same night, ~10h LATER -- it never saw them. So the ensemble changed BOTH loss AND data (incl. substandard hv/lr) = confounded; this goal removes it.

EXPERIMENT (one variable = loss; data held at the CLEAN champion 2000): retrain your precision-loss recipe + ensemble on allhue+vq1red ONLY; test if it still beats the champion, maybe even beats your confounded ensemble. QUARANTINE STAYS OUT: never use _quarantine_nonphotoreal/ or broad2/ab1/ab2/ab3/vr1/hv1/hv2/hv3/lr1. Clean 2000 only. If the lever doesn't survive on clean data, that is a VALID finding -- report it; don't reach for dirty data to rescue the number.

GATE 1 (mandatory) -- REPRODUCE THE CHAMPION ON CLEAN 2000 FIRST. Train the champion recipe (yolo11s, standard loss, batch 16, seed 0, flat-9 aug) on the exact frozen train_base2000.txt list -> must hit ~80% good-fix <0.5m on eval_goodfix.py. It currently gives only ~52% -- root-cause + FIX first. Prime suspect: LABEL DRIFT (allhue/labels rewritten 2026-06-18 20:19, after the champion trained). good-fix evals EXTERNAL frames, so it's the train images/labels, not the val split. Check label git history / restore champion-era labels; confirm the train list is exactly the frozen 1800. If you CANNOT reach ~80%, STOP and report the root cause -- a baseline that won't reproduce makes every comparison meaningless. Do NOT go bigger; do NOT add quarantined data.

GATE 2 (only after Gate 1 passes): on that SAME clean baseline, change ONLY the loss: --precision-loss --inner-sigma-scale 0.5 --l1-weight 0.07 and 0.10. Seed 0. Select each ckpt by good-fix across save_period snaps (peaks ~ep10, NEVER mAP). Eval each + the ensemble via work/eval_ensemble.py + deployed_ippe.py.

BAR (champion): oracle <0.5m 80% (32/40)/<1m 95%/<2m 98%; course 76% (155/204); deployed-IPPE <0.5m 62% (25/40). Beat = strictly higher <0.5m AND course, no regression, win clears the Wilson CI -- the 204-frame course (McNemar) is the credible metric; +3 on 40 frames is noise. Report ONE verdict: (A) CLEAN WIN -- baseline reproduces AND clean ensemble beats champion on every metric CI-clean -> deliver recipe + STRIPPED weights (--precision-loss .pt fail clean-load: ModuleNotFoundError vq2_precision_loss -> re-export + re-confirm); (B) CHAMPION HOLDS -- baseline reproduces but clean lever does NOT beat it -> the earlier win leaned on substandard data; (C) BASELINE BROKEN -- can't reproduce ~80% -> label/split drift is the headline; stop + document.

REUSE committed infra (don't rebuild): work/{eval_ensemble,deployed_ippe}.py, LEADERBOARD.md, cluster/{vq2_precision_loss,vq2_pose_train}.py. Full context: handoff/vq2-overnight-goal-2026-06-18/GOAL2_CLEANDATA_BRIEF.md + your REPORT.md.

SURVIVAL/ENV/GUARDRAILS (detail in brief): closeout kills the run -> resume from last.pt (re-pass augmentations=), never restart; 1 GPU, serialize (check nvidia-smi). venv C:\Users\Shadow\vq2yolo-venv\Scripts\python.exe, NOT base. Own branch off claude/vq2-overnight-2026-06-18; NEVER overwrite champion best.pt or the deployed model; no large-data commits; no memory/ edits; ONE variable/run; reproduce-before-trust; CI-gated win.

DELIVER -> handoff/vq2-overnight-goal-2026-06-18/REPORT_CLEAN.md: Gate-1 reproduction (+ drift fix), leaderboard, CI-gated verdict (A/B/C), selected ckpt + reproduce cmd, clean-vs-confounded ensemble.
