# PROPOSED, NOT APPLIED (this session makes NO src/ edits). The overall commander / P1 lands
# this with the regression test (proposed_test_boresight_calibration.py) once P3's lock test
# supplies the measured magnitude+sign. At BORESIGHT_*_RAD = 0.0 the stack is byte-identical.
#
# --- src/racer/frames.py  (insertion point #1: next to CAMERA_PITCH_RAD, ~line 18) ---

 CAMERA_PITCH_RAD = np.deg2rad(20.0)
+
+# Calibrated camera-mount BORESIGHT correction (rad), composed INTO R_camera_from_body() below so
+# the ONE constant propagates to every consumer of the mount (localization fix, association forward
+# model, fix_surrogate, navigator, characterize_perception) automatically. DEFAULT 0.0 -> the mount
+# is byte-identical to the 20deg-only construction, so the whole stack stays VQ1 byte-identical
+# until calibrated. Set from P3's static head-on/LEVEL lock test at g2/g4 (bearing ~ 0) via
+#     BORESIGHT_PITCH_RAD = -atan(mean_gate_vertical_residual_m / head_on_range_m)
+# (down-positive residual). Audit pins the SIGN: a physical camera pitched UP by +eps relative to
+# the 20deg model produces gate_vert = -range*tan(eps) (e.g. measured eps_vert ~ -0.215 m at 22 m
+# => +0.56 deg). The correction sets the decode mount to the physical pitch, zeroing the residual.
+# YAW is the optional lateral term (field lateral ~ 0 -> default 0; validate axis/order if ever set).
+# See handoff/p1-vision-accuracy-2026-06-14/AUDIT_REPORT.md.
+BORESIGHT_PITCH_RAD = 0.0
+BORESIGHT_YAW_RAD = 0.0

# --- src/racer/frames.py  (insertion point #2: R_camera_from_body, ~line 168) ---

 def R_camera_from_body() -> np.ndarray:
-    # Passive rotation by +20 deg about body Y (frame rotated, vector representation changes oppositely).
-    R_tilted_from_body = Rotation.from_euler("Y", -CAMERA_PITCH_RAD).as_matrix()
-    return _R_CAMERA_FROM_TILTED_BODY @ R_tilted_from_body
+    # Passive rotation by +(20 + boresight_pitch) deg about body Y (frame rotated, vector
+    # representation changes oppositely), then the OPTIONAL lateral boresight about body Z (yaw).
+    # BORESIGHT_*_RAD default 0.0 => byte-identical to the original 20deg-only mount.
+    R_tilted_from_body = Rotation.from_euler("Y", -(CAMERA_PITCH_RAD + BORESIGHT_PITCH_RAD)).as_matrix()
+    R_boresight_yaw = Rotation.from_euler("Z", -BORESIGHT_YAW_RAD).as_matrix()
+    return _R_CAMERA_FROM_TILTED_BODY @ R_boresight_yaw @ R_tilted_from_body

# NOTES
# * Single source of truth: every mount consumer calls R_camera_from_body(), so this is the ONLY
#   edit needed. NO change to the +L lever (localization.py:86/87), NO change to the 20-dim obs
#   contract, NO change to CAMERA_INTRINSICS_K (fy=320, cy=180), NO change to the 20deg CAMERA_PITCH_RAD.
# * The pitch term is the load-bearing, audit-validated correction. The yaw term is insurance:
#   at 0.0 it is identity; before ever setting it non-zero, validate the body-Z axis + compose order
#   against a lateral lock test (the audit only validated the pitch axis).
# * Calibration estimator (verified end-to-end in calib_estimator_demo.py, recovers an injected
#   0.56deg to <0.01deg, post-correction residual <2mm across 18/22/30 m):
#       def solve_boresight_pitch_rad(residual_m, range_m): return -atan(residual_m / range_m)
